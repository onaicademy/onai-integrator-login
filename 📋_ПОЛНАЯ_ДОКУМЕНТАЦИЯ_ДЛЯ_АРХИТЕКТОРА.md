# 📋 ПОЛНАЯ ТЕХНИЧЕСКАЯ ДОКУМЕНТАЦИЯ - TRAFFIC DASHBOARD

**Дата**: 19 декабря 2025  
**Версия**: 2.0 Complete  
**Для**: Архитектор (Кодер)  
**Владелец**: Александр  
**Проект**: OnAI Academy - Traffic Dashboard

---

## 📌 ОГЛАВЛЕНИЕ

1. [Обзор проекта](#обзор-проекта)
2. [Архитектура системы](#архитектура-системы)
3. [Функционал админпанели для таргетологов](#функционал-админпанели-для-таргетологов)
4. [Функционал админпанели для администратора](#функционал-админпанели-для-администратора)
5. [Структура проекта и пути к файлам](#структура-проекта-и-пути-к-файлам)
6. [База данных](#база-данных)
7. [API Endpoints](#api-endpoints)
8. [Troubleshooting Guide](#troubleshooting-guide)
9. [Деплой на Digital Ocean](#деплой-на-digital-ocean)
10. [Работа с терминалами](#работа-с-терминалами)

---

## 🎯 ОБЗОР ПРОЕКТА

### Что это?

**Traffic Dashboard** - это аналитическая платформа для таргетологов и администраторов, которая:

1. **Собирает данные** из Facebook Ads API и AmoCRM API
2. **Анализирует эффективность** рекламных кампаний (ROAS, CPA, CTR)
3. **Предоставляет AI рекомендации** для улучшения результатов
4. **Отслеживает безопасность** (IP трекинг, device fingerprinting)
5. **Анализирует UTM метки** для определения источников продаж

### Для кого?

- **Таргетологи** (4 команды: Kenesary, Arystan, Muha, Traf4)
- **Администратор** (Александр - владелец)

### Технологический стек:

**Frontend**:
- React 18 + TypeScript
- Vite (build tool)
- TanStack Query (data fetching)
- Tailwind CSS (styling)
- Framer Motion (animations)
- Recharts (charts)
- Driver.js (onboarding)

**Backend**:
- Node.js + Express
- TypeScript
- Supabase (PostgreSQL)
- JWT Authentication
- Axios (external APIs)

**Infrastructure**:
- Digital Ocean Droplet (Ubuntu)
- Nginx (web server)
- PM2 (process manager)
- Let's Encrypt (SSL)
- Git (version control)

---

## 🏗️ АРХИТЕКТУРА СИСТЕМЫ

### High-Level Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    ПОЛЬЗОВАТЕЛИ                             │
│  (Таргетологи + Администратор)                              │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│              traffic.onai.academy (Frontend)                │
│  • React SPA                                                │
│  • Nginx (static files)                                     │
│  • SSL (Let's Encrypt)                                      │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼ HTTPS
┌─────────────────────────────────────────────────────────────┐
│              api.onai.academy (Backend)                     │
│  • Express.js REST API                                      │
│  • JWT Auth                                                 │
│  • PM2 Process Manager                                      │
└────────┬────────────┬────────────┬─────────────────────────┘
         │            │            │
         ▼            ▼            ▼
   ┌─────────┐  ┌──────────┐  ┌────────────┐
   │Supabase │  │Facebook  │  │  AmoCRM    │
   │  (DB)   │  │Ads API   │  │   API      │
   └─────────┘  └──────────┘  └────────────┘
```

### Data Flow

```
1. Таргетолог логинится → JWT token сохраняется в localStorage
2. Frontend делает запрос к /api/traffic/combined-analytics
3. Backend параллельно запрашивает:
   • Facebook Ads API (затраты, клики, показы)
   • AmoCRM API (продажи, revenue)
4. Backend рассчитывает метрики (ROAS, CPA, CTR)
5. Frontend отображает данные в красивом UI
```

---

## 👥 ФУНКЦИОНАЛ АДМИНПАНЕЛИ ДЛЯ ТАРГЕТОЛОГОВ

### URL: `https://traffic.onai.academy/login`

### Роль: `targetologist`

### Что видят таргетологи:

#### 1. **Личный Dashboard** (`/cabinet/:team`)

**Описание**: Главная страница с метриками команды

**Компоненты**:
- Топ-бар с кнопками (БЕЗ sidebar!)
- Крупная зелёная пульсирующая кнопка "МОИ РЕЗУЛЬТАТЫ"
- Полная таблица всех команд
- AI рекомендации (только для своей команды)

**Технические детали**:
- **Файл**: `src/pages/traffic/TrafficTargetologistDashboard.tsx`
- **API**: `/api/traffic/combined-analytics?preset=30d`
- **Аутентификация**: JWT токен из `localStorage.getItem('traffic_token')`
- **Данные**: Facebook Ads + AmoCRM, группировка по командам

**Метрики**:
- 💰 **Revenue** (Доход): Сумма продаж × 5000₸
- 💵 **Spend** (Затраты): Из Facebook Ads API
- 📊 **ROAS**: Revenue / Spend (с учётом валют!)
- 🎯 **CPA**: Spend / Sales
- 📈 **Sales** (Продажи): Из AmoCRM API
- 👆 **Clicks**: Из Facebook Ads API
- 👁️ **Impressions**: Из Facebook Ads API
- 📱 **CTR**: (Clicks / Impressions) × 100

**Кнопка "МОИ РЕЗУЛЬТАТЫ"**:
- Прокручивает к строке своей команды в таблице
- Подсвечивает строку зелёным цветом
- Анимация пульсации

#### 2. **Детальная Аналитика** (`/detailed-analytics`)

**Описание**: Видео метрики, топ креативы, кампании

**Файл**: `src/pages/traffic/TrafficDetailedAnalytics.tsx`

**API**: `/api/traffic/combined-analytics?preset=30d` (те же данные, другое отображение)

**Что показывает**:
- 🎬 **Video Metrics**:
  - Plays (просмотры)
  - Completions (завершения)
  - Completion Rate (процент завершений)
  - Thruplay (просмотры >15 сек)
  - Avg Watch Time (среднее время)
- 🏆 **Top 3 Video Creatives** (по completion rate)
- 📊 **Список кампаний** с затратами и метриками

#### 3. **Настройки** (`/settings`)

**Описание**: Добавление Facebook кабинетов, выбор кампаний, UTM метки

**Файл**: `src/pages/traffic/TrafficSettings.tsx`

**Функционал**:

##### A. Добавление Facebook Кабинетов

**Как работает**:
1. Нажать кнопку "Загрузить доступные кабинеты"
2. Frontend → `/api/traffic/fb-accounts/list`
3. Backend делает запрос к Facebook Ads API
4. Возвращает все кабинеты пользователя
5. Таргетолог выбирает нужные (checkbox)
6. Сохранение → `/api/traffic/settings/save`
7. Данные сохраняются в `traffic_targetologist_settings` таблица

**Данные в БД**:
```json
{
  "fb_ad_accounts": [
    {
      "id": "act_123456789",
      "name": "Nutcab Ads",
      "enabled": true
    }
  ]
}
```

##### B. Выбор отслеживаемых кампаний

**Как работает**:
1. После выбора кабинетов автоматически загружаются все кампании
2. Frontend → `/api/traffic/campaigns/list?ad_account_id=act_123`
3. Backend запрашивает Facebook Ads API
4. Показывает список с Toggle switch (вкл/выкл)
5. Сохранение → `/api/traffic/settings/save`

**Данные в БД**:
```json
{
  "tracked_campaigns": [
    {
      "id": "123456",
      "name": "Tripwire Dec 2025",
      "ad_account_id": "act_123456789",
      "enabled": true
    }
  ]
}
```

##### C. Настройки UTM меток

**Поля**:
- `utm_source` (например: facebook)
- `utm_medium` (например: cpc)
- `utm_content` (например: team_name)

**Как используется**:
- Для автоопределения команды по UTM меткам в AmoCRM
- Функция `identifyTrafficTeam()` в `backend/src/routes/traffic-stats.ts`

#### 4. **AI Рекомендации**

**Описание**: Персональные AI советы для улучшения результатов

**Файл**: `src/components/traffic/AIRecommendations.tsx`

**API**: `/api/facebook-ads/recommendations/generate`

**Вход**:
```json
{
  "team": "Kenesary",
  "spend": 1500,
  "revenue": 150000,
  "roas": 2.13,
  "sales": 30,
  "cpa": 50
}
```

**Выход**:
```json
{
  "recommendations": [
    "🎯 Ваш ROAS 2.13 - отлично! Продолжайте...",
    "💰 CPA $50 можно снизить до $40...",
    "📊 Попробуйте A/B тесты креативов..."
  ]
}
```

**Как работает**:
- Использует OpenAI API (GPT-4)
- Анализирует метрики команды
- Генерирует 3-5 конкретных рекомендаций
- Обновляется при изменении периода

---

## 👑 ФУНКЦИОНАЛ АДМИНПАНЕЛИ ДЛЯ АДМИНИСТРАТОРА

### URL: `https://traffic.onai.academy/login`

### Роль: `admin`

### Пользователь: **Александр** (владелец)

### Email: `admin@onai.academy`

### Что видит администратор:

#### ВСЁ ЧТО ЕСТЬ У ТАРГЕТОЛОГОВ + ДОПОЛНИТЕЛЬНО:

#### 1. **Конструктор Команд** (`/admin/team-constructor`)

**Описание**: Создание команд и пользователей

**Файл**: `src/pages/traffic/TrafficTeamConstructor.tsx`

**API**:
- `/api/traffic-auth/teams/create` - создать команду
- `/api/traffic-auth/users/create` - создать пользователя

##### A. Создание команды

**Поля**:
- `name` (например: New Team) - REQUIRED
- `company` (например: ABC Corp) - REQUIRED
- `direction` (например: tripwire) - REQUIRED
- `fb_ad_account_id` (например: act_123) - OPTIONAL
- `color` (например: #00FF88) - OPTIONAL
- `emoji` (например: 🔥) - OPTIONAL

**Что происходит**:
1. Frontend → POST `/api/traffic-auth/teams/create`
2. Backend валидирует данные
3. Создаёт запись в `traffic_teams` таблице
4. Возвращает новую команду

**БД**:
```sql
INSERT INTO traffic_teams (name, company, direction, fb_ad_account_id, color, emoji)
VALUES ('New Team', 'ABC Corp', 'tripwire', NULL, '#00FF88', '📊');
```

##### B. Создание пользователя

**Поля**:
- `email` (например: newuser@onai.academy) - REQUIRED
- `full_name` (например: New User) - REQUIRED
- `password` (минимум 6 символов) - REQUIRED
- `role` (`targetologist` или `admin`) - REQUIRED
- `team_id` (UUID команды из dropdown) - REQUIRED

**Что происходит**:
1. Frontend → POST `/api/traffic-auth/users/create`
2. Backend:
   - Проверяет что email не занят
   - Хэширует пароль (bcrypt)
   - Создаёт запись в `traffic_users`
3. Пользователь может сразу войти

**БД**:
```sql
INSERT INTO traffic_users (email, full_name, password_hash, role, team_id, team_name)
VALUES ('newuser@onai.academy', 'New User', '$2b$...', 'targetologist', 'uuid...', 'Team Name');
```

#### 2. **Безопасность** (`/admin/security`)

**Описание**: Трекинг всех входов, подозрительная активность

**Файл**: `src/pages/traffic/TrafficSecurityPanel.tsx`

**API**: `/api/traffic-security/*`

**3 вкладки**:

##### A. Все входы

**API**: `/api/traffic-security/sessions?days=7`

**Что показывает**:
- Email пользователя
- IP адрес
- Устройство (desktop/mobile/tablet)
- Браузер + версия
- ОС + версия
- Город, страна
- Дата и время входа

**Данные из**:
```sql
SELECT * FROM traffic_user_sessions 
WHERE login_at > NOW() - INTERVAL '7 days'
ORDER BY login_at DESC;
```

##### B. История IP

**API**: `/api/traffic-security/ip-history`

**Что показывает**:
- Для каждого пользователя:
  - Все уникальные IP адреса
  - Количество входов с каждого IP
  - Последний вход с этого IP
  - Город/страна

##### C. Подозрительная активность

**API**: `/api/traffic-security/suspicious`

**Критерии**:
- Более 3 разных IP адресов за 7 дней
- Более 3 разных устройств за 7 дней
- Вход из необычной страны
- Вход в необычное время (например, 3 AM)

**Авто-детекция**:
```sql
SELECT u.email, COUNT(DISTINCT s.ip_address) as unique_ips
FROM traffic_users u
JOIN traffic_user_sessions s ON u.id = s.user_id
WHERE s.login_at > NOW() - INTERVAL '7 days'
GROUP BY u.email
HAVING COUNT(DISTINCT s.ip_address) > 3;
```

**⚠️ ВАЖНО**: Эта функция требует миграцию!  
Файл: `supabase/migrations/20251219_create_traffic_sessions.sql`

#### 3. **Источники продаж** (`/admin/utm-sources`)

**Описание**: UTM аналитика, топ источники, продажи без меток

**Файл**: `src/pages/traffic/UTMSourcesPanel.tsx`

**API**: `/api/traffic-sales/*`

**4 вкладки**:

##### A. Обзор

**API**: `/api/traffic-sales/all`

**Что показывает**:
- Все продажи с UTM метками
- utm_source, utm_medium, utm_campaign, utm_content
- Дата продажи
- Команда (автоопределённая)
- Сумма

##### B. Топ источников

**API**: `/api/traffic-sales/top-sources`

**Что показывает**:
- Рейтинг по utm_source
- Количество продаж из каждого источника
- Total revenue

**Пример**:
```
1. facebook - 45 продаж - 225,000₸
2. google - 12 продаж - 60,000₸
3. instagram - 8 продаж - 40,000₸
```

##### C. Топ кампании

**API**: `/api/traffic-sales/top-campaigns`

**Что показывает**:
- Рейтинг по utm_campaign
- Количество продаж из каждой кампании
- Total revenue

##### D. Продажи без UTM

**API**: `/api/traffic-sales/no-utm`

**Что показывает**:
- ⚠️ Красные предупреждения
- Продажи где utm_source = NULL
- Проблема: не можем определить откуда пришёл клиент

**⚠️ ВАЖНО**: Эта функция требует миграцию!  
Файл: `supabase/migrations/20251219_create_all_sales_tracking.sql`

#### 4. **Настройки всех команд**

**Описание**: Админ может смотреть и редактировать настройки любого таргетолога

**Где**: `/settings` + dropdown выбора юзера

**Что видит**:
- Подключенные FB кабинеты каждого таргетолога
- Отслеживаемые кампании
- Настройки UTM меток
- Может всё редактировать

**API**: `/api/traffic/settings/:userId`

#### 5. **Onboarding Tour**

**Описание**: Интерактивное обучение при первом входе

**Файл**: `src/hooks/useOnboardingTour.ts`

**Библиотека**: Driver.js

**Как работает**:
1. Пользователь впервые логинится
2. Через 1.5 сек появляется модальное окно
3. 8 шагов для таргетолога / 6 шагов для админа
4. Подсветка элементов
5. Прогресс сохраняется в БД

**API**: `/api/traffic-onboarding/*`

**БД**:
```sql
INSERT INTO traffic_onboarding_progress (user_id, tour_type, is_completed)
VALUES ('uuid', 'targetologist', false);
```

**⚠️ ВАЖНО**: Эта функция требует миграцию!  
Файл: `supabase/migrations/20251219_create_onboarding_progress.sql`

---

## 📁 СТРУКТУРА ПРОЕКТА И ПУТИ К ФАЙЛАМ

### Корень проекта:
```
/Users/miso/onai-integrator-login/
```

### Frontend (React + Vite):

```
src/
├── App.tsx                          # Главный файл, все routes
├── main.tsx                         # Entry point
│
├── pages/
│   └── traffic/
│       ├── TrafficLogin.tsx         # Страница логина
│       ├── TrafficTargetologistDashboard.tsx  # Dashboard таргетолога
│       ├── TrafficDetailedAnalytics.tsx       # Детальная аналитика
│       ├── TrafficSettings.tsx                # Настройки
│       ├── TrafficAdminPanel.tsx              # Админ панель
│       ├── TrafficSecurityPanel.tsx           # Безопасность (админ)
│       ├── UTMSourcesPanel.tsx                # Источники продаж (админ)
│       └── TrafficTeamConstructor.tsx         # Конструктор команд (админ)
│
├── components/
│   └── traffic/
│       ├── TrafficCabinetLayout.tsx  # Layout с топ-баром
│       ├── AIRecommendations.tsx     # AI советы
│       └── OnAILogo.tsx              # Логотип
│
├── hooks/
│   ├── useOnboardingTour.ts         # Onboarding hook
│   └── useLanguage.ts               # Переключение языков
│
└── utils/
    └── apiClient.ts                 # Axios instance с JWT
```

### Backend (Node.js + Express):

```
backend/
├── src/
│   ├── server.ts                    # Entry point
│   │
│   ├── routes/
│   │   ├── traffic-auth.ts          # Авторизация + CRUD команд/юзеров
│   │   ├── traffic-stats.ts         # /api/traffic/combined-analytics
│   │   ├── traffic-security.ts      # Безопасность API
│   │   └── traffic-sales.ts         # UTM источники API
│   │
│   ├── services/
│   │   ├── facebookAdsService.ts    # FB Ads API wrapper
│   │   ├── amocrmService.ts         # AmoCRM API wrapper
│   │   └── openaiService.ts         # OpenAI API для AI рекомендаций
│   │
│   └── config/
│       └── supabase-tripwire.ts     # Supabase client
│
├── env.env                          # Environment variables
└── ecosystem.config.cjs             # PM2 конфиг
```

### Database Migrations:

```
supabase/migrations/
├── 20251219_create_traffic_sessions.sql      # Безопасность
├── 20251219_create_all_sales_tracking.sql    # UTM источники
└── 20251219_create_onboarding_progress.sql   # Onboarding
```

### Config Files:

```
/Users/miso/onai-integrator-login/
├── nginx-traffic.onai.academy.conf   # Nginx конфиг для поддомена
├── package.json                      # Frontend dependencies
├── vite.config.ts                    # Vite конфиг
└── backend/
    ├── package.json                  # Backend dependencies
    └── tsconfig.json                 # TypeScript конфиг
```

### Documentation:

```
/Users/miso/onai-integrator-login/
├── ТАРГЕТОЛОГИ_АККАУНТЫ.md          # Все аккаунты
├── АДМИНКА_ТАРГЕТОЛОГОВ_СТАТУС.md   # Статус админки
├── ДЕПЛОЙ_TRAFFIC_SUBDOMAIN.md      # Инструкция по деплою
├── КАРТОЧКА_KENESARY.txt            # Карточка для Kenesary
├── КАРТОЧКА_ARYSTAN.txt             # Карточка для Arystan
├── КАРТОЧКА_MUHA.txt                # Карточка для Muha
├── КАРТОЧКА_TRAF4.txt               # Карточка для Traf4
└── КАРТОЧКА_ADMIN.txt               # Карточка для админа
```

---

## 🗄️ БАЗА ДАННЫХ

### Supabase Project: Tripwire DB

**Project ID**: `pjmvxecykysfrzppdcto`

### Главные таблицы:

#### 1. `traffic_users`

**Описание**: Все пользователи системы (таргетологи + админы)

```sql
CREATE TABLE traffic_users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  email TEXT UNIQUE NOT NULL,
  full_name TEXT NOT NULL,
  password_hash TEXT NOT NULL,
  role TEXT NOT NULL,  -- 'targetologist' | 'admin'
  team_id UUID REFERENCES traffic_teams(id),
  team_name TEXT NOT NULL,
  is_active BOOLEAN DEFAULT true,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

**Текущие пользователи**:
```sql
-- Админ (Александр - владелец)
admin@onai.academy | Александр | admin

-- Таргетологи
kenesary@onai.academy | Kenesary | targetologist
arystan@onai.academy | Arystan | targetologist
muha@onai.academy | Muha | targetologist
traf4@onai.academy | Traf4 | targetologist
```

#### 2. `traffic_teams`

**Описание**: Команды таргетологов

```sql
CREATE TABLE traffic_teams (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name TEXT UNIQUE NOT NULL,
  company TEXT NOT NULL,
  direction TEXT NOT NULL,
  fb_ad_account_id TEXT,
  color TEXT DEFAULT '#00FF88',
  emoji TEXT DEFAULT '📊',
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

**Текущие команды**:
```sql
Kenesary | Nutcab | nutcab_tripwire | #00FF88 | 👑
Arystan  | Arystan | arystan | #3B82F6 | ⚡
Muha     | OnAI | onai_zapusk | #F59E0B | 🚀
Traf4    | ProfTest | proftest | #8B5CF6 | 🎯
```

#### 3. `traffic_targetologist_settings`

**Описание**: Настройки каждого таргетолога

```sql
CREATE TABLE traffic_targetologist_settings (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES traffic_users(id),
  
  -- Facebook настройки
  fb_ad_accounts JSONB DEFAULT '[]'::jsonb,
  fb_access_token TEXT,
  
  -- Отслеживаемые кампании
  tracked_campaigns JSONB DEFAULT '[]'::jsonb,
  
  -- UTM настройки
  utm_source TEXT DEFAULT 'facebook',
  utm_medium TEXT DEFAULT 'cpc',
  utm_content TEXT,
  utm_templates JSONB DEFAULT '[]'::jsonb,
  
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

**Пример данных**:
```json
{
  "user_id": "uuid-kenesary",
  "fb_ad_accounts": [
    {
      "id": "act_123456789",
      "name": "Nutcab Ads",
      "enabled": true
    }
  ],
  "tracked_campaigns": [
    {
      "id": "123456",
      "name": "Tripwire Dec 2025",
      "ad_account_id": "act_123456789",
      "enabled": true
    }
  ],
  "utm_source": "facebook",
  "utm_medium": "cpc"
}
```

#### 4. `traffic_user_sessions` ⚠️ ТРЕБУЕТ МИГРАЦИЮ

**Описание**: История всех входов (безопасность)

```sql
CREATE TABLE traffic_user_sessions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES traffic_users(id),
  email TEXT NOT NULL,
  team_name TEXT NOT NULL,
  role TEXT NOT NULL,
  
  -- IP и локация
  ip_address TEXT NOT NULL,
  ip_country TEXT,
  ip_city TEXT,
  
  -- Device info
  device_type TEXT, -- desktop/mobile/tablet
  device_fingerprint TEXT,
  browser_name TEXT,
  browser_version TEXT,
  os_name TEXT,
  os_version TEXT,
  
  -- Session info
  login_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  logout_at TIMESTAMP WITH TIME ZONE,
  
  -- Security flags
  is_suspicious BOOLEAN DEFAULT FALSE,
  suspicious_reason TEXT
);
```

#### 5. `all_sales` ⚠️ ТРЕБУЕТ МИГРАЦИЮ

**Описание**: Все продажи с UTM метками

```sql
CREATE TABLE all_sales (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  lead_id TEXT NOT NULL,
  
  -- UTM метки
  utm_source TEXT,
  utm_medium TEXT,
  utm_campaign TEXT,
  utm_content TEXT,
  utm_term TEXT,
  
  -- Автоопределённая команда
  identified_team TEXT,
  
  -- Финансы
  amount NUMERIC DEFAULT 5000,
  currency TEXT DEFAULT 'KZT',
  
  -- Даты
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  closed_at TIMESTAMP WITH TIME ZONE
);
```

#### 6. `traffic_onboarding_progress` ⚠️ ТРЕБУЕТ МИГРАЦИЮ

**Описание**: Прогресс обучения

```sql
CREATE TABLE traffic_onboarding_progress (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL,
  
  is_completed BOOLEAN DEFAULT false,
  current_step INTEGER DEFAULT 0,
  total_steps INTEGER DEFAULT 0,
  tour_type TEXT, -- 'targetologist' | 'admin'
  
  started_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  completed_at TIMESTAMP WITH TIME ZONE,
  
  view_count INTEGER DEFAULT 1,
  skip_count INTEGER DEFAULT 0
);
```

---

## 🔌 API ENDPOINTS

### Base URL (Production):
```
https://api.onai.academy
```

### Base URL (Local Dev):
```
http://localhost:3000
```

### Authentication:

Все запросы (кроме `/login`) требуют JWT токен в заголовке:

```javascript
headers: {
  'Authorization': `Bearer ${token}`
}
```

Токен получаем при логине и сохраняем в `localStorage`:

```javascript
const token = localStorage.getItem('traffic_token');
```

---

### 1. АУТЕНТИФИКАЦИЯ

#### POST `/api/traffic-auth/login`

**Описание**: Логин пользователя

**Body**:
```json
{
  "email": "kenesary@onai.academy",
  "password": "changeme123"
}
```

**Response**:
```json
{
  "token": "eyJhbGc...",
  "user": {
    "id": "uuid",
    "email": "kenesary@onai.academy",
    "fullName": "Kenesary",
    "role": "targetologist",
    "team": "Kenesary"
  }
}
```

**Файл**: `backend/src/routes/traffic-auth.ts`

---

### 2. АНАЛИТИКА

#### GET `/api/traffic/combined-analytics`

**Описание**: Главный endpoint для всех метрик (FB Ads + AmoCRM)

**Query Parameters**:
- `preset`: `7d` | `14d` | `30d` (default: `30d`)
- `date`: `YYYY-MM-DD` (для конкретного дня)

**Example**:
```
GET /api/traffic/combined-analytics?preset=30d
```

**Response**:
```json
{
  "success": true,
  "dateRange": "2024-11-19 to 2024-12-19",
  "exchangeRate": {
    "usdToKzt": 470
  },
  "totals": {
    "spend": 5000,
    "revenue": 750000,
    "sales": 150,
    "roas": 3.19,
    "cpa": 33.33,
    "impressions": 500000,
    "clicks": 10000,
    "ctr": 2.0
  },
  "teams": [
    {
      "team": "Kenesary",
      "spend": 1500,
      "revenue": 155000,
      "roas": 2.20,
      "sales": 31,
      "cpa": 48.39,
      "clicks": 3000,
      "impressions": 150000,
      "ctr": 2.0,
      "videoMetrics": {
        "plays": 50000,
        "completions": 10000,
        "completionRate": 20,
        "thruplayRate": 35,
        "avgWatchTime": 15.5
      },
      "topVideoCreatives": [
        {
          "name": "Video Ad 1",
          "plays": 10000,
          "completions": 3000,
          "completionRate": "30%"
        }
      ],
      "campaigns": [...]
    },
    ...
  ]
}
```

**Файл**: `backend/src/routes/traffic-stats.ts` (строки 497-1046)

**Как работает**:
1. Параллельно запрашивает FB Ads API и AmoCRM API
2. Facebook Ads → затраты, клики, показы, видео метрики
3. AmoCRM → платные лиды (продажи)
4. Группирует по командам (по UTM меткам)
5. Рассчитывает ROAS, CPA, CTR
6. Возвращает агрегированные данные

**Важно**: Использует функцию `identifyTrafficTeam()` для определения команды по UTM меткам

---

### 3. КОНСТРУКТОР КОМАНД (АДМИН)

#### POST `/api/traffic-auth/teams/create`

**Описание**: Создать новую команду

**Auth**: Только админ

**Body**:
```json
{
  "name": "New Team",
  "company": "ABC Corp",
  "direction": "tripwire",
  "fb_ad_account_id": "act_123",
  "color": "#00FF88",
  "emoji": "🔥"
}
```

**Response**:
```json
{
  "success": true,
  "team": {
    "id": "uuid",
    "name": "New Team",
    ...
  }
}
```

#### POST `/api/traffic-auth/users/create`

**Описание**: Создать нового пользователя

**Auth**: Только админ

**Body**:
```json
{
  "email": "newuser@onai.academy",
  "full_name": "New User",
  "password": "password123",
  "role": "targetologist",
  "team_id": "uuid"
}
```

**Response**:
```json
{
  "success": true,
  "user": {
    "id": "uuid",
    "email": "newuser@onai.academy",
    ...
  }
}
```

**Файл**: `backend/src/routes/traffic-auth.ts`

---

### 4. НАСТРОЙКИ

#### GET `/api/traffic/settings/:userId`

**Описание**: Получить настройки таргетолога

**Response**:
```json
{
  "success": true,
  "settings": {
    "fb_ad_accounts": [...],
    "tracked_campaigns": [...],
    "utm_source": "facebook",
    "utm_medium": "cpc"
  }
}
```

#### POST `/api/traffic/settings/save`

**Описание**: Сохранить настройки

**Body**:
```json
{
  "fb_ad_accounts": [...],
  "tracked_campaigns": [...],
  "utm_source": "facebook"
}
```

---

### 5. FACEBOOK ADS

#### GET `/api/traffic/fb-accounts/list`

**Описание**: Получить все FB кабинеты пользователя

**Response**:
```json
{
  "success": true,
  "accounts": [
    {
      "id": "act_123456789",
      "name": "Nutcab Ads",
      "account_status": 1
    }
  ]
}
```

#### GET `/api/traffic/campaigns/list?ad_account_id=act_123`

**Описание**: Получить все кампании кабинета

**Response**:
```json
{
  "success": true,
  "campaigns": [
    {
      "id": "123456",
      "name": "Tripwire Dec 2025",
      "status": "ACTIVE"
    }
  ]
}
```

**Файл**: `backend/src/services/facebookAdsService.ts`

---

### 6. AI РЕКОМЕНДАЦИИ

#### POST `/api/facebook-ads/recommendations/generate`

**Описание**: Генерация AI рекомендаций

**Body**:
```json
{
  "team": "Kenesary",
  "spend": 1500,
  "revenue": 155000,
  "roas": 2.13,
  "sales": 31,
  "cpa": 50
}
```

**Response**:
```json
{
  "success": true,
  "recommendations": [
    "🎯 Ваш ROAS 2.13 - отлично!...",
    "💰 CPA $50 можно снизить...",
    ...
  ]
}
```

**Файл**: `backend/src/services/openaiService.ts`

---

### 7. БЕЗОПАСНОСТЬ (АДМИН) ⚠️ ТРЕБУЕТ МИГРАЦИЮ

#### GET `/api/traffic-security/sessions?days=7`

**Описание**: Все входы за N дней

**Response**:
```json
{
  "success": true,
  "sessions": [
    {
      "email": "kenesary@onai.academy",
      "ip_address": "192.168.1.1",
      "device_type": "desktop",
      "browser_name": "Chrome",
      "os_name": "Mac OS",
      "login_at": "2025-12-19T10:00:00Z"
    }
  ]
}
```

#### GET `/api/traffic-security/suspicious`

**Описание**: Подозрительная активность

**Response**:
```json
{
  "success": true,
  "suspicious": [
    {
      "email": "kenesary@onai.academy",
      "unique_ips": 5,
      "unique_devices": 3,
      "last_login": "2025-12-19T10:00:00Z"
    }
  ]
}
```

**Файл**: `backend/src/routes/traffic-security.ts`

---

### 8. ИСТОЧНИКИ ПРОДАЖ (АДМИН) ⚠️ ТРЕБУЕТ МИГРАЦИЮ

#### GET `/api/traffic-sales/all`

**Описание**: Все продажи с UTM

#### GET `/api/traffic-sales/top-sources`

**Описание**: Топ источников

#### GET `/api/traffic-sales/top-campaigns`

**Описание**: Топ кампании

#### GET `/api/traffic-sales/no-utm`

**Описание**: Продажи без UTM меток

**Файл**: `backend/src/routes/traffic-sales.ts`

---

## 🔧 TROUBLESHOOTING GUIDE

### Проблема 1: Не работают данные (пустая таблица)

**Симптомы**:
- Таблица команд пустая
- Показывает "No data available"
- Loading бесконечно

**Причины**:
1. Backend не запущен
2. Facebook Ads API token истёк
3. AmoCRM API token истёк
4. Неверные UTM метки в AmoCRM

**Диагностика**:

```bash
# 1. Проверить backend
ssh root@207.154.231.30
pm2 list
# Должен быть: onai-backend | online

# 2. Проверить логи backend
pm2 logs onai-backend --lines 100

# 3. Найти ошибки FB Ads API
# Ищи: "FB Ads error" или "401" или "403"

# 4. Найти ошибки AmoCRM API
# Ищи: "AmoCRM error" или "Unauthorized"
```

**Решение**:

A. Если FB Ads token истёк:
```bash
# В env.env обновить:
FB_ACCESS_TOKEN=новый_токен

# Перезапустить backend:
pm2 restart onai-backend
```

B. Если AmoCRM token истёк:
```bash
# В env.env обновить:
AMOCRM_TOKEN=новый_токен

# Перезапустить:
pm2 restart onai-backend
```

C. Если UTM метки неверные:
- Проверь функцию `identifyTrafficTeam()` в `backend/src/routes/traffic-stats.ts` (строка 229)
- Добавь нужные паттерны для команд

---

### Проблема 2: Неверные цифры (расхождение данных)

**Симптомы**:
- Revenue не совпадает с AmoCRM
- ROAS показывает странные значения
- Продажи отличаются от реальных

**Причины**:
1. Разные временные диапазоны
2. Неверное определение команды по UTM
3. Курс валют устарел
4. Продажи без UTM меток не учитываются

**Диагностика**:

```bash
# Проверить логи с диагностикой:
ssh root@207.154.231.30
pm2 logs onai-backend --lines 200

# Найти:
# "🔍 === ДИАГНОСТИКА AMOCRM LEADS ==="
# "📊 Всего платных лидов: N"
# "⚠️ Unknown/Другие лиды: N"
```

**Решение**:

A. Проверить Unknown лиды:
- Логи покажут примеры UTM меток для Unknown лидов
- Добавь паттерны в `TRAFFIC_TEAM_PATTERNS` (traffic-stats.ts, строка 186)

B. Проверить временной диапазон:
- Frontend по умолчанию: 30 дней
- Убедись что сравниваешь одинаковый период

C. Обновить курс валют:
- Функция `getUSDtoKZTRate()` в backend
- Можно захардкодить или использовать API

---

### Проблема 3: Не работает конструктор админа

**Симптомы**:
- Кнопка "Создать команду" не работает
- Кнопка "Создать пользователя" не работает
- Ошибка 403 Forbidden

**Причины**:
1. Пользователь не админ (role !== 'admin')
2. JWT токен истёк
3. Backend route не настроен

**Диагностика**:

```javascript
// Открой DevTools Console
const user = JSON.parse(localStorage.getItem('traffic_user'));
console.log('Role:', user.role);
// Должно быть: admin
```

**Решение**:

A. Если role !== 'admin':
```sql
-- В Supabase исправить роль:
UPDATE traffic_users 
SET role = 'admin' 
WHERE email = 'admin@onai.academy';
```

B. Если JWT истёк:
- Выйти и войти заново
- Новый токен сохранится автоматически

---

### Проблема 4: Не работает настройка FB кабинетов

**Симптомы**:
- Кнопка "Загрузить кабинеты" не работает
- Показывает пустой список
- Ошибка "Failed to load accounts"

**Причины**:
1. FB Access Token неверный
2. У токена нет прав на чтение кабинетов
3. Токен привязан к другому пользователю

**Решение**:

A. Получить новый FB токен:
1. https://developers.facebook.com/tools/explorer
2. Select App → Выбрать приложение
3. Permissions → `ads_read`, `ads_management`
4. Generate Access Token
5. Скопировать токен

B. Обновить в backend:
```bash
ssh root@207.154.231.30
cd /var/www/onai-integrator-login-main/backend
nano env.env

# Изменить:
FB_ACCESS_TOKEN=новый_токен

# Сохранить и перезапустить:
pm2 restart onai-backend
```

---

### Проблема 5: Не работает безопасность/источники продаж

**Симптомы**:
- Вкладки "Безопасность" и "Источники продаж" пустые
- Ошибка "Table does not exist"
- 404 Not Found

**Причины**:
- **Не применены миграции БД!**

**Решение**:

1. Открой: https://supabase.com/dashboard
2. Выбери: Tripwire DB (pjmvxecykysfrzppdcto)
3. SQL Editor → New Query
4. Скопируй файл `supabase/migrations/20251219_create_traffic_sessions.sql` → Run
5. Скопируй файл `supabase/migrations/20251219_create_all_sales_tracking.sql` → Run
6. Скопируй файл `supabase/migrations/20251219_create_onboarding_progress.sql` → Run
7. ✅ Готово! Теперь всё должно работать

---

## 🚀 ДЕПЛОЙ НА DIGITAL OCEAN

### Server Info:
- IP: `207.154.231.30`
- OS: Ubuntu 22.04
- User: `root`
- SSH Key: `/Users/miso/.ssh/id_rsa`

### Важные пути на сервере:

```
/var/www/onai.academy/                    # Основной сайт (НЕ ТРОГАТЬ!)
/var/www/traffic.onai.academy/            # Traffic Dashboard (frontend)
/var/www/onai-integrator-login-main/backend/  # Backend
/etc/nginx/sites-available/               # Nginx конфиги
/etc/nginx/sites-enabled/                 # Активные конфиги
/var/log/nginx/                           # Логи Nginx
/root/.pm2/logs/                          # Логи PM2
```

---

### ДЕПЛОЙ FRONTEND (Traffic Dashboard)

#### Шаг 1: Build локально

```bash
# На твоём Mac:
cd /Users/miso/onai-integrator-login

# Убедись что всё собирается:
npm run build

# Должно появиться:
# dist/
#   ├── index.html
#   ├── assets/
#   └── images/
```

#### Шаг 2: Загрузить на сервер

```bash
# Создать backup (на всякий случай):
ssh root@207.154.231.30 "tar -czf /root/backup-traffic-$(date +%Y%m%d-%H%M).tar.gz /var/www/traffic.onai.academy/"

# Загрузить новые файлы:
rsync -avz --delete dist/ root@207.154.231.30:/var/www/traffic.onai.academy/

# Исправить права:
ssh root@207.154.231.30 "chown -R www-data:www-data /var/www/traffic.onai.academy/ && chmod -R 755 /var/www/traffic.onai.academy/"

# Перезагрузить Nginx:
ssh root@207.154.231.30 "systemctl reload nginx"
```

#### Шаг 3: Проверка

```bash
# Проверить что сайт работает:
curl -I https://traffic.onai.academy

# Должно быть:
# HTTP/2 200
# content-type: text/html
```

#### Быстрый деплой (One-liner):

```bash
cd /Users/miso/onai-integrator-login && \
npm run build && \
rsync -avz --delete dist/ root@207.154.231.30:/var/www/traffic.onai.academy/ && \
ssh root@207.154.231.30 "chown -R www-data:www-data /var/www/traffic.onai.academy/ && systemctl reload nginx" && \
echo "✅ FRONTEND DEPLOYED!"
```

---

### ДЕПЛОЙ BACKEND

#### Шаг 1: Build локально

```bash
# На твоём Mac:
cd /Users/miso/onai-integrator-login/backend

# Build:
npm run build

# Должно появиться:
# dist/
#   ├── server.js
#   ├── routes/
#   └── services/
```

#### Шаг 2: Загрузить на сервер

```bash
# Создать backup:
ssh root@207.154.231.30 "tar -czf /root/backup-backend-$(date +%Y%m%d-%H%M).tar.gz /var/www/onai-integrator-login-main/backend/dist/"

# Остановить backend:
ssh root@207.154.231.30 "cd /var/www/onai-integrator-login-main/backend && pm2 stop onai-backend"

# Загрузить новые файлы:
rsync -avz dist/ root@207.154.231.30:/var/www/onai-integrator-login-main/backend/dist/

# Запустить backend:
ssh root@207.154.231.30 "cd /var/www/onai-integrator-login-main/backend && pm2 start ecosystem.config.cjs && pm2 save"
```

#### Шаг 3: Проверка

```bash
# Проверить статус:
ssh root@207.154.231.30 "pm2 list"

# Должно быть:
# onai-backend | online | ...

# Проверить что API работает:
curl -I https://api.onai.academy/api/health

# Должно быть:
# HTTP/2 200
```

#### Быстрый деплой backend (One-liner):

```bash
cd /Users/miso/onai-integrator-login/backend && \
npm run build && \
ssh root@207.154.231.30 "cd /var/www/onai-integrator-login-main/backend && pm2 stop onai-backend" && \
rsync -avz dist/ root@207.154.231.30:/var/www/onai-integrator-login-main/backend/dist/ && \
ssh root@207.154.231.30 "cd /var/www/onai-integrator-login-main/backend && pm2 restart onai-backend && pm2 save" && \
echo "✅ BACKEND DEPLOYED!"
```

---

## 💻 РАБОТА С ТЕРМИНАЛАМИ

### Подключение к серверу:

```bash
# SSH подключение:
ssh root@207.154.231.30

# Если нужен пароль - он в 1Password или у Александра
```

### Основные команды на сервере:

#### PM2 (Process Manager):

```bash
# Список процессов:
pm2 list

# Логи:
pm2 logs onai-backend
pm2 logs onai-backend --lines 100

# Рестарт:
pm2 restart onai-backend

# Стоп:
pm2 stop onai-backend

# Старт:
pm2 start ecosystem.config.cjs

# Сохранить список процессов:
pm2 save

# Автозапуск при перезагрузке:
pm2 startup
```

#### Nginx:

```bash
# Проверить конфиг:
nginx -t

# Перезагрузить:
systemctl reload nginx

# Рестарт:
systemctl restart nginx

# Статус:
systemctl status nginx

# Логи:
tail -f /var/log/nginx/error.log
tail -f /var/log/nginx/traffic.onai.academy.access.log
```

#### SSL Certificates:

```bash
# Обновить сертификат:
certbot renew

# Получить новый для поддомена:
certbot certonly --nginx -d traffic.onai.academy

# Проверить expiry:
certbot certificates
```

#### Files & Permissions:

```bash
# Проверить права:
ls -la /var/www/traffic.onai.academy/

# Исправить права:
chown -R www-data:www-data /var/www/traffic.onai.academy/
chmod -R 755 /var/www/traffic.onai.academy/
```

---

### Локальная разработка:

#### Запуск backend:

```bash
cd /Users/miso/onai-integrator-login/backend

# Установить зависимости (если нужно):
npm install

# Запустить в dev режиме:
npm run dev

# Или просто:
npm start

# Backend запустится на: http://localhost:3000
```

#### Запуск frontend:

```bash
cd /Users/miso/onai-integrator-login

# Установить зависимости (если нужно):
npm install

# Запустить dev сервер:
npm run dev

# Frontend запустится на: http://localhost:5173
```

#### Одновременный запуск:

```bash
# Терминал 1:
cd /Users/miso/onai-integrator-login/backend && npm start

# Терминал 2:
cd /Users/miso/onai-integrator-login && npm run dev

# Открыть в браузере:
# http://localhost:5173/traffic/login
```

---

## 📝 PROMPT ДЛЯ АРХИТЕКТОРА (КОДЕРА)

### Контекст:

Ты архитектор проекта **Traffic Dashboard** для OnAI Academy. Твоя задача - поддерживать и улучшать систему аналитики для таргетологов и администратора.

### Твой доступ:

- **Локальный репозиторий**: `/Users/miso/onai-integrator-login/`
- **Git remote**: подключен к удалённому репозиторию
- **Supabase**: Tripwire DB (pjmvxecykysfrzppdcto)
- **Production**: https://traffic.onai.academy

### Твои полномочия:

✅ Можешь:
- Читать и изменять код
- Создавать новые компоненты
- Оптимизировать существующие функции
- Добавлять новые API endpoints
- Улучшать UI/UX
- Писать тесты
- Создавать миграции БД

❌ НЕ можешь (требуется согласование с Александром):
- Деплоить на production без проверки
- Менять структуру БД без миграций
- Удалять существующие функции
- Менять алгоритмы расчёта ROAS/CPA без тестирования

### Workflow:

1. **Получаешь задачу** от Александра
2. **Читаешь эту документацию** для контекста
3. **Анализируешь** какие файлы нужно изменить
4. **Делаешь изменения** локально
5. **Тестируешь** на `http://localhost:5173`
6. **Создаёшь Pull Request** или коммитишь в branch
7. **Докладываешь** Александру о результатах

### Важные правила:

#### 1. Перед началом работы:

```bash
# ВСЕГДА обновляй код:
git pull origin main

# Проверяй что backend и frontend запускаются:
cd backend && npm start    # Терминал 1
cd .. && npm run dev       # Терминал 2
```

#### 2. При изменении API:

- Обновляй документацию в этом файле
- Добавляй логирование для отладки
- Проверяй что frontend корректно обрабатывает новые данные

#### 3. При изменении БД:

- **ВСЕГДА** создавай миграцию (файл в `supabase/migrations/`)
- **НЕ** делай изменения напрямую в Supabase UI
- Называй миграции: `YYYYMMDD_description.sql`

#### 4. При деплое:

- **НЕ** деплой на production без проверки
- Сначала тестируй локально
- Потом создай preview деплой (если есть staging)
- Только потом production

#### 5. При возникновении проблем:

- Проверь логи: `pm2 logs onai-backend` на сервере
- Проверь консоль браузера (F12)
- Проверь Troubleshooting Guide в этой документации
- Если не можешь решить - спроси Александра

### Твои инструменты:

- **VS Code / Cursor**: редактор кода
- **Terminal**: для команд
- **Chrome DevTools**: для отладки frontend
- **Supabase UI**: для проверки БД
- **PM2 Logs**: для отладки backend
- **Эта документация**: для справки

### Часто выполняемые задачи:

#### Задача: Добавить новую метрику

1. Добавь расчёт в `/api/traffic/combined-analytics` (backend)
2. Добавь поле в интерфейс (frontend)
3. Протестируй что данные корректно отображаются
4. Обнови документацию

#### Задача: Исправить баг

1. Воспроизведи баг локально
2. Найди причину (логи, console)
3. Исправь код
4. Протестируй что баг исчез
5. Проверь что не сломал другие функции

#### Задача: Оптимизировать производительность

1. Найди узкое место (профилирование)
2. Измерь текущую производительность
3. Сделай оптимизацию
4. Измерь новую производительность
5. Убедись что улучшение значительное

### Контакты для вопросов:

- **Владелец**: Александр (admin@onai.academy)
- **Эта документация**: всегда актуальная справка

---

## ✅ CHECKLIST ДЛЯ АРХИТЕКТОРА

Перед началом работы над любой задачей, пройдись по этому чеклисту:

- [ ] Прочитал задачу и понял требования
- [ ] Обновил код (`git pull`)
- [ ] Запустил backend и frontend локально
- [ ] Проверил что всё работает ДО изменений
- [ ] Определил какие файлы нужно изменить
- [ ] Прочитал соответствующий раздел этой документации
- [ ] Сделал изменения
- [ ] Протестировал локально
- [ ] Проверил логи на ошибки
- [ ] Проверил console.log в браузере
- [ ] Убедился что не сломал существующий функционал
- [ ] Обновил документацию (если нужно)
- [ ] Закоммитил изменения с понятным commit message
- [ ] Доложил Александру о результатах

---

**Удачи в работе! Эта документация всегда актуальна и будет обновляться.**

**Вопросы → Александр**

