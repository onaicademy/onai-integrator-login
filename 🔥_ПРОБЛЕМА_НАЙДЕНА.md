# 🔥 ПРОБЛЕМА НАЙДЕНА: Дубликаты лидов в Telegram

## ❌ ПРИЧИНА:

### **8 процессов Node.js работают ОДНОВРЕМЕННО!**

```bash
$ ps aux | grep node
# Найдено 8 процессов node/nodemon

$ lsof -ti:3000
# Несколько процессов слушают порт 3000
```

**Результат:** Каждый запрос обрабатывается несколькими процессами одновременно!

- 1 лид → 8 процессов → 8 сообщений в Telegram! 💥
- То же самое с SMS (видно в логах - "Sending SMS" x3)

---

## 🎯 РЕШЕНИЕ:

### 1. **Убить ВСЕ процессы Node**
```bash
killall -9 node nodemon
```

### 2. **Запустить ОДИН процесс**
```bash
cd /Users/miso/onai-integrator-login/backend
npm run dev
```

### 3. **Проверить что запущен только 1 процесс**
```bash
ps aux | grep "[n]ode.*server" | wc -l
# Должно быть: 3 (nodemon + tsx + node worker)
```

---

## 📊 ЛОГИ подтверждают:

```
📱 Sending SMS via Mobizon: { recipient: '770***', textLength: 55 }
📱 Sending SMS via Mobizon: { recipient: '770***', textLength: 55 }
📱 Sending SMS via Mobizon: { recipient: '770***', textLength: 55 }
```

**3 SMS для одного лида** = 3+ процесса работают!

---

## ✅ ПОСЛЕ ИСПРАВЛЕНИЯ:

- ✅ Только 1 процесс Node.js
- ✅ 1 лид = 1 сообщение в Telegram
- ✅ 1 лид = 1 SMS
- ✅ Никаких дубликатов!

---

## 🛡️ КАК ПРЕДОТВРАТИТЬ В БУДУЩЕМ:

### **Используйте PM2 для управления процессами:**

```bash
# Install PM2
npm install -g pm2

# Start with PM2 (гарантирует 1 процесс)
pm2 start npm --name "onai-backend" -- run dev

# Check status
pm2 status

# View logs
pm2 logs onai-backend

# Stop
pm2 stop onai-backend

# Restart
pm2 restart onai-backend
```

**PM2 benefits:**
- ✅ Гарантирует только 1 инстанс
- ✅ Auto-restart on crash
- ✅ Логи в одном месте
- ✅ Мониторинг CPU/Memory

---

## 📝 КАК ЭТО ПРОИЗОШЛО:

Скорее всего:
1. Сервер перезапускали несколько раз
2. Старые процессы не завершились (npm run dev висят)
3. Каждый запуск создавал новый процесс
4. В итоге: 8 процессов работают параллельно!

---

## ⚠️ ВАЖНО:

**Всегда проверяй процессы перед запуском:**
```bash
# 1. Убей старые процессы
killall -9 node nodemon

# 2. Подожди 2 секунды
sleep 2

# 3. Запусти новый
npm run dev

# 4. Проверь что только 1 работает
ps aux | grep "[n]ode.*server"
```

---

**Проблема решена! Дубликатов больше не будет!** ✅



