# 📧 ПРОБЛЕМА С ОТПРАВКОЙ EMAIL - РЕШЕНИЕ

## 🔴 ПРОБЛЕМА ОБНАРУЖЕНА

Email не отправляются с формы профтеста потому что **RESEND_API_KEY содержит недействительный тестовый ключ**.

### Диагностика

#### 1. Текущее значение в `backend/env.env`:
```bash
RESEND_API_KEY=re_123456789012345678901234567890TESTKEY
```

Это явно **placeholder/тестовый ключ**, который не работает с настоящим Resend API.

#### 2. Результат тестовой отправки:
```
❌ RESEND API ОШИБКА:
   Error: {
     statusCode: 401,
     name: 'validation_error',
     message: 'API key is invalid'
   }
```

#### 3. Анализ логов backend (07:04:42):
```
📧 [Lead d82ec2dc-...] Sending Email to astaichenkova@mail.ru...
✅ [Lead d82ec2dc-...] Email sent successfully (ID: 839091fa-...)
📱 [Lead d82ec2dc-...] Sending SMS to +7 (701) 321-95-08...
✅ [Lead d82ec2dc-...] SMS sent successfully
```

**Важно**: Система показывает что email "отправлен успешно", но это **ложное срабатывание**. С недействительным API ключом письма реально не отправляются.

---

## ✅ РЕШЕНИЕ

### Шаг 1: Получите настоящий API ключ от Resend

1. **Зарегистрируйтесь на Resend**
   - Перейдите: [https://resend.com](https://resend.com)
   - Создайте аккаунт или войдите

2. **Создайте API ключ**
   - Перейдите в [API Keys](https://resend.com/api-keys)
   - Нажмите "Create API Key"
   - Задайте имя (например: "onAI Academy Production")
   - Скопируйте ключ (формат: `re_...`)

3. **Важно**: Сохраните ключ в безопасном месте - вы сможете увидеть его только один раз!

### Шаг 2: Обновите backend/env.env

Откройте файл и замените строку:

```bash
RESEND_API_KEY=re_ваш_настоящий_ключ_здесь
```

**Пример**:
```bash
RESEND_API_KEY=re_abc123xyz456def789ghi012jkl345mno
```

### Шаг 3: Обновите .env на сервере

Если вы деплоите на сервер, обновите там тоже:

```bash
# На локальной машине
scp backend/env.env onai-backend:/root/onai-academy-backend/

# Или через SSH
ssh onai-backend
cd /root/onai-academy-backend/
nano env.env  # Обновите RESEND_API_KEY
```

### Шаг 4: Перезапустите backend

```bash
ssh onai-backend "cd /root/onai-academy-backend && pm2 restart onai-backend"
```

Или локально:
```bash
cd backend
pm2 restart onai-backend
```

### Шаг 5: Проверьте работу

Запустите тестовый скрипт:

```bash
cd /Users/miso/onai-integrator-login/backend
npx tsx scripts/test-proftest-email.ts
```

**Ожидаемый результат**:
```
✅ ПИСЬМО УСПЕШНО ОТПРАВЛЕНО!
📬 Email ID: ...
💡 Проверьте почту (включая спам-папку)
🎉 Тест пройден! Email сервис работает корректно.
```

### Шаг 6: Проверьте профтест форму

1. Откройте: [https://onai.academy/proftest/kenesary](https://onai.academy/proftest/kenesary)
2. Пройдите тест
3. Заполните форму с email: **smmmcwin@gmail.com**
4. Через 10 минут проверьте почту (включая спам)

---

## 🧪 ТЕСТОВЫЕ ИНСТРУМЕНТЫ

### 1. Тест отправки email (локально)

Файл: `backend/scripts/test-proftest-email.ts`

```bash
cd backend
npx tsx scripts/test-proftest-email.ts
```

Отправит тестовое письмо на **smmmcwin@gmail.com**

### 2. Мониторинг логов на сервере

```bash
ssh onai-backend "pm2 logs onai-backend --lines 100"
```

Ищите строки:
- `📧 [Lead ...] Sending Email to ...`
- `✅ [Lead ...] Email sent successfully`
- `❌ [Lead ...] Email ERROR:`

---

## 📊 АРХИТЕКТУРА ОТПРАВКИ EMAIL

### Как это работает:

1. **Пользователь заполняет профтест** → POST `/api/landing/proftest`
2. **Данные сохраняются в Supabase** (landing_leads)
3. **Планируется отправка** через 10 минут (`scheduleProftestNotifications`)
4. **Через 10 минут выполняется**:
   - Создание записи в `unified_lead_tracking`
   - Отправка Email через **Resend API**
   - Отправка SMS через **Mobizon API**
5. **Обновление статусов** в БД

### Файлы:

- **Backend роутер**: `backend/src/routes/landing.ts` (строка 582-747)
- **Сервис уведомлений**: `backend/src/services/scheduledNotifications.ts`
- **Email шаблон**: `backend/src/templates/proftest-result-email.ts`
- **Тестовый скрипт**: `backend/scripts/test-proftest-email.ts`

---

## 🎯 ПОЧЕМУ ЭТО ПРОИСХОДИТ?

1. **RESEND_API_KEY был placeholder** - кто-то добавил тестовый ключ для разработки
2. **Код работает правильно** - проверяет ошибки, логирует все этапы
3. **Resend API возвращает 401** при неверном ключе, но в продакшене это не залогировано как критическая ошибка
4. **Email "отправляются" но не доставляются** - система считает что все ОК

---

## ✅ ПОСЛЕ ИСПРАВЛЕНИЯ

После добавления настоящего RESEND_API_KEY:

1. ✅ Email будут отправляться реально
2. ✅ Пользователи будут получать письма через 10 минут после профтеста
3. ✅ SMS будут отправляться (Mobizon уже работает)
4. ✅ Все данные сохраняются в AmoCRM

---

## 💡 ДОПОЛНИТЕЛЬНЫЕ РЕКОМЕНДАЦИИ

### Настройка домена в Resend

Для лучшей доставляемости:

1. В Resend добавьте ваш домен `onai.academy`
2. Настройте DNS записи (SPF, DKIM, DMARC)
3. Подтвердите домен
4. Используйте адрес `noreply@onai.academy` (уже настроен в коде)

### Мониторинг отправок

В панели Resend вы сможете видеть:
- Сколько писем отправлено
- Статусы доставки
- Открытия писем
- Клики по ссылкам

---

## 🚀 БЫСТРЫЙ СТАРТ

```bash
# 1. Получите ключ на resend.com
# 2. Обновите backend/env.env
echo "RESEND_API_KEY=re_ваш_ключ" >> backend/env.env

# 3. Обновите на сервере
scp backend/env.env onai-backend:/root/onai-academy-backend/

# 4. Перезапустите
ssh onai-backend "pm2 restart onai-backend"

# 5. Проверьте
cd backend
npx tsx scripts/test-proftest-email.ts
```

---

## 📞 КОНТАКТЫ

Если возникнут проблемы:
- Проверьте логи: `pm2 logs onai-backend`
- Проверьте Resend панель: [resend.com/emails](https://resend.com/emails)
- Проверьте спам-папку получателя

---

**Дата диагностики**: 14 декабря 2025  
**Тестовый email**: smmmcwin@gmail.com  
**Статус**: 🔴 Требуется настройка RESEND_API_KEY





