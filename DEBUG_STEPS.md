# 🔧 Инструкция по отладке проблемы с загрузкой студентов

## Шаг 1: Открой DevTools → Network

1. Нажми **F12** (откроется DevTools)
2. Перейди на вкладку **Network** (Сеть)
3. Нажми **Ctrl + Shift + R** (жёсткая перезагрузка)
4. Посмотри на запросы, которые идут к Supabase

---

## Шаг 2: Найди запрос к `auth/v1/user`

В списке запросов найди:
```
https://arqhkacellqbhjhbebfh.supabase.co/auth/v1/user
```

Кликни на него и посмотри:
- **Status**: должен быть `200` (если `401` или `403` - проблема с авторизацией)
- **Response**: должен содержать данные пользователя

Скриншот этого запроса!

---

## Шаг 3: Найди запрос к `rest/v1/profiles`

В списке запросов найди:
```
https://arqhkacellqbhjhbebfh.supabase.co/rest/v1/profiles
```

Кликни на него и посмотри:
- **Status**: должен быть `200`
- **Response**: должен содержать массив студентов `[{...}, {...}]`

Если ответ пустой `[]` - значит проблема в RLS или данных нет.

Скриншот этого запроса!

---

## Шаг 4: Проверь localStorage

В консоли (Console) выполни:
```javascript
localStorage.getItem('sb-arqhkacellqbhjhbebfh-auth-token')
```

Должен вернуться JSON с токеном. Если `null` - сессия протухла.

---

## Шаг 5: Проверь таблицу profiles в Supabase Dashboard

1. Открой https://supabase.com/dashboard
2. Выбери проект `arqhkacellqbhjhbebfh`
3. Перейди в **Table Editor** → `profiles`
4. Посмотри, сколько записей там есть

Если 0 записей - студенты не создались.
Если есть записи - проблема в RLS правилах.

---

## 📤 Что прислать мне:

1. ✅ Скриншот вкладки Network с запросами к Supabase
2. ✅ Скриншот ответа от `/auth/v1/user` (Status + Response)
3. ✅ Скриншот ответа от `/rest/v1/profiles` (Status + Response)
4. ✅ Результат команды `localStorage.getItem('sb-arqhkacellqbhjhbebfh-auth-token')` из консоли
5. ✅ Скриншот таблицы `profiles` из Supabase Dashboard (сколько записей)

