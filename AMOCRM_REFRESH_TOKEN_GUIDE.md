# 🔄 Автоматическое обновление токенов amoCRM (Refresh Token)

## 🎯 Зачем это нужно?

**Проблема:** Access Token amoCRM живёт всего **24 часа**. После этого интеграция перестаёт работать.

**Решение:** Refresh Token автоматически обновляет токены без вашего участия!

---

## ✅ Что было добавлено

### 1. Автоматическое обновление токенов
- При ошибке 401 (токен истёк) → автоматически запрашивается новый
- Используется Refresh Token для получения новой пары токенов
- Оригинальный запрос автоматически повторяется с новым токеном

### 2. Axios Interceptors
- **Request Interceptor**: автоматически добавляет текущий токен к каждому запросу
- **Response Interceptor**: перехватывает 401 и запускает обновление токена

### 3. Очередь запросов
- Если несколько запросов пришли одновременно во время обновления токена
- Они добавляются в очередь и выполняются после обновления
- Не создаётся 10 параллельных запросов на обновление токена

### 4. Сохранение токенов в памяти
- Новые токены сохраняются в переменные
- TODO: добавить сохранение в БД для персистентности между перезапусками

---

## 🔧 Настройка OAuth интеграции в amoCRM

### Шаг 1: Создайте интеграцию

1. Зайдите в amoCRM: **Настройки → Интеграции**
2. Нажмите **"Создать интеграцию"**
3. Заполните форму:
   - **Название**: onAI Academy Integration
   - **Redirect URI**: `https://onai.academy` (или ваш домен)
   - **Права доступа**: выберите минимум:
     - Контакты (чтение)
     - Сделки (чтение и запись)

4. Сохраните интеграцию

5. Скопируйте полученные данные:
   - **Client ID** (UUID формата)
   - **Client Secret** (длинная строка)

### Шаг 2: Получите Refresh Token

#### Вариант А: Через OAuth Flow (Рекомендуется)

1. Откройте в браузере (замените значения):
   ```
   https://yoursubdomain.amocrm.ru/oauth?
     client_id=YOUR_CLIENT_ID&
     redirect_uri=https://onai.academy&
     response_type=code&
     state=random_string
   ```

2. Авторизуйтесь в amoCRM

3. Вы будете перенаправлены на:
   ```
   https://onai.academy/?code=AUTHORIZATION_CODE&state=random_string
   ```

4. Скопируйте `AUTHORIZATION_CODE` из URL

5. Обменяйте код на токены:
   ```bash
   curl -X POST https://yoursubdomain.amocrm.ru/oauth2/access_token \
     -H "Content-Type: application/json" \
     -d '{
       "client_id": "YOUR_CLIENT_ID",
       "client_secret": "YOUR_CLIENT_SECRET",
       "grant_type": "authorization_code",
       "code": "AUTHORIZATION_CODE",
       "redirect_uri": "https://onai.academy"
     }'
   ```

6. В ответе получите:
   ```json
   {
     "token_type": "Bearer",
     "expires_in": 86400,
     "access_token": "eyJ...",
     "refresh_token": "def..."
   }
   ```

7. Скопируйте оба токена!

#### Вариант Б: Через Postman (Проще)

1. Установите [Postman](https://www.postman.com/)
2. Создайте новый запрос GET к amoCRM API
3. Во вкладке **Authorization**:
   - Type: **OAuth 2.0**
   - Grant Type: **Authorization Code**
   - Callback URL: `https://onai.academy`
   - Auth URL: `https://yoursubdomain.amocrm.ru/oauth`
   - Access Token URL: `https://yoursubdomain.amocrm.ru/oauth2/access_token`
   - Client ID: ваш Client ID
   - Client Secret: ваш Client Secret

4. Нажмите **"Get New Access Token"**
5. Авторизуйтесь в amoCRM
6. Postman покажет оба токена

### Шаг 3: Добавьте в .env

Добавьте в `backend/.env`:

```bash
# OAuth Credentials
AMOCRM_CLIENT_ID=abc123-def456-ghi789
AMOCRM_CLIENT_SECRET=SuperSecretKey123456789
AMOCRM_REFRESH_TOKEN=def50200a1b2c3d4e5f6...
AMOCRM_REDIRECT_URI=https://onai.academy

# Также обновите Access Token
AMOCRM_ACCESS_TOKEN=eyJ0eXAiOiJKV1QiLCJhbGc...
```

### Шаг 4: Перезапустите бэкенд

```bash
# В режиме разработки
npm run dev

# В продакшене
pm2 restart backend
```

---

## 🧪 Тестирование

### Тест 1: Проверка конфигурации

```bash
# Запустите тестовый скрипт
npx ts-node backend/src/scripts/test-amocrm.ts
```

Должно вывести:
```
✅ AMOCRM_CLIENT_ID: abc123-def456-ghi789
✅ AMOCRM_CLIENT_SECRET: ***скрыто***
✅ AMOCRM_REFRESH_TOKEN: def50200a1b2c3d4e5f6...
✅ Все переменные настроены!
✅ Подключение к amoCRM успешно!
```

### Тест 2: Ручное обновление токена

Запустите Node.js консоль:

```bash
node
```

Выполните:

```javascript
const amoCrm = require('./backend/src/services/amoCrmService');

// Ручное обновление токена (для теста)
await amoCrm.manualRefreshToken();

// Должно вывести:
// 🔄 [AmoCRM] Токен истёк, запрашиваем новый через Refresh Token...
// ✅ [AmoCRM] Токены успешно обновлены!
```

### Тест 3: Симуляция истёкшего токена

1. Временно установите неправильный Access Token в `.env`:
   ```bash
   AMOCRM_ACCESS_TOKEN=invalid_token_for_testing
   ```

2. Перезапустите бэкенд

3. Попробуйте завершить урок или выполнить:
   ```bash
   node
   const amoCrm = require('./backend/src/services/amoCrmService');
   await amoCrm.testConnection();
   ```

4. В логах должно появиться:
   ```
   🔄 [AmoCRM] Токен истёк, запрашиваем новый через Refresh Token...
   ✅ [AmoCRM] Токены успешно обновлены!
   ✅ [AmoCRM] Подключение успешно!
   ```

5. Верните правильный токен в `.env`

---

## 🔥 Как это работает

### Схема автообновления:

```
1. Запрос к amoCRM API
         ↓
2. Ошибка 401 (токен истёк)
         ↓
3. Response Interceptor перехватывает ошибку
         ↓
4. Проверка: уже обновляется? 
   → Да: добавить в очередь
   → Нет: продолжить
         ↓
5. POST /oauth2/access_token с Refresh Token
         ↓
6. Получили новые токены
         ↓
7. Сохранили в память (и БД - TODO)
         ↓
8. Обработали очередь ожидающих запросов
         ↓
9. Повторили оригинальный запрос с новым токеном
         ↓
10. ✅ Запрос успешен!
```

### Код (упрощённо):

```typescript
// Response Interceptor
amoClient.interceptors.response.use(
  (response) => response,
  async (error) => {
    // Если 401 и ещё не пытались обновить
    if (error.response?.status === 401 && !originalRequest._retry) {
      originalRequest._retry = true;
      
      // Обновляем токен
      const refreshed = await refreshAccessToken();
      
      if (refreshed) {
        // Повторяем запрос с новым токеном
        originalRequest.headers.Authorization = `Bearer ${newToken}`;
        return amoClient(originalRequest);
      }
    }
    
    return Promise.reject(error);
  }
);
```

---

## ⚠️ Важные моменты

### 1. Refresh Token тоже истекает!

**Срок жизни:** Обычно 3 месяца (зависит от настроек amoCRM)

**Что делать:**
- Refresh Token автоматически обновляется при каждом запросе на обновление Access Token
- Если приложение работает, Refresh Token будет постоянно актуальным
- Если приложение не работало 3 месяца → нужна повторная OAuth авторизация

### 2. Сохранение токенов в БД (TODO)

Сейчас токены хранятся в памяти. При перезапуске бэкенда они **сбросятся** к значениям из `.env`.

**Для продакшена добавьте:**

```typescript
// backend/src/services/amoCrmService.ts

async function saveTokens(accessToken: string, refreshToken: string): Promise<void> {
  currentAccessToken = accessToken;
  currentRefreshToken = refreshToken;
  
  // 🔥 ДОБАВЬТЕ СОХРАНЕНИЕ В БД
  await supabase
    .from('system_settings')
    .update({
      amocrm_access_token: accessToken,
      amocrm_refresh_token: refreshToken,
      updated_at: new Date().toISOString(),
    })
    .eq('key', 'amocrm_tokens');
  
  console.log('💾 [AmoCRM] Токены сохранены в БД');
}
```

Также добавьте загрузку токенов при старте:

```typescript
// При инициализации сервиса
async function loadTokensFromDB(): Promise<void> {
  const { data } = await supabase
    .from('system_settings')
    .select('amocrm_access_token, amocrm_refresh_token')
    .eq('key', 'amocrm_tokens')
    .single();
  
  if (data) {
    currentAccessToken = data.amocrm_access_token || currentAccessToken;
    currentRefreshToken = data.amocrm_refresh_token || currentRefreshToken;
    console.log('📂 [AmoCRM] Токены загружены из БД');
  }
}

// Вызовите при запуске приложения
await loadTokensFromDB();
```

### 3. Ручная авторизация всё ещё может понадобиться

**Когда:**
- Refresh Token истёк (не использовался 3 месяца)
- Интеграция была деактивирована в amoCRM
- Изменились права доступа интеграции

**Что делать:**
- Повторите OAuth Flow (Шаг 2 выше)
- Получите новые токены
- Обновите `.env`

---

## 📊 Мониторинг

### Логи успешного обновления:

```
🔄 [AmoCRM] Токен истёк, запрашиваем новый через Refresh Token...
✅ [AmoCRM] Токены успешно обновлены!
   - Access Token: eyJ0eXAiOiJKV1QiLCJh...
   - Refresh Token: def50200a1b2c3d4e5...
💾 [AmoCRM] Токены обновлены в памяти (TODO: сохранить в БД)
✅ [AmoCRM] Сделка 789012 успешно перемещена на этап 12345678
```

### Логи ошибок:

```
❌ [AmoCRM] Не удалось обновить токен: {
  message: 'Request failed with status code 400',
  status: 400,
  data: { error: 'invalid_grant', hint: 'Refresh token expired' }
}
💡 [AmoCRM] Возможно, Refresh Token истёк или интеграция деактивирована.
   Необходима повторная OAuth авторизация через amoCRM.
```

**Действия при ошибке:**
1. Проверьте статус интеграции в amoCRM
2. Повторите OAuth Flow для получения новых токенов
3. Обновите `.env` и перезапустите бэкенд

---

## 🎉 Преимущества нового подхода

### ❌ Было (без Refresh Token):
- Токен работает 24 часа
- Каждый день нужно вручную обновлять
- Ночью интеграция перестаёт работать
- Студенты завершают уроки → сделки не обновляются

### ✅ Стало (с Refresh Token):
- Токены обновляются автоматически
- Работает бесконечно долго (пока используется)
- Никакого ручного вмешательства
- Надёжная интеграция 24/7

---

## 🆘 Troubleshooting

### Ошибка: "AMOCRM_REFRESH_TOKEN не задан"

**Решение:** Пройдите OAuth Flow (Шаг 2) для получения Refresh Token

---

### Ошибка: "invalid_grant" при обновлении токена

**Причины:**
1. Refresh Token истёк (не использовался 3 месяца)
2. Интеграция деактивирована в amoCRM
3. Изменились права доступа

**Решение:** Повторите OAuth Flow для получения новых токенов

---

### Токены не сохраняются между перезапусками

**Причина:** Токены хранятся только в памяти

**Решение:** Реализуйте сохранение в БД (см. раздел "Сохранение токенов в БД" выше)

---

### Интеграция всё ещё падает через 24 часа

**Проверьте:**
1. Все OAuth переменные заданы в `.env`
2. Логи показывают попытку обновления токена
3. Refresh Token актуален (получен недавно)

---

## 📚 Полезные ссылки

- **OAuth в amoCRM:** https://www.amocrm.ru/developers/content/oauth/step-by-step
- **Обновление токенов:** https://www.amocrm.ru/developers/content/oauth/easy-auth
- **Интеграции amoCRM:** https://www.amocrm.ru/settings/integrations/

---

## ✅ Чеклист готовности

- [ ] Создана интеграция в amoCRM
- [ ] Получены Client ID и Client Secret
- [ ] Пройден OAuth Flow, получены токены
- [ ] Все переменные добавлены в `backend/.env`:
  - `AMOCRM_CLIENT_ID`
  - `AMOCRM_CLIENT_SECRET`
  - `AMOCRM_REFRESH_TOKEN`
  - `AMOCRM_ACCESS_TOKEN`
  - `AMOCRM_REDIRECT_URI`
- [ ] Бэкенд перезапущен
- [ ] Тест `testConnection()` → успешно
- [ ] Тест `manualRefreshToken()` → успешно
- [ ] (Опционально) Реализовано сохранение токенов в БД

---

**Готово!** Теперь интеграция с amoCRM работает бесконечно долго без вашего участия! 🎉
