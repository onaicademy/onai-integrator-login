# 🔧 ДИАГНОСТИКА: Кнопка "Создать модуль"

## ✅ BACKEND ПРОТЕСТИРОВАН - РАБОТАЕТ!

```powershell
# Тест показал что модуль создаётся успешно:
POST http://localhost:3000/api/modules
✅ Status: 200 OK
✅ Module created: ID 16, order_index 15
```

**Backend работает отлично!** Проблема в Frontend.

---

## 🔍 ЧТО ПРОВЕРИТЬ:

### Шаг 1: Проверь Console (F12)

1. Открой страницу курса: `http://localhost:8080/course/1`
2. Открой Console (F12)
3. Проверь логи при загрузке страницы:

**Должно быть:**
```
🔥 Course.tsx - userRole: admin
🔥 Course.tsx - isAdmin: true
```

**Если НЕТ:**
- ❌ `userRole: null` или `userRole: student` → **Ты не залогинен как admin!**
- ❌ `isAdmin: false` → **Роль не admin!**

**Решение:** Войди как admin (saint@onaiacademy.kz)

---

### Шаг 2: Проверь видна ли кнопка "Добавить модуль"

1. На странице курса найди секцию **"Модули курса"**
2. Справа от заголовка должна быть **зелёная кнопка** с текстом:
   - На desktop: **"Добавить модуль"**
   - На mobile: **"Модуль"**

**Если кнопки НЕТ:**
- Проверь Console → должно быть `isAdmin: true`
- Сделай Hard Refresh: `Ctrl + Shift + R`

---

### Шаг 3: Нажми на кнопку и проверь Console

1. Нажми **"Добавить модуль"**
2. В Console должны появиться логи:

```
=======================================
🔥 handleAddModule вызвана
🔥 courseId: 1
🔥 moduleDialog before: { open: false, module: null }
=======================================
✅ setModuleDialog вызван с open: true
=======================================
🔥 ModuleEditDialog render
🔥 open: true
🔥 module: null
🔥 courseId: 1
=======================================
```

**Если логи есть → диалог открывается!**

**Если логов НЕТ:**
- Кнопка не кликабельна (возможно перекрыта другим элементом)
- JavaScript ошибка блокирует клик

---

### Шаг 4: Проверь открылся ли диалог

После клика должен появиться **модальный диалог** с:
- Заголовок: **"Создать новый модуль"** 📦
- Поле: **"Название модуля"** *
- Поле: **"Описание модуля"** (опционально)
- Кнопки: **"Отмена"** и **"✓ Сохранить модуль"**

**Если диалог НЕ открылся:**
- Проверь Console на ошибки
- Проверь что `moduleDialog.open = true` в логах
- Возможно z-index проблема - диалог за другими элементами

---

## 🧪 РУЧНОЙ ТЕСТ BACKEND (если нужно):

```powershell
# Создай модуль через API напрямую:
$body = @{ 
  title = 'Test Module Manual'; 
  description = 'Created via API'; 
  course_id = 1 
} | ConvertTo-Json

Invoke-RestMethod -Uri "http://localhost:3000/api/modules" `
  -Method Post `
  -Body $body `
  -ContentType "application/json"
```

**Если модуль создался → Backend работает, проблема в Frontend!**

---

## 🎯 ВОЗМОЖНЫЕ ПРИЧИНЫ:

### 1️⃣ Не залогинен как Admin
**Симптомы:**
- Кнопки "Добавить модуль" нет
- Console: `isAdmin: false`

**Решение:**
- Войди как admin: `saint@onaiacademy.kz`
- Проверь в Supabase Dashboard → Authentication → Users → роль должна быть `admin`

### 2️⃣ Z-index перекрытие
**Симптомы:**
- Кнопка видна, но не кликается
- Нет логов в Console при клике

**Решение:**
```typescript
// В Course.tsx кнопка уже имеет:
style={{ zIndex: 101, pointerEvents: 'auto' }}
```

Если не помогает - проверь что над кнопкой нет других элементов (Inspect Element)

### 3️⃣ Диалог открывается но не виден
**Симптомы:**
- Console показывает `open: true`
- Но диалога не видно на экране

**Решение:**
```typescript
// В ModuleEditDialog.tsx уже установлен:
style={{ zIndex: 10001 }}
```

Проверь что нет CSS, который скрывает диалог

### 4️⃣ JavaScript ошибка
**Симптомы:**
- Console показывает красные ошибки
- Клики не работают

**Решение:**
- Покажи ошибки из Console
- Перезапусти Frontend: `npm run dev`

---

## 📋 ЧЕКЛИСТ ПРОВЕРКИ:

```
□ Backend запущен (http://localhost:3000)
□ Frontend запущен (http://localhost:8080)
□ Залогинен как admin (saint@onaiacademy.kz)
□ Console показывает: isAdmin: true
□ Кнопка "Добавить модуль" видна
□ При клике появляются логи в Console
□ Диалог открывается
□ Можно ввести название и сохранить
```

---

## 🚀 ЕСЛИ ВСЁ ПРОВЕРИЛ И НЕ РАБОТАЕТ:

**Пришли мне:**
1. Скриншот страницы курса
2. Скриншот Console (F12) со всеми логами
3. Скажи что именно происходит:
   - Кнопки нет?
   - Кнопка есть но не кликается?
   - Диалог не открывается?
   - Диалог открывается но не сохраняет?

Я найду проблему!

---

**ДАВАЙ ПРОВЕРИМ ПО ШАГАМ!** 🔥

