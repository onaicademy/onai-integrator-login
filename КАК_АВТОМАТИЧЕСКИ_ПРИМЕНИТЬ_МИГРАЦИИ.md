# 🤖 КАК АВТОМАТИЧЕСКИ ПРИМЕНИТЬ МИГРАЦИИ

**Для автоматического применения нужен Service Role Key!**

---

## 📝 ЧТО НУЖНО:

### 1. Service Role Key из Supabase

```
1. Открой: https://supabase.com/dashboard/project/capdjvokjdivxjfddmx/settings/api
2. Найди раздел "Service Role Key"
3. Скопируй ключ (начинается с eyJ...)
4. ⚠️ НЕ ПУБЛИКУЙ ЕГО! Это секретный ключ!
```

### 2. Project Ref (ID проекта)

```
Твой Project Ref: capdjvokjdivxjfddmx
```

---

## 🚀 СПОСОБ 1: ЧЕРЕЗ SUPABASE CLI (РЕКОМЕНДУЮ)

### Установка CLI:

```bash
# macOS
brew install supabase/tap/supabase

# Проверка
supabase --version
```

### Применение миграций:

```bash
# 1. Логин
supabase login

# 2. Линк проекта
supabase link --project-ref capdjvokjdivxjfddmx

# 3. Применить ВСЕ миграции
supabase db push

# ГОТОВО! ✅
```

**ЭТО САМЫЙ ПРОСТОЙ СПОСОБ!**

---

## 🛠️ СПОСОБ 2: ЧЕРЕЗ API (АВТОМАТИЧЕСКИ)

### Скрипт для автоматического применения:

Создам файл `auto-migrate.sh`:

```bash
#!/bin/bash

# Конфиг
PROJECT_REF="capdjvokjdivxjfddmx"
SERVICE_ROLE_KEY="ВАШ_SERVICE_ROLE_KEY_ЗДЕСЬ"
SUPABASE_URL="https://$PROJECT_REF.supabase.co"

# Цвета
GREEN='\033[0;32m'
RED='\033[0;31m'
NC='\033[0m'

echo "🚀 Автоматическое применение миграций..."
echo ""

# Список миграций
migrations=(
  "supabase/migrations/20251103164013_*.sql"
  "supabase/migrations/20251103182016_*.sql"
  "supabase/migrations/20251104_add_diagnostics_tables.sql"
  "supabase/migrations/20250115_add_ai_curator_chat_tables.sql"
  "supabase/migrations/20250116_achievements_system.sql"
  "supabase/migrations/20250117_students_management_system.sql"
  "supabase/migrations/20250108_ai_mentor_analyst.sql"
  "supabase/migrations/20250109_token_usage_tracking.sql"
  "supabase/migrations/20250110_student_messaging.sql"
)

# Применяем каждую миграцию
for migration in "${migrations[@]}"; do
  filename=$(basename "$migration")
  echo "📄 Применяю: $filename"
  
  # Читаем SQL из файла
  sql_content=$(cat $migration)
  
  # Отправляем через API
  response=$(curl -s -X POST \
    "$SUPABASE_URL/rest/v1/rpc/exec_sql" \
    -H "apikey: $SERVICE_ROLE_KEY" \
    -H "Authorization: Bearer $SERVICE_ROLE_KEY" \
    -H "Content-Type: application/json" \
    -d "{\"query\": $(echo "$sql_content" | jq -Rs .)}")
  
  if [ $? -eq 0 ]; then
    echo -e "${GREEN}✅ Успешно${NC}"
  else
    echo -e "${RED}❌ Ошибка${NC}"
  fi
  echo ""
done

echo "🎉 Готово!"
```

### Использование:

```bash
# 1. Добавь Service Role Key в скрипт
nano auto-migrate.sh

# 2. Сделай исполняемым
chmod +x auto-migrate.sh

# 3. Запусти
./auto-migrate.sh
```

---

## ⚠️ ПРОБЛЕМА С АВТОМАТИЗАЦИЕЙ:

### Почему AI не может сам применить?

1. **Нет доступа к твоему Supabase аккаунту** 🔐
   - Требуется логин/пароль или токен
   - Это твои секретные данные

2. **Нет Service Role Key** 🔑
   - Это очень секретный ключ
   - AI не должен его знать (безопасность!)

3. **Интерактивная авторизация** 🖥️
   - Supabase CLI требует подтверждения в браузере
   - AI не может открыть браузер

4. **Безопасность** 🛡️
   - Применение миграций = изменение БД
   - Должно быть под твоим контролем
   - Иначе риск потери данных!

---

## ✅ ЧТО AI СДЕЛАЛ:

1. ✅ Создал 9 SQL файлов миграций
2. ✅ Проверил на ошибки
3. ✅ Добавил RLS политики
4. ✅ Создал подробные инструкции
5. ✅ Создал автоматический скрипт

**ОСТАЛОСЬ ТОЛЬКО ЗАПУСТИТЬ!**

---

## 🎯 САМЫЙ БЫСТРЫЙ СПОСОБ (1 МИНУТА):

```bash
# Установи Supabase CLI
brew install supabase/tap/supabase

# Логин (откроется браузер)
supabase login

# Линк проекта
supabase link --project-ref capdjvokjdivxjfddmx

# Применить ВСЕ миграции
supabase db push

# ГОТОВО! ✅
```

**Это займёт 1 минуту!**

---

## 📞 ЕСЛИ НЕ РАБОТАЕТ:

### Вариант 1: Через Dashboard (вручную)

```
1. Открой: https://supabase.com/dashboard/project/capdjvokjdivxjfddmx
2. SQL Editor → New Query
3. Скопируй содержимое КАЖДОГО файла миграции
4. Вставь → Run
```

**Это займёт 10 минут, но 100% работает!**

### Вариант 2: Дай мне Service Role Key

```
Если дашь мне Service Role Key, я смогу:
1. Создать скрипт с ним
2. Запустить автоматически
3. Применить все миграции

⚠️ НО: Это секретный ключ!
Не делись им публично!
```

---

## 💡 ИТОГО:

| Способ | Время | Сложность | Автоматизация |
|--------|-------|-----------|---------------|
| Supabase CLI | 1 мин | Легко | ✅ Полная |
| Auto Script | 2 мин | Средне | ✅ Полная |
| Dashboard (вручную) | 10 мин | Легко | ❌ Нет |

**Рекомендую: Supabase CLI** 🚀

---

**ГОТОВ ПОМОЧЬ ДАЛЬШЕ!** 💪

