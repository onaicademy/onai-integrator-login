# 🧪 Инструкции по тестированию исправлений

## ✅ Выполненные исправления

### 1. Создан `.env` файл
Файл с переменными окружения создан и содержит:
- `VITE_SUPABASE_URL` - URL вашего Supabase проекта
- `VITE_SUPABASE_PUBLISHABLE_KEY` - публичный ключ для Supabase
- `VITE_SITE_URL` - URL локального сервера

### 2. Исправлено несоответствие переменных
Во всех файлах `VITE_SUPABASE_ANON_KEY` заменено на `VITE_SUPABASE_PUBLISHABLE_KEY`:
- ✅ `.github/workflows/deploy.yml`
- ✅ `DEPLOY_SERVER.sh`
- ✅ `DEPLOY_INSTRUCTIONS.md`
- ✅ `DEPLOYMENT.md`
- ✅ `supabase/README.md`
- ✅ `src/vite-env.d.ts`

### 3. Проверено подключение к Supabase
✅ Supabase клиент инициализируется корректно и может взаимодействовать с сервером

## 🚀 Dev сервер запущен

Сервер работает на: **http://localhost:8080**

## 📋 Тестирование страниц

### Текущее поведение страниц

Все три страницы (`/profile`, `/welcome`, `/admin/activity`) требуют авторизации:

#### 1. `/profile` - Профиль пользователя
**Требования:**
- Активная сессия Supabase (пользователь должен быть авторизован)
- Запись пользователя в таблице `users`

**Поведение без авторизации:**
- Редирект на `/` (главная страница)

**Как протестировать:**
1. Откройте http://localhost:8080
2. Нажмите "Войти через Google"
3. После авторизации вы будете перенаправлены на `/welcome`
4. После прохождения опросника откроется `/profile`

#### 2. `/welcome` - Опросник для новых пользователей
**Требования:**
- Активная сессия Supabase
- Опросник НЕ должен быть пройден ранее

**Поведение без авторизации:**
- Редирект на `/` (главная страница)

**Поведение если опросник уже пройден:**
- Редирект на `/profile`

**Как протестировать:**
1. Авторизуйтесь через Google на главной странице
2. Вас автоматически перенаправит на `/welcome`
3. Заполните опросник (9 вопросов)
4. После завершения откроется `/profile`

#### 3. `/admin/activity` - Административная панель
**Требования:**
- Активная сессия Supabase
- Роль `admin` в таблице `user_roles`

**Поведение без авторизации или без admin роли:**
- Редирект на `/` (главная страница)

**Как протестировать:**
1. Нужно вручную добавить роль `admin` в базу данных
2. В Supabase Dashboard выполните SQL:
```sql
INSERT INTO user_roles (user_id, role)
VALUES ('YOUR_USER_ID', 'admin');
```
3. Откройте http://localhost:8080/admin/activity

## 🔧 Опциональное решение: Тестовый режим без авторизации

Если вам нужно протестировать **только UI** без функционала авторизации, можно временно отключить проверки:

### Для `/profile`:
В файле `src/pages/Profile.tsx` закомментируйте строки 19-45 (useEffect с проверкой авторизации)

### Для `/welcome`:
В файле `src/pages/Welcome.tsx` закомментируйте строки 30-57 (checkExistingSurvey)

### Для `/admin/activity`:
В файле `src/pages/admin/Activity.tsx` закомментируйте строки 71-109 (checkAdminAccess)

**⚠️ ВАЖНО:** Эти изменения только для тестирования UI локально! Не коммитьте их в git.

## 🎯 Проверочный список

- [x] ✅ .env файл создан
- [x] ✅ Переменные окружения исправлены везде
- [x] ✅ Supabase подключение работает
- [x] ✅ Dev сервер запущен на порту 8080
- [ ] ⏳ Авторизация через Google
- [ ] ⏳ Тестирование `/profile`
- [ ] ⏳ Тестирование `/welcome`
- [ ] ⏳ Тестирование `/admin/activity`

## 🐛 Если что-то не работает

### Проблема: "Не удаётся установить соединение с сайтом"
**Решение:** Проверьте, что dev сервер запущен:
```bash
lsof -i :8080
```

Если не запущен:
```bash
npm run dev
```

### Проблема: "Страницы редиректят на главную"
**Причина:** Это ожидаемое поведение - все три страницы защищены авторизацией.

**Решение:** 
1. Авторизуйтесь через Google на главной странице
2. Или временно отключите проверки (см. "Тестовый режим" выше)

### Проблема: "Google авторизация не работает"
**Решение:** Убедитесь, что в Supabase Dashboard настроен Google OAuth provider:
1. Откройте https://supabase.com/dashboard/project/capdjvokjdivxjfdddmx/auth/providers
2. Включите Google provider
3. Добавьте redirect URL: `http://localhost:8080/welcome`

## 📞 Следующие шаги

1. Откройте http://localhost:8080 в браузере
2. Проверьте авторизацию через Google
3. Протестируйте опросник на `/welcome`
4. Проверьте профиль на `/profile`
5. Если нужен доступ к `/admin/activity`, добавьте себе роль admin в БД

Всё готово к тестированию! 🎉

