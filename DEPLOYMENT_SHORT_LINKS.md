# 🚀 Деплой системы коротких ссылок - Чек-лист

## ✅ Чек-лист перед деплоем

### 1. База данных (Supabase)

- [ ] **Применить миграцию**:
  ```bash
  # Вариант 1: Через Supabase CLI
  supabase db push
  
  # Вариант 2: Через Supabase Dashboard
  # SQL Editor → Выполнить файл: supabase/migrations/20250114_create_short_links.sql
  ```

- [ ] **Проверить таблицы**:
  ```sql
  -- В Supabase SQL Editor выполнить:
  SELECT * FROM short_links LIMIT 1;
  SELECT * FROM short_link_clicks LIMIT 1;
  ```
  Должны вернуться пустые таблицы (без ошибок)

- [ ] **Проверить RLS политики**:
  ```sql
  SELECT tablename, policyname FROM pg_policies 
  WHERE tablename IN ('short_links', 'short_link_clicks');
  ```
  Должно быть 5 политик

### 2. Backend

- [ ] **Проверить environment variables**:
  ```bash
  # В backend/.env должны быть:
  SUPABASE_URL=https://your-project.supabase.co
  SUPABASE_SERVICE_ROLE_KEY=your-key
  MOBIZON_API_KEY=your-mobizon-key
  ```

- [ ] **Установить зависимости** (если нужно):
  ```bash
  cd backend
  npm install
  ```

- [ ] **Собрать и запустить**:
  ```bash
  npm run build
  npm start
  ```

- [ ] **Проверить логи**:
  Должны быть строки:
  ```
  🚀 Backend API запущен на http://localhost:3000
  ```
  Без ошибок импорта `urlShortener` или `short-links`

### 3. Frontend

- [ ] **Проверить импорты**:
  ```bash
  # Проверить что нет ошибок TypeScript
  npm run type-check
  ```

- [ ] **Собрать**:
  ```bash
  npm run build
  ```

- [ ] **Проверить роуты**:
  В `src/App.tsx` должны быть строки:
  ```typescript
  const ShortLinksStats = lazy(() => import("./pages/admin/ShortLinksStats"));
  <Route path="/admin/short-links" element={<AdminGuard><ShortLinksStats /></AdminGuard>} />
  ```

### 4. Тестирование

#### Тест 1: Создание короткой ссылки (через код)

```bash
# Выполнить в Postman или curl:
curl -X POST http://localhost:3000/api/short-links/create \
  -H "Content-Type: application/json" \
  -d '{
    "originalUrl": "https://onai.academy/integrator/expresscourse?utm_source=test&lead_id=123",
    "leadId": "test-lead-123",
    "campaign": "test",
    "source": "manual"
  }'
```

**Ожидаемый результат**:
```json
{
  "success": true,
  "data": {
    "shortCode": "a1b2c3",
    "shortUrl": "https://onai.academy/l/a1b2c3",
    "originalUrl": "https://onai.academy/..."
  }
}
```

#### Тест 2: Редирект

```bash
# Открыть в браузере:
http://localhost:3000/l/a1b2c3
```

**Ожидаемый результат**:
- Редирект на оригинальную ссылку
- В БД должна появиться запись в `short_link_clicks`
- В `short_links` должен увеличиться `clicks_count`

#### Тест 3: SMS отправка

**Запустить тестовую отправку SMS** (через ваш код):
```typescript
await sendProftestResultSMS('+77771234567', 'test-lead-uuid');
```

**Проверить**:
- [ ] В логах должно быть: `🔗 Creating short link for lead...`
- [ ] В логах должно быть: `✅ Short link created: https://onai.academy/l/...`
- [ ] В логах должно быть: `📊 Saved X characters (Y% reduction)`
- [ ] SMS должна прийти с короткой ссылкой

#### Тест 4: Админ панель

```bash
# Открыть в браузере:
http://localhost:5173/admin/short-links
# или
http://localhost:5173/integrator/admin/short-links
```

**Проверить**:
- [ ] Загружается без ошибок
- [ ] Видны карточки со статистикой
- [ ] Видны созданные ссылки
- [ ] Работает поиск
- [ ] Работает копирование ссылки

---

## 🔍 Проверка после деплоя

### 1. Проверка production

- [ ] **Backend работает**:
  ```bash
  curl https://api.onai.academy/health
  ```

- [ ] **Короткие ссылки работают**:
  ```bash
  curl -I https://onai.academy/l/test123
  ```
  Должен вернуть 302 Redirect (или 404 если ссылки нет)

- [ ] **Админка доступна**:
  Открыть: https://onai.academy/admin/short-links

### 2. Мониторинг

- [ ] **Проверить логи backend**:
  ```bash
  pm2 logs backend
  # или
  tail -f /var/log/backend.log
  ```

- [ ] **Проверить Sentry** (если настроен):
  Ошибок не должно быть

- [ ] **Проверить Supabase Dashboard**:
  - Таблица `short_links` - появляются новые записи
  - Таблица `short_link_clicks` - растет количество кликов

### 3. Первое реальное SMS

- [ ] **Отправить SMS реальному пользователю**
- [ ] **Проверить что пришла короткая ссылка**
- [ ] **Попросить пользователя кликнуть**
- [ ] **Проверить что клик зарегистрировался в админке**

---

## 🚨 Откат (если что-то пошло не так)

### Вариант 1: Откат кода (без удаления БД)

```bash
# Откатить изменения в git
git revert HEAD

# Или вернуться к предыдущему коммиту
git checkout <previous-commit>

# Пересобрать и задеплоить
npm run build
```

**Важно**: Таблицы в БД останутся, но код вернется к старому.  
SMS будут отправляться со старыми длинными ссылками.

### Вариант 2: Удаление таблиц (полный откат)

```sql
-- В Supabase SQL Editor выполнить:
DROP TABLE IF EXISTS short_link_clicks CASCADE;
DROP TABLE IF EXISTS short_links CASCADE;
```

**Осторожно**: Это удалит всю статистику кликов!

### Вариант 3: Деактивация функционала (софт-откат)

В файле `backend/src/services/mobizon.ts` закомментировать создание короткой ссылки:

```typescript
export async function sendProftestResultSMS(phone: string, leadId?: string): Promise<boolean> {
  // ОТКАТ: Отключаем короткие ссылки
  const url = leadId 
    ? `https://api.onai.academy/api/landing/track/${leadId}?source=sms`
    : 'https://onai.academy/integrator/expresscourse';
  
  const text = `Тест пройден. Ваша ссылка ${url}`;
  return await sendSMS({ recipient: phone, text });
}
```

Пересобрать и задеплоить backend.

---

## 📊 Метрики успеха (через неделю)

Проверить в админке (`/admin/short-links`):

- [ ] **Всего ссылок создано**: > 50
- [ ] **Всего кликов**: > 30
- [ ] **CTR (Click-Through Rate)**: > 50%
- [ ] **Экономия символов**: в среднем 60-70%
- [ ] **Уникальные клики**: > 25

Если метрики ниже:
- Проверить что SMS действительно отправляются
- Проверить что пользователи получают SMS
- Проверить что ссылки работают (не 404)

---

## 📝 Дополнительные задачи (опционально)

После успешного деплоя можно добавить:

- [ ] **Экспорт статистики в CSV**
- [ ] **Геолокация по IP** (определение страны/города)
- [ ] **A/B тестирование** (разные тексты SMS)
- [ ] **Интеграция с Google Analytics**
- [ ] **Уведомления в Telegram** при создании ссылки
- [ ] **Автоматические отчеты** (ежедневно/еженедельно)

---

## ✅ Деплой завершен!

Если все пункты чек-листа отмечены ✅, то:

🎉 **Система коротких ссылок успешно задеплоена!**

**Следующее SMS уже отправится с короткой ссылкой и полной аналитикой!** 🚀

---

## 📞 Контакты для поддержки

Если возникли проблемы:
1. Проверьте логи backend
2. Проверьте Supabase Dashboard
3. Проверьте Sentry (если настроен)
4. Откатите изменения по инструкции выше

**Удачи!** 💪




