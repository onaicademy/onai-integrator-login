# 📊 ОТСЛЕЖИВАНИЕ ТОКЕНОВ + ЧАТ УЧЕНИКОВ

**Дата:** 7 ноября 2025  
**Статус:** ✅ БАЗЫ ГОТОВЫ, UI НУЖЕН

---

## 📋 СОДЕРЖАНИЕ

1. [Система отслеживания токенов](#система-отслеживания-токенов)
2. [Чат между учениками](#чат-между-учениками)
3. [Как использовать](#как-использовать)
4. [Что дальше](#что-дальше)

---

---

# СИСТЕМА ОТСЛЕЖИВАНИЯ ТОКЕНОВ

## 🎯 ЧТО СДЕЛАНО:

### ✅ **БАЗА ДАННЫХ**

**Миграция:** `20250109_token_usage_tracking.sql`

#### 1. **`ai_token_usage`** - Детальное логирование

```sql
Каждый запрос к OpenAI:
- agent_type: 'ai_curator', 'ai_mentor', 'ai_analyst'
- model: 'gpt-4o', 'gpt-3.5-turbo', 'whisper-1'
- operation_type: 'text_message', 'voice_transcription', etc
- prompt_tokens: количество входных токенов
- completion_tokens: количество выходных токенов
- total_tokens: сумма
- audio_duration_seconds: для Whisper
- cost_usd: стоимость в USD
- cost_kzt: стоимость в тенге (автоматически)
- user_id, thread_id, request_id
- date: дата для группировки
```

#### 2. **`ai_token_usage_daily`** - Дневная агрегация

```sql
Агрегированные данные за день:
- По агентам:
  * curator_tokens, curator_cost_kzt
  * mentor_tokens, mentor_cost_kzt
  * analyst_tokens, analyst_cost_kzt

- По моделям:
  * gpt4o_tokens, gpt4o_cost_kzt
  * gpt35_tokens, gpt35_cost_kzt
  * whisper_minutes, whisper_cost_kzt

- Общие:
  * total_tokens, total_cost_kzt, total_cost_usd
  * request_count, unique_users
```

#### 3. **`ai_budget_limits`** - Лимиты и бюджет

```sql
Настройки лимитов:
- daily_limit_kzt: 5,000 KZT (дефолт)
- monthly_limit_kzt: 100,000 KZT (дефолт)
- alert_threshold_percentage: 80% (алерт при 80%)
- alert_enabled: true
- alert_contacts: JSON с email/Telegram
```

---

### ✅ **ФУНКЦИИ**

#### 1. **`log_token_usage()`** - Логирование

```sql
Автоматически:
- Вычисляет стоимость по модели
- Конвертирует USD → KZT (курс 450)
- Обновляет дневную агрегацию
- Возвращает usage_id

Цены OpenAI (встроены):
GPT-4o:
  Input:  $2.50 per 1M tokens
  Output: $10.00 per 1M tokens

GPT-3.5-turbo:
  Input:  $0.50 per 1M tokens
  Output: $1.50 per 1M tokens

Whisper:
  Audio: $0.006 per minute
```

#### 2. **`update_daily_aggregation()`** - Агрегация

```sql
Обновляет дневную таблицу:
- Суммирует токены по агентам
- Суммирует токены по моделям
- Вычисляет общие метрики
- Вызывается автоматически при log_token_usage()
```

#### 3. **`check_budget_limits()`** - Проверка лимитов

```sql
Возвращает:
- limit_exceeded: превышен ли лимит
- current_daily_kzt: потрачено сегодня
- current_monthly_kzt: потрачено за месяц
- alert_message: текст алерта

Алерты:
⚠️ При 80% лимита
🚨 При превышении лимита
```

---

### ✅ **API ДЛЯ КОДА**

**Файл:** `src/lib/token-tracker.ts`

#### Основные функции:

```typescript
// 1. Логирование токенов
await logTokenUsage({
  agentType: 'ai_curator',
  model: 'gpt-4o',
  operationType: 'text_message',
  promptTokens: 1000,
  completionTokens: 200,
  userId: 'student-123',
  threadId: 'thread-456'
});

// 2. Получить дневную статистику
const { data } = await getDailyTokenUsage(
  '2025-11-01', 
  '2025-11-30'
);

// 3. Получить статистику по агенту
const { data } = await getAgentStats('ai_curator', 30); // за 30 дней

// 4. Проверить лимиты
const { data } = await checkBudgetLimits();
// {
//   limit_exceeded: false,
//   current_daily_kzt: 1234.56,
//   current_monthly_kzt: 45678.90,
//   alert_message: "✅ В пределах лимитов"
// }

// 5. Логирование Whisper
await logWhisperUsage(30, { // 30 секунд
  userId: 'student-123',
  threadId: 'thread-456'
});

// 6. Обёртка для OpenAI с автоматическим логированием
const result = await trackOpenAIRequest(
  'ai_curator',
  'text_message',
  async () => {
    return await openai.chat.completions.create({...});
  },
  { userId, threadId }
);
```

---

## 📊 ИНТЕГРАЦИЯ В КОД:

### В `src/lib/openai-assistant.ts`:

```typescript
import { logTokenUsage, logWhisperUsage } from './token-tracker';

// AI-куратор (текст)
export async function sendMessageToAI(...) {
  const response = await client.beta.threads.messages.create(...);
  
  // Логируем токены
  await logTokenUsage({
    agentType: 'ai_curator',
    model: 'gpt-4o',
    operationType: 'text_message',
    promptTokens: run.usage?.prompt_tokens || 0,
    completionTokens: run.usage?.completion_tokens || 0,
    userId: userId,
    threadId: threadId
  });
}

// AI-куратор (голос)
export async function transcribeAudioToText(audioBlob: Blob) {
  const duration = await getAudioDuration(audioBlob);
  const transcription = await client.audio.transcriptions.create(...);
  
  // Логируем Whisper
  await logWhisperUsage(duration);
  
  return transcription.text;
}

// AI-наставник (ежедневный анализ)
export async function analyzeStu dent(userId: string) {
  const response = await client.chat.completions.create(...);
  
  // Логируем токены
  await logTokenUsage({
    agentType: 'ai_mentor',
    model: 'gpt-4o',
    operationType: 'daily_analysis',
    promptTokens: response.usage?.prompt_tokens || 0,
    completionTokens: response.usage?.completion_tokens || 0,
    userId: userId
  });
}

// AI-аналитик (недельный отчёт)
export async function generateWeeklyReport() {
  const response = await client.chat.completions.create(...);
  
  await logTokenUsage({
    agentType: 'ai_analyst',
    model: 'gpt-4o',
    operationType: 'weekly_report',
    promptTokens: response.usage?.prompt_tokens || 0,
    completionTokens: response.usage?.completion_tokens || 0
  });
}
```

---

## 🎨 UI ДЛЯ АДМИН ПАНЕЛИ:

### Нужно создать: `src/pages/admin/TokenUsage.tsx`

**Что показывать:**

```typescript
1. DASHBOARD (общая статистика):
   - Сегодня: токены, стоимость
   - Этот месяц: токены, стоимость
   - Прогресс до лимита (прогресс-бар)
   - Алерты (если есть)

2. КАЛЕНДАРЬ:
   - Интерактивный календарь
   - При клике на день → детальная статистика
   - Цветовая индикация (зелёный = мало, красный = много)

3. ГРАФИКИ:
   📊 График 1: Стоимость по дням (линейный)
   📊 График 2: Токены по агентам (столбчатый)
   📊 График 3: Распределение по моделям (круговой)
   📊 График 4: Топ пользователей по токенам

4. ТАБЛИЦА ДЕТАЛЬНАЯ:
   - Фильтры: агент, модель, дата
   - Сортировка по любой колонке
   - Пагинация
   - Экспорт в CSV

5. НАСТРОЙКИ ЛИМИТОВ:
   - Изменить daily_limit_kzt
   - Изменить monthly_limit_kzt
   - Настроить алерты
```

**Библиотеки:**

```bash
npm install recharts date-fns
```

**Пример компонента:**

```typescript
import { useState, useEffect } from 'react';
import { getDailyTokenUsage, checkBudgetLimits } from '@/lib/token-tracker';
import { LineChart, Line, BarChart, Bar, PieChart, Pie } from 'recharts';
import { Calendar } from '@/components/ui/calendar';

export default function TokenUsagePage() {
  const [dailyData, setDailyData] = useState([]);
  const [limits, setLimits] = useState(null);

  useEffect(() => {
    loadData();
  }, []);

  async function loadData() {
    // Загрузить данные за 30 дней
    const { data } = await getDailyTokenUsage(
      getDateXDaysAgo(30),
      getToday()
    );
    setDailyData(data);

    // Проверить лимиты
    const { data: limitsData } = await checkBudgetLimits();
    setLimits(limitsData);
  }

  return (
    <div className="p-6">
      <h1>Отслеживание токенов OpenAI</h1>

      {/* Алерты */}
      {limits?.limit_exceeded && (
        <Alert variant="destructive">
          {limits.alert_message}
        </Alert>
      )}

      {/* Метрики */}
      <div className="grid grid-cols-4 gap-4">
        <MetricCard
          title="Сегодня"
          value={`${todayTokens} токенов`}
          cost={`${todayCost} ₸`}
        />
        <MetricCard
          title="Этот месяц"
          value={`${monthTokens} токенов`}
          cost={`${monthCost} ₸`}
        />
        {/* ... */}
      </div>

      {/* График */}
      <Card>
        <CardHeader>
          <CardTitle>Стоимость по дням</CardTitle>
        </CardHeader>
        <CardContent>
          <LineChart data={dailyData}>
            <Line dataKey="total_cost_kzt" stroke="#8884d8" />
            {/* ... */}
          </LineChart>
        </CardContent>
      </Card>

      {/* Календарь */}
      <Calendar
        mode="single"
        onDayClick={handleDayClick}
        modifiers={{ expensive: expensiveDays }}
      />
    </div>
  );
}
```

---

---

# ЧАТ МЕЖДУ УЧЕНИКАМИ

## 🎯 ЧТО СДЕЛАНО:

### ✅ **БАЗА ДАННЫХ**

**Миграция:** `20250110_student_messaging.sql`

#### 1. **ПРИВАТНЫЕ ЧАТЫ (1-на-1)**

```sql
student_private_chats:
- user1_id, user2_id (два участника)
- last_message_at, last_message_text
- unread_count_user1, unread_count_user2
- is_archived_user1, is_archived_user2
- is_blocked, blocked_by

student_private_messages:
- chat_id (ссылка на чат)
- sender_id (кто отправил)
- message_type ('text', 'image', 'file', 'voice')
- content (текст сообщения)
- attachment_url (ссылка на файл)
- is_read, read_at
- is_edited, edited_at
- is_deleted, deleted_at
- reply_to_message_id (ответ на сообщение)
```

#### 2. **ГРУППОВЫЕ ЧАТЫ (до 50 человек)**

```sql
student_group_chats:
- name, description, avatar_url
- created_by (создатель)
- is_public (публичная или приватная)
- max_members (макс. 50)
- last_message_at, last_message_text

student_group_members:
- group_id, user_id
- role ('admin', 'moderator', 'member')
- unread_count (непрочитанные)
- is_muted, muted_until

student_group_messages:
- group_id, sender_id
- message_type, content, attachment_url
- mentions (массив ID упомянутых @username)
- reply_to_message_id

student_group_message_reads:
- message_id, user_id, read_at
- (кто прочитал сообщение)
```

---

### ✅ **ФУНКЦИИ**

#### 1. **`get_or_create_private_chat()`**

```sql
-- Получить существующий или создать новый чат
SELECT get_or_create_private_chat(
  'user-1-uuid',
  'user-2-uuid'
);

-- Возвращает chat_id
```

#### 2. **`send_private_message()`**

```sql
-- Отправить сообщение в приватный чат
SELECT send_private_message(
  sender_id := 'user-1-uuid',
  recipient_id := 'user-2-uuid',
  content := 'Привет! Как дела?',
  message_type := 'text'
);

Автоматически:
- Создаёт чат если не существует
- Увеличивает unread_count для получателя
- Обновляет last_message в чате
```

#### 3. **`mark_messages_as_read()`**

```sql
-- Отметить все сообщения как прочитанные
SELECT mark_messages_as_read(
  chat_id := 'chat-uuid',
  user_id := 'user-uuid'
);

Автоматически:
- Отмечает is_read = true
- Обнуляет unread_count
```

---

### ✅ **БЕЗОПАСНОСТЬ (RLS)**

```sql
Приватные чаты:
- Только участники видят чат
- Только участники видят сообщения
- Каждый может отправлять сообщения
- Каждый может редактировать свои сообщения

Групповые чаты:
- Участники видят группу
- Публичные группы видны всем
- Только участники видят сообщения
- Только участники могут отправлять сообщения
```

---

## 🎨 UI ДЛЯ СТУДЕНТОВ:

### Нужно создать: `src/pages/Messages.tsx`

**Структура:**

```
┌─────────────────────────────────────────────┐
│  ВКЛАДКА "СООБЩЕНИЯ"                        │
├─────────────┬───────────────────────────────┤
│             │                               │
│  СПИСОК     │  АКТИВНЫЙ ЧАТ                 │
│  ЧАТОВ      │                               │
│             │  [Сообщение 1]                │
│  • Андрей   │  [Сообщение 2]                │
│    "Привет" │  [Сообщение 3]                │
│    (2)      │                               │
│             │  ┌─────────────────────────┐  │
│  • Мария    │  │ Напишите сообщение...   │  │
│    "Ок!"    │  │ [📎] [🎤] [📷] [Send]  │  │
│             │  └─────────────────────────┘  │
│  • Группа   │                               │
│    "Хело"   │                               │
│             │                               │
└─────────────┴───────────────────────────────┘
```

**Функции:**

```typescript
1. СПИСОК ЧАТОВ (слева):
   - Приватные чаты
   - Групповые чаты
   - Поиск по имени
   - Счётчик непрочитанных (красная точка)
   - Время последнего сообщения
   - Аватарка собеседника

2. АКТИВНЫЙ ЧАТ (справа):
   - История сообщений
   - Автопрокрутка вниз
   - Отображение: текст, изображения, файлы
   - Ответ на сообщение (reply)
   - Редактирование своих сообщений
   - Удаление своих сообщений
   - Копирование текста

3. ВВОД СООБЩЕНИЯ (внизу):
   - Textarea с авторасширением
   - Кнопка "Прикрепить файл" (📎)
   - Кнопка "Голосовое" (🎤)
   - Кнопка "Фото" (📷)
   - Кнопка "Отправить" (▶)

4. ДОПОЛНИТЕЛЬНО:
   - Индикатор "печатает..." (typing)
   - Статус "онлайн/офлайн"
   - Уведомления о новых сообщениях
   - Поиск по сообщениям
```

**Пример компонента:**

```typescript
import { useState, useEffect, useRef } from 'react';
import { supabase } from '@/lib/supabase';

export default function MessagesPage() {
  const [chats, setChats] = useState([]);
  const [activeChat, setActiveChat] = useState(null);
  const [messages, setMessages] = useState([]);
  const [inputText, setInputText] = useState('');

  useEffect(() => {
    loadChats();
  }, []);

  useEffect(() => {
    if (activeChat) {
      loadMessages(activeChat.id);
      subscribeToMessages(activeChat.id);
    }
  }, [activeChat]);

  async function loadChats() {
    const { data: { user } } = await supabase.auth.getUser();
    if (!user) return;

    // Получить все чаты пользователя
    const { data } = await supabase
      .from('student_private_chats')
      .select(`
        *,
        user1:auth.users!user1_id(id, full_name, avatar_url),
        user2:auth.users!user2_id(id, full_name, avatar_url)
      `)
      .or(`user1_id.eq.${user.id},user2_id.eq.${user.id}`)
      .order('updated_at', { ascending: false });

    setChats(data || []);
  }

  async function loadMessages(chatId: string) {
    const { data } = await supabase
      .from('student_private_messages')
      .select('*, sender:auth.users!sender_id(full_name, avatar_url)')
      .eq('chat_id', chatId)
      .eq('is_deleted', false)
      .order('created_at', { ascending: true });

    setMessages(data || []);

    // Отметить как прочитанные
    await supabase.rpc('mark_messages_as_read', {
      p_chat_id: chatId,
      p_user_id: (await supabase.auth.getUser()).data.user?.id
    });
  }

  function subscribeToMessages(chatId: string) {
    // Real-time подписка на новые сообщения
    const channel = supabase
      .channel(`chat:${chatId}`)
      .on(
        'postgres_changes',
        {
          event: 'INSERT',
          schema: 'public',
          table: 'student_private_messages',
          filter: `chat_id=eq.${chatId}`
        },
        (payload) => {
          setMessages((prev) => [...prev, payload.new]);
        }
      )
      .subscribe();

    return () => {
      supabase.removeChannel(channel);
    };
  }

  async function sendMessage() {
    if (!inputText.trim() || !activeChat) return;

    const { data: { user } } = await supabase.auth.getUser();
    if (!user) return;

    // Определяем получателя
    const recipientId = activeChat.user1_id === user.id 
      ? activeChat.user2_id 
      : activeChat.user1_id;

    // Отправляем через функцию
    await supabase.rpc('send_private_message', {
      p_sender_id: user.id,
      p_recipient_id: recipientId,
      p_content: inputText,
      p_message_type: 'text'
    });

    setInputText('');
  }

  return (
    <div className="flex h-screen">
      {/* Список чатов */}
      <aside className="w-80 border-r">
        <div className="p-4">
          <h2 className="text-xl font-bold">Сообщения</h2>
        </div>
        <div className="overflow-y-auto">
          {chats.map((chat) => (
            <ChatItem
              key={chat.id}
              chat={chat}
              active={activeChat?.id === chat.id}
              onClick={() => setActiveChat(chat)}
            />
          ))}
        </div>
      </aside>

      {/* Активный чат */}
      <main className="flex-1 flex flex-col">
        {activeChat ? (
          <>
            {/* Заголовок */}
            <header className="p-4 border-b">
              <h3 className="font-semibold">
                {getChatName(activeChat)}
              </h3>
            </header>

            {/* Сообщения */}
            <div className="flex-1 overflow-y-auto p-4 space-y-2">
              {messages.map((msg) => (
                <MessageBubble key={msg.id} message={msg} />
              ))}
            </div>

            {/* Ввод */}
            <footer className="p-4 border-t">
              <div className="flex gap-2">
                <Input
                  value={inputText}
                  onChange={(e) => setInputText(e.target.value)}
                  onKeyPress={(e) => e.key === 'Enter' && sendMessage()}
                  placeholder="Напишите сообщение..."
                />
                <Button onClick={sendMessage}>
                  Отправить
                </Button>
              </div>
            </footer>
          </>
        ) : (
          <div className="flex-1 flex items-center justify-center text-muted-foreground">
            Выберите чат
          </div>
        )}
      </main>
    </div>
  );
}
```

---

---

# КАК ИСПОЛЬЗОВАТЬ

## 📝 ШАГ 1: Применить миграции

```bash
# В Supabase Dashboard → SQL Editor

# 1. Токены
cat "supabase/migrations/20250109_token_usage_tracking.sql"
# → Копировать → Вставить → Run

# 2. Чат
cat "supabase/migrations/20250110_student_messaging.sql"
# → Копировать → Вставить → Run
```

## 📝 ШАГ 2: Интегрировать логирование токенов

```typescript
// src/lib/openai-assistant.ts

import { logTokenUsage } from './token-tracker';

// После каждого вызова OpenAI:
await logTokenUsage({
  agentType: 'ai_curator',
  model: 'gpt-4o',
  operationType: 'text_message',
  promptTokens: usage.prompt_tokens,
  completionTokens: usage.completion_tokens,
  userId, threadId
});
```

## 📝 ШАГ 3: Создать UI

```typescript
// src/pages/admin/TokenUsage.tsx
// src/pages/Messages.tsx

// Использовать функции из:
import { getDailyTokenUsage, checkBudgetLimits } from '@/lib/token-tracker';
```

---

---

# ЧТО ДАЛЬШЕ

## ⏳ ОСТАЛОСЬ СДЕЛАТЬ:

### 1. **UI админ панели для токенов** (2-3 часа)
```
- Dashboard с метриками
- Календарь
- Графики (recharts)
- Таблица детальная
- Настройки лимитов
```

### 2. **UI чата для учеников** (4-5 часов)
```
- Список чатов
- Активный чат
- Отправка сообщений
- Real-time подписка
- Вложения (файлы, изображения)
```

### 3. **Telegram боты** (опционально)
```
- Бот для студентов (AI-наставник)
- Бот для админов (AI-аналитик)
```

---

## 📊 ИТОГОВАЯ СТРУКТУРА:

```
✅ БАЗЫ ДАННЫХ:
├── 20250109_token_usage_tracking.sql (отслеживание токенов)
├── 20250110_student_messaging.sql (чат учеников)

✅ API:
├── src/lib/token-tracker.ts (функции для токенов)

⏳ UI (нужно создать):
├── src/pages/admin/TokenUsage.tsx (админ панель токенов)
├── src/pages/Messages.tsx (чат учеников)
```

---

## 🎯 ПРИОРИТЕТЫ:

```
ВЫСОКИЙ:
1. UI для отслеживания токенов (для контроля затрат)
2. Интеграция логирования в openai-assistant.ts

СРЕДНИЙ:
3. UI чата учеников (для коммуникации)

НИЗКИЙ:
4. Telegram боты
```

---

**ВСЁ ГОТОВО! БАЗЫ СОЗДАНЫ! 🚀**

**Открой этот файл для инструкций:**
```
📊_ТОКЕНЫ_И_ЧАТ_ИНСТРУКЦИЯ.md
```

---

**Автор:** AI Development System  
**Дата:** 7 ноября 2025  
**Версия:** 1.0

