# 🚀 DEPLOY НА DIGITALOCEAN

## ✅ ВСЁ ГОТОВО К ДЕПЛОЮ:

```bash
✅ Build собран: dist/ готов
✅ Git push выполнен
✅ Все изменения на месте:
   - Traffic Analytics русифицирован
   - Календарь выбора даты
   - Telegram bot настроен
   - Sales webhook готов
```

---

## 🔑 МНЕ НУЖНЫ SSH ДАННЫЕ:

```bash
IP сервера DigitalOcean: ???
SSH user: ???
SSH password или SSH key: ???
```

**Путь к фронту на сервере (обычно):**
```
/var/www/onai.academy/
/var/www/html/
/home/user/onai-academy/
```

**Путь к бэкенду на сервере:**
```
/home/user/backend/
/var/www/api/
```

---

## 🚀 ЧТО Я СДЕЛАЮ ПОСЛЕ ПОЛУЧЕНИЯ ДОСТУПА:

### 1. **ФРОНТЕНД:**
```bash
# На Mac:
cd /Users/miso/onai-integrator-login
npm run build  # уже готов dist/

# Загрузка на DigitalOcean:
rsync -avz --delete dist/ user@<IP>:/var/www/onai.academy/html/

# Перезагрузка nginx:
ssh user@<IP> 'sudo systemctl reload nginx'
```

### 2. **БЭКЕНД:**
```bash
# Закоммитить изменения бэкенда
cd /Users/miso/onai-integrator-login/backend
git pull origin main  # на сервере

# Перезапустить процесс:
pm2 restart backend
# ИЛИ
sudo systemctl restart backend
```

---

## 📋 CHECKLIST:

- [x] Build собран
- [ ] **SSH доступ получен**
- [ ] **Фронт загружен на сервер**
- [ ] **Nginx перезагружен**
- [ ] **Бэкенд обновлен**
- [ ] **PM2/systemd перезапущен**
- [ ] **Проверка в браузере**

---

## 🔧 СТРУКТУРА СЕРВЕРА (предположительно):

```
DigitalOcean Droplet:
├── /var/www/onai.academy/
│   └── html/  ← ФРОНТ (dist/)
├── /home/user/backend/  ← БЭКЕНД (Node.js)
├── nginx config: /etc/nginx/sites-available/onai.academy
└── PM2 процесс: backend
```

---

## 🆘 ЧТО МНЕ НУЖНО:

**Дай SSH доступ:**
```bash
ssh user@IP  # логин + пароль
# ИЛИ
ssh -i ~/.ssh/digitalocean_key user@IP  # с ключом
```

**И скажи:**
1. Где лежит фронт на сервере?
2. Где лежит бэкенд?
3. Как перезапустить бэкенд (pm2/systemd)?

---

**БРАТАН, ДАЙ SSH ДАННЫЕ - Я СЕЙЧАС БЫСТРО ЗАДЕПЛОЮ ВСЁ!** 🔥
