# 🗄️ SQL СКРИПТ - ЧТО ОН ДЕЛАЕТ

**Файл:** `setup-ai-mentor-tables.sql`  
**Зачем:** Создаёт таблицы и функции для работы рейтингов и аналитики AI-наставника

---

## 📊 ЧТО СОЗДАЁТСЯ

### 1. Таблица `ai_analytics_metrics`
**Назначение:** Хранение метрик для AI-аналитики студентов

**Колонки:**
- `id` - уникальный номер записи
- `user_id` - ID студента
- `metric_type` - тип метрики (daily_progress, weekly_summary, study_streak, etc)
- `metric_data` - JSON с данными метрики
- `created_at` - дата создания

**Для чего:** AI сможет анализировать прогресс студента и давать персонализированные рекомендации.

---

### 2. Таблица `student_rankings`
**Назначение:** Рейтинг студентов по прогрессу и активности

**Колонки:**
- `id` - уникальный номер записи
- `user_id` - ID студента
- `total_completed_lessons` - сколько уроков завершил
- `total_xp` - сколько XP заработал
- `streak_days` - сколько дней подряд занимался
- `rank_position` - позиция в рейтинге (1, 2, 3...)
- `percentile` - процентиль (топ-10%, топ-25%, etc)
- `last_updated` - когда последний раз обновлялось

**Для чего:** Отображение рейтинга студента на странице /neurohub

**Пример данных:**
```
user_id: 123
total_completed_lessons: 15
total_xp: 450
streak_days: 7
rank_position: 3
percentile: 95.5 (студент в топ-5%)
```

---

### 3. Триггер `update_student_ranking()`
**Назначение:** Автоматически пересчитывает рейтинги при изменении прогресса

**Как работает:**
1. Студент завершил урок → запись в `student_progress`
2. Триггер срабатывает автоматически
3. Пересчитывает рейтинг для всех студентов
4. Обновляет таблицу `student_rankings`

**Зачем:** Не нужно вручную обновлять рейтинги - они всегда актуальные!

---

### 4. Функция `get_top_students(limit)`
**Назначение:** Получить топ N студентов по рейтингу

**Параметры:**
- `limit` - сколько студентов вернуть (например, 10)

**Возвращает:**
```sql
user_id | name        | completed_lessons | total_xp | rank_position | percentile
--------|-------------|-------------------|----------|---------------|------------
abc123  | Иван И.     | 35                | 1050     | 1             | 100.00
def456  | Мария К.    | 32                | 960      | 2             | 98.50
ghi789  | Алексей П.  | 28                | 840      | 3             | 96.00
```

**Для чего:** Отображение топ-студентов на дашборде, лидерборды

---

### 5. Функция `calculate_user_streak(user_id)`
**Назначение:** Посчитать сколько дней подряд студент занимался

**Параметры:**
- `user_id` - ID студента

**Возвращает:** число (например, 7 дней)

**Как работает:**
1. Берёт текущую дату
2. Идёт по дням назад
3. Проверяет была ли активность в этот день
4. Считает пока активность есть каждый день
5. Возвращает количество дней

**Пример:**
- Студент занимался: 20 ноября, 19 ноября, 18 ноября, 17 ноября
- Не занимался: 16 ноября
- Streak = 4 дня

---

### 6. Функция `update_all_streaks()`
**Назначение:** Обновить streak для всех студентов сразу

**Как использовать:** Запускать раз в день (можно через cron или вручную)

```sql
SELECT update_all_streaks();
```

**Что делает:**
1. Для каждого студента вызывает `calculate_user_streak()`
2. Обновляет `streak_days` в таблице `student_rankings`

**Зачем:** Streak нужно обновлять ежедневно (например, в полночь)

---

### 7. Функция `get_student_progress_stats(user_id)`
**Назначение:** Получить полную статистику конкретного студента

**Параметры:**
- `user_id` - ID студента

**Возвращает:**
```sql
completed_lessons | total_lessons | completion_percentage | total_xp | rank_position | streak_days
------------------|---------------|----------------------|----------|---------------|-------------
15                | 40            | 37.50                | 450      | 3             | 7
```

**Для чего:** Отображение на дашборде /neurohub

---

### 8. RLS (Row Level Security) политики
**Назначение:** Безопасность - кто что может видеть

**Правила:**
1. **Студенты** видят только свои метрики (не чужие)
2. **Админы** видят все метрики всех студентов
3. **Все** видят рейтинги (но без личных данных)

**Зачем:** Защита данных студентов

---

## 🚀 КАК ВЫПОЛНИТЬ

### Вариант 1: Через Supabase Dashboard (РЕКОМЕНДУЕТСЯ)

1. Открой https://supabase.com
2. Выбери проект `onAI Academy`
3. Левое меню → **SQL Editor**
4. Нажми **New query**
5. Скопируй **весь код** из файла `setup-ai-mentor-tables.sql`
6. Вставь в редактор
7. Нажми **Run** или `Ctrl+Enter`
8. Должно вывести: `✅ AI-Mentor tables setup complete!`

### Вариант 2: Через psql (если есть доступ к PostgreSQL)

```bash
psql -h your-database-host -U postgres -d your-database-name -f setup-ai-mentor-tables.sql
```

---

## ✅ ПРОВЕРКА ЧТО ВСЁ РАБОТАЕТ

После выполнения скрипта, выполни в SQL Editor:

```sql
-- Проверка 1: Таблицы существуют?
SELECT COUNT(*) FROM ai_analytics_metrics;
SELECT COUNT(*) FROM student_rankings;

-- Проверка 2: Функции работают?
SELECT * FROM get_top_students(10);

-- Проверка 3: Есть ли данные в рейтингах?
SELECT 
  u.full_name,
  sr.total_completed_lessons,
  sr.total_xp,
  sr.rank_position,
  sr.percentile
FROM student_rankings sr
JOIN users u ON sr.user_id = u.id
ORDER BY sr.rank_position ASC
LIMIT 10;
```

**Ожидаемый результат:**
- Запросы выполняются без ошибок
- Возвращаются данные (могут быть пустыми если нет прогресса)

---

## 🐛 ЧТО ДЕЛАТЬ ЕСЛИ ОШИБКИ

### Ошибка: "table already exists"
```sql
-- Удали старые таблицы и пересоздай:
DROP TABLE IF EXISTS ai_analytics_metrics CASCADE;
DROP TABLE IF EXISTS student_rankings CASCADE;

-- Потом выполни скрипт заново
```

### Ошибка: "column does not exist"
- Убедись что таблицы `users`, `student_progress`, `lessons` существуют
- Проверь что колонки `role`, `completed`, `xp_earned` существуют

### Ошибка: "function already exists"
```sql
-- Удали функцию и пересоздай:
DROP FUNCTION IF EXISTS get_top_students CASCADE;
DROP FUNCTION IF EXISTS calculate_user_streak CASCADE;

-- Потом выполни скрипт заново
```

---

## 📈 ЧТО ДАЛЬШЕ

После выполнения SQL скрипта:

1. ✅ Рейтинги студентов будут обновляться автоматически
2. ✅ На странице /neurohub будут показываться реальные данные
3. ✅ AI-аналитика сможет собирать метрики

**Следующие шаги:**
- Протестировать страницу /neurohub
- Проверить что данные отображаются
- Задеплоить на продакшн

---

## 💡 ПОЛЕЗНЫЕ ЗАПРОСЫ

### Посмотреть топ-10 студентов:
```sql
SELECT * FROM get_top_students(10);
```

### Посмотреть прогресс конкретного студента:
```sql
SELECT * FROM get_student_progress_stats('твой_user_id');
```

### Обновить streak для всех:
```sql
SELECT update_all_streaks();
```

### Посмотреть рейтинги всех студентов:
```sql
SELECT * FROM student_rankings ORDER BY rank_position ASC;
```

---

**Вопросы?** Пиши в чат! 💬




