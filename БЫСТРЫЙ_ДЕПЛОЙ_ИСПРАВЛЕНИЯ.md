# ⚡ БЫСТРЫЙ ДЕПЛОЙ ИСПРАВЛЕНИЯ АВТОРИЗАЦИИ

## 🎯 ЧТО ИСПРАВЛЕНО
✅ API key теперь передается в headers  
✅ Console.log не удаляются в production  
✅ Старый домен заменен на onai.academy  

---

## 🚀 ДЕПЛОЙ В 3 КОМАНДЫ

### 1️⃣ Локально - коммит и пуш
```bash
git add -A
git commit -m "fix: исправлена авторизация - добавлен API key в headers"
git push origin main
```

### 2️⃣ На сервере - обновить и собрать
```bash
ssh root@178.128.203.40 << 'EOF'
cd /var/www/onai-integrator-login
git pull origin main
npm install
npm run build
pm2 restart onai-app
pm2 save
exit
EOF
```

### 3️⃣ Проверить
Открой: **https://onai.academy**

Войди с:
- Email: `saint@onaiacademy.kz`
- Password: `Onai2134!`

---

## ✅ ЧТО ДОЛЖНО РАБОТАТЬ

В консоли браузера (F12):
```
✅ Supabase initialized: { url: "...", keyLength: 208, keyPreview: "eyJhbG..." }
```

При входе в Network → Headers:
```
✅ apikey: eyJhbGciOiJIUzI1NiIs...
✅ Authorization: Bearer eyJhbGciOiJIUzI1NiIs...
```

❌ НЕ должно быть:
```
"No API key found in request"
400 Bad Request
```

---

## 🐛 Если не работает

### Очистить кеш браузера:
`Ctrl+Shift+R` (Windows) или `Cmd+Shift+R` (Mac)

### Пересобрать на сервере:
```bash
ssh root@178.128.203.40
cd /var/www/onai-integrator-login
rm -rf dist
npm run build
pm2 restart onai-app
```

---

## 📞 Нужна помощь?
Смотри полную инструкцию: `🔧_ИСПРАВЛЕНИЕ_АВТОРИЗАЦИИ.md`

