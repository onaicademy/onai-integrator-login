# 📋 ПЛАН РАЗРАБОТКИ: VIDEO PLAYER + КОНСТРУКТОР КУРСОВ

**Дата:** 7 ноября 2025  
**Цель:** Полноценная система управления курсами как GetCourse

---

## 🎯 ЧТО НУЖНО СДЕЛАТЬ:

### **1. CUSTOM VIDEO PLAYER** 📹
- Кастомный плеер с аналитикой
- Скорость воспроизведения (0.5x, 1x, 1.5x, 2x)
- Трекинг просмотров до секунды
- Прогресс по каждому видео
- Запрет перемотки вперёд (опционально)

### **2. СИСТЕМА ЗАГРУЗКИ ВИДЕО** ⬆️
- Загрузка через Supabase Storage
- Поддержка больших файлов (chunked upload)
- Прогресс бар загрузки
- Превью перед загрузкой
- Поддержка форматов: MP4, WebM, MOV

### **3. КОНСТРУКТОР КУРСОВ** 🏗️
- Создание/редактирование курсов
- Создание/редактирование модулей
- Создание/редактирование уроков
- Drag & Drop для порядка
- Rich Text Editor для описаний
- Загрузка видео, файлов, изображений

### **4. СМЕНА ДОМЕНА** 🌐
- Новый домен: `onai.academy`
- SSL сертификаты (Let's Encrypt)
- Обновление GitHub Actions
- Обновление Nginx конфигурации

---

## 📊 РАЗБИВКА ПО ЭТАПАМ:

---

## **ЭТАП 1: БАЗА ДАННЫХ ДЛЯ ВИДЕО И АНАЛИТИКИ** 🗄️

### Задачи:
1. ✅ Создать таблицы для видео
2. ✅ Создать таблицы для аналитики просмотров
3. ✅ Создать Storage bucket для видео
4. ✅ Настроить RLS политики

### Таблицы:
```sql
-- Видео уроков
CREATE TABLE lesson_videos (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  lesson_id INTEGER NOT NULL,
  course_id INTEGER NOT NULL,
  video_url TEXT NOT NULL, -- Ссылка на видео в Storage
  duration INTEGER NOT NULL, -- Длительность в секундах
  thumbnail_url TEXT, -- Превью видео
  file_size BIGINT, -- Размер файла в байтах
  format VARCHAR(10), -- mp4, webm, mov
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- Аналитика просмотров
CREATE TABLE video_watch_analytics (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES auth.users(id),
  lesson_id INTEGER NOT NULL,
  video_id UUID NOT NULL REFERENCES lesson_videos(id),
  
  -- Прогресс просмотра
  current_time INTEGER NOT NULL DEFAULT 0, -- Текущая секунда
  total_duration INTEGER NOT NULL, -- Общая длительность
  progress_percent DECIMAL(5,2) DEFAULT 0, -- Процент просмотра
  
  -- Флаги
  is_completed BOOLEAN DEFAULT false, -- Досмотрел до конца
  is_first_watch BOOLEAN DEFAULT true, -- Первый просмотр
  
  -- Статистика
  watch_count INTEGER DEFAULT 1, -- Сколько раз смотрел
  total_watch_time INTEGER DEFAULT 0, -- Общее время просмотра (сек)
  playback_speed DECIMAL(3,2) DEFAULT 1.0, -- Скорость воспроизведения
  
  -- Временные метки
  started_at TIMESTAMPTZ DEFAULT NOW(),
  last_watched_at TIMESTAMPTZ DEFAULT NOW(),
  completed_at TIMESTAMPTZ,
  
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW(),
  
  UNIQUE(user_id, video_id)
);

-- Детальная история просмотров (для глубокой аналитики)
CREATE TABLE video_watch_events (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  analytics_id UUID NOT NULL REFERENCES video_watch_analytics(id),
  user_id UUID NOT NULL REFERENCES auth.users(id),
  video_id UUID NOT NULL REFERENCES lesson_videos(id),
  
  -- События
  event_type VARCHAR(20) NOT NULL, -- play, pause, seek, complete, speed_change
  timestamp_in_video INTEGER NOT NULL, -- В какой секунде видео произошло
  playback_speed DECIMAL(3,2), -- Скорость в момент события
  
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

### Файл миграции:
`supabase/migrations/20250107_video_player_analytics.sql`

**Время:** 1 час

---

## **ЭТАП 2: CUSTOM VIDEO PLAYER** 📹

### Задачи:
1. ✅ Создать компонент `VideoPlayer.tsx`
2. ✅ Интеграция с HTML5 Video API
3. ✅ Кастомные контролы (play, pause, volume, speed, fullscreen)
4. ✅ Прогресс бар с текущим временем
5. ✅ Выбор скорости (0.5x, 0.75x, 1x, 1.25x, 1.5x, 2x)
6. ✅ Трекинг просмотров (каждые 5 секунд → сохранение в БД)
7. ✅ Отображение прогресса (зелёные участки = просмотрено)

### Компонент:
```typescript
// src/components/video/VideoPlayer.tsx
interface VideoPlayerProps {
  videoId: string;
  videoUrl: string;
  lessonId: number;
  courseId: number;
  duration: number;
  onProgress?: (currentTime: number, totalTime: number) => void;
  onComplete?: () => void;
}
```

### Функционал:
- 🎬 Автосохранение позиции каждые 5 секунд
- 🎯 Возобновление с последней позиции
- 📊 Отправка событий в аналитику
- ⚡ Горячие клавиши (Space = play/pause, Arrow = +/- 5 сек)
- 📱 Адаптив (мобильные контролы)

### Файлы:
- `src/components/video/VideoPlayer.tsx` - главный компонент
- `src/components/video/VideoControls.tsx` - контролы
- `src/components/video/VideoProgressBar.tsx` - прогресс бар
- `src/hooks/useVideoAnalytics.ts` - хук для аналитики

**Время:** 4-6 часов

---

## **ЭТАП 3: СИСТЕМА ЗАГРУЗКИ ВИДЕО** ⬆️

### Задачи:
1. ✅ Создать Storage bucket для видео
2. ✅ Компонент загрузки `VideoUploader.tsx`
3. ✅ Chunked upload для больших файлов
4. ✅ Прогресс бар загрузки
5. ✅ Генерация превью (thumbnail)
6. ✅ Валидация формата и размера
7. ✅ Обработка ошибок

### Компонент:
```typescript
// src/components/admin/VideoUploader.tsx
interface VideoUploaderProps {
  lessonId: number;
  courseId: number;
  onUploadComplete: (videoId: string, videoUrl: string) => void;
}
```

### Функционал:
- 📤 Drag & Drop для загрузки
- 📊 Прогресс бар (процент + скорость + оставшееся время)
- 🎞️ Автоматическая генерация превью
- ✅ Поддержка форматов: MP4, WebM, MOV, AVI
- ⚠️ Валидация (макс. размер, формат)
- 🔄 Возможность отмены загрузки

### Storage bucket:
```
lesson-videos/
  ├── course-1/
  │   ├── module-1/
  │   │   ├── lesson-1/
  │   │   │   ├── video.mp4
  │   │   │   └── thumbnail.jpg
  │   │   └── lesson-2/
  │   └── module-2/
  └── course-2/
```

**Время:** 3-4 часа

---

## **ЭТАП 4: БАЗА ДАННЫХ ДЛЯ КОНСТРУКТОРА** 🗄️

### Задачи:
1. ✅ Обновить таблицу `courses`
2. ✅ Обновить таблицу `modules`
3. ✅ Обновить таблицу `lessons`
4. ✅ Добавить таблицу `lesson_content`
5. ✅ Настроить связи и индексы

### Таблицы:
```sql
-- Курсы (обновлённая структура)
CREATE TABLE courses (
  id SERIAL PRIMARY KEY,
  title VARCHAR(255) NOT NULL,
  description TEXT,
  cover_image_url TEXT,
  status VARCHAR(20) DEFAULT 'draft', -- draft, published, archived
  order_index INTEGER DEFAULT 0,
  created_by UUID REFERENCES auth.users(id),
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- Модули (обновлённая структура)
CREATE TABLE course_modules (
  id SERIAL PRIMARY KEY,
  course_id INTEGER NOT NULL REFERENCES courses(id) ON DELETE CASCADE,
  title VARCHAR(255) NOT NULL,
  description TEXT,
  order_index INTEGER DEFAULT 0,
  is_locked BOOLEAN DEFAULT false,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- Уроки (обновлённая структура)
CREATE TABLE lessons (
  id SERIAL PRIMARY KEY,
  module_id INTEGER NOT NULL REFERENCES course_modules(id) ON DELETE CASCADE,
  course_id INTEGER NOT NULL REFERENCES courses(id) ON DELETE CASCADE,
  title VARCHAR(255) NOT NULL,
  description TEXT,
  content_type VARCHAR(20) DEFAULT 'video', -- video, text, quiz, file
  order_index INTEGER DEFAULT 0,
  is_locked BOOLEAN DEFAULT false,
  duration INTEGER, -- Длительность в секундах (для видео)
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- Контент уроков (Rich Content)
CREATE TABLE lesson_content (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  lesson_id INTEGER NOT NULL REFERENCES lessons(id) ON DELETE CASCADE,
  content_type VARCHAR(20) NOT NULL, -- video, text, heading, image, file, quiz
  content_data JSONB NOT NULL, -- Гибкое хранение данных
  order_index INTEGER DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

-- Примеры content_data:
-- Видео: {"video_id": "uuid", "video_url": "...", "duration": 600}
-- Текст: {"text": "...", "format": "markdown"}
-- Заголовок: {"text": "Введение", "level": 2}
-- Изображение: {"image_url": "...", "caption": "..."}
-- Файл: {"file_url": "...", "filename": "...", "size": 1024}
```

### Файл миграции:
`supabase/migrations/20250107_course_constructor.sql`

**Время:** 2 часа

---

## **ЭТАП 5: КОНСТРУКТОР КУРСОВ** 🏗️

### Задачи:
1. ✅ Страница списка курсов (админ)
2. ✅ Страница создания/редактирования курса
3. ✅ Drag & Drop для модулей
4. ✅ Drag & Drop для уроков
5. ✅ Rich Text Editor (заголовки, текст, списки)
6. ✅ Загрузка обложек курсов
7. ✅ Предпросмотр курса

### Компоненты:
```
src/pages/admin/
  ├── CourseList.tsx - Список всех курсов
  ├── CourseEditor.tsx - Редактор курса
  ├── ModuleEditor.tsx - Редактор модуля
  └── LessonEditor.tsx - Редактор урока

src/components/admin/constructor/
  ├── CourseCard.tsx - Карточка курса
  ├── ModuleList.tsx - Список модулей (DnD)
  ├── ModuleItem.tsx - Элемент модуля
  ├── LessonList.tsx - Список уроков (DnD)
  ├── LessonItem.tsx - Элемент урока
  ├── ContentEditor.tsx - Редактор контента
  ├── RichTextEditor.tsx - Rich Text Editor
  └── MediaUploader.tsx - Загрузка медиа
```

### Функционал:

**Курсы:**
- ➕ Создать новый курс
- ✏️ Редактировать курс
- 🗑️ Удалить курс
- 📦 Дублировать курс
- 👁️ Предпросмотр
- 📊 Статистика (студенты, прогресс)

**Модули:**
- ➕ Добавить модуль
- ✏️ Редактировать модуль
- 🔄 Изменить порядок (Drag & Drop)
- 🔒 Заблокировать/разблокировать
- 🗑️ Удалить модуль

**Уроки:**
- ➕ Добавить урок
- ✏️ Редактировать урок
- 🔄 Изменить порядок (Drag & Drop)
- 📹 Добавить видео
- 📝 Добавить текст (Markdown/HTML)
- 🖼️ Добавить изображения
- 📎 Добавить файлы
- ❓ Добавить тест/опрос
- 🗑️ Удалить урок

### UI/UX:
- Drag & Drop (react-beautiful-dnd или dnd-kit)
- Rich Text Editor (TipTap или Quill)
- Модальные окна для редактирования
- Автосохранение (каждые 30 сек)
- Отмена/Повтор (Undo/Redo)

**Время:** 10-12 часов

---

## **ЭТАП 6: ИНТЕГРАЦИЯ ПЛЕЕРА В УРОКИ** 🎬

### Задачи:
1. ✅ Обновить страницу `Lesson.tsx`
2. ✅ Интегрировать `VideoPlayer`
3. ✅ Показывать прогресс урока
4. ✅ Кнопка "Отметить как пройденный"
5. ✅ Навигация к следующему уроку
6. ✅ Блокировка следующих уроков (опционально)

### Компоненты:
- `src/pages/Lesson.tsx` - обновлённая страница урока
- `src/components/lesson/LessonHeader.tsx` - шапка урока
- `src/components/lesson/LessonContent.tsx` - контент урока
- `src/components/lesson/LessonNavigation.tsx` - навигация

### Функционал:
- 📹 Отображение видео с кастомным плеером
- 📝 Отображение текста (Markdown)
- 📎 Список файлов для скачивания
- ✅ Прогресс урока (0-100%)
- ⏭️ Автопереход к следующему уроку
- 💬 Комментарии (будущее)

**Время:** 3-4 часа

---

## **ЭТАП 7: АДМИН ПАНЕЛЬ - АНАЛИТИКА ВИДЕО** 📊

### Задачи:
1. ✅ Страница аналитики по курсам
2. ✅ Аналитика по урокам
3. ✅ Детальная статистика по студентам
4. ✅ Графики и диаграммы
5. ✅ Экспорт данных (CSV)

### Компоненты:
```
src/pages/admin/analytics/
  ├── CourseAnalytics.tsx - Аналитика курса
  ├── LessonAnalytics.tsx - Аналитика урока
  └── StudentProgress.tsx - Прогресс студента
```

### Метрики:
- 👥 Количество студентов
- 📹 Средний процент просмотра
- ⏱️ Среднее время просмотра
- 📊 Распределение по урокам
- 🎯 Процент завершивших курс
- 📉 Drop-off points (где бросают)
- 🏆 Топ уроков (по просмотрам)

**Время:** 4-5 часов

---

## **ЭТАП 8: СМЕНА ДОМЕНА** 🌐

### Задачи:
1. ✅ Купить/подключить домен `onai.academy`
2. ✅ Настроить DNS записи
3. ✅ Обновить Nginx конфигурацию
4. ✅ Установить SSL сертификаты (Certbot)
5. ✅ Обновить `.env` файлы
6. ✅ Обновить GitHub Actions
7. ✅ Обновить Supabase redirect URLs

### Шаги:

**1. DNS записи:**
```
Тип: A
Имя: @
Значение: 178.128.203.40 (IP Digital Ocean)

Тип: A
Имя: www
Значение: 178.128.203.40
```

**2. Nginx конфигурация:**
```nginx
server {
    listen 80;
    server_name onai.academy www.onai.academy;
    return 301 https://onai.academy$request_uri;
}

server {
    listen 443 ssl http2;
    server_name onai.academy www.onai.academy;
    
    ssl_certificate /etc/letsencrypt/live/onai.academy/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/onai.academy/privkey.pem;
    
    # ... остальная конфигурация
}
```

**3. SSL сертификаты:**
```bash
sudo certbot --nginx -d onai.academy -d www.onai.academy
```

**4. Обновить `.env`:**
```
VITE_APP_URL=https://onai.academy
VITE_SUPABASE_URL=https://your-project.supabase.co
```

**5. Supabase redirect URLs:**
- Добавить `https://onai.academy/**` в Allowed URLs
- Обновить OAuth providers (если есть)

**Время:** 2-3 часа

---

## 📊 ОБЩИЙ ПЛАН:

| Этап | Задача | Время | Приоритет |
|------|--------|-------|-----------|
| 1 | База данных (видео + аналитика) | 1 ч | 🔴 Высокий |
| 2 | Custom Video Player | 4-6 ч | 🔴 Высокий |
| 3 | Система загрузки видео | 3-4 ч | 🔴 Высокий |
| 4 | База данных (конструктор) | 2 ч | 🟡 Средний |
| 5 | Конструктор курсов | 10-12 ч | 🟡 Средний |
| 6 | Интеграция плеера в уроки | 3-4 ч | 🟡 Средний |
| 7 | Админ панель - аналитика | 4-5 ч | 🟢 Низкий |
| 8 | Смена домена | 2-3 ч | 🔴 Высокий |

**Общее время:** 29-37 часов  
**Оценка:** 4-5 рабочих дней

---

## 🎯 ПОРЯДОК ВЫПОЛНЕНИЯ:

### **День 1:** Фундамент
1. ✅ Этап 8: Смена домена (чтобы сразу на правильном работать)
2. ✅ Этап 1: База данных (видео + аналитика)
3. ✅ Этап 4: База данных (конструктор)

### **День 2:** Video Player
1. ✅ Этап 2: Custom Video Player
2. ✅ Этап 3: Система загрузки видео
3. ✅ Этап 6: Интеграция плеера в уроки

### **День 3-4:** Конструктор
1. ✅ Этап 5: Конструктор курсов (большой этап)

### **День 5:** Аналитика
1. ✅ Этап 7: Админ панель - аналитика
2. ✅ Тестирование всех компонентов
3. ✅ Деплой на сервер

---

## 🚀 С ЧЕГО НАЧАТЬ?

### **Вариант 1: Сначала домен**
1. Сменить домен на `onai.academy`
2. Настроить SSL
3. Затем делать функционал

### **Вариант 2: Сначала функционал**
1. Делать всё на текущем домене
2. В конце сменить домен

**Рекомендую Вариант 1** - чтобы сразу всё на правильном домене тестировать.

---

## 🤔 ВОПРОСЫ:

1. **С какого этапа начинаем?** (Рекомендую: Смена домена → Видео плеер)
2. **Нужна ли блокировка уроков?** (пока не досмотришь предыдущий)
3. **Нужны ли тесты/опросы в уроках?** (или только видео+текст)
4. **Максимальный размер видео?** (например, 5GB на урок)
5. **Где будут храниться видео?** (Supabase Storage или внешний CDN?)

---

**Скажи с чего начинаем и поехали! 🚀**

