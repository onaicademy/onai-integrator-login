# ✅ КРИТИЧЕСКИЕ ИСПРАВЛЕНИЯ ЗАВЕРШЕНЫ

**Дата:** 8 ноября 2025  
**Статус:** Готово к тестированию

---

## 🔧 ЧТО ИСПРАВЛЕНО

### **1. Edge Function `create-student`**

#### **Проблемы:**
- ❌ Роль всегда = 'student' (игнорировалась из формы)
- ❌ phone не сохранялся (NULL)
- ❌ full_name не сохранялся (NULL)
- ❌ Курсы не назначались

#### **Исправления:**
- ✅ Принимается `role` из формы и сохраняется в profiles
- ✅ `phone` сохраняется в student_profiles
- ✅ `full_name` сохраняется везде
- ✅ `account_expires_at = NULL` для не-студентов (вечный доступ)
- ✅ Добавлена логика назначения курсов в `student_courses`
- ✅ Исправлено: `user_id` → `id` в student_profiles

**Файл:** `supabase/functions/create-student/index.ts`

---

### **2. Форма создания пользователя**

#### **Добавлено:**
- ✅ Чекбоксы для выбора курсов (Интегратор 2.0, Креатор)
- ✅ Валидация пароля (только латиница + символы)
- ✅ "Срок действия" скрывается для не-студентов
- ✅ `account_expires_at = null` для кураторов/админов

#### **Удалено:**
- ✅ Дублирующая кнопка деактивации (осталась только ⛔)

**Файл:** `src/pages/admin/StudentsActivity.tsx`

---

## 📋 ТЕСТИРОВАНИЕ

### **Шаг 1: Создай НОВОГО тестового пользователя**

```
URL: https://localhost:8080/admin/students-activity

Данные:
- Email: test-fixed@gmail.com
- Полное имя: Тест Фикс
- Телефон: +7 777 999 0000
- Пароль: testpass123 (только латиница!)
- Роль: 👨‍🏫 Куратор
- Курсы: ✅ Интегратор 2.0, ✅ Креатор
- Срок действия: СКРЫТО (т.к. role !== student)
```

**Ожидаемый результат:**
- ✅ Уведомление "Пользователь создан!"
- ✅ Пользователь появился в таблице

---

### **Шаг 2: Проверь в Supabase**

#### **Таблица `profiles`:**
```sql
SELECT * FROM profiles WHERE email = 'test-fixed@gmail.com';

-- Ожидаемый результат:
-- ✅ role = 'curator' (НЕ student!)
-- ✅ full_name = 'Тест Фикс'
-- ✅ account_expires_at = NULL (вечный доступ)
-- ✅ is_active = true
```

#### **Таблица `student_profiles`:**
```sql
SELECT * FROM student_profiles WHERE id = (
  SELECT id FROM profiles WHERE email = 'test-fixed@gmail.com'
);

-- Ожидаемый результат:
-- ❌ НЕТ записи (т.к. role = 'curator', а не 'student')
```

#### **Таблица `student_courses`:**
```sql
SELECT * FROM student_courses WHERE student_id = (
  SELECT id FROM profiles WHERE email = 'test-fixed@gmail.com'
);

-- Ожидаемый результат:
-- ✅ 2 записи:
--    course_id = '6518f042-54b9-4b69-8e93-b18df98cd7eb' (Интегратор 2.0)
--    course_id = 'febcb120-b8b4-4a8a-bd2e-62275f6d0115' (Креатор)
--    status = 'active'
```

---

### **Шаг 3: Создай студента для проверки**

```
Данные:
- Email: test-student@gmail.com
- Полное имя: Тестовый Студент
- Телефон: +7 777 888 0000
- Пароль: studentpass
- Роль: 🎓 Студент
- Курсы: ✅ Интегратор 2.0
- Срок действия: 3 месяца (ВИДИМО т.к. role = student)
```

**Проверь в Supabase:**
```sql
-- profiles:
SELECT role, full_name, account_expires_at 
FROM profiles WHERE email = 'test-student@gmail.com';

-- Ожидаемый результат:
-- ✅ role = 'student'
-- ✅ full_name = 'Тестовый Студент'
-- ✅ account_expires_at через ~90 дней

-- student_profiles:
SELECT full_name, phone 
FROM student_profiles 
WHERE id = (SELECT id FROM profiles WHERE email = 'test-student@gmail.com');

-- Ожидаемый результат:
-- ✅ full_name = 'Тестовый Студент'
-- ✅ phone = '+7 777 888 0000'
```

---

## 🔍 ЛОГИ ДЛЯ ПРОВЕРКИ

### **В консоли браузера (F12):**

```
📤 Создание пользователя: {email, fullName, phone, password, role, selectedCourses}
📅 Срок действия: вечный (role !== student)  // для кураторов
или
📅 Срок действия: 3 месяцев                   // для студентов
📅 Истекает: 2025-02-08T...
```

### **В Edge Function логах (Supabase Dashboard):**

```
Открой: https://supabase.com/dashboard/project/arqhkacellqbhjhbebfh/functions
→ create-student → Logs

Должно быть:
📥 Полученные данные: {email, full_name, phone, role, account_expires_at, course_ids}
🔑 Creating user with provided password...
✅ User created in auth.users: <user_id>
👤 Creating profile...
✅ Создан profiles с role: curator
📚 Назначаю курсы: ['6518f042...', 'febcb120...']
✅ Назначено курсов: 2
🎉 User created successfully!
```

---

## ⚠️ ВОЗМОЖНЫЕ ПРОБЛЕМЫ

### **Проблема: "Phone is required"**

**Причина:** Edge Function не задеплоена

**Решение:**
```bash
supabase functions deploy create-student --project-ref arqhkacellqbhjhbebfh
```

---

### **Проблема: "Failed to create profile"**

**Причина:** RLS политики блокируют создание

**Решение:**
```sql
-- Проверь политики:
SELECT * FROM pg_policies WHERE tablename = 'profiles';

-- Убедись что есть политика для insert с service_role
```

---

### **Проблема: Курсы не назначаются**

**Причина:** Таблица `student_courses` не существует или нет колонок

**Решение:**
```sql
-- Проверь структуру таблицы:
SELECT * FROM information_schema.columns WHERE table_name = 'student_courses';

-- Должны быть колонки:
-- - student_id
-- - course_id
-- - status
-- - enrolled_at
```

---

## ✅ ФИНАЛЬНЫЙ ЧЕКЛИСТ

- [ ] Создан куратор с role='curator'
- [ ] У куратора account_expires_at = NULL
- [ ] У куратора НЕТ записи в student_profiles
- [ ] Куратору назначены 2 курса в student_courses
- [ ] Создан студент с role='student'
- [ ] У студента account_expires_at через ~90 дней
- [ ] У студента ЕСТЬ запись в student_profiles с phone
- [ ] У студента назначен курс в student_courses
- [ ] Валидация пароля (латиница) работает
- [ ] "Срок действия" скрывается для не-студентов
- [ ] В таблице только одна кнопка "⛔ Деактивировать"

---

## 🚀 СЛЕДУЮЩИЙ ШАГ

После успешного тестирования:

1. **Деплой Edge Function:**
```bash
supabase functions deploy create-student --project-ref arqhkacellqbhjhbebfh
```

2. **Commit изменений:**
```bash
git add -A
git commit -m "fix: исправлена роль, phone, full_name, курсы в create-student"
git push origin main
```

3. **Деплой на сервер:**
```bash
./deploy.sh
```

---

**ВСЁ ГОТОВО К ТЕСТИРОВАНИЮ!** 🎯

