# 🧪 ИНСТРУКЦИЯ ПО ТЕСТИРОВАНИЮ РЕДИРЕКТА НА /login

## ❓ Проблема:
Страница `/admin/students-activity` не редиректит на `/login` для незалогиненных пользователей.

---

## ✅ ПРАВИЛЬНЫЙ СПОСОБ ТЕСТИРОВАНИЯ:

### **Метод 1: Режим инкогнито** ⭐ (Самый надёжный)

1. **Открой новое окно инкогнито:**
   - Chrome/Edge: `Ctrl+Shift+N` (Windows) или `Cmd+Shift+N` (Mac)
   - Firefox: `Ctrl+Shift+P` (Windows) или `Cmd+Shift+P` (Mac)

2. **Перейди на:**
   ```
   https://localhost:8080/admin/students-activity
   ```

3. **Ожидаемый результат:**
   - ✅ Редирект на `https://localhost:8080/login`
   - ✅ В консоли (F12): `🔒 Проверка авторизации...` → `❌ Не авторизован → редирект на /login`

4. **Если НЕ работает:**
   - Открой консоль (F12) и скопируй ВСЕ логи
   - Проверь что в консоли нет ошибок

---

### **Метод 2: Очистка localStorage**

1. **Открой сайт:**
   ```
   https://localhost:8080
   ```

2. **Открой консоль (F12) и выполни:**
   ```javascript
   console.log('🧹 Очистка всех данных...');
   localStorage.clear();
   sessionStorage.clear();
   console.log('✅ Очищено!');
   location.reload();
   ```

3. **После перезагрузки попробуй зайти на:**
   ```
   https://localhost:8080/admin/students-activity
   ```

4. **Ожидаемый результат:**
   - ✅ Редирект на `/login`
   - ✅ В консоли: `🔒 Проверка авторизации...` → `❌ Не авторизован → редирект на /login`

---

### **Метод 3: Явный выход**

1. **Залогинься на сайт:**
   ```
   https://localhost:8080/login
   ```

2. **Перейди в настройки:**
   ```
   https://localhost:8080/settings
   ```

3. **Нажми кнопку "Выйти"**

4. **Проверь что попал на `/login`**

5. **Теперь попробуй зайти на:**
   ```
   https://localhost:8080/admin/students-activity
   ```

6. **Ожидаемый результат:**
   - ✅ Редирект на `/login`

---

## 🔍 ЧТО СМОТРЕТЬ В КОНСОЛИ:

### ✅ **ПРАВИЛЬНОЕ ПОВЕДЕНИЕ (незалогиненный):**
```
🔒 Проверка авторизации...
❌ Не авторизован → редирект на /login
```

### ✅ **ПРАВИЛЬНОЕ ПОВЕДЕНИЕ (залогиненный):**
```
🔒 Проверка авторизации...
✅ Авторизован: saint@onaiacademy.kz
📋 Начало загрузки студентов...
✅ Загружено студентов: 12
```

### ❌ **НЕПРАВИЛЬНОЕ ПОВЕДЕНИЕ:**
```
(нет логов о проверке авторизации)
ИЛИ
(страница загружается без редиректа)
```

---

## 🐛 ЕСЛИ НЕ РАБОТАЕТ:

1. **Открой консоль (F12) в режиме инкогнито**
2. **Перейди на `https://localhost:8080/admin/students-activity`**
3. **Скопируй ВСЕ логи из консоли**
4. **Отправь мне логи + скриншот того, что видишь на экране**

---

## 📊 ПРОВЕРКА localStorage/sessionStorage:

В консоли (F12) выполни:

```javascript
console.log('📦 localStorage:', localStorage);
console.log('📦 sessionStorage:', sessionStorage);
console.log('🔑 Supabase keys:', Object.keys(localStorage).filter(k => k.includes('supabase')));
```

Это покажет все данные, которые хранятся локально.

---

## ⚠️ ВАЖНО:

- Обычная очистка кэша браузера **НЕ УДАЛЯЕТ** localStorage/sessionStorage
- Supabase хранит JWT токен в localStorage под ключом `sb-<project_ref>-auth-token`
- Режим инкогнито **всегда** даст чистое состояние

