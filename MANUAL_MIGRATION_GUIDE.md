# 📋 Руководство по применению миграций Supabase

**Дата:** 7 ноября 2025  
**Проект:** onAI Academy Platform

---

## 🎯 Цель

Применить 5 существующих миграций к базе данных Supabase для активации:
- ✅ Системы достижений
- ✅ Диалогов AI-куратора
- ✅ Таблиц диагностики

---

## 🚀 СПОСОБ 1: Автоматический (РЕКОМЕНДУЕТСЯ)

### Запустите скрипт:

```bash
cd "/Users/miso/Documents/MVP onAI Academy Platform/onai-integrator-login"
chmod +x apply-migrations.sh
./apply-migrations.sh
```

### Что произойдёт:
1. ✅ Установка Supabase CLI (если нужно)
2. ✅ Авторизация в Supabase (потребуется токен)
3. ✅ Связывание проекта
4. ✅ Применение всех миграций
5. ✅ Проверка результата

### Получение Access Token:
1. Откройте: https://supabase.com/dashboard/account/tokens
2. Нажмите "Generate new token"
3. Скопируйте токен
4. Вставьте в терминал когда попросит

---

## 🖱️ СПОСОБ 2: Через Dashboard (Если скрипт не работает)

### Шаг 1: Откройте Supabase Dashboard

```
https://supabase.com/dashboard/project/capdjvokjdivxjfddmx
```

### Шаг 2: Откройте SQL Editor

1. В левом меню найдите **"SQL Editor"**
2. Нажмите **"New query"**

### Шаг 3: Применение миграций

**ВАЖНО: Применяйте СТРОГО по порядку!**

---

#### Миграция 1: `20251103164013_*.sql`

<details>
<summary>📂 Показать содержимое</summary>

```bash
# В терминале выполните:
cat "supabase/migrations/20251103164013_1d2beacd-796b-43a7-98a9-e19360b685c3.sql"
```

Скопируйте весь вывод, вставьте в SQL Editor, нажмите **Run** (Ctrl+Enter)

</details>

**Проверка:**
```sql
-- Должна выполниться без ошибок
-- Если ошибка "already exists" - это нормально, миграция уже применена
```

---

#### Миграция 2: `20251103182016_*.sql`

<details>
<summary>📂 Показать содержимое</summary>

```bash
cat "supabase/migrations/20251103182016_f71c5f0a-2e59-406d-a8ff-3bdf4795a1e9.sql"
```

Скопируйте, вставьте в SQL Editor, нажмите **Run**

</details>

---

#### Миграция 3: `20251104_add_diagnostics_tables.sql`

<details>
<summary>📂 Показать содержимое</summary>

```bash
cat "supabase/migrations/20251104_add_diagnostics_tables.sql"
```

Скопируйте, вставьте в SQL Editor, нажмите **Run**

</details>

---

#### Миграция 4: `20250115_add_ai_curator_chat_tables.sql` ⭐

**ВАЖНО: Это таблицы для AI-куратора!**

<details>
<summary>📂 Показать содержимое</summary>

```bash
cat "supabase/migrations/20250115_add_ai_curator_chat_tables.sql"
```

Скопируйте, вставьте в SQL Editor, нажмите **Run**

</details>

**Ожидаемый результат:**
```
✅ Создана: ai_curator_threads
✅ Создана: ai_curator_messages
✅ Создана: ai_curator_attachments
✅ Создана: ai_curator_metrics
```

---

#### Миграция 5: `20250116_achievements_system.sql` ⭐

**ВАЖНО: Это система достижений!**

<details>
<summary>📂 Показать содержимое</summary>

```bash
cat "supabase/migrations/20250116_achievements_system.sql"
```

Скопируйте, вставьте в SQL Editor, нажмите **Run**

</details>

**Ожидаемый результат:**
```
✅ Создана: user_achievements
✅ Создана: achievement_history
✅ Создана: user_statistics
✅ Создана функция: initialize_user_statistics()
✅ Создан триггер: on_new_user_initialize_statistics
```

---

### Шаг 4: Проверка успешного применения

Выполните в SQL Editor:

```sql
-- Проверка всех созданных таблиц
SELECT 
  table_name,
  table_type
FROM information_schema.tables 
WHERE table_schema = 'public' 
AND (
  table_name LIKE '%ai_curator%' 
  OR table_name LIKE '%achievement%'
  OR table_name = 'user_statistics'
  OR table_name LIKE '%diagnostic%'
)
ORDER BY table_name;
```

**Ожидаемый результат (минимум 7 таблиц):**

```
ai_curator_attachments
ai_curator_messages
ai_curator_metrics
ai_curator_threads
achievement_history
user_achievements
user_statistics
```

**Если видишь эти таблицы - ВСЁ ОТЛИЧНО! ✅**

---

## 🆘 Решение проблем

### Ошибка: "relation already exists"

**Это нормально!** Таблица уже создана ранее.

Решение:
```sql
-- Можно пропустить или удалить таблицу:
DROP TABLE IF EXISTS <table_name> CASCADE;
-- Затем применить миграцию заново
```

### Ошибка: "permission denied"

**Причина:** Недостаточно прав.

Решение:
1. Проверьте что вы Owner проекта
2. Или используйте Service Role Key вместо Anon Key

### Ошибка: "syntax error"

**Причина:** Не весь SQL код скопирован.

Решение:
1. Скопируйте ВЕСЬ файл миграции от начала до конца
2. Убедитесь что не потерялись символы при копировании

### Ошибка: "timeout"

**Причина:** Большая миграция.

Решение:
1. Подождите 30-60 секунд
2. Попробуйте снова
3. Разбейте миграцию на части

---

## ✅ После применения миграций

### Что изменится:

#### 1. AI-куратор получит доступ к реальным данным:
```typescript
// Вместо:
const userId = 'user-1'; // Mock

// Будет:
const { data: { user } } = await supabase.auth.getUser();
const userId = user.id; // Реальный ID
```

#### 2. Достижения начнут сохраняться:
```typescript
// Вместо:
const MOCK_USER_ACHIEVEMENTS = { ... }

// Будет:
const { data: stats } = await supabase
  .from('user_statistics')
  .select('*')
  .eq('user_id', userId)
  .single();
```

#### 3. Админ-панель покажет реальные диалоги:
```typescript
// Вместо:
const mockDialogs = MOCK_AI_CHAT_DATA;

// Будет:
const { data: dialogs } = await supabase
  .from('ai_curator_threads')
  .select('*, messages(*)')
  .eq('student_id', studentId);
```

---

## 🔄 Следующие шаги

После успешного применения миграций:

### 1. ✅ Обновить код (автоматически через Cursor)

Я создам скрипт который обновит:
- `src/lib/achievements-api.ts` - подключение к Supabase
- `src/components/profile/v2/AIChatDialog.tsx` - сохранение диалогов
- `src/pages/admin/AICuratorChats.tsx` - загрузка реальных данных

### 2. ✅ Протестировать на localhost

```bash
npm run dev
```

Проверить:
- [ ] AI-куратор сохраняет диалоги
- [ ] Достижения обновляются
- [ ] Админ-панель показывает данные

### 3. ✅ Задеплоить на Digital Ocean

```bash
./DEPLOY_SERVER.sh
```

---

## 📊 Статистика миграций

| Миграция | Размер | Таблиц | Важность |
|----------|--------|--------|----------|
| 20251103164013 | 662 B | ? | 🟡 Средняя |
| 20251103182016 | 2.8 KB | ? | 🟡 Средняя |
| 20251104_diagnostics | 4.6 KB | 3-4 | 🟢 Низкая |
| 20250115_ai_curator | 12.8 KB | 4 | 🔴 КРИТИЧНАЯ |
| 20250116_achievements | 7.8 KB | 3 | 🔴 КРИТИЧНАЯ |
| **ИТОГО** | **28.6 KB** | **10-12** | - |

---

## 💡 Полезные SQL команды

### Просмотр всех таблиц:
```sql
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public'
ORDER BY table_name;
```

### Просмотр структуры таблицы:
```sql
SELECT 
  column_name,
  data_type,
  is_nullable
FROM information_schema.columns
WHERE table_name = 'user_achievements'
ORDER BY ordinal_position;
```

### Проверка RLS политик:
```sql
SELECT 
  schemaname,
  tablename,
  policyname,
  permissive,
  roles,
  cmd,
  qual
FROM pg_policies
WHERE tablename LIKE '%achievement%'
   OR tablename LIKE '%ai_curator%';
```

### Тестовый запрос (после применения):
```sql
-- Должен вернуть пустой результат (таблица пустая)
SELECT * FROM user_achievements LIMIT 5;

-- Должен вернуть 0
SELECT COUNT(*) FROM user_statistics;
```

---

## 🎉 Поздравляю!

После выполнения всех шагов у вас будет:

✅ **Полноценная система достижений**  
✅ **Хранилище диалогов AI-куратора**  
✅ **Метрики и аналитика**  
✅ **Готовность к production**

---

## 📞 Помощь

Если что-то пошло не так:
1. Проверьте логи в Supabase Dashboard → Database → Logs
2. Посмотрите SUPABASE_MIGRATIONS_STATUS.md
3. Попросите помощь у AI ассистента

---

**Удачи! 🚀**

