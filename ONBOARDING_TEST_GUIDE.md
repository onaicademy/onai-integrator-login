# 🎓 ТЕСТИРОВАНИЕ ИНТЕРАКТИВНОГО ОБУЧЕНИЯ

**Дата**: 19 декабря 2025  
**Статус**: ✅ Готово к тестированию  
**Режим**: ТОЛЬКО ЛОКАЛЬНО

---

## 🚨 ВАЖНО: ПЕРЕД ТЕСТИРОВАНИЕМ

### 1. Применить миграцию БД (2 минуты):

```sql
-- В Supabase Tripwire DB → SQL Editor:
-- Скопируй весь контент из файла:
-- supabase/migrations/20251219_create_onboarding_progress.sql
-- Вставь и выполни (Run)

-- Проверка:
SELECT * FROM traffic_onboarding_progress;
-- Должна вернуть пустую таблицу (это OK)
```

### 2. Серверы запущены:
```
✅ Backend:  http://localhost:3000
✅ Frontend: http://localhost:8080
```

---

## 🧪 ТЕСТ 1: ОБУЧЕНИЕ ТАРГЕТОЛОГА (3 минуты)

### Шаг 1: Очистить localStorage
```
1. Открой Chrome
2. F12 → Console
3. Выполни:
   localStorage.clear();
   location.reload();
```

### Шаг 2: Войти как новый пользователь
```
1. http://localhost:8080/traffic/login
2. Email: kenesary@onai.academy
3. Password: changeme123
4. Нажми "Войти"
```

### Шаг 3: Проверить запуск обучения
```
✅ Через 1.5 секунды должно появиться:
   - Затемнение экрана (черный оверлей с блюром)
   - Модальное окно с приветствием
   - Текст: "🎉 Добро пожаловать, Kenesary!"
   - Кнопка "Далее →"
```

### Шаг 4: Пройти все 8 шагов
```
Шаг 1/8: Приветствие 🎉
  ✅ Видно модальное окно?
  ✅ Текст читаем?
  ✅ Прогресс показывается вверху?

Шаг 2/8: Переключение языков 🌐
  ✅ Подсвечивается кнопка "РУС"?
  ✅ Зеленая рамка вокруг кнопки?
  ✅ Остальное затемнено?

Шаг 3/8: Мои результаты ⭐
  ✅ Подсвечивается БОЛЬШАЯ зеленая кнопка?
  ✅ Описание понятное?
  ✅ Текст: "твой лучший друг"?

Шаг 4/8: Статистика 📊
  ✅ Подсвечиваются ВСЕ 6 карточек метрик?
  ✅ Описание каждой метрики?
  ✅ Доход, Затраты, ROAS, CPA, Клики, Показы?

Шаг 5/8: Таблица результатов 📈
  ✅ Подсвечивается таблица команд?
  ✅ Текст: "твоя команда - где активна AI"?

Шаг 6/8: AI Рекомендации 🤖
  ✅ Подсвечивается кнопка AI (иконка Sparkles)?
  ✅ Описание функций AI?

Шаг 7/8: Sidebar меню 📱
  ✅ Подсвечивается боковое меню?
  ✅ Описание навигации?

Шаг 8/8: Готово! 🎉
  ✅ Поздравление?
  ✅ Желтая карточка с предупреждением про UTM?
  ✅ Кнопка "Завершить"?
```

### Шаг 5: Проверить сохранение в БД
```sql
-- В Supabase SQL Editor:
SELECT * FROM traffic_onboarding_progress 
WHERE user_id = (
  SELECT id FROM traffic_users 
  WHERE email = 'kenesary@onai.academy'
);

-- Ожидаемый результат:
-- is_completed: true
-- tour_type: 'targetologist'
-- view_count: 1
```

---

## 🧪 ТЕСТ 2: ОБУЧЕНИЕ АДМИНА (3 минуты)

### Шаг 1: Выйти и очистить
```
1. Нажми "Выйти" в sidebar
2. F12 → Console:
   localStorage.clear();
```

### Шаг 2: Войти как админ
```
1. http://localhost:8080/traffic/login
2. Email: admin@onai.academy
3. Password: admin123
4. Нажми "Войти"
```

### Шаг 3: Проверить админский тур
```
✅ Через 1.5 сек появляется обучение?
✅ Текст: "👑 Добро пожаловать, Admin!"?
✅ Упоминается "полный доступ"?

Шаги админа:
1/6: Приветствие 👑
2/6: Dashboard (подсветка пункта меню)
3/6: Источники продаж (подсветка)
4/6: Безопасность (подсветка)
5/6: Настройки (подсветка)
6/6: Готово!
```

---

## 🧪 ТЕСТ 3: ПОВТОРНЫЙ ВХОД (без обучения)

### Шаг 1: Выйти и войти снова
```
1. Выйди из админа
2. Войди снова: admin@onai.academy / admin123
3. ✅ Обучение НЕ должно запуститься!
4. ✅ Сразу попадаешь в Dashboard
```

### Проверка в БД:
```sql
SELECT is_completed FROM traffic_onboarding_progress
WHERE user_id = (SELECT id FROM traffic_users WHERE email = 'admin@onai.academy');

-- Должно вернуть: true
```

---

## 🧪 ТЕСТ 4: ПРОПУСТИТЬ ОБУЧЕНИЕ

### Шаг 1: Создать нового юзера (или очистить прогресс)
```sql
-- В БД:
DELETE FROM traffic_onboarding_progress 
WHERE user_id = (SELECT id FROM traffic_users WHERE email = 'arystan@onai.academy');
```

### Шаг 2: Войти и пропустить
```
1. Очисти localStorage
2. Войди как: arystan@onai.academy / changeme123
3. Когда появится обучение - нажми ✕ (закрыть)
4. ✅ Обучение должно закрыться
5. ✅ Попадаешь в Dashboard
```

### Проверка в БД:
```sql
SELECT is_completed, skip_count FROM traffic_onboarding_progress
WHERE user_id = (SELECT id FROM traffic_users WHERE email = 'arystan@onai.academy');

-- Ожидается:
-- is_completed: true (потому что закрыли)
-- skip_count: 0 (не реализовано в текущей версии)
```

---

## 🧪 ТЕСТ 5: ПЕРЕЗАПУСК ОБУЧЕНИЯ

### Вариант 1: Через БД
```sql
UPDATE traffic_onboarding_progress
SET is_completed = false,
    current_step = 0
WHERE user_id = (SELECT id FROM traffic_users WHERE email = 'kenesary@onai.academy');
```

### Вариант 2: Через localStorage
```javascript
// В консоли браузера:
localStorage.clear();
location.reload();
```

### Проверка:
```
1. Войди как kenesary@onai.academy
2. ✅ Обучение запускается снова?
3. ✅ view_count увеличился на 1?
```

---

## 🎨 ТЕСТ 6: ДИЗАЙН И UX

### Проверить стили:

```
✅ Модальное окно:
   - Градиентный фон (темный)
   - Зеленая рамка (#00FF88)
   - Тени и свечение
   - Скругленные углы

✅ Кнопки:
   - "Далее" - яркая зеленая
   - "Назад" - прозрачная с рамкой
   - "Закрыть" (✕) - в правом верхнем углу

✅ Подсветка элементов:
   - Зеленая рамка вокруг элемента
   - Остальное затемнено
   - Пульсация подсветки

✅ Анимации:
   - Плавное появление модалки (fade + scale)
   - Hover эффекты на кнопках
   - Transition между шагами
```

---

## 🧪 ТЕСТ 7: MOBILE АДАПТАЦИЯ

### Шаг 1: DevTools
```
1. F12 → Toggle device toolbar (Ctrl+Shift+M)
2. Выбери: iPhone 12 Pro
3. Перезагрузи страницу
```

### Шаг 2: Проверить на мобильном
```
✅ Модальное окно адаптировано?
✅ Текст читаем?
✅ Кнопки на всю ширину?
✅ Подсветка работает?
```

---

## 📊 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

### После тестирования должно быть:

```sql
-- В БД должны появиться записи:
SELECT 
  u.email,
  o.tour_type,
  o.is_completed,
  o.view_count,
  o.started_at,
  o.completed_at
FROM traffic_onboarding_progress o
JOIN traffic_users u ON o.user_id = u.id
ORDER BY o.started_at DESC;

-- Ожидается минимум 2 записи:
-- kenesary@onai.academy | targetologist | true | 1 | ...
-- admin@onai.academy    | admin         | true | 1 | ...
```

### Визуально:

✅ Обучение запускается автоматически при первом входе  
✅ Подсветка элементов работает  
✅ Все 8 шагов для таргетолога проходятся  
✅ Все 6 шагов для админа проходятся  
✅ Дизайн премиум (градиенты, свечение, анимации)  
✅ Кнопки работают (Далее, Назад, Закрыть)  
✅ Прогресс показывается (Шаг X из Y)  
✅ Повторный вход - обучение не показывается  
✅ Mobile адаптация работает  

---

## 🐛 TROUBLESHOOTING

### Проблема: Обучение не запускается

**Причины**:
1. Миграция не применена
2. API недоступен
3. User ID не передается

**Решение**:
```javascript
// F12 → Console:
// Проверь ошибки
console.log(localStorage.getItem('traffic_user'));

// Должно быть что-то вроде:
// {"id":"uuid","email":"kenesary@onai.academy",...}
```

### Проблема: Элементы не подсвечиваются

**Причины**:
1. data-tour атрибут не добавлен
2. Элемент не видим на странице
3. Селектор неверный

**Решение**:
```javascript
// F12 → Console:
document.querySelector('[data-tour="my-results-button"]');
// Должен вернуть элемент
```

### Проблема: Стили не применяются

**Решение**:
```bash
# Очистить кеш и перезапустить:
rm -rf node_modules/.vite
npm run dev -- --port 8080
```

### Проблема: Backend ошибка 500

**Решение**:
```bash
# Проверить логи backend:
# В терминале где запущен backend
# Должны быть строки про onboarding

# Или проверь в терминалах:
cat /Users/miso/.cursor/projects/Users-miso-onai-integrator-login/terminals/*.txt | grep onboarding
```

---

## ✅ ЧЕКЛИСТ

### Перед тестированием:
- [ ] Миграция `20251219_create_onboarding_progress.sql` применена
- [ ] Backend перезапущен с новым кодом
- [ ] Frontend перезапущен с новым кодом
- [ ] Driver.js установлен (`npm install driver.js`)

### Во время тестирования:
- [ ] Обучение таргетолога (8 шагов)
- [ ] Обучение админа (6 шагов)
- [ ] Пропуск обучения (✕)
- [ ] Повторный вход (обучение не показывается)
- [ ] Mobile адаптация
- [ ] Прогресс сохраняется в БД

### После тестирования:
- [ ] БД содержит записи прогресса
- [ ] Все шаги пройдены успешно
- [ ] Подсветка работает корректно
- [ ] Дизайн премиум
- [ ] Нет ошибок в консоли

---

## 📸 ЧТО ДОЛЖНО ВЫГЛЯДЕТЬ

### Модальное окно:
```
┌─────────────────────────────────────────┐
│  ✕                                      │
│  🎉 Добро пожаловать, Kenesary!         │
│                                         │
│  Привет! Я твой виртуальный помощник... │
│                                         │
│  ╔═══════════════════════════════════╗  │
│  ║  [зеленая рамка вокруг элемента]  ║  │
│  ╚═══════════════════════════════════╝  │
│                                         │
│  ─────────────────────────────────────  │
│  Шаг 1 из 8   [← Назад]   [Далее →]    │
└─────────────────────────────────────────┘
```

### Подсветка элемента:
- Зеленая рамка (3px, #00FF88)
- Отступ 6px от элемента
- Пульсация (плавная анимация)
- Остальное затемнено (80% черный)

---

## 🚀 ПОСЛЕ УСПЕШНОГО ТЕСТА

### Система готова!

✅ Интерактивное обучение работает  
✅ Подсветка элементов корректная  
✅ Дизайн премиум  
✅ Прогресс сохраняется  
✅ Повторные входы без обучения  

**Можно использовать!** 🎉

---

**Создано**: 19 декабря 2025, 04:15 AM  
**Статус**: Ready to Test  
**Режим**: Local Only 🧪
