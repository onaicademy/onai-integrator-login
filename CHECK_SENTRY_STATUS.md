# 🔍 ПРОВЕРКА СТАТУСА SENTRY

## ✅ ЧТО УЖЕ НАСТРОЕНО:

1. ✅ `VITE_SENTRY_DSN` в `.env` (frontend)
2. ✅ `SENTRY_DSN` в `backend/.env.production` (backend)
3. ✅ Код инициализации работает
4. ✅ Серверы перезапущены

---

## 🎯 ПРОВЕРЬ СЕЙЧАС:

### 1️⃣ FRONTEND (локально):

**Если запущен `npm run dev`:**

```
1. Открой http://localhost:5173
2. Нажми F12 → Console
3. Ищи сообщение:
   ✅ Sentry initialized for frontend monitoring
   
   ИЛИ
   
   ⚠️ VITE_SENTRY_DSN not configured
```

**Результат:**
- ✅ Если видишь "initialized" - ВСЁ РАБОТАЕТ!
- ❌ Если видишь "not configured" - нужно перезапустить `npm run dev`

---

### 2️⃣ FRONTEND (на Digital Ocean):

**Проверь что фронтенд пересобран на сервере:**

```bash
# Подключись к серверу:
ssh root@your-server

# Перейди в проект:
cd /root/onai-integrator-login

# Проверь что .env содержит DSN:
cat .env | grep VITE_SENTRY_DSN

# Если DSN есть - ПЕРЕСОБЕРИ:
npm run build

# Подожди 1-2 минуты
```

**Потом проверь на сайте:**
```
1. Открой https://onai.academy
2. Нажми F12 → Console
3. Ищи: ✅ Sentry initialized for frontend monitoring
```

---

### 3️⃣ BACKEND (Digital Ocean):

**Проверь логи pm2:**

```bash
# На сервере:
pm2 logs onai-backend --lines 50

# Ищи строку:
# ✅ Sentry initialized for backend monitoring

# Если НЕТ этой строки:
pm2 restart onai-backend --update-env
pm2 logs onai-backend --lines 50
```

---

## 📊 ТЕСТОВАЯ ОШИБКА:

**После того как увидел "✅ Sentry initialized" в консоли:**

1. Открой любую страницу сайта
2. Открой Console (F12)
3. Напиши:
   ```javascript
   throw new Error('🧪 Test Sentry');
   ```
4. Нажми Enter
5. Подожди 10 секунд
6. Зайди на https://sentry.io/issues/
7. Обнови страницу (F5)
8. Должна появиться ошибка "🧪 Test Sentry"

---

## ✅ ЕСЛИ ВСЁ РАБОТАЕТ:

Ты увидишь:
- ✅ "Sentry initialized" в консоли (frontend)
- ✅ "Sentry initialized" в pm2 логах (backend)
- ✅ Тестовая ошибка появляется на sentry.io

**Sentry Dashboard:** https://sentry.io/issues/

---

## ❌ ЕСЛИ НЕ РАБОТАЕТ:

### Проблема 1: Frontend не видит DSN
```bash
# Решение:
cd c:\onai-integrator-login\onai-integrator-login
Ctrl+C   # Закрой npm run dev
npm run dev   # Запусти заново
```

### Проблема 2: Backend не видит DSN
```bash
# На сервере:
cd /root/onai-integrator-login/backend
pm2 restart onai-backend --update-env
pm2 logs onai-backend --lines 50
```

### Проблема 3: Ошибки не появляются на sentry.io
```
1. Убедись что DSN правильный (скопируй заново с sentry.io)
2. Проверь Network в F12 → должны быть POST запросы к sentry.io
3. Убедись что не превышен лимит (5k errors/месяц на free плане)
```

---

## 🎯 БЫСТРАЯ ДИАГНОСТИКА:

Покажи мне скриншот или текст из консоли браузера (F12 → Console) - сразу скажу в чём дело!

---

**ГЛАВНОЕ: Должен появиться текст "✅ Sentry initialized" - это значит что всё работает!** 🚀
