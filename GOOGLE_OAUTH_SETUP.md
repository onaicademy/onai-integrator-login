# 🔐 Google OAuth Setup Guide

## ✅ Что уже настроено в коде

1. ✅ Клиент Supabase с настройками auth
2. ✅ Автоматическая синхронизация пользователей в таблицу `users`
3. ✅ Кнопка "Войти через Google" на странице входа
4. ✅ Защита маршрута `/profile` с редиректом на `/` если не авторизован
5. ✅ Автоматический редирект на `/profile` после успешной авторизации

---

## 🚀 Нужно настроить в Supabase Dashboard

**Google OAuth требует настройки в Supabase Dashboard!**

### Шаг 1: Создайте OAuth приложение в Google

1. Перейдите на https://console.cloud.google.com/
2. Выберите проект или создайте новый
3. Перейдите в **APIs & Services** → **Credentials**
4. Нажмите **Create Credentials** → **OAuth client ID**
5. Выберите **Web application**
6. Добавьте **Authorized redirect URIs**:
   ```
   https://onai-academy.supabase.co/auth/v1/callback
   ```
7. Сохраните **Client ID** и **Client Secret**

### Шаг 2: Настройте Google OAuth в Supabase

1. Откройте https://supabase.com/dashboard
2. Выберите проект **onai-academy**
3. Перейдите в **Authentication** → **Providers**
4. Найдите **Google** и включите его
5. Вставьте **Client ID** и **Client Secret** из Google Console
6. Сохраните изменения

### Шаг 3: Проверьте Site URL

1. В Supabase Dashboard перейдите в **Authentication** → **URL Configuration**
2. Убедитесь, что **Site URL** указан:
   ```
   http://localhost:8080  (для разработки)
   ```
3. В списке **Redirect URLs** добавьте:
   ```
   http://localhost:8080/profile
   http://localhost:8080/**
   ```

---

## 🧪 Тестирование

### После настройки:

1. Откройте http://localhost:8080
2. Нажмите **"Войти через Google"**
3. Откроется окно Google авторизации
4. Выберите аккаунт Google
5. После подтверждения:
   - ✅ Произойдёт редирект на `/profile`
   - ✅ Пользователь автоматически добавится в таблицу `users`
   - ✅ Данные из Google (имя, email, фото) синхронизируются

### Проверка в консоли:

Откройте DevTools Console (F12) и проверьте:
```
Google login initiated: {...}
User synced to database: { id: ..., email: ... }
```

---

## 📝 Примечания

### Если не работает:

1. **Проверьте Redirect URIs** в Google Console:
   - Должен быть точно: `https://onai-academy.supabase.co/auth/v1/callback`
   
2. **Проверьте Site URL** в Supabase:
   - Для локальной разработки: `http://localhost:8080`
   - Для продакшена: ваш домен

3. **Проверьте Browser Console** на ошибки

4. **Убедитесь**, что Google Provider включён в Supabase

### После авторизации:

- Пользователь автоматически создаётся в таблице `users`
- ID из `auth.users` совпадает с `users.id`
- Данные синхронизируются из Google:
  - `email` → users.email
  - `full_name` или `name` → users.full_name
  - `avatar_url` или `picture` → users.avatar_url

---

## 🔗 Полезные ссылки

- Supabase Auth Docs: https://supabase.com/docs/guides/auth
- Google OAuth Setup: https://supabase.com/docs/guides/auth/social-login/auth-google
- Supabase Dashboard: https://supabase.com/dashboard/project/_/auth/providers

---

**После настройки в Dashboard Google OAuth будет работать!** 🎉

