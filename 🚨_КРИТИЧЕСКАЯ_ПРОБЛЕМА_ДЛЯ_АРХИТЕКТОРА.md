# 🚨 КРИТИЧЕСКАЯ ПРОБЛЕМА: NGINX НЕ ОТДАЁТ ПРАВИЛЬНЫЙ БИЛД

**Дата**: 19 декабря 2025  
**Приоритет**: 🔴 КРИТИЧЕСКИЙ  
**Для**: Архитектор (Кодер)

---

## 🔥 ПРОБЛЕМА:

При заходе на `https://traffic.onai.academy/login` открывается **НЕПРАВИЛЬНАЯ** страница!

### Что происходит:

1. **ОЖИДАЕТСЯ**: Traffic Dashboard логин (с заголовком "TRAFFIC COMMAND")
2. **РЕАЛЬНО**: Обычный логин основной платформы (с плейсхолдером "student@onaiacademy.kz")

### Скриншот проблемы:

Пользователь видит:
- ❌ "ВХОД" вместо "TRAFFIC COMMAND" 
- ❌ "student@onaiacademy.kz" вместо "your@email.com"
- ❌ Footer основной платформы вместо Traffic Dashboard footer

---

## 🔍 ДИАГНОСТИКА:

### 1. Проверь что отдаёт сервер:

```bash
ssh root@207.154.231.30
curl -I https://traffic.onai.academy/login
```

### 2. Проверь Nginx конфиг:

```bash
cat /etc/nginx/sites-available/traffic.onai.academy
```

### 3. Проверь директорию:

```bash
ls -la /var/www/traffic.onai.academy/
```

### 4. Проверь что в index.html:

```bash
head -20 /var/www/traffic.onai.academy/index.html
```

---

## ⚠️ ВЕРОЯТНЫЕ ПРИЧИНЫ:

### Причина 1: Nginx отдаёт основной сайт вместо Traffic Dashboard

**Проблема**: В конфиге `server_name` неправильно настроен или есть конфликт с основным сайтом.

**Что проверить**:
```nginx
# В /etc/nginx/sites-available/traffic.onai.academy должно быть:
server {
    listen 443 ssl http2;
    server_name traffic.onai.academy;
    
    root /var/www/traffic.onai.academy;  # ← КРИТИЧЕСКИ ВАЖНО!
    index index.html;
    
    # ...
}
```

### Причина 2: Symlink не создан или указывает не туда

**Проблема**: `/etc/nginx/sites-enabled/traffic.onai.academy` не существует или битый.

**Что проверить**:
```bash
ls -la /etc/nginx/sites-enabled/ | grep traffic
```

Должно быть:
```
lrwxrwxrwx 1 root root 52 Dec 19 09:00 traffic.onai.academy -> ../sites-available/traffic.onai.academy
```

### Причина 3: Nginx не перезагрузился корректно

**Проблема**: После загрузки файлов Nginx не подхватил изменения.

**Что сделать**:
```bash
nginx -t  # Проверить конфиг
systemctl restart nginx  # FULL RESTART вместо reload
```

### Причина 4: Основной сайт перехватывает запросы

**Проблема**: В конфиге `onai.academy` есть `server_name` с wildcard или `default_server`.

**Что проверить**:
```bash
cat /etc/nginx/sites-available/onai.academy | grep server_name
```

Должно быть:
```nginx
server_name onai.academy www.onai.academy;  # БЕЗ *.onai.academy!
```

### Причина 5: Директория `/var/www/traffic.onai.academy/` пустая или содержит не те файлы

**Проблема**: Файлы загрузились не туда или rsync не отработал.

**Что проверить**:
```bash
ls -la /var/www/traffic.onai.academy/assets/ | head -10
```

Должны быть файлы типа:
```
TrafficLogin-BqFpjMf8.js
TrafficAdminPanel-oMLQtHS5.js
TrafficTargetologistDashboard-f07ec8Ku.js
```

Если их нет → файлы в другой директории!

---

## ✅ РЕШЕНИЕ (ПОШАГОВО):

### Шаг 1: Проверь правильный конфиг Nginx

```bash
ssh root@207.154.231.30

# Смотри конфиг:
cat /etc/nginx/sites-available/traffic.onai.academy
```

**Должно быть ТАК**:

```nginx
# HTTP → HTTPS redirect
server {
    listen 80;
    listen [::]:80;
    server_name traffic.onai.academy;
    return 301 https://$server_name$request_uri;
}

# HTTPS server для traffic.onai.academy
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name traffic.onai.academy;

    # ✅ КРИТИЧЕСКИ ВАЖНО: правильный root!
    root /var/www/traffic.onai.academy;
    index index.html;

    # SSL
    ssl_certificate /etc/letsencrypt/live/traffic.onai.academy/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/traffic.onai.academy/privkey.pem;

    # SPA routing - все запросы → index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Статика с кешем
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Проксирование API на backend
    location /api/ {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

**Если конфига НЕТ** → создай его:

```bash
nano /etc/nginx/sites-available/traffic.onai.academy
# Вставь конфиг выше
```

### Шаг 2: Проверь symlink

```bash
# Удали старый (если есть):
rm /etc/nginx/sites-enabled/traffic.onai.academy

# Создай новый:
ln -s /etc/nginx/sites-available/traffic.onai.academy /etc/nginx/sites-enabled/traffic.onai.academy

# Проверь:
ls -la /etc/nginx/sites-enabled/ | grep traffic
```

### Шаг 3: Проверь основной конфиг (НЕ ДОЛЖЕН перехватывать поддомен!)

```bash
cat /etc/nginx/sites-available/onai.academy | grep server_name
```

**Должно быть**:
```nginx
server_name onai.academy www.onai.academy;
```

**НЕ ДОЛЖНО быть**:
```nginx
server_name *.onai.academy;  # ❌ ЭТО ПЛОХО!
```

Если есть wildcard → убери его!

### Шаг 4: Проверь директорию и файлы

```bash
# Директория должна существовать:
ls -la /var/www/traffic.onai.academy/

# Должны быть файлы:
ls -la /var/www/traffic.onai.academy/assets/ | grep Traffic

# Должны быть:
# TrafficLogin-*.js
# TrafficAdminPanel-*.js
# TrafficTargetologistDashboard-*.js
```

**Если файлов НЕТ** → перезагрузи:

```bash
# На локальной машине:
cd /Users/miso/onai-integrator-login
npm run build
rsync -avz --delete dist/ root@207.154.231.30:/var/www/traffic.onai.academy/
ssh root@207.154.231.30 "chown -R www-data:www-data /var/www/traffic.onai.academy/"
```

### Шаг 5: Проверь и перезагрузи Nginx

```bash
# Тест конфига:
nginx -t

# Если OK → full restart:
systemctl restart nginx

# Проверь статус:
systemctl status nginx

# Проверь что слушает на 443:
netstat -tlnp | grep :443
```

### Шаг 6: Очисть кеш браузера!

**КРИТИЧЕСКИ ВАЖНО**:

В браузере:
1. Открой DevTools (F12)
2. Правый клик на кнопку "Обновить"
3. Выбери "Очистить кеш и жёсткая перезагрузка"

Или:
- Chrome: `Cmd + Shift + R` (Mac) или `Ctrl + Shift + R` (Windows)
- Safari: `Cmd + Option + E`, потом `Cmd + R`

### Шаг 7: Проверь результат

```bash
# Смотри что отдаёт сервер:
curl -I https://traffic.onai.academy/login

# Смотри начало HTML:
curl https://traffic.onai.academy/login | head -20
```

Должно быть:
```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>onAI Academy - Платформа обучения AI</title>
    <script type="module" crossorigin src="/assets/index-BtHdWxpN.js"></script>
    ...
```

---

## 🔍 ДОПОЛНИТЕЛЬНАЯ ДИАГНОСТИКА:

### Проверь логи Nginx:

```bash
# Логи ошибок:
tail -50 /var/log/nginx/error.log

# Логи доступа для traffic:
tail -50 /var/log/nginx/traffic.onai.academy.access.log

# Если логи для traffic не пишутся → конфиг не работает!
```

### Проверь DNS:

```bash
# Резолвится ли домен:
dig traffic.onai.academy +short

# Должно вернуть:
# 207.154.231.30

# Ping:
ping -c 3 traffic.onai.academy
```

### Проверь SSL:

```bash
# Проверь сертификат:
certbot certificates | grep traffic

# Должно быть:
# Certificate Name: traffic.onai.academy
#   Domains: traffic.onai.academy
#   Expiry Date: ...
```

---

## 🚨 ЕСЛИ НИЧЕГО НЕ ПОМОГАЕТ:

### Вариант 1: Удали всё и создай заново

```bash
# 1. Удали конфиг:
rm /etc/nginx/sites-enabled/traffic.onai.academy
rm /etc/nginx/sites-available/traffic.onai.academy

# 2. Удали директорию:
rm -rf /var/www/traffic.onai.academy

# 3. Создай заново:
mkdir -p /var/www/traffic.onai.academy

# 4. На локалке:
cd /Users/miso/onai-integrator-login
npm run build
rsync -avz --delete dist/ root@207.154.231.30:/var/www/traffic.onai.academy/

# 5. Создай конфиг заново (см. выше)

# 6. Создай symlink:
ln -s /etc/nginx/sites-available/traffic.onai.academy /etc/nginx/sites-enabled/

# 7. Перезапусти Nginx:
systemctl restart nginx
```

### Вариант 2: Проверь порядок server блоков

Nginx выбирает первый подходящий `server` блок. Если `onai.academy` идёт РАНЬШЕ `traffic.onai.academy` и имеет wildcard → он перехватит запрос!

```bash
# Смотри порядок:
grep -n "server_name" /etc/nginx/sites-enabled/* | grep -E "(onai|traffic)"

# Должно быть:
# traffic.onai.academy:XX:    server_name traffic.onai.academy;
# onai.academy:YY:    server_name onai.academy www.onai.academy;
```

Если порядок неправильный → переименуй конфиги чтобы `traffic` шёл первым:

```bash
mv /etc/nginx/sites-available/traffic.onai.academy /etc/nginx/sites-available/01-traffic.onai.academy
mv /etc/nginx/sites-available/onai.academy /etc/nginx/sites-available/02-onai.academy

# Пересоздай symlinks:
rm /etc/nginx/sites-enabled/traffic.onai.academy
rm /etc/nginx/sites-enabled/onai.academy
ln -s /etc/nginx/sites-available/01-traffic.onai.academy /etc/nginx/sites-enabled/
ln -s /etc/nginx/sites-available/02-onai.academy /etc/nginx/sites-enabled/

# Перезапусти:
systemctl restart nginx
```

---

## 📋 CHECKLIST ДЛЯ АРХИТЕКТОРА:

Пройдись по этому списку:

- [ ] Конфиг `/etc/nginx/sites-available/traffic.onai.academy` существует
- [ ] В конфиге `root /var/www/traffic.onai.academy;`
- [ ] В конфиге `server_name traffic.onai.academy;`
- [ ] Symlink `/etc/nginx/sites-enabled/traffic.onai.academy` существует
- [ ] В `/var/www/traffic.onai.academy/` есть файлы (index.html, assets/)
- [ ] В `assets/` есть файлы типа `TrafficLogin-*.js`
- [ ] В основном конфиге `onai.academy` НЕТ wildcard `*.onai.academy`
- [ ] `nginx -t` показывает OK
- [ ] Nginx перезапущен (`systemctl restart nginx`)
- [ ] `curl https://traffic.onai.academy/login` возвращает HTML с Traffic Dashboard
- [ ] Логи Nginx не показывают ошибок
- [ ] DNS резолвится: `dig traffic.onai.academy` → `207.154.231.30`
- [ ] SSL сертификат валиден для `traffic.onai.academy`
- [ ] Кеш браузера очищен (Hard Reload)

---

## 🎯 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:

После фикса при заходе на `https://traffic.onai.academy/login` должно быть:

✅ Заголовок: **"TRAFFIC COMMAND"**  
✅ Подзаголовок: **"Dashboard Login"**  
✅ Плейсхолдер email: **"your@email.com"**  
✅ Demo credentials: **"admin@onai.academy / admin123"**  
✅ Footer: **"OnAI Academy Traffic Dashboard"**  
✅ IP warning: **"⚠️ IP-адреса отслеживаются..."**

---

## 💡 ПОДСКАЗКА:

Если ты видишь что при заходе на `traffic.onai.academy` открывается основной сайт → **100% проблема в Nginx**!

Nginx либо:
1. Не знает про поддомен (конфиг не подключён)
2. Отдаёт файлы из неправильной директории
3. Основной сайт перехватывает запросы (wildcard или default_server)

---

## 📞 ЕСЛИ ЗАСТРЯЛ:

1. Скопируй вывод команд:
```bash
nginx -t
ls -la /var/www/traffic.onai.academy/
cat /etc/nginx/sites-available/traffic.onai.academy
curl -I https://traffic.onai.academy/login
```

2. Отправь мне или Александру

3. Я помогу найти проблему

---

**УДАЧИ, АРХИТЕКТОР!** 🚀

**Проблема должна решиться за 10-15 минут!**
