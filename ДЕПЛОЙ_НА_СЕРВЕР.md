# 🚀 ДЕПЛОЙ НА DIGITAL OCEAN - ПОШАГОВАЯ ИНСТРУКЦИЯ

**Дата:** 7 ноября 2025  
**Статус:** ✅ Готов к деплою

---

## 📋 ЧТО МЫ ДЕПЛОИМ:

✅ **Все новые функции:**
- AI-куратор с сохранением в Supabase
- База знаний (миграции готовы)
- Система достижений (50+ достижений)
- AI-Mentor Telegram бот (протестирован)
- AI-Analyst Telegram бот (протестирован)
- Admin панель (диалоги, аналитика, студенты)
- Система сообщений студентов
- Токен-трекинг OpenAI
- Конфликты бота (детекция ошибок)
- Улучшенный UI/UX (анимации, адаптивность)

✅ **Последние коммиты:**
```
98e7610 - Улучшенные шаблоны Telegram ботов + тестирование
5047d37 - JavaScript версия теста Telegram ботов + инструкция
b9f009a - Краткая инструкция по деплою
efa0179 - Финальная подготовка к деплою
788b94c - Главная страница теперь ведет на курсы
```

---

## 🎯 ПОШАГОВЫЙ ПЛАН ДЕПЛОЯ

### **ШАГ 1: ПОДГОТОВКА SUPABASE** 🗄️

**1.1. Очистка базы данных:**

```bash
# Открой Supabase Dashboard:
https://supabase.com/dashboard/project/<your-project-id>

# Перейди в SQL Editor
# Скопируй содержимое файла: SUPABASE_CLEAN_DATABASE.sql
# Выполни запрос
```

**Что это сделает:**
- ✅ Удалит все mock данные
- ✅ Удалит всех тестовых студентов
- ✅ Оставит только админа: `saint@onaiacademy.kz`
- ✅ Обнулит счётчики и метрики

---

**1.2. Смена пароля админа:**

```sql
-- После очистки, измени пароль админа на новый:
UPDATE auth.users 
SET encrypted_password = crypt('ТвойНовыйБезопасныйПароль', gen_salt('bf'))
WHERE email = 'saint@onaiacademy.kz';
```

⚠️ **ВАЖНО:** Запомни новый пароль! Старый был: `Onai2134!`

---

**1.3. Проверка миграций:**

Все миграции уже применены через `SUPABASE_ALL_IN_ONE.sql`:
- ✅ AI-куратор (чаты, сообщения, метрики)
- ✅ Достижения (50+ готовых)
- ✅ AI-Mentor (анализ, рекомендации)
- ✅ AI-Analyst (отчёты, инсайты)
- ✅ Конфликты бота
- ✅ Токен-трекинг
- ✅ Сообщения студентов

**Если нужно проверить:**
```sql
SELECT * FROM ai_curator_threads LIMIT 1;
SELECT * FROM achievements LIMIT 5;
SELECT * FROM ai_token_usage LIMIT 1;
```

---

### **ШАГ 2: ПОДКЛЮЧЕНИЕ К СЕРВЕРУ DIGITAL OCEAN** 🌐

**2.1. SSH подключение:**

```bash
# Подключись к серверу
ssh root@<твой-IP-адрес>

# Или с указанием ключа
ssh -i ~/.ssh/your_key root@<твой-IP-адрес>
```

---

**2.2. Перейди в директорию проекта:**

```bash
cd /var/www/onai-integrator-login
# или
cd /root/onai-integrator-login
```

⚠️ **Если не знаешь где проект:**
```bash
find / -name "onai-integrator-login" 2>/dev/null
```

---

### **ШАГ 3: ОБНОВЛЕНИЕ КОДА** 📥

**3.1. Backup текущей версии (на всякий случай):**

```bash
# Создай бэкап
cd ..
cp -r onai-integrator-login onai-integrator-login.backup-$(date +%Y%m%d-%H%M%S)
cd onai-integrator-login
```

---

**3.2. Pull свежего кода из GitHub:**

```bash
# Проверь текущую ветку
git branch

# Получи последние изменения
git fetch origin main

# Посмотри что изменилось
git log HEAD..origin/main --oneline

# Pull изменений
git pull origin main
```

**Должно показать:**
```
98e7610 - Улучшенные шаблоны Telegram ботов + тестирование
5047d37 - JavaScript версия теста Telegram ботов + инструкция
b9f009a - Краткая инструкция по деплою
... (и другие последние коммиты)
```

---

### **ШАГ 4: НАСТРОЙКА ПЕРЕМЕННЫХ ОКРУЖЕНИЯ** 🔐

**4.1. Проверь `.env` файл:**

```bash
nano .env
# или
vim .env
```

**Обязательные переменные:**

```bash
# === SUPABASE ===
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=твой_anon_key

# === OPENAI ===
VITE_OPENAI_API_KEY=твой_openai_api_key
VITE_ASSISTANT_ID=твой_assistant_id

# === TELEGRAM БОТЫ ===
VITE_TELEGRAM_AI_MENTOR_TOKEN=8380600260:AAGtuSG9GqFOmkyThhWqRzilHi3gKdKiOSo
VITE_TELEGRAM_AI_ANALYST_TOKEN=8400927507:AAF1w1H8lyE2vonPY-Z61vBybBT8dkN-Ip4

# === FEATURE FLAGS (пока отключены) ===
VITE_AI_MENTOR_ENABLED=false
VITE_AI_ANALYST_ENABLED=false

# === ДОМЕН (для production) ===
VITE_APP_URL=https://onai.academy
```

**Сохрани и выйди:**
- `Ctrl + X` → `Y` → `Enter` (nano)
- `:wq` (vim)

---

### **ШАГ 5: УСТАНОВКА ЗАВИСИМОСТЕЙ** 📦

**5.1. Обнови зависимости:**

```bash
# Убедись что Node.js >= 18
node --version

# Установи/обнови пакеты
npm install

# Если возникли ошибки с peer dependencies:
npm install --legacy-peer-deps
```

---

### **ШАГ 6: СБОРКА PRODUCTION БИЛДА** 🏗️

**6.1. Собери проект:**

```bash
# Очисти предыдущий билд
rm -rf dist

# Собери новый билд
npm run build
```

**Должно пройти успешно:**
```
✓ built in Xms
✓ X modules transformed.
dist/index.html                  X kB
dist/assets/index-[hash].js      X kB
dist/assets/index-[hash].css     X kB
```

⚠️ **Если ошибки:**
```bash
# Проверь логи
npm run build 2>&1 | tee build.log

# Проверь линтер
npm run lint
```

---

### **ШАГ 7: НАСТРОЙКА NGINX** 🌐

**7.1. Проверь конфигурацию Nginx:**

```bash
nano /etc/nginx/sites-available/onai.academy
```

**Должно быть примерно так:**

```nginx
server {
    listen 80;
    server_name onai.academy www.onai.academy;
    
    # Redirect HTTP to HTTPS
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name onai.academy www.onai.academy;
    
    # SSL сертификаты (Let's Encrypt)
    ssl_certificate /etc/letsencrypt/live/onai.academy/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/onai.academy/privkey.pem;
    
    # Root директория
    root /var/www/onai-integrator-login/dist;
    index index.html;
    
    # Gzip сжатие
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # Кэширование статики
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

**7.2. Проверь и перезагрузи Nginx:**

```bash
# Проверь синтаксис
nginx -t

# Если OK, перезагрузи
systemctl reload nginx

# Проверь статус
systemctl status nginx
```

---

### **ШАГ 8: SSL СЕРТИФИКАТЫ (если нет)** 🔒

**Если SSL ещё не настроен:**

```bash
# Установи Certbot
apt update
apt install certbot python3-certbot-nginx -y

# Получи сертификат
certbot --nginx -d onai.academy -d www.onai.academy

# Автоматическое обновление
certbot renew --dry-run
```

---

### **ШАГ 9: PM2 (если используешь Node.js сервер)** ⚙️

**Если у тебя есть отдельный Node.js сервер для API:**

```bash
# Установи PM2 (если нет)
npm install -g pm2

# Запусти сервер
pm2 start server.js --name "onai-backend"

# Или перезапусти существующий
pm2 restart onai-backend

# Автозапуск при перезагрузке
pm2 startup
pm2 save

# Проверь статус
pm2 status
pm2 logs onai-backend
```

---

### **ШАГ 10: ПРОВЕРКА ДЕПЛОЯ** ✅

**10.1. Проверь сайт:**

```bash
# Проверь что файлы на месте
ls -lh /var/www/onai-integrator-login/dist/

# Проверь права доступа
chmod -R 755 /var/www/onai-integrator-login/dist/
```

**10.2. Открой в браузере:**

```
https://onai.academy
```

**Проверь:**
- ✅ Страница загружается
- ✅ Можешь войти под админом: `saint@onaiacademy.kz`
- ✅ AI-куратор работает
- ✅ Админ панель доступна
- ✅ Достижения загружаются
- ✅ Нет ошибок в консоли (F12 → Console)

---

**10.3. Проверь логи:**

```bash
# Nginx логи
tail -f /var/log/nginx/access.log
tail -f /var/log/nginx/error.log

# PM2 логи (если используешь)
pm2 logs

# Системные логи
journalctl -u nginx -f
```

---

### **ШАГ 11: ТЕСТИРОВАНИЕ TELEGRAM БОТОВ** 🤖

**11.1. Проверь боты в Telegram:**

- Открой `@onaimentor_bot` (AI-Mentor)
- Открой `@academyadmin_bot` (AI-Analyst)
- Отправь `/start` если ещё не отправлял

**11.2. Проверь Supabase:**

```sql
-- Проверь что логи сохраняются
SELECT * FROM ai_curator_messages ORDER BY created_at DESC LIMIT 5;
SELECT * FROM ai_token_usage ORDER BY created_at DESC LIMIT 5;
```

---

## 🎯 БЫСТРЫЙ ЧЕКЛИСТ ДЕПЛОЯ

```bash
# 1. Supabase: очистка + новый пароль админа ✅
# 2. SSH на сервер ✅
# 3. cd /var/www/onai-integrator-login ✅
# 4. git pull origin main ✅
# 5. nano .env (проверь токены Telegram) ✅
# 6. npm install ✅
# 7. npm run build ✅
# 8. nginx -t && systemctl reload nginx ✅
# 9. pm2 restart all (если есть) ✅
# 10. Открой https://onai.academy ✅
# 11. Войди под админом ✅
# 12. Проверь AI-куратор ✅
# 13. Проверь Telegram боты ✅
```

---

## ⚠️ ЕСЛИ ЧТО-ТО ПОШЛО НЕ ТАК

### **Проблема: 502 Bad Gateway**

```bash
# Проверь Nginx
systemctl status nginx
journalctl -u nginx -n 50

# Перезапусти
systemctl restart nginx
```

---

### **Проблема: Белый экран**

```bash
# Проверь что билд собрался
ls -lh dist/

# Проверь права
chmod -R 755 dist/

# Проверь консоль браузера (F12)
# Обычно: ошибка загрузки .js файлов
```

---

### **Проблема: API не работает**

```bash
# Проверь .env
cat .env | grep VITE

# Проверь что Supabase доступен
curl -I https://your-project.supabase.co

# Проверь OpenAI API
curl https://api.openai.com/v1/models \
  -H "Authorization: Bearer $VITE_OPENAI_API_KEY"
```

---

### **Проблема: Telegram боты не отвечают**

```bash
# Проверь токены в .env
cat .env | grep TELEGRAM

# Проверь боты через API
curl https://api.telegram.org/bot<TOKEN>/getMe

# Проверь логи в Supabase
# (ai_bot_conflicts, ai_token_usage)
```

---

## 📊 МОНИТОРИНГ ПОСЛЕ ДЕПЛОЯ

**Что проверять регулярно:**

1. **Логи ошибок:**
```bash
tail -f /var/log/nginx/error.log
pm2 logs --err
```

2. **Использование ресурсов:**
```bash
htop
df -h
free -m
```

3. **Telegram боты:**
- Проверь что отправляют отчёты
- Проверь логи в Supabase

4. **OpenAI токены:**
- Админ панель → AI Analytics → Token Usage
- Следи за расходами

---

## 🎉 ФИНАЛЬНЫЙ ЧЕКЛИСТ

После деплоя убедись:

- ✅ Сайт работает: `https://onai.academy`
- ✅ Админ может войти: `saint@onaiacademy.kz`
- ✅ AI-куратор отвечает на вопросы
- ✅ Диалоги сохраняются в Supabase
- ✅ Достижения загружаются
- ✅ Админ панель работает (все разделы)
- ✅ Telegram боты отвечают
- ✅ Логи пишутся в Supabase
- ✅ Нет ошибок в консоли браузера
- ✅ Нет ошибок в логах сервера

---

## 🆘 ПОМОЩЬ

**Если нужна помощь:**
1. Скопируй логи ошибок
2. Скриншот консоли браузера (F12)
3. Покажи мне - разберём вместе!

---

## 🚀 УСПЕШНОГО ДЕПЛОЯ!

**Всё готово к запуску на production! 🎉**

