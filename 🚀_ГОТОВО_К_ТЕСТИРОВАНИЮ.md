# 🚀 AI-НАСТАВНИК ГОТОВ К ТЕСТИРОВАНИЮ!

**Дата:** 21 ноября 2025  
**Статус:** ✅ ВСЁ СОЗДАНО И ГОТОВО!

---

## ✅ ЧТО УЖЕ СДЕЛАНО:

- ✅ **9 таблиц** AI-наставника созданы
- ✅ **7 функций** работают
- ✅ **12 записей** в базе знаний
- ✅ **RLS политики** настроены
- ✅ **Индексы** созданы

**База данных полностью готова!** 🎉

---

## 📋 СЛЕДУЮЩИЕ ШАГИ

### ШАГ 1: Проверь тестовые данные (3 минуты)

Если ты еще НЕ запускал `CREATE_TEST_DATA_AI_MENTOR.sql`, запусти его сейчас:

```bash
# В Supabase SQL Editor:
# Открой файл CREATE_TEST_DATA_AI_MENTOR.sql
# Нажми Run
```

Затем проверь что данные создались:

```bash
# Открой файл QUICK_CHECK_TEST_DATA.sql
# Нажми Run
# Должно показать:
#   ✅ 10 студентов
#   ✅ Курсы и уроки
#   ✅ Активность студентов
#   ✅ Миссии и задачи
```

---

### ШАГ 2: Протестируй триггеры (5 минут)

```bash
# Открой файл TEST_AI_MENTOR_TRIGGERS.sql
# Нажми Run
# Смотри в логах (Messages) результаты:
#   ✅ ТЕСТ 1 ПРОЙДЕН: Триггер streak работает!
#   ✅ ТЕСТ 2 ПРОЙДЕН: Триггер video_struggle работает!
#   ✅ ТЕСТ 3 ПРОЙДЕН: Достижения разблокируются!
#   ✅ ТЕСТ 4 ПРОЙДЕН: Контекст для AI работает!
#   ✅ ТЕСТ 5 ПРОЙДЕН: Миссии обновляются!
```

**Если все тесты прошли → система работает!** ✅

---

### ШАГ 3: Запусти Backend (2 минуты)

```bash
# Терминал 1
cd backend
npm install    # Если еще не установлено
npm run dev

# Должно показать:
# ✅ Backend запущен на http://localhost:3000
# ✅ Supabase подключен
```

---

### ШАГ 4: Запусти Frontend (2 минуты)

```bash
# Терминал 2 (новый терминал)
npm install    # Если еще не установлено
npm run dev

# Должно показать:
# VITE v5.x.x  ready in xxx ms
# ➜  Local:   http://localhost:5173/
```

---

### ШАГ 5: Открой NeuroHub и протестируй (10 минут)

```bash
# Открой в браузере:
http://localhost:5173/neurohub
```

**Что должно работать:**

#### ✅ Чат с AI:
- Напиши: "Привет! Как мои дела?"
- AI должен ответить с твоим именем
- AI должен знать твой прогресс

#### ✅ Карточки статистики:
- "Прогресс обучения" - показывает уроки
- "Текущий Streak" - показывает дни подряд
- "XP и уровень" - показывает очки опыта
- "Достижения" - показывает разблокированные

#### ✅ Миссии:
- Должны отображаться активные миссии
- Прогресс должен показываться (например, 3/5)

#### ✅ Цели:
- Недельные цели должны быть видны
- Прогресс обновляется

#### ✅ Советы от AI:
- Должны показываться персонализированные советы
- Если студент застрял на уроке - совет об этом

---

## 🧪 РУЧНОЕ ТЕСТИРОВАНИЕ

### ТЕСТ 1: Проверь что AI знает тебя

1. Открой чат в NeuroHub
2. Напиши: "Кто я?"
3. AI должен ответить с твоим именем и статистикой

**Ожидаемый ответ:**
```
Привет, [ТвоеИмя]! Ты студент onAI Academy.
Твои достижения:
- Уровень: X
- Уроков пройдено: X
- Streak: X дней
```

---

### ТЕСТ 2: Проверь базу знаний

1. Напиши: "Как получить API ключ OpenAI?"
2. AI должен дать готовый ответ из базы знаний

**Ожидаемый ответ:**
```
Для получения API ключа OpenAI:
1. Перейди на platform.openai.com
2. Войди в аккаунт
3. Перейди в "API Keys"
4. Нажми "Create new secret key"
5. Скопируй ключ
```

---

### ТЕСТ 3: Симулируй проблему с видео

Выполни в Supabase SQL Editor:

```sql
-- Создай сессию просмотра с проблемой
DO $$
DECLARE
  v_your_user_id UUID := 'ТВОЙ_USER_ID_СЮДА'; -- Замени на свой!
  v_lesson_id UUID;
BEGIN
  -- Берем любой урок
  SELECT id INTO v_lesson_id FROM lessons LIMIT 1;
  
  -- Создаем проблемную сессию
  INSERT INTO video_watch_sessions (
    user_id, lesson_id, seeks_count, max_second_reached
  )
  VALUES (v_your_user_id, v_lesson_id, 8, 300);
  
  -- Проверяем что задача создалась
  SELECT * FROM ai_mentor_tasks 
  WHERE student_id = v_your_user_id 
  ORDER BY created_at DESC 
  LIMIT 1;
END $$;
```

**Ожидаемый результат:**
- ✅ Создана задача с `triggered_by = 'video_struggle'`
- ✅ Priority = 'medium' или 'high'
- ✅ В context_data указано: `seeks_count: 8`

---

### ТЕСТ 4: Проверь триггер streak

Выполни в Supabase SQL Editor:

```sql
-- Замени 'ТВОЙ_USER_ID_СЮДА' на свой реальный user_id!
DO $$
DECLARE
  v_your_user_id UUID := 'ТВОЙ_USER_ID_СЮДА';
  v_lesson_id UUID;
BEGIN
  SELECT id INTO v_lesson_id FROM lessons LIMIT 1;
  
  -- Симулируем активность
  INSERT INTO user_progress (user_id, lesson_id, last_accessed_at, status)
  VALUES (v_your_user_id, v_lesson_id, NOW(), 'in_progress')
  ON CONFLICT (user_id, lesson_id) DO UPDATE
  SET last_accessed_at = NOW();
  
  -- Проверяем streak
  SELECT full_name, current_streak, last_activity_at
  FROM profiles
  WHERE id = v_your_user_id;
END $$;
```

**Ожидаемый результат:**
- ✅ `current_streak` увеличился (если новый день)
- ✅ `last_activity_at` обновилось

---

## 📊 ПРОВЕРКА МАСШТАБИРУЕМОСТИ

### Как узнать сколько запросов выдержит система?

**Выполни в Supabase:**

```sql
-- Нагрузочный тест (симулирует 100 одновременных пользователей)
DO $$
DECLARE
  i INTEGER;
  v_user_id UUID;
  v_lesson_id UUID;
  v_start TIMESTAMP;
  v_end TIMESTAMP;
BEGIN
  v_start := clock_timestamp();
  
  FOR i IN 1..100 LOOP
    SELECT id INTO v_user_id FROM profiles WHERE role = 'student' OFFSET (random() * 9)::int LIMIT 1;
    SELECT id INTO v_lesson_id FROM lessons OFFSET (random() * 9)::int LIMIT 1;
    
    -- Симулируем активность
    INSERT INTO user_progress (user_id, lesson_id, last_accessed_at, status)
    VALUES (v_user_id, v_lesson_id, NOW(), 'in_progress')
    ON CONFLICT (user_id, lesson_id) DO UPDATE
    SET last_accessed_at = NOW();
  END LOOP;
  
  v_end := clock_timestamp();
  
  RAISE NOTICE '100 запросов выполнено за: % мс', EXTRACT(MILLISECONDS FROM (v_end - v_start));
  RAISE NOTICE 'Среднее время: % мс на запрос', EXTRACT(MILLISECONDS FROM (v_end - v_start)) / 100;
END $$;
```

**Интерпретация результатов:**
- ⚡ < 1000 мс (100 запросов) = **ОТЛИЧНО** (выдержит 500+ пользователей)
- ✅ 1000-3000 мс = **ХОРОШО** (выдержит 300-500 пользователей)
- ⚠️ 3000-5000 мс = **СРЕДНЕ** (выдержит 100-300 пользователей)
- ❌ > 5000 мс = **ПЛОХО** (нужна оптимизация)

---

## ❓ ЧТО ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ?

### Проблема 1: Backend не видит таблицы

**Решение:**
```bash
# Проверь переменные окружения в backend/.env
SUPABASE_URL=https://твой-проект.supabase.co
SUPABASE_SERVICE_ROLE_KEY=твой_service_role_key

# Перезапусти Backend
cd backend
npm run dev
```

---

### Проблема 2: Frontend показывает ошибки

**Решение:**
```bash
# Проверь консоль браузера (F12)
# Проверь что Backend запущен (http://localhost:3000)
# Проверь .env в корне проекта:
VITE_API_URL=http://localhost:3000
VITE_SUPABASE_URL=https://твой-проект.supabase.co
VITE_SUPABASE_ANON_KEY=твой_anon_key
```

---

### Проблема 3: AI не отвечает

**Решение:**
```bash
# Проверь backend/.env:
OPENAI_API_KEY=sk-proj-твой_ключ
OPENAI_ASSISTANT_CURATOR_ID=asst_твой_ассистент

# Проверь баланс OpenAI:
# https://platform.openai.com/account/usage
```

---

### Проблема 4: Триггеры не срабатывают

**Решение:**
```sql
-- Проверь что триггеры созданы
SELECT trigger_name, event_object_table
FROM information_schema.triggers
WHERE trigger_name IN (
  'on_lesson_activity',
  'on_video_struggle',
  'on_user_progress_updated'
);

-- Если нет - примени снова:
-- supabase/migrations/20251121_ai_mentor_functions.sql
```

---

## 🎯 ЧЕКЛИСТ ГОТОВНОСТИ

### База данных:
- [x] ✅ 9 таблиц созданы
- [x] ✅ 7 функций работают
- [x] ✅ 12 записей в базе знаний
- [ ] Тестовые данные созданы (CREATE_TEST_DATA_AI_MENTOR.sql)
- [ ] Триггеры протестированы (TEST_AI_MENTOR_TRIGGERS.sql)

### Backend:
- [ ] Backend запущен (npm run dev)
- [ ] Supabase подключен
- [ ] OpenAI API настроен

### Frontend:
- [ ] Frontend запущен (npm run dev)
- [ ] NeuroHub открывается
- [ ] Чат работает
- [ ] Статистика отображается

### Тестирование:
- [ ] AI знает имя и прогресс
- [ ] База знаний отвечает
- [ ] Триггер streak работает
- [ ] Триггер video_struggle работает
- [ ] Миссии обновляются

---

## 🚀 ГОТОВО К PRODUCTION?

**Перед деплоем на production:**

- [ ] Удалить тестовых студентов (или оставить как демо)
- [ ] Проверить все переменные окружения
- [ ] Настроить мониторинг (логи, метрики)
- [ ] Провести нагрузочное тестирование
- [ ] Настроить резервное копирование БД
- [ ] Добавить обработку ошибок в Backend
- [ ] Оптимизировать OpenAI (кэш, gpt-4o-mini)

**Подробности в файле:**
📄 `AI_MENTOR_COMPREHENSIVE_REPORT.md`

---

## 📞 ПОДДЕРЖКА

**Если что-то не работает:**
1. Проверь логи Backend (`npm run dev`)
2. Проверь консоль браузера (F12)
3. Выполни `QUICK_CHECK_TEST_DATA.sql`
4. Выполни `TEST_AI_MENTOR_TRIGGERS.sql`
5. Прочитай раздел "ЧТО ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ?" выше

---

**🎉 ПОЗДРАВЛЯЮ! СИСТЕМА ГОТОВА К ТЕСТИРОВАНИЮ!**

**Следующий шаг:** Запусти тестовые данные и протестируй на localhost! 🚀

