# ✅ Интеграция amoCRM - Выполнено!

## 🎯 Что было реализовано

### 1. Сервис интеграции с amoCRM
**Файл:** `backend/src/services/amoCrmService.ts`

**Функционал:**
- ✅ Поиск контакта в amoCRM по email
- ✅ Поиск сделки пользователя в нужной воронке
- ✅ Перемещение сделки на этап при завершении урока
- ✅ Обработка ошибок (не ломает UX студента)
- ✅ Проверка конфигурации (graceful fallback)
- ✅ Утилиты для тестирования:
  - `testConnection()` - проверка подключения
  - `listPipelines()` - список воронок и этапов
  - `findLeadIdByEmail()` - поиск сделки по email

---

### 2. Интеграция в Tripwire роутер
**Файл:** `backend/src/routes/tripwire-lessons.ts`

**Изменения:**
- ✅ Импортирован `amoCrmService`
- ✅ После успешного завершения урока (POST `/api/tripwire/complete`):
  1. Получаем email пользователя из БД
  2. Определяем номер урока (67→1, 68→2, 69→3)
  3. Асинхронно вызываем `amoCrmService.onLessonCompleted()`
- ✅ Fire-and-forget подход: ошибки amoCRM не блокируют студента

**Маппинг уроков:**
```
Lesson 67 (Module 16) → Урок 1 → AMOCRM_STAGE_LESSON_1
Lesson 68 (Module 17) → Урок 2 → AMOCRM_STAGE_LESSON_2
Lesson 69 (Module 18) → Урок 3 → AMOCRM_STAGE_LESSON_3
```

---

### 3. Документация

#### 📚 Полная инструкция
**Файл:** `AMOCRM_INTEGRATION_SETUP.md`

**Содержание:**
- Пошаговая настройка (как получить токены, ID воронки, ID этапов)
- Инструкции по тестированию
- Troubleshooting (решение типичных проблем)
- Важные нюансы (Fire-and-Forget, Refresh Token, маппинг уроков)
- Мониторинг и логи

#### ⚡ Быстрый старт
**Файл:** `AMOCRM_QUICK_START.md`

**Содержание:**
- 5 шагов до запуска
- Быстрая проверка работоспособности
- Ссылки на troubleshooting

#### 🔧 Переменные окружения
**Файл:** `backend/ENV_VARIABLES_AMOCRM.txt`

**Содержание:**
- Пример всех необходимых переменных для `.env`
- Пояснения к каждой переменной
- Ссылки на документацию

#### 🧪 Тестовый скрипт
**Файл:** `backend/src/scripts/test-amocrm.ts`

**Функционал:**
- Проверка всех переменных окружения
- Тест подключения к amoCRM API
- Вывод списка воронок и этапов
- Опциональный тест поиска сделки по email

**Запуск:**
```bash
npx ts-node backend/src/scripts/test-amocrm.ts
```

---

## 🚀 Как использовать

### Минимальная настройка (5 минут):

1. **Получите данные из amoCRM:**
   - Access Token
   - Pipeline ID (ID воронки)
   - Status IDs (ID этапов 1, 2, 3)

2. **Добавьте в `backend/.env`:**
   ```bash
   AMOCRM_SUBDOMAIN=yoursubdomain
   AMOCRM_ACCESS_TOKEN=your_token
   AMOCRM_PIPELINE_ID=10350882
   AMOCRM_STAGE_LESSON_1=12345678
   AMOCRM_STAGE_LESSON_2=23456789
   AMOCRM_STAGE_LESSON_3=34567890
   ```

3. **Перезапустите бэкенд:**
   ```bash
   npm run dev  # или pm2 restart backend
   ```

4. **Проверьте:**
   ```bash
   npx ts-node backend/src/scripts/test-amocrm.ts
   ```

5. **Готово!** Теперь при завершении уроков сделки в amoCRM будут обновляться автоматически.

---

## 📊 Архитектура решения

### Поток данных:

```
Студент завершает урок (Frontend)
         ↓
POST /api/tripwire/complete (Backend Router)
         ↓
Транзакция БД (lesson completion, achievements, etc.)
         ↓
✅ COMMIT - студент видит успех
         ↓
[Асинхронно, не блокирует ответ]
         ↓
amoCrmService.onLessonCompleted(email, lessonNumber)
         ↓
    ┌────┴────┐
    ↓         ↓
findLeadIdByEmail()  → Ищем контакт по email
    ↓                → Находим сделку в воронке
moveLeadToStage()    → Перемещаем на нужный этап
    ↓
✅ Сделка обновлена в amoCRM
```

### Ключевые особенности:

#### 1. **Fire-and-Forget (Не блокируем студента)**
```typescript
amoCrmService.onLessonCompleted(email, lessonNumber)
  .catch(err => console.error('[AMOCRM] Фоновая ошибка:', err));
```
- Студент **всегда** видит "✅ Урок завершён"
- amoCRM обновляется в фоне
- Ошибки amoCRM логируются, но не ломают UX

#### 2. **Graceful Fallback**
```typescript
if (!isAmoCrmConfigured()) {
  console.log('[AmoCRM] Интеграция не настроена, пропускаем');
  return;
}
```
- Если переменные не заданы → интеграция не запускается
- Платформа работает как обычно
- Нет критических ошибок

#### 3. **Детальное логирование**
```typescript
console.log(`🎯 [AmoCRM] Пользователь ${email} завершил урок ${lessonNumber}`);
console.log(`[AmoCRM] Найдена сделка: ID ${leadId}, статус ${statusId}`);
console.log(`✅ [AmoCRM] Сделка ${leadId} успешно перемещена на этап ${stageId}`);
```
- Легко отследить, что происходит
- Понятные сообщения на русском
- Эмодзи для быстрой визуальной диагностики

---

## 🔐 Безопасность

### ✅ Что учтено:

1. **Access Token в .env**
   - Не коммитится в Git
   - Доступен только на сервере

2. **Таймаут запросов** (10 секунд)
   - Не висим бесконечно при проблемах с amoCRM

3. **Обработка всех ошибок**
   - 401 (Unauthorized) → "Проверьте токен"
   - 403 (Forbidden) → "Проверьте права токена"
   - 404 (Not Found) → "Проверьте SUBDOMAIN и PIPELINE_ID"
   - 429 (Rate Limit) → "Превышен лимит запросов"

4. **Не блокируем платформу**
   - Ошибки amoCRM = только логи
   - Студент получает нормальный ответ

---

## ⚠️ Важные замечания

### 1. Access Token живёт 24 часа

**Для тестирования:**
- Можно использовать временный токен из DevTools
- Обновлять вручную каждый день

**Для продакшена:**
- Настройте автообновление через Refresh Token
- Или используйте библиотеку `amocrm-js` (делает это автоматически)

### 2. Проверьте ID этапов

❌ **Не используйте заглушки:**
```bash
AMOCRM_STAGE_LESSON_1=12345678  # ← Это пример!
```

✅ **Получите реальные ID:**
```bash
npx ts-node backend/src/scripts/test-amocrm.ts
# → Выведет список воронок и этапов с реальными ID
```

### 3. Email должен совпадать

- Email в amoCRM **=** email в вашей БД
- Иначе контакт не будет найден
- Регистр не важен (API amoCRM ищет case-insensitive)

### 4. Сделка должна быть в нужной воронке

- Если у контакта несколько сделок
- Берётся **самая свежая** (по `updated_at`)
- Только из воронки с `AMOCRM_PIPELINE_ID`

---

## 🧪 Тестирование

### Автоматический тест:
```bash
npx ts-node backend/src/scripts/test-amocrm.ts
```

**Проверяет:**
- ✅ Все переменные заданы
- ✅ Подключение к amoCRM работает
- ✅ Воронки и этапы доступны
- ✅ (Опционально) Поиск тестовой сделки

### Ручной тест через Node.js консоль:
```bash
node

const amoCrm = require('./backend/src/services/amoCrmService');

// Тест 1: Подключение
await amoCrm.testConnection();

// Тест 2: Список воронок
await amoCrm.listPipelines();

// Тест 3: Поиск сделки
await amoCrm.findLeadIdByEmail('student@example.com');

// Тест 4: Полный цикл (замените email на реальный)
await amoCrm.onLessonCompleted('student@example.com', 1);
```

### Боевой тест:
1. Зайдите на платформу под студентом Tripwire
2. Завершите Урок 1 (lesson_id: 67)
3. Проверьте логи:
   ```bash
   pm2 logs backend --lines 100 | grep AMOCRM
   ```
4. Откройте amoCRM → Проверьте сделку переместилась на "ПРОШЕЛ ПЕРВЫЙ УРОК"

---

## 📈 Мониторинг

### Логи успеха:
```
🎯 [AmoCRM] Пользователь student@example.com завершил урок 1. Обновляем CRM...
[AmoCRM] Поиск контакта по email: student@example.com
[AmoCRM] Найден контакт: ID 123456, имя "Иван Иванов"
[AmoCRM] Найдена сделка: ID 789012, статус 65432100
[AmoCRM] Перемещаем сделку 789012 на этап 12345678
✅ [AmoCRM] Сделка 789012 успешно перемещена на этап 12345678
```

### Логи предупреждений (не критично):
```
⚠️ amoCRM не сконфигурирован. Проверьте переменные окружения
[AmoCRM] Интеграция не настроена, пропускаем обновление CRM
[AmoCRM] Контакт с email student@example.com не найден
[AmoCRM] Сделка для student@example.com не найдена в amoCRM
```

### Логи ошибок (требуют внимания):
```
❌ [AmoCRM] Ошибка авторизации. Проверьте AMOCRM_ACCESS_TOKEN
💡 Токен мог истечь. Обновите его через API amoCRM
```

---

## 🎓 Полезные ссылки

- **Документация amoCRM API v4:** https://www.amocrm.ru/developers/content/crm_platform/platform-api
- **OAuth авторизация:** https://www.amocrm.ru/developers/content/oauth/step-by-step
- **Работа со сделками:** https://www.amocrm.ru/developers/content/crm_platform/leads-api
- **Библиотека amocrm-js:** https://www.npmjs.com/package/amocrm-js

---

## ✅ Чеклист готовности

- [x] ✅ Сервис `amoCrmService.ts` создан
- [x] ✅ Интеграция в `tripwire-lessons.ts` добавлена
- [x] ✅ Документация написана
- [x] ✅ Тестовый скрипт создан
- [x] ✅ Обработка ошибок реализована
- [x] ✅ Fire-and-forget подход применён
- [x] ✅ Graceful fallback настроен
- [x] ✅ Логирование добавлено
- [ ] ⏳ Переменные окружения настроены (зависит от пользователя)
- [ ] ⏳ Тесты пройдены (зависит от пользователя)

---

## 🎉 Итоги

### Что получили:
1. **Полностью автоматизированный процесс** обновления сделок в amoCRM
2. **Надёжное решение** (не ломает UX студента при проблемах с amoCRM)
3. **Подробную документацию** на русском языке
4. **Инструменты для тестирования** и отладки
5. **Гибкую настройку** через переменные окружения

### Следующие шаги:
1. Получите Access Token из amoCRM
2. Добавьте переменные в `backend/.env`
3. Запустите тест: `npx ts-node backend/src/scripts/test-amocrm.ts`
4. Проверьте работу на реальном уроке
5. (Опционально) Настройте автообновление токена для продакшена

---

## 📞 Поддержка

Если что-то не работает:
1. Проверьте раздел **Troubleshooting** в `AMOCRM_INTEGRATION_SETUP.md`
2. Посмотрите логи бэкенда (`pm2 logs backend | grep AMOCRM`)
3. Запустите тестовый скрипт для диагностики

---

**Дата создания:** 2025-12-10  
**Версия:** 1.0  
**Статус:** ✅ Готово к использованию
