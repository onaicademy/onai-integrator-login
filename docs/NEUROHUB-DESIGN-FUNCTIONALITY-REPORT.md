# 📊 ОТЧЕТ ПО ДИЗАЙНУ И ФУНКЦИОНАЛУ СТРАНИЦЫ NEUROHUB

**Дата создания:** 21 ноября 2025  
**Страница:** `/neurohub` (AI-Наставник)  
**Файл:** `src/pages/NeuroHub.tsx`

---

## 🎯 ОБЩАЯ АРХИТЕКТУРА LAYOUT

### Desktop Layout (lg: ≥1024px)
Используется **CSS Grid с 12 колонками** для равномерного распределения блоков:

```
┌─────────────────────────────────────────────────────────┐
│  РЯД 1: ЧАТ (6 кол) | СТАТИСТИКА (6 кол)                │
│  - Чат с AI         | - Прогресс курса                   │
│  - 650px высота     | - Streak                           │
│                     | - XP & Уровень                     │
│                     | - Достижения                       │
├─────────────────────────────────────────────────────────┤
│  РЯД 2: МОИ ЦЕЛИ (6 кол) | ТЕКУЩАЯ МИССИЯ (6 кол)      │
│  - Список целей          | - Активное задание           │
│  - Редактирование        | - Прогресс выполнения        │
│                          | - Кнопка сдать ДЗ            │
├─────────────────────────────────────────────────────────┤
│  РЯД 3: СОВЕТ ДНЯ (6 кол) | СОВЕТ ПО ЗДОРОВЬЮ (6 кол) │
│  - Мотивационный совет   | - Советы по здоровью        │
├─────────────────────────────────────────────────────────┤
│  РЯД 4: ЧЕЛЛЕНДЖ ДНЯ (12 колонок на всю ширину)        │
│  - Ежедневный челлендж + награда XP                     │
└─────────────────────────────────────────────────────────┘

ВНЕ GRID (на всю ширину):
├─────────────────────────────────────────────────────────┤
│  ВСЕ ДОСТИЖЕНИЯ (Табы: Неделя / Месяц / Постоянные)    │
├─────────────────────────────────────────────────────────┤
│  ТОП СОВЕТОВ ПО ПОСЛЕДНИМ ВОПРОСАМ (Accordion)         │
├─────────────────────────────────────────────────────────┤
│  FAQ (Частые вопросы) (Accordion)                       │
└─────────────────────────────────────────────────────────┘
```

### Mobile Layout (< 1024px)
Используются **Tabs** для переключения между:
- 💬 **Чат** - полноэкранный чат с AI
- 📊 **Дашборд** - все карточки статистики вертикально

---

## 📦 ДЕТАЛЬНОЕ ОПИСАНИЕ ВСЕХ БЛОКОВ

---

## 1️⃣ ЧАТ С AI (Левая колонка, 6 колонок)

### 🎨 Дизайн
- **Размер:** `lg:col-span-6`, высота `650px`
- **Стиль:** Темная карточка с зеленой неоновой границей (`border-[#00ff00]/30`)
- **Анимации:** 
  - Киберпанк-углы (4 угла с границами)
  - Вертикальная бегущая линия зеленого цвета
  - Плавное появление сообщений

### ⚙️ Функционал

#### Кнопки и элементы:
| Элемент | Функционал | Статус |
|---------|-----------|--------|
| **➕ Новый чат** | Создает новую беседу, сохраняя старую в истории | ✅ Реализовано |
| **📎 Прикрепить файл** | Загрузка файлов (изображения, PDF, DOC, TXT) | ✅ Реализовано |
| **🎤 Голосовой ввод** | Распознавание речи (Speech-to-Text) | ⚠️ В разработке |
| **📤 Отправить** | Отправка сообщения AI-наставнику | ✅ Реализовано |

#### Технические детали:
- **API:** `sendMessageToAI()` из `lib/openai-assistant.ts`
- **История чата:** Загружается из `getChatHistory(user.id)`
- **Thread Management:** Использует `localStorage.getItem('openai_thread_id')`
- **Markdown:** Поддержка форматирования (ReactMarkdown + remarkGfm)
- **Прикрепленные файлы:**
  - Отображение превью с иконками (🖼️ Изображения, 📄 PDF, 📝 DOC)
  - Размер файла в KB
  - Кнопка удаления ❌

#### Состояния:
- `messages` - массив сообщений (user/assistant)
- `isSending` - индикатор отправки
- `attachedFiles` - массив прикрепленных файлов
- `isLoadingHistory` - загрузка истории

---

## 2️⃣ КАРТОЧКИ СТАТИСТИКИ (Правая колонка верх, 6 колонок)

Grid `2x2` с 4 карточками:

### 📊 Карточка 1: Прогресс курса
- **Иконка:** 📈 TrendingUp (зеленая)
- **Данные:** `courseProgress%` (0-100%)
- **Прогресс-бар:** Градиент `from-[#00ff00] to-[#00cc00]`
- **Формула:** `(completedLessons / totalLessons) * 100`
- **Источник данных:** `dashboardData?.week_activity`
- **Анимация:** Прогресс-бар заполняется за 1 сек с задержкой 0.5с
- **Hover:** Увеличение scale 1.02, поднятие вверх на 5px

### 🔥 Карточка 2: Streak (Стрик)
- **Иконка:** 🔥 Flame (оранжевая)
- **Данные:** Количество дней подряд обучения
- **Прогресс-бар:** Градиент `from-orange-500 to-red-500`
- **Цель:** 30 дней (прогресс до 100%)
- **Источник данных:** `dashboardData?.user_info?.current_streak`
- **Анимация:** 
  - Огонь пульсирует и вращается
  - Прогресс-бар заполняется за 1 сек с задержкой 0.7с
- **Hover:** Увеличение scale 1.02, поднятие вверх на 5px

### ⚡ Карточка 3: XP и Уровень
- **Иконка:** ⚡ Zap (желтая)
- **Данные:** 
  - Текущий XP пользователя
  - Уровень (Badge `Lvl X`)
  - XP до следующего уровня
- **Прогресс-бар:** Градиент `from-yellow-500 via-orange-500 to-yellow-500`
- **Формула XP:** `xpProgress = (userXP % 100) / 100 * 100`
- **Источник данных:** 
  - `dashboardData?.user_info?.xp`
  - `dashboardData?.user_info?.level`
- **Анимация:** 
  - Бегущий блеск по прогресс-бару
  - Прогресс-бар заполняется за 1 сек с задержкой 0.9с
- **Hover:** Увеличение scale 1.02, поднятие вверх на 5px

### 🏆 Карточка 4: Достижения
- **Иконка:** 🏆 Trophy (зеленая)
- **Данные:** Количество полученных достижений
- **Отображение:** 3 последних иконки достижений с анимацией
- **Источник данных:** `dashboardData?.recent_achievements`
- **Анимация:** 
  - Иконки появляются с вращением (-180° → 0°)
  - Hover на иконке: scale 1.2 + rotate 10°
  - Красная точка "NEW" пульсирует
- **Hover карточки:** Увеличение scale 1.02, поднятие вверх на 5px

---

## 3️⃣ МОИ ЦЕЛИ (Левая колонка низ, 6 колонок)

### 🎨 Дизайн
- **Размер:** `lg:col-span-6`, высота `h-full`
- **Стиль:** Темная карточка с зеленой границей
- **Кнопка:** "Редактировать" / "Готово"

### ⚙️ Функционал

#### Режим просмотра:
- Список целей с иконкой ✅ CheckCircle
- Каждая цель в отдельной карточке с hover-эффектом

#### Режим редактирования:
| Действие | Функционал |
|----------|-----------|
| **➕ Добавить цель** | Input + кнопка Plus, добавление по Enter |
| **❌ Удалить цель** | Кнопка X появляется при hover |

#### Технические детали:
- **State:** `myGoals` (массив строк)
- **State:** `isEditingGoals` (boolean)
- **State:** `newGoal` (string для нового input)
- **Анимация:** Плавное появление слева (x: -20 → 0) с задержкой

#### Функции для подключения:
```typescript
// TODO для backend:
// 1. saveGoals(userId, goals) - сохранение целей в БД
// 2. loadGoals(userId) - загрузка целей из БД
// 3. deleteGoal(userId, goalId) - удаление цели
```

---

## 4️⃣ ТЕКУЩАЯ МИССИЯ (Правая колонка низ, 6 колонок)

### 🎨 Дизайн
- **Размер:** `lg:col-span-6`, высота `h-full`
- **Стиль:** Темная карточка с зеленой границей + бегущий градиент
- **Иконка:** 🎯 Target

### ⚙️ Функционал

#### Если есть активная миссия:
| Элемент | Данные | Источник |
|---------|--------|---------|
| **Заголовок** | Название миссии | `mission.title` |
| **Описание** | Детали задания | `mission.description` |
| **Badge XP** | Награда за выполнение | `mission.xp_reward` |
| **Прогресс** | Текущий/целевой | `mission.current_value / mission.target_value` |
| **Прогресс-бар** | Визуальный прогресс | `mission.progress_percent%` |

#### Кнопки:
| Кнопка | Функционал | API |
|--------|-----------|-----|
| **📤 Сдать задание** | Открытие модалки загрузки ДЗ | `setIsUploadModalOpen(true)` |
| **💬 Задать вопрос** | Скролл к чату | `scrollIntoView()` |

#### Если нет активных миссий:
- Пустое состояние с иконкой 🎯
- Текст: "Пока нет активных заданий"

#### Технические детали:
- **Источник данных:** `dashboardData?.active_missions[0]`
- **Анимация прогресс-бара:** Заполнение с бегущим блеском
- **Состояние:** Показывается только первая активная миссия

#### Функции для подключения:
```typescript
// TODO для backend:
// 1. getActiveMissions(userId) - получение активных миссий
// 2. submitHomework(userId, missionId, file, comment) - сдача ДЗ
// 3. updateMissionProgress(userId, missionId, progress) - обновление прогресса
```

---

## 5️⃣ СОВЕТ ДНЯ (Левая колонка ряд 3, 6 колонок)

### 🎨 Дизайн
- **Размер:** `lg:col-span-6`
- **Стиль:** Градиент `from-[#00ff00]/10 to-[#00cc00]/10`
- **Иконка:** 💡 Lightbulb (желтая, анимированная)

### ⚙️ Функционал
- **Данные:** Мотивационный совет дня
- **Источник:** Массив `dailyTips[]` (5 советов)
- **Ротация:** По дате (`new Date().getDate() % dailyTips.length`)
- **Анимация:** 
  - Бегущий градиент фона
  - Лампочка вращается и пульсирует

#### Советы в массиве:
1. "💡 Учись каждый день по 15 минут..."
2. "🚀 Практикуй полученные знания сразу..."
3. "🎯 Ставь маленькие цели..."
4. "⚡ Используй технику Pomodoro..."
5. "🔥 Твоя регулярность важнее скорости..."

#### Функции для подключения:
```typescript
// TODO для backend:
// 1. getDailyTip(date) - получение совета дня из БД
// 2. Можно добавить персонализированные советы на основе прогресса
```

---

## 6️⃣ СОВЕТ ПО ЗДОРОВЬЮ (Правая колонка ряд 3, 6 колонок)

### 🎨 Дизайн
- **Размер:** `lg:col-span-6`
- **Стиль:** Градиент `from-blue-500/10 to-cyan-500/10`
- **Иконка:** Динамическая (💧/👀/🧘/🌬️/🛌), анимированная

### ⚙️ Функционал
- **Данные:** Совет по здоровью и благополучию
- **Источник:** Массив `healthTips[]` (5 советов)
- **Ротация:** По времени (`new Date().getHours() % healthTips.length`)
- **Анимация:** Иконка пульсирует

#### Советы в массиве:
| Иконка | Заголовок | Текст |
|--------|-----------|-------|
| 💧 | Пей воду | "Выпей стакан воды прямо сейчас!" |
| 👀 | Отдых для глаз | "Каждые 20 минут смотри вдаль 20 секунд" |
| 🧘 | Разминка | "Встань и потянись — 2 минуты движения" |
| 🌬️ | Дыши глубже | "5 глубоких вдохов для концентрации" |
| 🛌 | Сон важен | "Спи 7-8 часов для лучшего усвоения" |

#### Функции для подключения:
```typescript
// TODO для backend:
// 1. getHealthTip(hour) - получение совета по времени суток
// 2. Можно добавить таймеры и напоминания (push-уведомления)
```

---

## 7️⃣ ЧЕЛЛЕНДЖ ДНЯ (Полная ширина, 12 колонок)

### 🎨 Дизайн
- **Размер:** `lg:col-span-12`
- **Стиль:** Градиент `from-yellow-500/10 to-orange-500/10`
- **Иконка:** Динамическая (🏃/🌅/🤝/⭐/🔍), анимированная
- **Условие:** Показывается только если `!challengeAccepted`

### ⚙️ Функционал

#### Данные челленджа:
| Поле | Описание | Источник |
|------|----------|---------|
| **Заголовок** | Название челленджа | `todayChallenge.title` |
| **Описание** | Условие выполнения | `todayChallenge.description` |
| **XP награда** | Награда за выполнение | `todayChallenge.xp` |
| **Иконка** | Emoji иконка | `todayChallenge.icon` |

#### Челленджи в массиве:
1. 🏃 "Марафонец" - Пройди 2 урока сегодня (+50 XP)
2. 🌅 "Ранняя пташка" - Начни обучение до 10:00 (+30 XP)
3. 🤝 "Помощник" - Помоги другу с заданием (+40 XP)
4. ⭐ "Перфекционист" - Сдай ДЗ на 100% (+60 XP)
5. 🔍 "Исследователь" - Изучи доп. материалы (+35 XP)

#### Кнопки:
| Кнопка | Функционал | Результат |
|--------|-----------|-----------|
| **✅ Принять** | Принятие челленджа | Toast + скрытие блока |
| **Позже** | Отклонение челленджа | Скрытие блока |

#### Технические детали:
- **State:** `challengeAccepted` (boolean)
- **Ротация:** По дате (`new Date().getDate() % dailyChallenges.length`)
- **Анимация:** Иконка пульсирует и вращается

#### Функции для подключения:
```typescript
// TODO для backend:
// 1. acceptChallenge(userId, challengeId) - принятие челленджа
// 2. checkChallengeCompletion(userId, challengeId) - проверка выполнения
// 3. rewardChallenge(userId, xp) - начисление XP за выполнение
// 4. getDailyChallenge(date) - получение челленджа дня
```

---

## 8️⃣ ВСЕ ДОСТИЖЕНИЯ (Полная ширина, вне grid)

### 🎨 Дизайн
- **Стиль:** Темная карточка с зеленой границей
- **Иконка:** 🏆 Trophy
- **Layout:** Tabs (3 категории) + Grid карточек

### ⚙️ Функционал

#### Badges в Header:
| Badge | Данные | Формула |
|-------|--------|---------|
| **⭐ Завершено** | Количество выполненных | `filter(status === "completed").length` |
| **⏳ В работе** | Количество в процессе | `filter(status === "in_progress").length` |

#### Табы категорий:
1. **📅 Неделя** - Еженедельные достижения (3 достижения)
2. **📆 Месяц** - Месячные достижения (3 достижения)
3. **⭐ Постоянные** - Milestone достижения (4 достижения)

### Структура карточки достижения:

#### Статусы:
| Статус | Визуал | Функционал |
|--------|--------|-----------|
| **completed** | Зеленая граница + галочка ✅ | Анимация масштаба при hover |
| **in_progress** | Желтая граница + прогресс-бар | Показ прогресса X/Y |
| **locked** | Серый + замок 🔒 | Показ требования |

#### Элементы карточки:
| Элемент | Данные | Условие |
|---------|--------|---------|
| **Иконка** | Emoji (граyscale если locked) | Всегда |
| **Заголовок** | Название достижения | Всегда |
| **Описание** | Награда XP | Всегда |
| **Прогресс-бар** | Текущий/целевой прогресс | Если in_progress |
| **Требование** | Условие разблокировки | Если locked |
| **Галочка** | CheckCircle | Если completed |
| **Tooltip** | Подсказка при hover | Всегда |

#### Анимации:
- Появление с задержкой `index * 0.1` сек
- Вращение из -180° → 0°
- Hover: scale 1.05 + поднятие вверх (если не locked)

#### Технические детали:
- **Компонент:** `AchievementCard` (строки 69-139)
- **Данные:** `allAchievements.week / month / permanent`
- **Grid:** `grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-4`

#### Функции для подключения:
```typescript
// TODO для backend:
// 1. getUserAchievements(userId) - получение всех достижений пользователя
// 2. unlockAchievement(userId, achievementId) - разблокировка достижения
// 3. updateAchievementProgress(userId, achievementId, progress) - обновление прогресса
// 4. Типы данных:
interface Achievement {
  id: string;
  icon: string;
  title: string;
  description: string;
  status: "completed" | "in_progress" | "locked";
  progress?: number;
  current?: number;
  target?: number;
  requirement?: string;
  category?: string;
  xp_reward?: number;
}
```

---

## 9️⃣ ТОП СОВЕТОВ ПО ПОСЛЕДНИМ ВОПРОСАМ (Полная ширина, вне grid)

### 🎨 Дизайн
- **Стиль:** Градиент `from-purple-500/5 to-pink-500/5`
- **Иконка:** ✨ Sparkles (фиолетовая)
- **Layout:** Accordion (раскрывающиеся блоки)

### ⚙️ Функционал

#### Заголовок:
- "Топ советов по твоим последним вопросам"
- "AI проанализировал твои вопросы и подготовил персональные рекомендации"

#### Структура каждого совета:

| Поле | Описание | Отображение |
|------|----------|-------------|
| **Иконка** | Emoji 3xl | Всегда видна |
| **Заголовок** | Название темы | Всегда виден |
| **Timestamp** | Время совета | Серый текст |
| **Краткое резюме** | Короткое описание | В раскрытом блоке |
| **Подробности** | Пошаговая инструкция | В раскрытом блоке |
| **Ссылка на урок** | Кнопка перехода | Если есть actionLink |

#### Советы в массиве (примеры):
1. 🔗 "Как правильно настроить вебхуки" - 7 шагов
2. ⚡ "Оптимизация работы с API" - Rate limits
3. 🐛 "Debugging в n8n" - 6 шагов отладки

#### Кнопки:
| Кнопка | Функционал | Стиль |
|--------|-----------|-------|
| **📤 Открыть урок** | Переход на `/course/X/lesson/Y` | Градиент purple→pink |

#### Анимации:
- Раскрытие accordion с плавным появлением (y: -10 → 0)
- Hover на заголовке: цвет → purple-400

#### Технические детали:
- **Компонент:** Accordion (single, collapsible)
- **Данные:** Массив `topAdvices[]`
- **Стиль блока:** `border-purple-500/20`, открытый: `bg-purple-500/10`

#### Функции для подключения:
```typescript
// TODO для backend:
// 1. analyzeUserQuestions(userId) - анализ последних вопросов AI
// 2. generateAdvice(userId, topic) - генерация персонализированных советов
// 3. Типы данных:
interface Advice {
  id: string;
  title: string;
  summary: string;
  details: string;
  actionLink: string | null;
  icon: string;
  timestamp: string;
}
```

---

## 🔟 FAQ - ЧАСТЫЕ ВОПРОСЫ (Полная ширина, вне grid)

### 🎨 Дизайн
- **Стиль:** Темная карточка с зеленой границей
- **Иконка:** ✨ Sparkles
- **Layout:** Accordion (раскрывающиеся блоки)

### ⚙️ Функционал

#### Заголовок:
- "Частые вопросы"
- "Быстрые ответы на популярные вопросы студентов"

#### Структура каждого FAQ:

| Поле | Описание |
|------|----------|
| **Вопрос** | Заголовок с emoji (🔗/🔑/📊/🎤) |
| **Ответ** | Развернутый текст ответа |
| **Ссылка на урок** | `lessonLink` (опционально) |

#### FAQ в массиве:
1. 🔗 "Как настроить вебхук в n8n?" → Урок 3
2. 🔑 "Где получить API ключ OpenAI?" → Урок 1
3. 📊 "Как интегрировать бота с CRM?" → Урок 8
4. 🎤 "Как добавить голосовые сообщения?" → Урок 12

#### Кнопки:
| Кнопка | Функционал | Иконка |
|--------|-----------|--------|
| **📋 Копировать ответ** | Копирование в буфер обмена + Toast | Copy |
| **📤 Открыть урок** | Переход на урок | ExternalLink |

#### Анимации:
- Раскрытие accordion с плавным появлением (y: -10 → 0)
- Hover на заголовке: цвет → зеленый
- Открытый блок: `bg-[#00ff00]/5`

#### Технические детали:
- **Компонент:** Accordion (single, collapsible)
- **Данные:** Массив `faqItems[]`
- **Toast:** "✅ Скопировано" при копировании

#### Функции для подключения:
```typescript
// TODO для backend:
// 1. getFAQItems() - получение часто задаваемых вопросов
// 2. addFAQItem(question, answer, lessonLink) - добавление нового FAQ
// 3. trackFAQUsage(userId, faqId) - отслеживание популярности вопросов
// 4. Типы данных:
interface FAQItem {
  question: string;
  answer: string;
  lessonLink: string;
}
```

---

## 📱 MOBILE LAYOUT (< 1024px)

### Структура:
```
┌─────────────────────────────┐
│  TABS: [💬 Чат] [📊 Дашборд] │
├─────────────────────────────┤
│  TAB 1 - ЧАТ:               │
│  - Полноэкранный чат        │
│  - Высота 400-500px         │
│  - Все функции Desktop      │
├─────────────────────────────┤
│  TAB 2 - ДАШБОРД:           │
│  - Все карточки вертикально │
│  - Статистика дня           │
│  - Прогресс курса           │
│  - Streak                   │
│  - Совет дня                │
│  - Текущая миссия           │
└─────────────────────────────┘
```

---

## 🎬 ДОПОЛНИТЕЛЬНЫЕ ЭЛЕМЕНТЫ

### XP Pop-up уведомление
- **Положение:** `fixed top-20 right-4`
- **Условие:** Показывается при `todayStats.xp_earned > 0`
- **Дизайн:** Градиент зеленый + пульсирующее свечение
- **Анимация:** Появление + вращающаяся молния + автоскрытие через 3 сек
- **Данные:** `+{xpAmount} XP` + "Отличная работа!"

### Модалка загрузки ДЗ
- **Триггер:** Кнопка "Сдать задание" в Текущей миссии
- **Элементы:**
  - Drag & Drop зона загрузки
  - Input для комментария
  - Кнопки "Отмена" / "Отправить"
- **Форматы:** PDF, DOC, DOCX, ZIP (до 10MB)
- **State:** `isUploadModalOpen`, `uploadFile`

#### Функции для подключения:
```typescript
// TODO для backend:
// 1. uploadHomework(userId, missionId, file, comment) - загрузка ДЗ
// 2. Интеграция с хранилищем (Cloudflare R2 / S3)
// 3. Уведомление куратора о новом ДЗ
```

---

## 🎨 ОБЩИЕ ЭЛЕМЕНТЫ ДИЗАЙНА

### Цветовая палитра:
| Элемент | Цвет | Использование |
|---------|------|---------------|
| **Основной зеленый** | `#00ff00` | Границы, акценты, кнопки |
| **Темный зеленый** | `#00cc00` | Градиенты |
| **Оранжевый** | `#ff8c00` | Streak, предупреждения |
| **Желтый** | `#ffd700` | XP, челленджи |
| **Синий** | `#4169e1` | Здоровье, информация |
| **Фиолетовый** | `#9333ea` | Советы, рекомендации |
| **Фон** | `black → #0a0a14` | Градиент фона |

### Анимации:
1. **Бегущая линия** - вертикальная зеленая полоса (3 сек)
2. **Бегущий градиент** - горизонтальный блеск (3 сек)
3. **Пульсация** - scale 1 → 1.2 → 1 (2 сек)
4. **Вращение** - rotate 0° → 360° (4-8 сек)
5. **Hover эффекты** - scale 1.02 + translateY(-5px)
6. **Появление** - opacity 0 → 1, y 20 → 0

---

## 📊 ИСТОЧНИКИ ДАННЫХ

### API Endpoints (необходимо подключить):

```typescript
// 1. Dashboard данные
GET /api/dashboard/:userId
Response: {
  user_info: {
    level: number,
    xp: number,
    current_streak: number
  },
  week_activity: Array<{ lessons_completed: number }>,
  today_stats: {
    lessons_completed: number,
    watch_time_minutes: number,
    xp_earned: number
  },
  active_missions: Array<Mission>,
  recent_achievements: Array<Achievement>
}

// 2. AI Chat
POST /api/ai/send-message
Body: {
  message: string,
  attachments: Array<File>,
  userId: string,
  assistantType: 'mentor'
}

GET /api/ai/chat-history/:userId
Response: Array<ChatMessage>

// 3. Достижения
GET /api/achievements/:userId
Response: Array<Achievement>

POST /api/achievements/unlock
Body: { userId: string, achievementId: string }

// 4. Миссии
GET /api/missions/active/:userId
Response: Array<Mission>

POST /api/missions/submit
Body: { userId: string, missionId: string, file: File, comment: string }

// 5. Цели
GET /api/goals/:userId
POST /api/goals/:userId
DELETE /api/goals/:userId/:goalId

// 6. Челленджи
GET /api/challenges/daily
POST /api/challenges/accept
Body: { userId: string, challengeId: string }
```

---

## ✅ ГОТОВЫЕ ФУНКЦИИ

### Уже реализовано:
- ✅ Чат с AI (отправка/получение сообщений)
- ✅ Прикрепление файлов
- ✅ Markdown форматирование
- ✅ Новый чат
- ✅ История чата
- ✅ Редактирование целей
- ✅ Копирование ответов FAQ
- ✅ Toast уведомления
- ✅ Все анимации

### В разработке:
- ⚠️ Голосовой ввод (Speech-to-Text)
- ⚠️ Загрузка ДЗ (upload + backend)
- ⚠️ Система челленджей (проверка выполнения)
- ⚠️ Персонализированные советы (AI-анализ)

---

## 🔧 ТЕХНИЧЕСКОЕ РЕЗЮМЕ

### Технологии:
- **React** + **TypeScript**
- **Framer Motion** (анимации)
- **Tailwind CSS** (стилизация)
- **shadcn/ui** (компоненты)
- **ReactMarkdown** (форматирование чата)

### State Management:
- `useState` для локального состояния
- `useEffect` для загрузки данных
- `useAuth` для пользователя
- `useToast` для уведомлений

### Оптимизация:
- `useMemo` для тяжелых вычислений (particles)
- Lazy loading компонентов
- Оптимизированные анимации (GPU-акселерация)

---

## 📝 ИТОГОВЫЕ РЕКОМЕНДАЦИИ ДЛЯ РАЗРАБОТЧИКА

### Приоритет 1 (Критично):
1. Подключить API endpoints для Dashboard
2. Реализовать загрузку/сохранение Целей
3. Подключить систему Миссий
4. Реализовать загрузку ДЗ с backend

### Приоритет 2 (Важно):
1. Система Челленджей с проверкой выполнения
2. Разблокировка Достижений автоматически
3. Персонализированные Советы на основе AI-анализа
4. Push-уведомления для напоминаний

### Приоритет 3 (Улучшения):
1. Голосовой ввод (Speech-to-Text)
2. Gamification (звуки, конфетти при достижениях)
3. Таймеры и напоминания о здоровье
4. Экспорт статистики

---

**Конец отчета** 🎯



