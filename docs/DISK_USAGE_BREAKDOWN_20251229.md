# 💾 Детальная Разбивка Использования Диска
**Дата:** 29 декабря 2025
**Сервер:** 207.154.231.30
**Общий размер:** 24GB
**Текущее использование:** 9.8GB (43%)

---

## 📊 ОБЩАЯ СТРУКТУРА (9.8GB)

```
/dev/vda1 = 24GB total
├── /var        6.8GB (69%)  ← Основное использование
├── /usr        3.6GB (37%)  ← Системные утилиты
├── /root       2.0GB (20%)  ← NPM кэш + бэкапы
├── /tmp        487MB (5%)   ← Временные файлы
├── /boot       123MB (1%)   ← Ядро Linux
└── Остальное    27MB (<1%)
─────────────────────────────
ИТОГО:          9.8GB (43% диска)
```

---

## 🔍 ДЕТАЛЬНАЯ РАЗБИВКА

### 1. /var (6.8GB) - Переменные данные

```
/var = 6.8GB
├── /var/lib          4.3GB (63%)  ← Docker + containerd
│   ├── containerd    4.0GB        ← Docker слои (сейчас пересобирается)
│   ├── docker        3.4GB        ← Docker rootfs
│   ├── apt           219MB        ← Пакеты APT
│   └── остальное     100MB
│
├── /var/www          2.1GB (31%)  ← Код приложений
│   ├── onai-integrator-login-main  2.0GB
│   │   ├── node_modules           1.2GB  ← Frontend зависимости
│   │   ├── backend                455MB  ← Backend код
│   │   │   └── node_modules       398MB
│   │   ├── dist                   19MB   ← Скомпилированный код
│   │   ├── deploy-*.tar.gz        70MB   ← 🗑️ МОЖНО УДАЛИТЬ (старые бэкапы)
│   │   ├── logs                   8MB
│   │   ├── playwright-report      7.1MB  ← 🗑️ МОЖНО УДАЛИТЬ (тесты)
│   │   ├── test-results           6.7MB  ← 🗑️ МОЖНО УДАЛИТЬ (тесты)
│   │   ├── public                 12MB
│   │   └── src                    4.9MB
│   │
│   ├── onai.academy               29MB   ← Старый код? 🤔
│   ├── traffic.onai.academy       18MB   ← Старый код? 🤔
│   ├── onai-academy               18MB   ← Дубликат? 🤔
│   ├── onai-academy-frontend      17MB   ← Старый код? 🤔
│   ├── referral.onai.academy      19MB   ← Старый код? 🤔
│   ├── backups                    19MB
│   ├── deploy-*.tar.gz            30MB   ← 🗑️ МОЖНО УДАЛИТЬ
│   └── html                       18MB
│
├── /var/log          303MB (4%)   ← Логи
│   ├── journal       258MB        ← systemd логи
│   ├── auth.log      16MB
│   ├── nginx         3.9MB
│   └── остальное     25MB
│
└── /var/cache        238MB (3%)   ← Кэш пакетов
```

### 2. /usr (3.6GB) - Системные программы

```
/usr = 3.6GB (НОРМАЛЬНО - системные утилиты)
├── /usr/lib          1.8GB  ← Библиотеки (Python, Node, gcc, etc)
├── /usr/bin          497MB  ← Исполняемые файлы
├── /usr/share        420MB  ← Документация, иконки
├── /usr/src          311MB  ← Исходники ядра
└── /usr/local        240MB  ← Локально установленное
```

**Вывод:** /usr НЕ чистим - это системные файлы

### 3. /root (2.0GB) - Домашняя директория root

```
/root = 2.0GB
├── .npm              1.0GB  ← 🗑️ NPM кэш (МОЖНО ПОЧИСТИТЬ!)
├── .cache            609MB  ← 🗑️ Общий кэш (МОЖНО ПОЧИСТИТЬ!)
├── .pm2              25MB   ← PM2 логи
└── backup-*.tar.gz   140MB  ← 🗑️ Старые бэкапы (МОЖНО УДАЛИТЬ!)
    ├── backup-onai-academy-20251216-1914.tar.gz         27MB
    ├── backup-onai-academy-20251225-1800.tar.gz         16MB
    ├── backup-onai-academy-20251220-1411.tar.gz         15MB
    ├── backup-onai-academy-filters-20251219-1340.tar.gz 14MB
    ├── backup-onai-academy-20251225-1805.tar.gz         14MB
    ├── backup-onai-academy-20251223-1810...tar.gz       14MB
    └── остальные...                                      40MB
```

**Можно освободить:** ~1.75GB

### 4. /tmp (487MB) - Временные файлы

```
/tmp = 487MB (автоматически чистится при перезагрузке)
```

---

## 🎯 ЧТО МОЖНО БЕЗОПАСНО УДАЛИТЬ

### Высокий приоритет (~2GB):

1. **NPM кэш в /root/.npm (1.0GB)**
   ```bash
   rm -rf /root/.npm/_cacache
   ```
   - Это кэш установленных пакетов
   - Безопасно удалять (пересоздастся при следующем npm install)

2. **Общий кэш в /root/.cache (609MB)**
   ```bash
   rm -rf /root/.cache/*
   ```
   - Python pip cache, wget cache, etc
   - Безопасно удалять

3. **Старые бэкапы в /root (140MB)**
   ```bash
   rm -f /root/backup-onai-academy-*.tar.gz
   ```
   - У тебя бэкапы в Git
   - Старше 2 недель - можно удалить

4. **Старые деплой архивы в /var/www (100MB)**
   ```bash
   cd /var/www/onai-integrator-login-main
   rm -f deploy-*.tar.gz traffic-deploy-*.tar.gz
   rm -f /var/www/deploy-*.tar.gz /var/www/dist.zip
   ```
   - Старые версии кода
   - Есть в Git

5. **Тестовые отчёты (14MB)**
   ```bash
   rm -rf /var/www/onai-integrator-login-main/playwright-report
   rm -rf /var/www/onai-integrator-login-main/test-results
   ```
   - Локальные результаты тестов
   - Не нужны на продакшене

### Средний приоритет (~200MB):

6. **Старые директории в /var/www**
   ```bash
   # ПРОВЕРИТЬ сначала используются ли:
   ls -la /var/www/onai.academy
   ls -la /var/www/traffic.onai.academy
   ls -la /var/www/onai-academy-frontend
   ```
   - Возможно старые версии сайтов
   - Нужно проверить используются ли Nginx

7. **Журналы systemd старше месяца (100MB)**
   ```bash
   journalctl --vacuum-time=30d
   ```
   - Оставит логи за последние 30 дней

---

## 💡 БЕЗОПАСНЫЙ СКРИПТ ЧИСТКИ

```bash
#!/bin/bash
# Освобождает ~2GB места безопасно

echo "=== НАЧИНАЕМ БЕЗОПАСНУЮ ЧИСТКУ ==="
echo ""

# 1. NPM кэш
echo "1. Очистка NPM кэша..."
rm -rf /root/.npm/_cacache
echo "✅ NPM кэш очищен"
echo ""

# 2. Общий кэш
echo "2. Очистка общего кэша..."
rm -rf /root/.cache/*
echo "✅ Общий кэш очищен"
echo ""

# 3. Старые бэкапы
echo "3. Удаление старых бэкапов..."
rm -f /root/backup-onai-academy-*.tar.gz
echo "✅ Старые бэкапы удалены"
echo ""

# 4. Старые деплой архивы
echo "4. Удаление старых деплой архивов..."
cd /var/www/onai-integrator-login-main
rm -f deploy-*.tar.gz traffic-deploy-*.tar.gz
cd /var/www
rm -f deploy-*.tar.gz dist.zip
echo "✅ Деплой архивы удалены"
echo ""

# 5. Тестовые отчёты
echo "5. Удаление тестовых отчётов..."
rm -rf /var/www/onai-integrator-login-main/playwright-report
rm -rf /var/www/onai-integrator-login-main/test-results
echo "✅ Тестовые отчёты удалены"
echo ""

# 6. Журналы старше 30 дней
echo "6. Очистка старых журналов..."
journalctl --vacuum-time=30d
echo "✅ Старые журналы удалены"
echo ""

# 7. Docker build cache
echo "7. Очистка Docker build cache..."
docker builder prune -f
echo "✅ Docker build cache очищен"
echo ""

echo "=== РЕЗУЛЬТАТ ==="
df -h /
echo ""
echo "✅ ЧИСТКА ЗАВЕРШЕНА!"
```

---

## 📊 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ ПОСЛЕ ЧИСТКИ

### ДО чистки:
```
/dev/vda1  24GB  9.8GB  14GB  43%
```

### ПОСЛЕ чистки:
```
/dev/vda1  24GB  ~8GB  ~16GB  33%

Освобождено: ~2GB
```

### Структура после чистки:
```
├── /var/lib          4.0GB  (Docker - активно используется)
├── /usr              3.6GB  (Системные утилиты)
├── /var/www          2.0GB  (Код приложения - очищен от мусора)
├── /var/log          200MB  (Логи за 30 дней)
├── /root             100MB  (Без кэша)
└── Остальное         100MB
```

---

## 🚨 ЧТО НЕ ТРОГАЕМ

### 1. node_modules (1.2GB + 398MB)
**Почему:** Необходимы для работы приложения
**Когда чистить:** Только при обновлении зависимостей

### 2. /var/lib/docker (3.4GB) + /var/lib/containerd (4.0GB)
**Почему:** Docker активно используется (сейчас пересобирается)
**Когда чистить:** После сборки останется ~5-6GB (оптимизированные образы)

### 3. backend код (455MB)
**Почему:** Это исходный код бэкенда
**Нельзя удалять:** Нужен для работы

### 4. /usr (3.6GB)
**Почему:** Системные утилиты (Node.js, Python, gcc, библиотеки)
**Нельзя удалять:** Сломается вся система

---

## 🔄 РЕГУЛЯРНОЕ ОБСЛУЖИВАНИЕ

### Еженедельно (автоматически):
```bash
# Cron job - каждое воскресенье в 3:00
0 3 * * 0 /usr/bin/docker system prune -f && /usr/bin/journalctl --vacuum-time=30d
```

### Ежемесячно (вручную):
```bash
# Полная чистка
rm -rf /root/.npm/_cacache /root/.cache/*
docker system prune -a -f
journalctl --vacuum-time=30d
```

---

## ✅ ВЫВОДЫ

### Текущее использование (9.8GB) это НОРМАЛЬНО:
- **Docker (7.4GB)** - активно используется (сейчас пересобирается)
- **Системные файлы (3.6GB)** - необходимы для работы
- **Код приложения (2.0GB)** - node_modules + исходники
- **Кэши и мусор (~2GB)** - ← МОЖНО ПОЧИСТИТЬ!

### После чистки и оптимизации:
- **Использование:** ~8GB (33%)
- **Свободно:** ~16GB (67%)
- **Резерв для деплоев:** Достаточно

### Рекомендация:
**НЕ НУЖНО увеличивать диск.** 24GB достаточно при регулярной чистке.

---

**Отчёт подготовлен:** 29 декабря 2025
