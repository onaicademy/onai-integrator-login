# Инструкция по очистке кэша для Sales Manager Dashboard

**Дата:** 2025-12-30  
**Время:** 12:52 UTC  
**Проблема:** Браузер использует кэшированную версию JavaScript файла

---

## 🎯 Проблема

После исправления кода в `UsersTable.tsx` (строки 151-166), браузер всё ещё показывает старую ошибку:
```
tripwireManagerService.deleteTripwireUser is not a function
```

**Причина:** Браузер кэширует JavaScript bundle и не загружает новую версию.

---

## ✅ Решение

### Вариант 1: Hard Refresh (Рекомендуется)

**Windows/Linux:**
```
Ctrl + Shift + R
```

**Mac:**
```
Cmd + Shift + R
```

Это заставит браузер перезагрузить страницу без кэша.

---

### Вариант 2: Очистка кэша через DevTools

1. Откройте DevTools: `F12` или `Cmd+Option+I`
2. Перейдите на вкладку **Network**
3. Поставьте галочку **"Disable cache"**
4. Нажмите `F5` или `Cmd+R` для перезагрузки
5. После перезагрузки можно убрать галочку

---

### Вариант 3: Очистка кэша через настройки браузера

**Chrome:**
1. Откройте настройки: `chrome://settings/clearBrowserData`
2. Выберите "Cached images and files"
3. Нажмите "Clear data"

**Firefox:**
1. Откройте настройки: `about:preferences#privacy`
2. Нажмите "Clear History"
3. Выберите "Cache"
4. Нажмите "OK Now"

**Safari:**
1. Откройте меню Safari > Settings
2. Перейдите на вкладку Privacy
3. Нажмите "Manage Website Data"
4. Нажмите "Remove All"

---

### Вариант 4: Рестарт Dev-сервера (если работает локально)

Если вы запускаете dev-сервер локально:

```bash
# Остановите сервер (Ctrl+C)
# Затем запустите снова:
npm run dev
```

После рестарта откройте страницу с hard refresh.

---

## 🔍 Проверка после очистки кэша

### 1. Откройте DevTools (F12)

### 2. Перейдите на вкладку **Console**

### 3. Попробуйте удалить студента

Вы должны увидеть:
```
🗑️ [DELETE] Sales Manager amina@onaiacademy.kz deleting user: [user-id]
✅ [DELETE] User deleted successfully: { success: true, email: "...", ... }
```

### 4. Если всё работает, появится alert:
```
✅ Успешно удалено!

Студент: [имя]
Email: [email]

Все данные пользователя удалены из системы.
```

---

## 🐛 Если ошибка всё ещё появляется

### Проверьте, что загружается новый файл:

1. Откройте DevTools > **Sources**
2. Найдите файл `UsersTable.tsx` или скомпилированный JS файл
3. Проверьте строки 151-166
4. Должен быть код:
```typescript
// ✅ FIX: Вызываем RPC функцию напрямую через Supabase
const { data: result, error } = await tripwireSupabase.rpc('rpc_delete_tripwire_user', {
  p_user_id: userId
});
```

Если вы всё ещё видите старый код:
```typescript
const response = await api.delete(`/api/admin/tripwire/users/${userId}`);
```

Значит кэш не очистился. Попробуйте Вариант 3 (полная очистка кэша).

---

## 📝 Дополнительные проверки

### Проверка RPC функции в Supabase:

```sql
-- Проверьте, что функция существует
SELECT 
  proname as function_name,
  prosrc as source_code
FROM pg_proc
WHERE proname = 'rpc_delete_tripwire_user';
```

### Проверка прав доступа:

```sql
-- Проверьте, что функция имеет SECURITY DEFINER
SELECT 
  proname,
  prosecdef as security_definer
FROM pg_proc
WHERE proname = 'rpc_delete_tripwire_user';
```

---

## 🎯 Ожидаемый результат

После успешной очистки кэша:

1. ✅ Кнопка удаления работает без ошибок
2. ✅ В консоли появляется лог: `✅ [DELETE] User deleted successfully`
3. ✅ Появляется alert об успешном удалении
4. ✅ Студент исчезает из таблицы
5. ✅ Счётчик "ВСЕГО" уменьшается на 1

---

## 📞 Если ничего не помогает

1. Попробуйте открыть сайт в **инкогнито режиме**
2. Или попробуйте другой браузер
3. Проверьте, что файлы были сохранены на сервере (если это production)

---

## 🔗 Связанные документы

- [SALES_MANAGER_DELETE_FIX_REPORT_20251230.md](./SALES_MANAGER_DELETE_FIX_REPORT_20251230.md) - Детальный анализ исправления
- [SALES_MANAGER_DELETE_TESTING_INSTRUCTIONS_20251230.md](./SALES_MANAGER_DELETE_TESTING_INSTRUCTIONS_20251230.md) - Инструкции по тестированию

---

**Создано:** 2025-12-30 12:52 UTC  
**Статус:** ✅ Инструкция готова к использованию
