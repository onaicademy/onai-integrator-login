# 🔧 FIX: Короткие видео (< 1 минуты) и Auto-Reload

**Дата:** 2025-01-20  
**Проблема:** Видео < 60 секунд показывают "0 минут", и страница не обновляется после загрузки  
**Статус:** ✅ ИСПРАВЛЕНО

---

## 📊 **ПРОБЛЕМА #1: Короткие видео = 0 минут**

### Симптомы:
```
Урок "sdasdsa": 0 минут (вычислено из 11 секунд)
```

**11 секунд / 60 = 0.18 минут → Math.round(0.18) = 0!**

### Root Cause:
Backend и frontend использовали `Math.round()` для округления длительности:
```typescript
// ❌ ПЛОХО:
lesson.duration_minutes = Math.round(video.duration_seconds / 60);
// 11 секунд → 0.18 минут → 0 минут
```

### Решение:
Используем `Math.ceil()` (округление ВВЕРХ):
```typescript
// ✅ ХОРОШО:
lesson.duration_minutes = Math.ceil(video.duration_seconds / 60);
// 11 секунд → 0.18 минут → 1 минута
```

### Исправленные файлы:
1. `backend/src/routes/lessons.ts` (строка 71)
2. `src/pages/Module.tsx` (строка 331)

---

## 📊 **ПРОБЛЕМА #2: Страница не обновляется после загрузки видео**

### Симптомы:
1. Пользователь загружает видео
2. Видео успешно загружается
3. Страница модуля продолжает показывать "0 минут"
4. Нужно вручную перезагружать страницу (F5)

### Root Cause:
После загрузки видео в `LessonEditDialog.tsx` не было callback'а для перезагрузки списка уроков.

### Решение:
Добавлен prop `onVideoUploaded` который вызывается после успешной загрузки:

**1. Интерфейс (`LessonEditDialog.tsx`):**
```typescript
interface LessonEditDialogProps {
  // ... другие props
  onVideoUploaded?: () => void; // Callback после загрузки видео
}
```

**2. Вызов callback после загрузки (`LessonEditDialog.tsx`, строка 397-401):**
```typescript
if (newVideoUrl) {
  setVideoUrl(newVideoUrl);
  console.log('✅ Видео загружено:', newVideoUrl);
  
  // 🔥 FIX: Перезагружаем список уроков
  if (onVideoUploaded) {
    console.log('🔄 Перезагружаем список уроков...');
    onVideoUploaded();
  }
}
```

**3. Передача callback в Module.tsx (строка 1165-1168):**
```typescript
<LessonEditDialog
  // ... другие props
  onVideoUploaded={async () => {
    console.log('🔄 Видео загружено, перезагружаем уроки...');
    await loadLessonsFromAPI();
  }}
/>
```

### Теперь:
1. ✅ Видео загружается
2. ✅ Вызывается `onVideoUploaded()`
3. ✅ `loadLessonsFromAPI()` перезагружает уроки
4. ✅ Страница автоматически обновляется
5. ✅ Новая длительность отображается

---

## 🧪 **ТЕСТИРОВАНИЕ**

### Тест 1: Короткое видео
1. Загрузить видео < 1 минуты (например, 11 секунд)
2. **Ожидается:** "1 минута" (не "0 минут")

### Тест 2: Auto-reload
1. Загрузить любое видео
2. Дождаться завершения загрузки
3. **Ожидается:** Страница модуля автоматически обновится
4. **Не должно быть:** Необходимости нажимать F5

### Тест 3: Длинное видео
1. Загрузить видео > 1 минуты (например, 1800 секунд = 30 минут)
2. **Ожидается:** "30 минут"

---

## 📝 **ИЗМЕНЁННЫЕ ФАЙЛЫ**

| Файл | Изменение | Строки |
|------|-----------|---------|
| `backend/src/routes/lessons.ts` | Math.round → Math.ceil | 71 |
| `src/pages/Module.tsx` | Math.round → Math.ceil | 331 |
| `src/components/admin/LessonEditDialog.tsx` | Добавлен prop onVideoUploaded | 23, 26 |
| `src/components/admin/LessonEditDialog.tsx` | Вызов onVideoUploaded после загрузки | 397-401 |
| `src/pages/Module.tsx` | Передача callback onVideoUploaded | 1165-1168 |

---

## ✅ **РЕЗУЛЬТАТ**

### До fix'а:
```
📹 Видео 11 секунд → 0 минут ❌
Страница не обновляется ❌
```

### После fix'а:
```
📹 Видео 11 секунд → 1 минута ✅
Страница обновляется автоматически ✅
```

---

## 🚀 **ДЕПЛОЙ**

1. ✅ Backend пересобран (npm run build)
2. ✅ Backend перезапущен
3. ✅ Frontend перезапущен
4. ⏳ Тестирование пользователем
5. ⏳ Git push после подтверждения

---

**Теперь всё должно работать правильно! 🎯**

