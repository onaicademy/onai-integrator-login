# 🔍 ДИАГНОСТИКА: Проблема с endpoint GET /api/modules/:id

**Дата:** 2025-11-19  
**Время:** ~08:30  
**Статус:** 🔍 ДИАГНОСТИКА ЗАВЕРШЕНА

---

## 📋 ПРОБЛЕМА

Backend возвращает `{modules: []}` вместо `{module: {...}}` при запросе `GET /api/modules/2`.

---

## 🔍 РЕЗУЛЬТАТЫ ДИАГНОСТИКИ

### 1. ✅ Исходный код (backend/src/routes/modules.ts)

**Endpoint `GET /api/modules/:id` (строки 19-60):**
```typescript
// GET /api/modules/:id - получить один модуль по ID
router.get('/:id', async (req: Request, res: Response) => {
  try {
    const { id } = req.params;
    const moduleId = parseInt(id);

    if (isNaN(moduleId)) {
      return res.status(400).json({ error: 'Invalid module ID' });
    }

    console.log('📌 Получение модуля ID:', moduleId);

    const { data, error } = await supabase
      .from('modules')
      .select(`
        *,
        lessons:lessons!lessons_module_id_fkey(*)
      `)
      .eq('id', moduleId)
      .eq('is_archived', false)
      .eq('lessons.is_archived', false)
      .order('lessons.order_index', { foreignTable: 'lessons', ascending: true })
      .single();

    if (error) {
      console.error('❌ Ошибка получения модуля:', error);
      return res.status(404).json({ error: 'Module not found', details: error.message });
    }

    if (!data) {
      return res.status(404).json({ error: 'Module not found or archived' });
    }

    console.log('✅ Модуль найден:', data.title);
    console.log('📊 Уроков:', data.lessons?.length || 0);

    res.json({ module: data }); // ✅ ПРАВИЛЬНО - возвращает {module: {...}}
  } catch (error: any) {
    console.error('❌ Ошибка в GET /api/modules/:id:', error);
    res.status(500).json({ error: error.message || 'Внутренняя ошибка сервера' });
  }
});
```

**Вывод:** ✅ Код правильный — возвращает `{module: data}`

---

### 2. ✅ Скомпилированный код (backend/dist/routes/modules.js)

**Endpoint `GET /api/modules/:id` (строки 18-50):**
```javascript
// GET /api/modules/:id - получить один модуль по ID
router.get('/:id', async (req, res) => {
    try {
        const { id } = req.params;
        const moduleId = parseInt(id);
        if (isNaN(moduleId)) {
            return res.status(400).json({ error: 'Invalid module ID' });
        }
        console.log('📌 Получение модуля ID:', moduleId);
        const { data, error } = await supabase_1.supabase
            .from('modules')
            .select(`
        *,
        lessons:lessons!lessons_module_id_fkey(*)
      `)
            .eq('id', moduleId)
            .eq('is_archived', false)
            .eq('lessons.is_archived', false)
            .order('lessons.order_index', { foreignTable: 'lessons', ascending: true })
            .single();
        if (error) {
            console.error('❌ Ошибка получения модуля:', error);
            return res.status(404).json({ error: 'Module not found', details: error.message });
        }
        if (!data) {
            return res.status(404).json({ error: 'Module not found or archived' });
        }
        console.log('✅ Модуль найден:', data.title);
        console.log('📊 Уроков:', data.lessons?.length || 0);
        res.json({ module: data }); // ✅ ПРАВИЛЬНО - строка 47
    }
    catch (error) {
        console.error('❌ Ошибка в GET /api/modules/:id:', error);
        res.status(500).json({ error: error.message || 'Внутренняя ошибка сервера' });
    }
});
```

**Вывод:** ✅ Скомпилированный код правильный — возвращает `{module: data}` на строке 47

---

### 3. ✅ Все endpoints в modules.ts

**Найдено 6 endpoints:**
1. `router.get('/:id', ...)` — получить один модуль по ID → возвращает `{module: {...}}`
2. `router.get('/course/:courseId', ...)` — получить все модули курса → возвращает `{modules: [...]}`
3. `router.post('/', ...)` — создать модуль
4. `router.put('/:id', ...)` — обновить модуль
5. `router.put('/reorder', ...)` — изменить порядок модулей
6. `router.delete('/:id', ...)` — удалить модуль

**Вывод:** ✅ Endpoint `GET /:id` существует и правильный

---

### 4. ✅ Подключение роутера в server.ts

**Строка 188:**
```typescript
app.use('/api/modules', modulesRouter);
```

**Полный путь endpoint'а:**
- `GET /api/modules/:id` → `router.get('/:id', ...)` в modules.ts
- Должен обрабатывать запросы типа `GET /api/modules/2`

**Вывод:** ✅ Роутер подключен правильно

---

### 5. ✅ Тестовый запрос

**Команда:**
```bash
curl http://localhost:3000/api/modules/2
```

**Результат:**
```json
{
    "modules": []
}
```

**Вывод:** ❌ **ПРОБЛЕМА ПОДТВЕРЖДЕНА** — возвращается `{modules: []}` вместо `{module: {...}}`

---

### 6. ✅ Статус backend процесса

**Проверка процессов Node.js:**
```
PID 4224 - node (порт 3000)
```

**Вывод:** ✅ Backend запущен на порту 3000

---

### 7. ✅ Диагностическое логирование

**Добавлено в начало `backend/src/routes/modules.ts` (строки 6-17):**
```typescript
// ДИАГНОСТИКА: логировать ВСЕ запросы к /api/modules
router.use('*', (req, res, next) => {
  console.log('🔍 ДИАГНОСТИКА МОДУЛЕЙ:');
  console.log('  Method:', req.method);
  console.log('  URL:', req.url);
  console.log('  Full path:', req.path);
  console.log('  Original URL:', req.originalUrl);
  console.log('  Params:', JSON.stringify(req.params));
  console.log('  Query:', JSON.stringify(req.query));
  console.log('  Route:', req.route?.path || 'N/A');
  next();
});
```

**Вывод:** ✅ Диагностика добавлена, но **backend нужно перезапустить** для применения изменений

---

## 🔴 ВЫЯВЛЕННАЯ ПРОБЛЕМА

### Гипотеза #1: Backend не перезапущен после сборки

**Причина:**
- Код в `dist/` правильный (возвращает `{module: data}`)
- Но запущенный процесс использует старый код из памяти
- После изменений в исходниках нужно:
  1. Пересобрать: `npm run build`
  2. Перезапустить backend процесс

**Решение:**
1. Остановить текущий процесс backend (Ctrl+C или `kill PID`)
2. Пересобрать: `cd backend && npm run build`
3. Запустить заново: `npm start`
4. Проверить: `curl http://localhost:3000/api/modules/2`

---

### Гипотеза #2: Запрос попадает в другой endpoint

**Причина:**
- Express может обрабатывать запросы в порядке регистрации роутов
- Если есть конфликт роутов, запрос может попасть в `GET /course/:courseId` вместо `GET /:id`
- Endpoint `/course/:courseId` возвращает `{modules: [...]}`

**Проверка:**
- Нужно посмотреть логи backend при запросе `GET /api/modules/2`
- Диагностическое логирование покажет, какой endpoint вызывается

**Решение:**
- Убедиться, что `GET /:id` регистрируется ПЕРЕД `GET /course/:courseId`
- Или изменить порядок роутов в `modules.ts`

---

### Гипотеза #3: Проблема с Supabase запросом

**Причина:**
- Модуль с ID=2 может быть архивирован (`is_archived: true`)
- Или модуль не существует в БД
- Supabase возвращает пустой результат, но код обрабатывает это неправильно

**Проверка:**
- Нужно проверить логи backend — должны быть строки:
  - `📌 Получение модуля ID: 2`
  - `❌ Ошибка получения модуля:` или `✅ Модуль найден:`

**Решение:**
- Проверить в Supabase Dashboard, существует ли модуль с ID=2
- Проверить, не архивирован ли он (`is_archived: false`)

---

## 📊 СТАТИСТИКА

### Файлы проверены:
1. ✅ `backend/src/routes/modules.ts` — исходный код
2. ✅ `backend/dist/routes/modules.js` — скомпилированный код
3. ✅ `backend/src/server.ts` — подключение роутера
4. ✅ Тестовый запрос выполнен

### Endpoints найдены:
- 6 endpoints в `modules.ts`
- 2 GET endpoints (один модуль, все модули курса)

### Проблемы выявлены:
- ❌ Backend возвращает `{modules: []}` вместо `{module: {...}}`
- ⚠️ Backend может использовать старый код (не перезапущен)

---

## 🔧 РЕКОМЕНДАЦИИ

### Немедленные действия:

1. **Перезапустить backend:**
   ```bash
   # Остановить текущий процесс
   # Ctrl+C в терминале, где запущен backend
   # Или: kill 4224 (PID процесса)
   
   # Пересобрать
   cd backend
   npm run build
   
   # Запустить заново
   npm start
   ```

2. **Проверить логи backend:**
   - После перезапуска выполнить: `curl http://localhost:3000/api/modules/2`
   - Проверить логи в терминале backend:
     - Должны быть строки: `🔍 ДИАГНОСТИКА МОДУЛЕЙ:`
     - Должна быть строка: `📌 Получение модуля ID: 2`
     - Должна быть строка: `✅ Модуль найден:` или `❌ Ошибка получения модуля:`

3. **Проверить в Supabase:**
   - Открыть Supabase Dashboard
   - Проверить таблицу `modules`
   - Убедиться, что модуль с ID=2 существует и `is_archived = false`

4. **Если проблема останется:**
   - Проверить порядок роутов в `modules.ts`
   - Убедиться, что `GET /:id` регистрируется ПЕРЕД `GET /course/:courseId`
   - Добавить более детальное логирование в endpoint

---

## 📝 СЛЕДУЮЩИЕ ШАГИ

1. ⏳ Перезапустить backend и проверить логи
2. ⏳ Проверить данные в Supabase
3. ⏳ Если проблема останется — проверить порядок роутов
4. ⏳ Создать финальный отчёт с решением

---

## 🔗 СВЯЗАННЫЕ ФАЙЛЫ

- `backend/src/routes/modules.ts` — исходный код endpoints
- `backend/dist/routes/modules.js` — скомпилированный код
- `backend/src/server.ts` — подключение роутера
- `docs/reports/2025-11-19-r2-removal-and-modules-endpoint-fix.md` — предыдущий отчёт

---

**Отчёт создан:** 2025-11-19  
**Автор диагностики:** Cursor AI Assistant  
**Статус:** 🔍 Диагностика завершена, требуется перезапуск backend для проверки


