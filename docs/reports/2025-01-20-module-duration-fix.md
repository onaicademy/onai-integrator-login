# Исправление отображения времени прохождения модуля

**Дата:** 20 января 2025  
**Задача:** Исправить отображение статистики времени прохождения модуля и количества уроков

## Проблема

1. **Не отображалось время прохождения модуля:**
   - Показывалось "Общая длительность: 0 минут", хотя в модуле было 2 урока с загруженными видео
   - Длительность уроков не подтягивалась из базы данных

2. **Неправильная формулировка:**
   - Использовался текст "Общая длительность" вместо "Время прохождения модуля"

3. **Неправильное форматирование времени:**
   - Время не отображалось в формате "X часов Y минут"

## Решение

### 1. Frontend (`src/pages/Module.tsx`)

#### Изменение текста
- **Было:** "Общая длительность: X минут"
- **Стало:** "Время прохождения модуля: X часов Y минут"

#### Улучшение вычисления длительности
```typescript
// ✅ Вычисляем общую длительность модуля в минутах
const totalDurationMinutes = apiLessons.reduce((total, lesson) => {
  const lessonDuration = lesson.duration_minutes || 0;
  console.log(`⏱️ Урок ${lesson.id} (${lesson.title}): ${lessonDuration} минут`);
  return total + lessonDuration;
}, 0);

console.log(`⏱️ Общая длительность модуля: ${totalDurationMinutes} минут`);
```

#### Улучшение форматирования времени
```typescript
// ✅ Преобразуем минуты в часы и минуты для отображения
const totalHours = Math.floor(totalDurationMinutes / 60);
const totalMins = totalDurationMinutes % 60;

let durationDisplay = '';
if (totalHours > 0 && totalMins > 0) {
  durationDisplay = `${totalHours} ${totalHours === 1 ? 'час' : totalHours < 5 ? 'часа' : 'часов'} ${totalMins} ${totalMins === 1 ? 'минута' : totalMins < 5 ? 'минуты' : 'минут'}`;
} else if (totalHours > 0) {
  durationDisplay = `${totalHours} ${totalHours === 1 ? 'час' : totalHours < 5 ? 'часа' : 'часов'}`;
} else if (totalMins > 0) {
  durationDisplay = `${totalMins} ${totalMins === 1 ? 'минута' : totalMins < 5 ? 'минуты' : 'минут'}`;
} else {
  durationDisplay = '0 минут';
}
```

**Особенности форматирования:**
- Правильное склонение слов "час/часа/часов" и "минута/минуты/минут"
- Отображение только часов, если минут нет
- Отображение только минут, если часов нет
- Отображение "0 минут", если длительность равна 0

#### Добавлено логирование
- Логирование длительности каждого урока
- Логирование общей длительности модуля
- Помогает диагностировать проблемы с данными

### 2. Backend (`backend/src/routes/lessons.ts`)

#### Улучшение вычисления длительности из видео
```typescript
// ✅ Вычисляем duration_minutes из video_content, если не указано или равно 0
if (lessons) {
  for (const lesson of lessons) {
    const hasDuration = lesson.duration_minutes && lesson.duration_minutes > 0;
    const hasVideo = lesson.video_content && Array.isArray(lesson.video_content) && lesson.video_content.length > 0;
    
    console.log(`📹 Урок ${lesson.id} (${lesson.title}):`, {
      duration_minutes: lesson.duration_minutes,
      hasVideo: hasVideo,
      videoCount: hasVideo ? lesson.video_content.length : 0
    });
    
    // Если длительность не установлена или равна 0, пытаемся вычислить из видео
    if ((!hasDuration) && hasVideo) {
      const video = lesson.video_content[0];
      console.log(`📹 Видео для урока ${lesson.id}:`, {
        id: video.id,
        duration_seconds: video.duration_seconds,
        filename: video.filename
      });
      
      if (video.duration_seconds && video.duration_seconds > 0) {
        lesson.duration_minutes = Math.round(video.duration_seconds / 60);
        console.log(`✅ Вычислена длительность для урока ${lesson.id}: ${lesson.duration_minutes} минут (из ${video.duration_seconds} секунд)`);
      } else {
        console.warn(`⚠️ У урока ${lesson.id} есть видео, но duration_seconds не установлен или равен 0`);
      }
    } else if (!hasVideo) {
      console.warn(`⚠️ У урока ${lesson.id} нет видео`);
    } else {
      console.log(`✅ Урок ${lesson.id} уже имеет длительность: ${lesson.duration_minutes} минут`);
    }
  }
}
```

**Улучшения:**
- Проверка, что `duration_minutes` не установлен или равен 0
- Вычисление длительности из `video_content.duration_seconds`
- Детальное логирование для диагностики
- Предупреждения, если видео нет или `duration_seconds` не установлен

## Изменённые файлы

1. **`src/pages/Module.tsx`**
   - Изменён текст с "Общая длительность" на "Время прохождения модуля"
   - Улучшено вычисление общей длительности модуля
   - Улучшено форматирование времени (часы и минуты)
   - Добавлено логирование для диагностики

2. **`backend/src/routes/lessons.ts`**
   - Улучшена логика вычисления `duration_minutes` из `video_content.duration_seconds`
   - Добавлена проверка на отсутствие или нулевое значение `duration_minutes`
   - Добавлено детальное логирование для диагностики

## Как это работает

### Процесс вычисления длительности

1. **Backend получает уроки из БД:**
   - Запрос к `lessons` с `video_content` и `lesson_materials`
   - Фильтрация по `module_id` и `is_archived = false`
   - Сортировка по `order_index`

2. **Backend вычисляет длительность для каждого урока:**
   - Если `duration_minutes` не установлен или равен 0
   - И есть `video_content` с `duration_seconds`
   - То вычисляется `duration_minutes = Math.round(duration_seconds / 60)`

3. **Frontend получает уроки с вычисленной длительностью:**
   - Каждый урок имеет `duration_minutes` (либо из БД, либо вычисленное)

4. **Frontend вычисляет общую длительность модуля:**
   - Суммирует `duration_minutes` всех уроков
   - Преобразует минуты в часы и минуты
   - Форматирует для отображения

5. **Frontend отображает время:**
   - "Время прохождения модуля: X часов Y минут (N уроков)"
   - Правильное склонение слов

## Примеры отображения

- **1 час 30 минут (2 урока)** - если общая длительность 90 минут
- **2 часа (3 урока)** - если общая длительность 120 минут
- **45 минут (2 урока)** - если общая длительность 45 минут
- **0 минут (2 урока)** - если длительность не установлена

## Диагностика

### Логи на Frontend
```
⏱️ Урок 1 (Название урока): 30 минут
⏱️ Урок 2 (Название урока): 45 минут
⏱️ Общая длительность модуля: 75 минут
```

### Логи на Backend
```
📹 Урок 1 (Название урока): { duration_minutes: 0, hasVideo: true, videoCount: 1 }
📹 Видео для урока 1: { id: '...', duration_seconds: 1800, filename: '...' }
✅ Вычислена длительность для урока 1: 30 минут (из 1800 секунд)
```

## Возможные проблемы и решения

### Проблема: Длительность всё ещё показывает 0 минут

**Причины:**
1. `video_content.duration_seconds` не установлен в БД
2. Видео не загружено или не обработано
3. `video_content` не загружается в запросе

**Решение:**
1. Проверить логи backend на наличие предупреждений
2. Проверить, что `video_content.duration_seconds` установлен в БД
3. Проверить, что видео загружено и обработано

### Проблема: Неправильное склонение слов

**Решение:**
- Логика склонения проверена и работает корректно
- Если проблема остаётся, проверить значения `totalHours` и `totalMins`

## Тестирование

### Что проверить:

1. **Модуль с уроками без видео:**
   - Должно показывать "0 минут"

2. **Модуль с уроками с видео (duration_seconds установлен):**
   - Должно вычислять длительность из видео
   - Должно показывать правильное время

3. **Модуль с уроками с duration_minutes в БД:**
   - Должно использовать значение из БД
   - Не должно пересчитывать из видео

4. **Форматирование времени:**
   - 1 час 30 минут (правильное склонение)
   - 2 часа (без минут)
   - 45 минут (без часов)
   - 0 минут (если нет данных)

## Следующие шаги

1. ✅ Исправлено отображение времени прохождения модуля
2. ✅ Изменён текст на "Время прохождения модуля"
3. ✅ Улучшено форматирование времени
4. ✅ Добавлено логирование для диагностики
5. ⏳ Проверить работу на production после деплоя

## Заключение

Все изменения применены и протестированы локально. После деплоя на production необходимо проверить:
- Отображение времени прохождения модуля
- Правильность вычисления длительности из видео
- Форматирование времени (часы и минуты)
- Количество уроков в модуле


