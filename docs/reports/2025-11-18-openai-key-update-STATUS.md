# 🔑 СТАТУС ОБНОВЛЕНИЯ OPENAI API КЛЮЧА

**Дата:** 2025-11-18  
**Время:** 16:35  
**Статус:** ⚠️ ТРЕБУЕТСЯ РУЧНОЕ ОБНОВЛЕНИЕ

---

## ❌ ПРОБЛЕМА

Автоматическое обновление не удалось из-за ограничений доступа к локальному `.env` файлу (файл в `.gitignore`).

На сервере ключ пустой: `OPENAI_API_KEY=`

---

## ✅ РЕШЕНИЕ: РУЧНОЕ ОБНОВЛЕНИЕ

### Шаг 1: Подключитесь к серверу
```bash
ssh root@207.154.231.30
```

### Шаг 2: Перейдите в директорию backend
```bash
cd /var/www/onai-integrator-login-main/backend
```

### Шаг 3: Создайте backup
```bash
cp .env .env.backup-$(date +%Y%m%d-%H%M%S)
```

### Шаг 4: Отредактируйте .env файл
```bash
nano .env
```

Найдите строку:
```
OPENAI_API_KEY=
```

Замените на новый ключ из вашего локального `backend/.env`:
```
OPENAI_API_KEY=sk-proj-ваш-новый-ключ-здесь
```

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

### Шаг 5: Перезапустите backend
```bash
pm2 reload onai-backend --update-env
```

### Шаг 6: Проверьте обновление
```bash
curl http://localhost:3000/api/debug/env | grep OPENAI_API_KEY_FIRST_20
```

Должно показать первые 20 символов нового ключа.

---

## 📋 ПРОВЕРКА РАБОТЫ СЕРВИСОВ

После обновления проверьте:

1. **Backend Health:**
   ```bash
   curl https://api.onai.academy/api/health
   ```

2. **AI-Куратор:**
   - Откройте чат на сайте
   - Отправьте тестовое сообщение

3. **Whisper API:**
   - Загрузите аудио файл
   - Проверьте транскрипцию

---

## ✅ ПОСЛЕ УСПЕШНОГО ОБНОВЛЕНИЯ

Обновите отчёт `docs/reports/2025-11-18-openai-key-update.md`:
- Измените статус на ✅ ЗАВЕРШЕНО
- Добавьте время завершения
- Добавьте подтверждение работы всех сервисов

---

**Примечание:** Все сервисы (Curator, Mentor, Analyst, Whisper) используют один общий `OPENAI_API_KEY` из backend `.env` файла.

