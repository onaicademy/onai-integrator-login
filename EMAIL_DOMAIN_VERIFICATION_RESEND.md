# 📧 ПРОБЛЕМА EMAIL: ДОМЕН НЕ ВЕРИФИЦИРОВАН

**Дата**: 19 декабря 2025  
**Статус**: ⚠️ EMAILS НЕ ДОХОДЯТ

---

## 🔍 ДИАГНОСТИКА

### ✅ ЧТО РАБОТАЕТ:
1. ✅ RESEND_API_KEY загружается (`re_8YogT2EB_7JvsyYANkbHkUk2jZ9UfvHFk`)
2. ✅ Backend отправляет emails через Resend API
3. ✅ БД показывает `welcome_email_sent: true` (18 из 20 юзеров)
4. ✅ Тестовый email отправился успешно (ID: `8e89143f-13bc-4597-9155-6d355dbd6872`)

### ❌ ЧТО НЕ РАБОТАЕТ:
1. ❌ **EMAILS НЕ ДОХОДЯТ до получателей!**
2. ❌ Проверили inbox - НИЧЕГО
3. ❌ Проверили spam - НИЧЕГО

---

## 🎯 ПРИЧИНА

**ДОМЕН `onai.academy` НЕ ВЕРИФИЦИРОВАН в Resend!**

Resend принимает запросы на отправку (поэтому API возвращает success), но **НЕ ОТПРАВЛЯЕТ** письма, потому что домен не подтвержден.

---

## ✅ РЕШЕНИЕ: ВЕРИФИЦИРОВАТЬ ДОМЕН В RESEND

### 1. Залогинься в Resend Dashboard

```
https://resend.com/login
```

Используй аккаунт с API ключом: `re_8YogT2EB_7JvsyYANkbHkUk2jZ9UfvHFk`

---

### 2. Добавь домен `onai.academy`

1. Перейди в **Domains** → **Add Domain**
2. Введи: `onai.academy`
3. Нажми **Add Domain**

---

### 3. Получи DNS записи для верификации

Resend покажет тебе **3 DNS записи**, которые нужно добавить:

#### **Запись 1: SPF (TXT)**
```
Type: TXT
Name: onai.academy (или @)
Value: v=spf1 include:amazonses.com ~all
```

#### **Запись 2: DKIM (TXT)**
```
Type: TXT
Name: resend._domainkey.onai.academy
Value: <длинная строка, которую покажет Resend>
```

#### **Запись 3: DMARC (TXT)** (опционально, но рекомендуется)
```
Type: TXT
Name: _dmarc.onai.academy
Value: v=DMARC1; p=none; rua=mailto:dmarc@onai.academy
```

---

### 4. Добавь DNS записи в Cloudflare (или твой DNS провайдер)

Предполагаю, что `onai.academy` использует Cloudflare DNS.

#### Шаг 1: Залогинься в Cloudflare
```
https://dash.cloudflare.com/
```

#### Шаг 2: Выбери домен `onai.academy`

#### Шаг 3: Перейди в **DNS** → **Records**

#### Шаг 4: Добавь 3 TXT записи

**SPF запись:**
```
Type: TXT
Name: @ (или onai.academy)
Content: v=spf1 include:amazonses.com ~all
TTL: Auto
Proxy status: DNS only (серая тучка, НЕ оранжевая!)
```

**DKIM запись:**
```
Type: TXT
Name: resend._domainkey
Content: <скопируй из Resend Dashboard>
TTL: Auto
Proxy status: DNS only
```

**DMARC запись:**
```
Type: TXT
Name: _dmarc
Content: v=DMARC1; p=none; rua=mailto:dmarc@onai.academy
TTL: Auto
Proxy status: DNS only
```

---

### 5. Дождись верификации (5-15 минут)

После добавления DNS записей:

1. Вернись в **Resend Dashboard** → **Domains**
2. Нажми **Verify Domain** рядом с `onai.academy`
3. Resend проверит DNS записи
4. Статус изменится на **Verified** ✅

---

## 🧪 ТЕСТ ПОСЛЕ ВЕРИФИКАЦИИ

### 1. Отправь тестовый email:

```bash
cd /Users/miso/onai-integrator-login/backend
npx tsx test-email.js
```

### 2. Проверь inbox: `smmmcwin@gmail.com`

Должен прийти email от **onAI Academy <noreply@onai.academy>** ✅

---

### 3. Создай нового Tripwire студента

Зайди в админку Sales Manager:
```
https://onai.academy/admin/tripwire/users
```

Создай тестового пользователя с email: `smmmcwin@gmail.com`

**ДОЛЖЕН ПРИЙТИ Welcome Email!** 🎉

---

## 📊 ПРОВЕРКА ОТПРАВЛЕННЫХ EMAIL В RESEND

### Посмотри логи отправок:

1. Зайди в **Resend Dashboard** → **Emails**
2. Увидишь список всех отправленных писем
3. Проверь статус каждого:
   - ✅ **Delivered** - доставлено
   - ⏳ **Pending** - в процессе
   - ❌ **Bounced** - отклонено
   - ⚠️ **Blocked** - заблокировано (домен не верифицирован!)

---

## 🔥 ВАЖНО: ПОСЛЕ ВЕРИФИКАЦИИ

### 1. ВСЕ НОВЫЕ EMAILS БУДУТ РАБОТАТЬ ✅

С момента верификации домена все новые пользователи будут получать Welcome Email.

---

### 2. ОТПРАВЬ ПОВТОРНО СТАРЫМ ПОЛЬЗОВАТЕЛЯМ (опционально)

Если хочешь отправить Welcome Email тем 18 юзерам, которым не дошло:

```sql
-- Сбрось флаг welcome_email_sent для всех
UPDATE tripwire_users
SET welcome_email_sent = false,
    welcome_email_sent_at = NULL
WHERE welcome_email_sent = true
  AND created_at > '2025-12-17'::date;
```

Потом создай endpoint для ре-отправки:

```typescript
// backend/src/routes/tripwire-manager.ts
router.post('/resend-welcome-emails', authenticateJWT, requireAdmin, async (req, res) => {
  const { data: users } = await tripwireAdminSupabase
    .from('tripwire_users')
    .select('email, full_name, user_id')
    .eq('welcome_email_sent', false);

  let sent = 0;
  for (const user of users) {
    const success = await sendWelcomeEmail({
      toEmail: user.email,
      name: user.full_name,
      password: '***HIDDEN***' // Пароль уже хэширован в БД, не можем отправить
    });
    
    if (success) sent++;
  }

  res.json({ success: true, sent });
});
```

---

## 🎯 ФИНАЛЬНЫЙ ЧЕКЛИСТ

- [ ] 1. Залогинился в Resend Dashboard
- [ ] 2. Добавил домен `onai.academy`
- [ ] 3. Получил DNS записи (SPF, DKIM, DMARC)
- [ ] 4. Добавил DNS записи в Cloudflare
- [ ] 5. Дождался верификации (5-15 минут)
- [ ] 6. Статус домена: **Verified** ✅
- [ ] 7. Протестировал: `npx tsx test-email.js`
- [ ] 8. Email пришел на `smmmcwin@gmail.com` ✅
- [ ] 9. Создал нового Tripwire студента
- [ ] 10. Welcome Email пришел ✅

---

## 📧 АЛЬТЕРНАТИВА: ИСПОЛЬЗОВАТЬ ДРУГОЙ ДОМЕН (ВРЕМЕННО)

Если не можешь верифицировать `onai.academy` прямо сейчас, можешь временно использовать Resend тестовый домен:

```typescript
// backend/src/services/emailService.ts
from: 'onAI Academy <onboarding@resend.dev>' // Вместо noreply@onai.academy
```

Resend домен `resend.dev` уже верифицирован, письма будут доходить сразу! ✅

**НО**: Письма будут от `@resend.dev`, что выглядит не профессионально.

---

## 🔥 ИТОГО

**ПРОБЛЕМА**: Домен `onai.academy` не верифицирован в Resend  
**РЕШЕНИЕ**: Добавить DNS записи (SPF, DKIM, DMARC)  
**ВРЕМЯ**: 5-15 минут после добавления DNS  
**РЕЗУЛЬТАТ**: ✅ Все emails будут доходить!

---

**СДЕЛАЙ ПРЯМО СЕЙЧАС!** 🚀

1. Resend Dashboard: https://resend.com/domains
2. Добавь `onai.academy`
3. Скопируй DNS записи
4. Cloudflare DNS: https://dash.cloudflare.com/
5. Добавь TXT записи
6. Verify Domain
7. Тестируй!

---

**Дата**: 19 декабря 2025, 08:30 AM  
**Статус**: ⏳ WAITING FOR DOMAIN VERIFICATION

