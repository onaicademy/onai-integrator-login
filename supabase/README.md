# Supabase Database Setup

## 📋 Схема базы данных

Этот проект использует PostgreSQL через Supabase для хранения данных о пользователях, курсах, уроках и достижениях.

## 🗄️ Структура таблиц

### Таблицы:
1. **users** - пользователи платформы
2. **courses** - курсы обучения
3. **modules** - модули внутри курсов
4. **lessons** - уроки внутри модулей
5. **progress** - прогресс пользователей
6. **achievements** - достижения/награды
7. **user_achievements** - связь пользователей и достижений

## 🔐 Row Level Security (RLS)

Все таблицы защищены RLS политиками:
- **users** - пользователи могут видеть и редактировать только свои данные
- **progress** - пользователи управляют только своим прогрессом
- **courses, modules, lessons** - все могут читать, но не редактировать
- **achievements** - все могут читать
- **user_achievements** - пользователи видят только свои достижения

## 🚀 Применение миграции

### Вариант 1: Через Supabase Dashboard
1. Откройте https://supabase.com/dashboard
2. Выберите проект "onai-academy"
3. Перейдите в SQL Editor
4. Скопируйте содержимое `schema.sql`
5. Выполните SQL

### Вариант 2: Через Supabase CLI (требует авторизации)
```bash
npm run db:push
```

### Вариант 3: Через psql напрямую
```bash
psql -h db.onai-academy.supabase.co -U postgres -d postgres -f schema.sql
```

## 🔑 Необходимые переменные окружения

В файле `.env` должны быть настроены:
```
VITE_SUPABASE_URL=https://onai-academy.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=ваш_anon_key
```

## 📝 Примечания

- SQL файл идempotent (можно запускать многократно)
- Все таблицы создаются с помощью `create table if not exists`
- RLS политики используют `auth.uid()` для проверки владельца

