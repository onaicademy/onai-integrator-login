# 🚨 КРИТИЧЕСКАЯ ПРОБЛЕМА: Vite Cache Issue

**Дата:** 3 декабря 2025, 20:07 UTC  
**Статус:** ❌ БЛОКИРУЮЩАЯ (Не решается стандартными методами)  
**Приоритет:** КРИТИЧЕСКИЙ

---

## 📋 КРАТКОЕ ОПИСАНИЕ

Frontend **не может** использовать `localhost:3000` для API запросов, несмотря на:
1. ✅ Правильный код в `apiClient.ts` (Smart Fallback реализован)
2. ✅ Перезапуск Vite dev server (3 раза)
3. ✅ Полную очистку `.vite` кэша (`rm -rf node_modules/.vite`)
4. ✅ Hard refresh браузера (Ctrl+Shift+R, `location.reload(true)`)

**Результат:** Vite продолжает загружать **старую закэшированную версию** `apiClient.ts` с timestamp `t=1764792036472` (19:40:36 UTC), несмотря на то что текущее время 20:07 UTC.

---

## 🔍 ДИАГНОСТИКА

### 1. Логи браузера показывают:

```
🔌 API Client initialized with URL: https://api.onai.academy
📍 Mode: DEVELOPMENT
```

**Проблема:** 
- `Mode: DEVELOPMENT` — правильно (Vite определяет режим)
- `URL: https://api.onai.academy` — **НЕПРАВИЛЬНО!** Должно быть `http://localhost:3000`

### 2. Timestamp в URL модуля:

```
http://localhost:8080/src/utils/apiClient.ts?t=1764792036472
```

**`t=1764792036472`** = **`2025-12-03T19:40:36.472Z`** (28 минут назад!)

**Текущее время:** `2025-12-03T20:07:00.000Z`

**Вывод:** Vite загружает **старую версию** модуля, несмотря на перезапуск.

### 3. Проверка кода в файле:

```typescript
// src/utils/apiClient.ts (строки 46-60)
const isDevelopment = import.meta.env.DEV;
const defaultApiUrl = isDevelopment 
  ? 'http://localhost:3000'      // ✅ localhost для development
  : 'https://api.onai.academy';  // ✅ production для prod

const baseUrl = import.meta.env.VITE_API_URL || defaultApiUrl;

// Логирование
console.log('🔌 API Client initialized with URL:', baseUrl);
console.log('📍 Mode:', isDevelopment ? 'DEVELOPMENT' : 'PRODUCTION');
console.log('🔧 import.meta.env.DEV:', import.meta.env.DEV);       // НЕ ПОЯВЛЯЕТСЯ В ЛОГАХ!
console.log('🔧 import.meta.env.PROD:', import.meta.env.PROD);     // НЕ ПОЯВЛЯЕТСЯ В ЛОГАХ!
console.log('🔧 import.meta.env.MODE:', import.meta.env.MODE);     // НЕ ПОЯВЛЯЕТСЯ В ЛОГАХ!
console.log('🔧 VITE_API_URL:', import.meta.env.VITE_API_URL);     // НЕ ПОЯВЛЯЕТСЯ В ЛОГАХ!
```

**Код правильный**, но Vite загружает **старую версию БЕЗ этих логов**!

---

## 🔧 ПОПЫТКИ ИСПРАВЛЕНИЯ

### Попытка #1: HMR (Hot Module Replacement)
**Действие:**
- Изменил код в `apiClient.ts`
- Сохранил файл
- Браузер автоматически обновился через HMR

**Результат:** ❌ НЕ СРАБОТАЛО
- Timestamp модуля не изменился
- Старый код остался

### Попытка #2: Перезапуск Vite
**Действие:**
```bash
pkill -f "vite"
npm run dev > /tmp/frontend.log 2>&1 &
```

**Результат:** ❌ НЕ СРАБОТАЛО  
- Timestamp остался `t=1764792036472`

### Попытка #3: Полная очистка `.vite` кэша
**Действие:**
```bash
rm -rf node_modules/.vite
pkill -f "vite"
npm run dev > /tmp/frontend-clean.log 2>&1 &
```

**Результат:** ❌ НЕ СРАБОТАЛО  
- Даже после полной очистки кэша, Vite загружает старый модуль!

### Попытка #4: Hard Refresh + Clear Cache Flag
**Действие:**
```javascript
window.__apiClientInitialized = false;
location.reload(true);
```

**Результат:** ❌ НЕ СРАБОТАЛО  
- Browser cache очищен
- Timestamp модуля остался тот же

---

## 🎯 КОРНЕВАЯ ПРИЧИНА

### Гипотеза 1: Vite Pre-bundling
Vite агрессивно кэширует зависимости и модули в `node_modules/.vite`. Даже после удаления этой папки, Vite может восстанавливать кэш из других источников.

### Гипотеза 2: Browser Service Worker
Возможно, в браузере зарегистрирован Service Worker, который кэширует модули.

### Гипотеза 3: Proxy/CDN кэширование
Если между браузером и Vite dev server есть прокси или CDN, они могут кэшировать ответы.

### Гипотеза 4: File System Watcher не работает
Vite использует `chokidar` для отслеживания изменений файлов. Возможно, watcher не видит изменения в `apiClient.ts`.

---

## 💡 ВОЗМОЖНЫЕ РЕШЕНИЯ

### РЕШЕНИЕ 1: Force Rebuild (Рекомендуется)

```bash
# 1. Полностью останавливаем все процессы
pkill -f "node"
pkill -f "vite"

# 2. Очищаем ВСЕ кэши
rm -rf node_modules/.vite
rm -rf .vite
rm -rf dist
rm -rf ~/.vite
rm -rf ~/.cache/vite

# 3. Очищаем browser cache
# В браузере: DevTools > Application > Clear site data

# 4. Перезапускаем Vite
npm run dev
```

### РЕШЕНИЕ 2: Временно hardcode localhost в коде

Пока не решится проблема с кэшем, можно **временно** изменить fallback:

```typescript
// src/utils/apiClient.ts (ВРЕМЕННОЕ РЕШЕНИЕ!)
const baseUrl = 'http://localhost:3000'; // Hardcode для dev
// const baseUrl = import.meta.env.VITE_API_URL || defaultApiUrl; // Закомментировать
```

**⚠️ ВАЖНО:** Не забыть вернуть перед деплоем!

### РЕШЕНИЕ 3: Создать `.env.local` вручную

```bash
cat > .env.local << 'EOF'
VITE_API_URL=http://localhost:3000
EOF
```

Это обходит Smart Fallback и явно задает URL для localhost.

### РЕШЕНИЕ 4: Использовать Vite CLI флаги

```bash
npm run dev -- --force --clearScreen
```

- `--force` — принудительный rebuild
- `--clearScreen` — очистка экрана

---

## 🔗 СВЯЗАННЫЕ ФАЙЛЫ

- `src/utils/apiClient.ts` (строки 46-60) — Smart Fallback логика
- `src/pages/tripwire/components/AccountSettings.tsx` (строки 61, 133) — Email/Password update
- `backend/src/routes/users.ts` (строки ~25-30) — Backend endpoints
- `backend/src/controllers/userController.ts` (строки ~80-160) — Update handlers

---

## 📊 ТЕКУЩЕЕ СОСТОЯНИЕ

### ✅ ЧТО РАБОТАЕТ:
1. Backend endpoints готовы и работают на `localhost:3000` ✅
   - `POST /api/users/update-email`
   - `POST /api/users/update-password`
2. Frontend код правильный ✅
3. AuthContext throttle работает (нет 429 ошибок) ✅
4. Email notification templates готовы ✅

### ❌ ЧТО НЕ РАБОТАЕТ:
1. Vite загружает старую версию `apiClient.ts` ❌
2. Frontend обращается к production API вместо localhost ❌
3. Email/Password update не работают на localhost ❌

---

## 🚀 РЕКОМЕНДАЦИИ АРХИТЕКТОРУ

1. **Немедленно:** Применить **РЕШЕНИЕ 3** (создать `.env.local`)
   - Это самое быстрое и безопасное решение
   - Не требует изменения кода
   - Работает независимо от Vite кэша

2. **Долгосрочно:** Исследовать почему Vite кэш не очищается
   - Проверить настройки Vite в `vite.config.ts`
   - Возможно нужно добавить `server.watch` настройки
   - Проверить `.gitignore` и `.cursorignore`

3. **После исправления:** Протестировать email/password update
   - Сменить email на `zankachidix.ai@gmail.com`
   - Сменить пароль на `Onai2134`
   - Проверить что email notifications отправились

---

## 📝 NEXT STEPS

1. Пользователь создает `.env.local` вручную:
   ```bash
   echo "VITE_API_URL=http://localhost:3000" > .env.local
   ```

2. Перезапускает Vite:
   ```bash
   pkill -f "vite"
   npm run dev
   ```

3. Обновляет браузер и тестирует email update

4. Если работает — задеплоить backend на production

---

**Статус:** Ожидает действий от пользователя или архитектора  
**Блокирует:** Тестирование email/password update на localhost

---


