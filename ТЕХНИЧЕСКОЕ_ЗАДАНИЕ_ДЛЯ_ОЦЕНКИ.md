# 📋 ТЕХНИЧЕСКОЕ ЗАДАНИЕ: ОНЛАЙН-ПЛАТФОРМА ОБУЧЕНИЯ

**Версия:** 1.0  
**Дата:** 10 декабря 2025  
**Назначение:** Оценка стоимости разработки

---

## 📑 СОДЕРЖАНИЕ

1. [Общее описание проекта](#1-общее-описание-проекта)
2. [Архитектура системы](#2-архитектура-системы)
3. [Main Platform - Основная платформа](#3-main-platform---основная-платформа)
4. [Tripwire - Воронка продаж](#4-tripwire---воронка-продаж)
5. [Admin Panel - Панель управления](#5-admin-panel---панель-управления)
6. [База данных](#6-база-данных)
7. [API Endpoints](#7-api-endpoints)
8. [AI-системы](#8-ai-системы)
9. [Интеграции](#9-интеграции)
10. [Технический стек](#10-технический-стек)
11. [Требования к безопасности](#11-требования-к-безопасности)
12. [Требования к производительности](#12-требования-к-производительности)

---

## 1. ОБЩЕЕ ОПИСАНИЕ ПРОЕКТА

### 1.1. Назначение
Образовательная платформа для онлайн-обучения с видеокурсами, геймификацией, AI-наставником и системой продаж.

### 1.2. Ключевые особенности
- 2 отдельных продукта: **Main Platform** (полный курс) + **Tripwire** (триальная версия)
- 2 изолированные базы данных (для минимизации рисков)
- AI-агенты: Наставник, Куратор, Аналитик
- Интеграция с Telegram (уведомления, боты)
- Геймификация (XP, достижения, рейтинги, стрики)
- Видео с адаптивным стримингом (HLS)
- Система управления продажами (Sales Manager Dashboard)

### 1.3. Целевая аудитория
- **Студенты** - пользователи платформы
- **Sales менеджеры** - менеджеры по продажам
- **Кураторы** - преподаватели и наставники
- **Администраторы** - управление платформой

---

## 2. АРХИТЕКТУРА СИСТЕМЫ

### 2.1. Общая схема

```
┌─────────────────────────────────────────────────────────────┐
│                         FRONTEND                             │
│   Single Page Application (React + TypeScript)               │
│   - Main Platform Interface                                  │
│   - Tripwire Interface                                       │
│   - Admin Dashboard                                          │
└────────────────┬────────────────────────────────────────────┘
                 │ REST API (HTTPS)
                 ▼
┌─────────────────────────────────────────────────────────────┐
│                         BACKEND API                          │
│   Node.js + Express + TypeScript                             │
│   - Authentication & Authorization                           │
│   - Business Logic                                           │
│   - File Processing                                          │
│   - AI Integration                                           │
│   - Telegram Integration                                     │
└────────┬────────────────────────────┬────────────────────────┘
         │                            │
         ▼                            ▼
┌─────────────────────┐    ┌─────────────────────────┐
│   DATABASE #1        │    │   DATABASE #2            │
│   Main Platform      │    │   Tripwire               │
│   PostgreSQL         │    │   PostgreSQL             │
└──────────┬──────────┘    └──────────┬──────────────┘
           │                           │
           └───────────┬───────────────┘
                       ▼
         ┌────────────────────────────────┐
         │     EXTERNAL SERVICES           │
         │  - Video CDN (HLS Streaming)    │
         │  - File Storage                 │
         │  - AI API (GPT-4)               │
         │  - Email Service                │
         │  - Telegram Bot API             │
         │  - Payment Gateway              │
         └────────────────────────────────┘
```

### 2.2. Технические требования к инфраструктуре

#### Frontend Hosting
- **Требования:**
  - Поддержка SPA (Single Page Application)
  - SSL/TLS сертификаты
  - CDN для быстрой загрузки
  - Автоматический деплой из Git
  - Custom domain

#### Backend Hosting
- **Требования:**
  - Node.js 18+
  - 2+ CPU cores
  - 4+ GB RAM
  - 50+ GB Storage
  - Process Manager (PM2 или аналог)
  - Автоматический restart при сбоях
  - HTTPS
  - CORS настройка

#### Database (PostgreSQL)
- **Main Platform Database:**
  - 50+ таблиц
  - 100+ GB storage
  - Automatic backups
  - Connection pooling
  - Индексы для оптимизации
  
- **Tripwire Database:**
  - 15 таблиц
  - 10 GB storage
  - Изолированная от основной БД

#### File Storage
- **Video Storage:**
  - Поддержка больших файлов (до 5GB)
  - HLS transcoding (конвертация в .m3u8)
  - CDN для быстрой доставки
  - Adaptive bitrate streaming
  
- **Document Storage:**
  - PDF, DOCX, PNG, JPG
  - Публичный доступ (с подписанными URL)
  - 100 GB общего объема

---

## 3. MAIN PLATFORM - ОСНОВНАЯ ПЛАТФОРМА

### 3.1. Функционал для студентов

#### 3.1.1. Аутентификация
- ✅ Email/Password регистрация
- ✅ Email подтверждение
- ✅ Восстановление пароля
- ✅ Google OAuth (опционально)
- ✅ JWT токены
- ✅ Refresh tokens

#### 3.1.2. Личный кабинет
- **Профиль:**
  - Аватар (загрузка изображения)
  - ФИО, email
  - Telegram ID (для связи с ботом)
  - Статус аккаунта (активный/истекший)
  - Дата истечения доступа
  
- **Статистика:**
  - Общий XP (опыт)
  - Уровень (Level)
  - Текущий стрик (дни подряд)
  - Лучший стрик
  - Завершено уроков
  - Потрачено времени на обучение

#### 3.1.3. Система обучения

**Структура курса:**
```
Курс (Course)
  └─ Модули (Modules) - 30+ штук
       └─ Уроки (Lessons) - 5-10 на модуль
            ├─ Видео (HLS streaming)
            ├─ Текстовое описание
            ├─ Советы от AI
            ├─ Транскрипция видео
            └─ Материалы для скачивания (PDF, файлы)
```

**Видеоплеер:**
- HLS adaptive streaming (разные качества)
- Сохранение прогресса просмотра (на какой секунде остановился)
- Перемотка (backward/forward)
- Скорость воспроизведения (0.5x - 2x)
- Полноэкранный режим
- Трекинг: урок считается завершенным при просмотре 80%+ видео
- Аналитика просмотров (сколько раз смотрел, общее время)

**Прогресс:**
- Процент завершения курса
- Процент завершения модуля
- Список завершенных уроков
- Список активных уроков

**Блокировка уроков:**
- Линейное прохождение: следующий урок открывается только после завершения предыдущего
- Модули открываются последовательно

#### 3.1.4. Геймификация

**XP (Experience Points):**
- +100 XP за завершение урока
- +50 XP за ежедневный вход
- +200 XP за завершение модуля
- Система уровней (Level 1-50)

**Стрики (Streaks):**
- Счетчик дней подряд с активностью
- Активность = вход в платформу ИЛИ завершение урока
- Сброс при пропуске дня
- Отслеживание лучшего стрика

**Достижения (Achievements):**
- 24 типа достижений
- Категории:
  - Обучение (завершил N уроков)
  - Стрики (N дней подряд)
  - XP (достиг N опыта)
  - Скорость (завершил модуль за N дней)
- Уведомления при получении (анимация, конфетти)
- Страница со всеми достижениями

**Рейтинг:**
- Глобальная таблица лидеров
- Сортировка по XP
- Топ-10 студентов
- Позиция текущего пользователя

#### 3.1.5. AI-Наставник и AI-Куратор

**AI-Куратор (Чат 24/7):**
- Текстовый чат с AI
- Голосовые сообщения (запись и распознавание речи)
- Загрузка файлов (PDF, изображения, DOCX)
- Markdown форматирование ответов
- История всех диалогов
- Персонализированные ответы на основе прогресса студента

**AI-Наставник (Проактивный):**
- Автоматические отчеты каждые 3 дня
- Отправка через Telegram
- Анализ прогресса за последние 3 дня:
  - Сколько уроков завершено
  - Изменение XP
  - Стрик
  - Рекомендации и мотивация
- Напоминания о незавершенных уроках

#### 3.1.6. Цели и миссии

**Цели (Goals):**
- Персональные цели студента
- Типы: "Завершить N уроков до даты X"
- Трекинг прогресса (current_value / target_value)
- Дедлайн
- Статус: активная/завершенная/провалена

**Миссии (Missions):**
- Еженедельные челленджи
- Пример: "Завершить 5 уроков за неделю"
- Награда за завершение (XP)
- Автоматическая проверка

#### 3.1.7. NeuroHub - Главная страница

**Дашборд студента:**
- Карточки статистики:
  - Всего XP
  - Текущий уровень
  - Стрик
  - Завершено уроков
  
- Активные цели (список с прогрессом)
- Активные миссии
- Последние достижения
- Быстрый доступ к AI-куратору
- Последний просмотренный урок (кнопка "Продолжить")

---

### 3.2. Функционал для кураторов/преподавателей

- Доступ к чатам студентов с AI-куратором (просмотр)
- Метрики по студентам:
  - Кто активен/неактивен
  - Прогресс по курсу
  - Количество вопросов к AI

---

## 4. TRIPWIRE - ВОРОНКА ПРОДАЖ

### 4.1. Назначение
Триальная версия курса за символическую цену (5000₸ / ~$11 USD) для привлечения клиентов и последующего upsell в полный курс.

### 4.2. Отличия от Main Platform

| Параметр | Main Platform | Tripwire |
|----------|--------------|----------|
| **База данных** | DB #1 (отдельная) | DB #2 (изолированная) |
| **Модулей** | 30+ | 3 |
| **Геймификация** | Полная (XP, Levels, Streaks) | Упрощенная (только Achievements) |
| **Достижения** | 24 типа | 3 (по 1 на модуль) |
| **AI-Наставник** | Да (полный) | Да (упрощенный) |
| **AI-Куратор** | Да | Да |
| **Цели/Миссии** | Да | Нет |
| **Рейтинг** | Да | Нет |
| **Срок доступа** | Годовая подписка | Бессрочный |

### 4.3. Структура Tripwire

**3 Модуля:**
1. **Модуль 1: "Вводный модуль"**
   - 1 урок (45 минут)
   - Открыт сразу после покупки
   
2. **Модуль 2: "Создание GPT-бота"**
   - 1 урок (60 минут)
   - Открывается после завершения Модуля 1
   
3. **Модуль 3: "Создание вирусных Reels"**
   - 1 урок (50 минут)
   - Открывается после завершения Модуля 2

**Прогресс:**
- Линейная последовательность (нельзя перепрыгнуть)
- Правило 80%: урок считается завершенным при просмотре 80%+ видео

**Достижения:**
- 3 достижения (по 1 на каждый завершенный модуль)
- Анимация при получении

**Сертификат:**
- Генерируется автоматически после завершения всех 3 модулей
- PDF с именем студента, датой, QR-кодом
- Кнопка "Скачать сертификат"

**Landing Page:**
- Красивая продающая страница
- Описание программы
- Цена и кнопка "Купить"
- Интеграция с платежной системой

**Payment Page:**
- Форма оплаты
- Интеграция с платежным шлюзом
- Автоматическое создание аккаунта после оплаты
- Отправка welcome email с доступами

### 4.4. Sales Manager Dashboard (для Tripwire)

**Роль:** `sales_manager`

**Функционал:**
1. **Создание студентов:**
   - Форма: Email, Имя, Telegram ID
   - Генерация временного пароля
   - Автоматическая отправка welcome email
   - Привязка к менеджеру (поле `sales_manager_id`)

2. **Статистика:**
   - Всего студентов (у данного менеджера)
   - Активных студентов
   - Завершили курс (все 3 модуля)
   - Выручка (количество * цена)

3. **Таблица студентов:**
   - ФИО, Email, Telegram
   - Дата регистрации
   - Прогресс (модули завершены)
   - Сертификат выдан (Да/Нет)
   - Последняя активность
   - Действия:
     - Посмотреть профиль
     - Сбросить пароль
     - Удалить

4. **Фильтрация:**
   - По дате регистрации (календарь с выбором периода)
   - По статусу (Активные / Завершили / Неактивные)
   - Поиск по имени/email

5. **Leaderboard менеджеров:**
   - Рейтинг всех sales менеджеров
   - Метрики:
     - Студентов привлечено
     - Завершили курс
     - Выручка
   - Сортировка по выручке

6. **Динамика продаж:**
   - График продаж по дням
   - Фильтр по периоду (неделя/месяц/год)

7. **Activity Log:**
   - Лог всех действий на платформе
   - Типы событий:
     - Регистрация студента
     - Завершение урока
     - Получение сертификата
     - Вход в систему

---

## 5. ADMIN PANEL - ПАНЕЛЬ УПРАВЛЕНИЯ

### 5.1. Доступ
**Роль:** `admin`

### 5.2. Разделы

#### 5.2.1. Dashboard (Main Platform)
- **Статистика:**
  - Всего студентов
  - Активных (заходили за последние 7 дней)
  - Новых за неделю
  - Средний прогресс по курсу
  - Общее время просмотра видео

- **Графики:**
  - Активность по дням (линейный график)
  - Топ-10 студентов по XP
  - Распределение по уровням

- **Таблица пользователей:**
  - ФИО, Email, Роль
  - XP, Level, Стрик
  - Прогресс по курсу (%)
  - Дата регистрации
  - Последняя активность
  - Статус аккаунта
  - Дата истечения
  - Действия:
    - Просмотр профиля (модальное окно)
    - Редактировать
    - Продлить доступ
    - Заблокировать/Разблокировать
    - Удалить

- **Модальное окно студента:**
  - **Вкладка "Обзор":**
    - XP, Level, Стрик
    - Завершено уроков
    - Время на платформе
    - Достижения (количество)
    - График активности за месяц
  - **Вкладка "Достижения":**
    - Список всех 24 достижений
    - Статус: выполнено/не выполнено
  - **Вкладка "AI Диагностика":**
    - Кнопка "Запустить диагностику"
    - AI-анализ студента (GPT-4):
      - Сильные стороны
      - Слабые стороны
      - Риски отсева
      - Рекомендации

#### 5.2.2. Tripwire Manager (см. п. 4.4)

#### 5.2.3. Students (Main Platform)
- Полная таблица всех студентов
- Расширенные фильтры:
  - По роли
  - По статусу аккаунта
  - По дате регистрации
  - По прогрессу
- Массовые действия:
  - Продлить доступ (выбранным)
  - Экспорт в CSV

#### 5.2.4. AI Analytics
- **AI-Аналитик (ежедневные отчеты):**
  - Анализ ВСЕХ студентов платформы
  - Генерация через GPT-4o-mini (дешевле)
  - Метрики:
    - Новых студентов за день
    - Активных студентов
    - Средний прогресс
    - Завершено уроков (всего)
    - Топ-5 студентов
    - Проблемные зоны (студенты без активности 7+ дней)
  - История отчетов (сохраняется в БД)
  - Просмотр старых отчетов

#### 5.2.5. AI Curator Chats
- Список всех чатов студентов с AI-куратором
- Фильтры:
  - По студенту
  - По дате
- Просмотр диалогов (read-only)
- Метрики:
  - Всего сообщений
  - Всего потоков (threads)
  - Среднее время ответа AI

#### 5.2.6. Token Usage
- Мониторинг использования OpenAI API
- Графики:
  - Использование токенов по дням
  - Распределение по типам (curator / mentor / analyst)
- Стоимость:
  - Общая стоимость за период
  - Стоимость по каждому Assistant
- Детализация:
  - Сколько токенов на студента
  - Самые активные пользователи

#### 5.2.7. Video Analytics (Main Platform)
- Статистика по видео:
  - Сколько раз просмотрено каждое видео
  - Среднее время просмотра
  - % завершения (сколько досмотрели до конца)
  - Drop-off rate (на какой минуте уходят)
- Таблица всех видео с метриками
- Фильтр по модулю/уроку

#### 5.2.8. Transcriptions (Main Platform)
- Управление транскрипциями видео
- Таблица:
  - Урок
  - Статус транскрипции (есть/нет)
  - Язык
  - Длина текста
  - Дата создания
- Действия:
  - Просмотр текста транскрипции
  - Редактировать
  - Сгенерировать заново (через Whisper API)

#### 5.2.9. Tripwire Transcriptions
- Аналогично п. 5.2.8, но для Tripwire базы

#### 5.2.10. Costs (Tripwire)
- Аналитика затрат на Tripwire
- Метрики:
  - Расходы на AI (токены)
  - Расходы на хостинг
  - Расходы на email
  - Прибыль (доход - расходы)
- График рентабельности

#### 5.2.11. Course Constructor (Main Platform)
- **Создание/Редактирование курсов:**
  - Название, описание
  - Обложка (изображение)
  - Всего XP за курс
  
- **Создание/Редактирование модулей:**
  - Название, описание
  - Привязка к курсу
  - Порядок (order_index)
  
- **Создание/Редактирование уроков:**
  - Название
  - Привязка к модулю
  - Порядок (order_index)
  - **Вкладка "Видео":**
    - Загрузка видео (drag&drop, до 5GB)
    - Автоматическая транскодировка в HLS
    - Предпросмотр
    - Указание длительности
  - **Вкладка "Описание":**
    - Текстовое описание урока
    - Markdown редактор
    - Кнопка "Сгенерировать через AI" (GPT-4)
  - **Вкладка "Советы":**
    - Список советов/подсказок
    - Кнопка "Сгенерировать через AI"
  - **Вкладка "Материалы":**
    - Загрузка PDF/файлов
    - Drag&drop
    - Удаление
  - **Вкладка "Транскрипция":**
    - Автоматическая генерация через Whisper API
    - Редактирование текста
    - Кнопка "Обновить транскрипцию"

- **Drag & Drop сортировка:**
  - Изменение порядка модулей
  - Изменение порядка уроков внутри модуля

---

## 6. БАЗА ДАННЫХ

### 6.1. Main Platform Database (50+ таблиц)

#### 6.1.1. Пользователи и аутентификация

**users**
```
id (UUID, PK)
email (TEXT, UNIQUE)
full_name (TEXT)
avatar_url (TEXT)
role (TEXT) - 'student' | 'admin' | 'curator' | 'sales_manager' | 'tech_support'
telegram_chat_id (BIGINT)
onboarding_completed (BOOLEAN)
created_at (TIMESTAMP)
updated_at (TIMESTAMP)
```

**student_profiles**
```
id (UUID, FK → users.id)
current_xp (INTEGER)
level (INTEGER)
current_streak (INTEGER)
longest_streak (INTEGER)
last_active_date (DATE)
account_status (TEXT) - 'active' | 'suspended' | 'expired'
expiration_date (TIMESTAMP)
invited_at (TIMESTAMP)
total_watch_time_seconds (INTEGER)
```

#### 6.1.2. Курсы и обучение

**courses**
```
id (UUID, PK)
title (TEXT)
description (TEXT)
cover_image (TEXT)
total_xp (INTEGER)
created_at (TIMESTAMP)
```

**modules**
```
id (UUID, PK)
course_id (UUID, FK → courses.id)
title (TEXT)
description (TEXT)
order_index (INTEGER)
duration_minutes (INTEGER)
created_at (TIMESTAMP)
```

**lessons**
```
id (UUID, PK)
module_id (UUID, FK → modules.id)
title (TEXT)
description (TEXT)
video_url (TEXT) - HLS URL
duration_seconds (INTEGER)
order_index (INTEGER)
ai_tips (JSONB) - массив советов
created_at (TIMESTAMP)
```

**video_content**
```
id (UUID, PK)
lesson_id (UUID, FK → lessons.id)
video_id (TEXT) - ID видео в CDN
hls_url (TEXT) - URL .m3u8 плейлиста
thumbnail_url (TEXT)
duration_seconds (INTEGER)
status (TEXT) - 'processing' | 'ready' | 'failed'
created_at (TIMESTAMP)
```

**lesson_materials**
```
id (UUID, PK)
lesson_id (UUID, FK → lessons.id)
filename (TEXT)
display_name (TEXT)
file_url (TEXT)
file_size_bytes (INTEGER)
mime_type (TEXT)
uploaded_at (TIMESTAMP)
```

**transcriptions**
```
id (UUID, PK)
lesson_id (UUID, FK → lessons.id)
text (TEXT)
language (TEXT)
created_at (TIMESTAMP)
updated_at (TIMESTAMP)
```

#### 6.1.3. Прогресс студентов

**student_progress**
```
id (UUID, PK)
user_id (UUID, FK → users.id)
lesson_id (UUID, FK → lessons.id)
is_completed (BOOLEAN)
video_progress_percent (INTEGER) - 0-100
watch_time_seconds (INTEGER)
completed_at (TIMESTAMP)
last_watched_at (TIMESTAMP)
```

**video_tracking**
```
id (UUID, PK)
user_id (UUID, FK → users.id)
lesson_id (UUID, FK → lessons.id)
watch_time_seconds (INTEGER)
total_watches (INTEGER) - сколько раз начинал смотреть
last_position_seconds (INTEGER) - на какой секунде остановился
created_at (TIMESTAMP)
updated_at (TIMESTAMP)
```

**video_analytics**
```
id (UUID, PK)
lesson_id (UUID, FK → lessons.id)
total_views (INTEGER)
total_watch_time_seconds (INTEGER)
average_completion_rate (FLOAT) - 0.0-1.0
drop_off_points (JSONB) - на каких секундах уходят
updated_at (TIMESTAMP)
```

#### 6.1.4. Геймификация

**achievements**
```
id (UUID, PK)
title (TEXT)
description (TEXT)
icon (TEXT)
category (TEXT) - 'learning' | 'streak' | 'xp' | 'speed'
rarity (TEXT) - 'common' | 'rare' | 'epic' | 'legendary'
requirement (TEXT)
target_value (INTEGER)
```

**user_achievements**
```
id (UUID, PK)
user_id (UUID, FK → users.id)
achievement_id (UUID, FK → achievements.id)
progress (INTEGER)
is_completed (BOOLEAN)
unlocked_at (TIMESTAMP)
```

**user_statistics**
```
id (UUID, PK)
user_id (UUID, FK → users.id)
total_lessons_completed (INTEGER)
total_modules_completed (INTEGER)
total_xp (INTEGER)
current_level (INTEGER)
current_streak (INTEGER)
longest_streak (INTEGER)
total_watch_time_seconds (INTEGER)
updated_at (TIMESTAMP)
```

**student_rankings**
```
id (UUID, PK)
user_id (UUID, FK → users.id)
rank (INTEGER)
total_xp (INTEGER)
updated_at (TIMESTAMP)
```

#### 6.1.5. Цели и миссии

**user_goals**
```
id (UUID, PK)
user_id (UUID, FK → users.id)
goal_type (TEXT) - 'complete_lessons' | 'watch_time' | 'streak'
description (TEXT)
target_value (INTEGER)
current_value (INTEGER)
deadline (TIMESTAMP)
is_completed (BOOLEAN)
created_at (TIMESTAMP)
```

**missions**
```
id (UUID, PK)
title (TEXT)
description (TEXT)
mission_type (TEXT) - 'weekly' | 'daily'
requirement (TEXT)
target_value (INTEGER)
xp_reward (INTEGER)
start_date (DATE)
end_date (DATE)
```

**user_missions**
```
id (UUID, PK)
user_id (UUID, FK → users.id)
mission_id (UUID, FK → missions.id)
current_value (INTEGER)
is_completed (BOOLEAN)
completed_at (TIMESTAMP)
```

#### 6.1.6. AI-системы

**ai_curator_threads**
```
id (UUID, PK)
user_id (UUID, FK → users.id)
thread_id (TEXT) - OpenAI thread ID
assistant_type (TEXT) - 'curator' | 'mentor' | 'analyst'
created_at (TIMESTAMP)
last_message_at (TIMESTAMP)
```

**ai_curator_messages**
```
id (UUID, PK)
thread_id (UUID, FK → ai_curator_threads.id)
message_id (TEXT) - OpenAI message ID
role (TEXT) - 'user' | 'assistant'
content (TEXT)
created_at (TIMESTAMP)
```

**ai_curator_attachments**
```
id (UUID, PK)
message_id (UUID, FK → ai_curator_messages.id)
file_id (TEXT) - OpenAI file ID
filename (TEXT)
mime_type (TEXT)
file_size_bytes (INTEGER)
uploaded_at (TIMESTAMP)
```

**ai_curator_metrics**
```
id (UUID, PK)
user_id (UUID, FK → users.id)
total_messages (INTEGER)
total_threads (INTEGER)
total_files_uploaded (INTEGER)
total_tokens_used (INTEGER)
last_interaction (TIMESTAMP)
```

**ai_mentor_sessions**
```
id (UUID, PK)
user_id (UUID, FK → users.id)
session_type (TEXT) - 'motivation' | 'progress_report' | 'reminder'
status (TEXT) - 'pending' | 'sent' | 'failed'
sent_at (TIMESTAMP)
created_at (TIMESTAMP)
```

**ai_mentor_messages**
```
id (UUID, PK)
session_id (UUID, FK → ai_mentor_sessions.id)
message_text (TEXT)
telegram_message_id (INTEGER)
delivered (BOOLEAN)
delivered_at (TIMESTAMP)
```

**ai_analytics_reports**
```
id (UUID, PK)
report_date (DATE)
report_type (TEXT) - 'daily' | 'weekly' | 'monthly'
content (TEXT) - AI-generated report
metrics (JSONB) - статистика
generated_at (TIMESTAMP)
```

**token_usage**
```
id (UUID, PK)
user_id (UUID, FK → users.id, NULLABLE)
assistant_type (TEXT) - 'curator' | 'mentor' | 'analyst'
operation (TEXT) - 'chat' | 'report' | 'transcription'
input_tokens (INTEGER)
output_tokens (INTEGER)
total_tokens (INTEGER)
cost_usd (DECIMAL)
created_at (TIMESTAMP)
```

#### 6.1.7. Telegram интеграция

**telegram_connections**
```
id (UUID, PK)
user_id (UUID, FK → users.id)
telegram_chat_id (BIGINT, UNIQUE)
telegram_username (TEXT)
is_verified (BOOLEAN)
verification_code (TEXT)
connected_at (TIMESTAMP)
```

**telegram_notifications**
```
id (UUID, PK)
user_id (UUID, FK → users.id)
notification_type (TEXT) - 'mentor_report' | 'reminder' | 'achievement'
status (TEXT) - 'pending' | 'sent' | 'failed'
message_text (TEXT)
telegram_message_id (INTEGER)
sent_at (TIMESTAMP)
created_at (TIMESTAMP)
```

#### 6.1.8. Логи и аналитика

**user_activity**
```
id (UUID, PK)
user_id (UUID, FK → users.id)
activity_type (TEXT) - 'login' | 'lesson_completed' | 'achievement_unlocked'
metadata (JSONB)
created_at (TIMESTAMP)
```

**admin_actions**
```
id (UUID, PK)
admin_id (UUID, FK → users.id)
action_type (TEXT) - 'user_created' | 'user_deleted' | 'access_extended'
target_user_id (UUID, FK → users.id, NULLABLE)
details (JSONB)
created_at (TIMESTAMP)
```

---

### 6.2. Tripwire Database (15 таблиц)

**tripwire_users**
```
id (UUID, PK)
email (TEXT, UNIQUE)
full_name (TEXT)
telegram_chat_id (BIGINT)
role (TEXT) - 'student' | 'sales_manager' | 'admin'
sales_manager_id (UUID, FK → tripwire_users.id, NULLABLE)
created_at (TIMESTAMP)
updated_at (TIMESTAMP)
```

**tripwire_user_profile**
```
user_id (UUID, PK, FK → tripwire_users.id)
modules_completed (INTEGER) - 0-3
total_modules (INTEGER) - всегда 3
completion_percentage (INTEGER) - 0-100
certificate_issued (BOOLEAN)
certificate_url (TEXT)
last_active_at (TIMESTAMP)
```

**tripwire_progress**
```
id (UUID, PK)
tripwire_user_id (UUID, FK → tripwire_users.id)
lesson_id (INTEGER) - 67, 68, или 69
video_progress_percent (INTEGER) - 0-100
watch_time_seconds (INTEGER)
is_completed (BOOLEAN)
completed_at (TIMESTAMP)
last_watched_at (TIMESTAMP)
```

**tripwire_achievements**
```
id (UUID, PK)
user_id (UUID, FK → tripwire_users.id)
achievement_type (TEXT) - 'module_1' | 'module_2' | 'module_3'
title (TEXT)
description (TEXT)
icon (TEXT)
unlocked (BOOLEAN)
unlocked_at (TIMESTAMP)
```

**tripwire_certificates**
```
id (UUID, PK)
user_id (UUID, FK → tripwire_users.id)
certificate_url (TEXT)
issued_at (TIMESTAMP)
full_name (TEXT)
```

**tripwire_materials**
```
id (UUID, PK)
lesson_id (INTEGER) - 67, 68, или 69
filename (TEXT)
display_name (TEXT)
file_url (TEXT)
file_size_bytes (INTEGER)
uploaded_at (TIMESTAMP)
```

**tripwire_ai_curator_threads**
```
id (UUID, PK)
user_id (UUID, FK → tripwire_users.id)
thread_id (TEXT) - OpenAI thread ID
created_at (TIMESTAMP)
last_message_at (TIMESTAMP)
```

**tripwire_ai_curator_messages**
```
id (UUID, PK)
thread_id (UUID, FK → tripwire_ai_curator_threads.id)
message_id (TEXT)
role (TEXT) - 'user' | 'assistant'
content (TEXT)
created_at (TIMESTAMP)
```

**tripwire_sales_stats**
```
id (UUID, PK)
sales_manager_id (UUID, FK → tripwire_users.id)
date (DATE)
students_created (INTEGER)
students_completed (INTEGER)
revenue (DECIMAL)
```

**tripwire_activity_log**
```
id (UUID, PK)
user_id (UUID, FK → tripwire_users.id)
action (TEXT) - 'registered' | 'lesson_completed' | 'certificate_issued'
details (JSONB)
created_at (TIMESTAMP)
```

**tripwire_video_tracking**
```
id (UUID, PK)
user_id (UUID, FK → tripwire_users.id)
lesson_id (INTEGER)
watch_time_seconds (INTEGER)
last_position_seconds (INTEGER)
total_watches (INTEGER)
updated_at (TIMESTAMP)
```

**tripwire_transcriptions**
```
id (UUID, PK)
lesson_id (INTEGER) - 67, 68, или 69
text (TEXT)
language (TEXT)
created_at (TIMESTAMP)
```

---

## 7. API ENDPOINTS

### 7.1. Authentication

```
POST   /api/users/register          - Регистрация нового пользователя
POST   /api/users/login             - Вход (email/password)
POST   /api/users/logout            - Выход
POST   /api/users/refresh-token     - Обновление JWT токена
POST   /api/users/forgot-password   - Восстановление пароля
POST   /api/users/reset-password    - Сброс пароля
POST   /api/users/verify-email      - Подтверждение email
```

### 7.2. User Profile

```
GET    /api/users/profile           - Получить свой профиль
PUT    /api/users/profile           - Обновить профиль
POST   /api/users/avatar            - Загрузить аватар
GET    /api/users/:id               - Получить профиль пользователя (admin)
PUT    /api/users/:id               - Обновить пользователя (admin)
DELETE /api/users/:id               - Удалить пользователя (admin)
```

### 7.3. Courses & Lessons (Main Platform)

```
GET    /api/courses                 - Список всех курсов
GET    /api/courses/:id             - Получить курс по ID
POST   /api/courses                 - Создать курс (admin)
PUT    /api/courses/:id             - Обновить курс (admin)
DELETE /api/courses/:id             - Удалить курс (admin)

GET    /api/modules                 - Список модулей
GET    /api/modules/:id             - Получить модуль
POST   /api/modules                 - Создать модуль (admin)
PUT    /api/modules/:id             - Обновить модуль (admin)
DELETE /api/modules/:id             - Удалить модуль (admin)
PUT    /api/modules/reorder         - Изменить порядок модулей (drag&drop)

GET    /api/lessons                 - Список уроков
GET    /api/lessons/:id             - Получить урок
POST   /api/lessons                 - Создать урок (admin)
PUT    /api/lessons/:id             - Обновить урок (admin)
DELETE /api/lessons/:id             - Удалить урок (admin)
PUT    /api/lessons/reorder         - Изменить порядок уроков
```

### 7.4. Video & Materials

```
POST   /api/stream/upload           - Загрузить видео в CDN (HLS transcoding)
GET    /api/videos/:lessonId        - Получить видео по lesson_id
PUT    /api/videos/:lessonId        - Обновить видео
DELETE /api/videos/:lessonId        - Удалить видео

POST   /api/materials/upload        - Загрузить материал (PDF/файл)
GET    /api/materials/:lessonId     - Получить материалы урока
DELETE /api/materials/:id           - Удалить материал
```

### 7.5. Progress Tracking

```
GET    /api/progress                - Получить весь прогресс студента
GET    /api/progress/lesson/:id     - Получить прогресс по уроку
POST   /api/progress/lesson/:id     - Обновить прогресс урока
POST   /api/progress/complete/:id   - Отметить урок завершенным

POST   /api/progress/video-tracking - Сохранить позицию видео (каждые 5 сек)
GET    /api/progress/video/:lessonId - Получить позицию видео
```

### 7.6. Gamification

```
GET    /api/achievements            - Список всех достижений
GET    /api/achievements/user       - Мои достижения
POST   /api/achievements/unlock     - Разблокировать достижение (auto)

GET    /api/rankings                - Рейтинг студентов (top 10 + моя позиция)
GET    /api/statistics              - Моя статистика (XP, streak, etc)

GET    /api/goals                   - Мои цели
POST   /api/goals                   - Создать цель
PUT    /api/goals/:id               - Обновить цель
DELETE /api/goals/:id               - Удалить цель

GET    /api/missions                - Активные миссии
GET    /api/missions/user           - Мои миссии (с прогрессом)
```

### 7.7. AI Systems

```
POST   /api/openai/threads          - Создать новый thread с AI
POST   /api/openai/messages         - Отправить сообщение в thread
GET    /api/openai/threads/:id      - Получить историю сообщений thread
POST   /api/openai/upload           - Загрузить файл для AI (PDF, image, etc)
POST   /api/openai/voice            - Распознать голосовое сообщение (Whisper)

GET    /api/ai-mentor/reports       - История отчетов AI-наставника
POST   /api/ai-mentor/generate      - Сгенерировать отчет вручную (admin)

GET    /api/ai-analytics/reports    - История отчетов AI-аналитика
POST   /api/ai-analytics/generate   - Сгенерировать отчет (admin)

GET    /api/ai/generate-description - Сгенерировать описание урока (GPT-4)
GET    /api/ai/generate-tips        - Сгенерировать советы для урока
```

### 7.8. Admin Dashboard (Main Platform)

```
GET    /api/admin/dashboard         - Статистика для админ-панели
GET    /api/admin/students          - Список всех студентов
GET    /api/admin/students/:id      - Детальная информация о студенте
PUT    /api/admin/students/:id/access - Продлить/изменить доступ
POST   /api/admin/students/:id/diagnose - AI-диагностика студента

GET    /api/admin/video-analytics   - Аналитика по видео
GET    /api/admin/token-usage       - Использование AI токенов

GET    /api/admin/transcriptions    - Список транскрипций
POST   /api/admin/transcriptions/:lessonId - Создать транскрипцию
PUT    /api/admin/transcriptions/:id - Обновить транскрипцию
```

### 7.9. Tripwire API

```
POST   /api/tripwire/register       - Регистрация (после оплаты)
POST   /api/tripwire/login          - Вход
GET    /api/tripwire/profile        - Профиль студента
GET    /api/tripwire/progress       - Прогресс по модулям

GET    /api/tripwire/modules        - Список 3 модулей (с unlock статусом)
GET    /api/tripwire/lessons/:id    - Получить урок
POST   /api/tripwire/progress/:lessonId - Обновить прогресс

GET    /api/tripwire/achievements   - Мои достижения (3 шт)
GET    /api/tripwire/certificate    - Получить сертификат (если выдан)

POST   /api/tripwire/materials/:lessonId - Материалы урока
```

### 7.10. Sales Manager Dashboard (Tripwire)

```
GET    /api/admin/tripwire/stats    - Статистика менеджера
GET    /api/admin/tripwire/students - Список студентов менеджера
POST   /api/admin/tripwire/students - Создать студента
DELETE /api/admin/tripwire/students/:id - Удалить студента
POST   /api/admin/tripwire/reset-password/:id - Сброс пароля

GET    /api/admin/tripwire/leaderboard - Рейтинг менеджеров
GET    /api/admin/tripwire/sales-chart - Динамика продаж
GET    /api/admin/tripwire/activity-log - Лог активности
```

### 7.11. Telegram Integration

```
POST   /api/telegram/connect        - Начать подключение Telegram
POST   /api/telegram/verify         - Подтвердить код
DELETE /api/telegram/disconnect     - Отключить Telegram

POST   /api/telegram/send-notification - Отправить уведомление (admin)

Webhook:
POST   /api/webhooks/telegram       - Telegram Bot Webhook
```

### 7.12. Payment (Tripwire)

```
POST   /api/landing/leads           - Сохранить лид с landing page
POST   /api/payment/create          - Создать платеж
POST   /api/payment/verify          - Подтвердить оплату

Webhook:
POST   /api/webhooks/payment        - Payment Gateway Webhook
```

### 7.13. Webhooks

```
POST   /api/webhooks/video          - Video CDN Webhook (transcoding complete)
POST   /api/webhooks/email          - Email Service Webhook (delivery status)
```

---

## 8. AI-СИСТЕМЫ

### 8.1. Архитектура AI

```
┌─────────────────────────────────────────────────────────────┐
│                    AI AGENTS SYSTEM                          │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │ AI-Куратор   │  │ AI-Наставник │  │ AI-Аналитик  │       │
│  │              │  │              │  │              │       │
│  │ GPT-4o       │  │ GPT-4o       │  │ GPT-4o-mini  │       │
│  │              │  │              │  │              │       │
│  │ 24/7 Чат     │  │ Отчеты       │  │ Отчеты       │       │
│  │ Студент→AI   │  │ AI→Telegram  │  │ Админу       │       │
│  └──────────────┘  └──────────────┘  └──────────────┘       │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

### 8.2. AI-Куратор (AI Curator)

**Назначение:** Помощник студента 24/7

**Технология:**
- OpenAI Assistants API
- Model: GPT-4o
- Thread-based conversations

**Функции:**
- Ответы на вопросы по материалу курса
- Помощь с заданиями
- Мотивация и поддержка
- Анализ загруженных файлов (PDF, изображения, DOCX)
- Распознавание голосовых сообщений (Whisper API)

**System Prompt:**
```
Ты - AI-Куратор образовательной платформы. 
Ты помогаешь студентам разбираться в материале, 
отвечаешь на вопросы, объясняешь сложные темы.

Ты НЕ делаешь задания за студента.
Ты направляешь его к правильному решению через наводящие вопросы.
```

**Контекст:**
- Данные студента (имя, прогресс)
- Текущий урок
- История сообщений

**Хранение:**
- Threads сохраняются в OpenAI
- Копия сообщений в БД (для просмотра админом)

### 8.3. AI-Наставник (AI Mentor)

**Назначение:** Проактивная мотивация и отчеты

**Технология:**
- OpenAI Assistants API
- Model: GPT-4o
- Scheduled tasks (cron)

**Функции:**
- Автоматические отчеты каждые 3 дня
- Анализ прогресса студента за период
- Мотивационные сообщения
- Напоминания о незавершенных уроках
- Отправка через Telegram

**Расписание:**
- Каждые 3 дня в 08:00 (по часовому поясу студента)
- Cron: `0 8 */3 * *`

**Генерация отчета:**
1. Backend (cron) собирает данные студента:
   - Уроков завершено за 3 дня
   - Изменение XP
   - Стрик (сохранился/сломался)
   - Последняя активность
   
2. Отправляет запрос в GPT-4o с промптом:
```
Проанализируй прогресс студента {name} за последние 3 дня:
- Завершено уроков: {count}
- XP изменился: +{xp_diff}
- Стрик: {streak} дней
- Последняя активность: {last_active}

Напиши мотивационное сообщение (100-150 слов):
1. Отметь прогресс (если есть)
2. Мотивируй продолжать
3. Дай 1 конкретный совет
```

3. Получает текст от AI
4. Отправляет через Telegram Bot
5. Сохраняет в БД (таблица `ai_mentor_sessions`)

### 8.4. AI-Аналитик (AI Analyst)

**Назначение:** Аналитика для админа

**Технология:**
- OpenAI Assistants API
- Model: GPT-4o-mini (дешевле)
- Scheduled tasks (cron)

**Функции:**
- Ежедневные отчеты по ВСЕМ студентам платформы
- Выявление проблемных зон
- Рекомендации для админа

**Расписание:**
- Каждый день в 09:00
- Cron: `0 9 * * *`

**Генерация отчета:**
1. Backend собирает агрегированные метрики:
   - Новых студентов за день
   - Активных студентов
   - Средний прогресс
   - Топ-5 студентов
   - Студенты без активности 7+ дней
   
2. Отправляет в GPT-4o-mini:
```
Проанализируй данные платформы за {date}:

Метрики:
- Новых студентов: {new_count}
- Активных: {active_count}
- Средний прогресс: {avg_progress}%
- Завершено уроков: {lessons_completed}

Топ-5:
{top_students_list}

Неактивные 7+ дней:
{inactive_students_list}

Создай отчет для админа:
1. Общее состояние платформы
2. Проблемные зоны (риски отсева)
3. Рекомендации
```

3. Сохраняет отчет в БД
4. Отправляет админу (email или в админ-панель)

### 8.5. AI для контента (AI Lesson Generator)

**Назначение:** Генерация описаний и советов для уроков

**Технология:**
- OpenAI Chat Completions API
- Model: GPT-4o

**Функции:**
- Генерация описания урока из названия
- Генерация советов/подсказок
- Улучшение существующего текста

**Использование:**
- Админ создает урок
- Кликает "Сгенерировать через AI"
- Backend отправляет запрос:
```
POST /api/ai/generate-description
{
  "lessonTitle": "Создание Telegram бота",
  "moduleContext": "Модуль 2: Интеграции"
}
```

- GPT-4o возвращает описание (200-300 слов)
- Админ может отредактировать

### 8.6. Транскрипция видео (Whisper API)

**Назначение:** Автоматическая генерация текста из видео

**Технология:**
- OpenAI Whisper API
- Model: whisper-1

**Процесс:**
1. После загрузки видео → извлечь аудио (ffmpeg)
2. Отправить аудио в Whisper API
3. Получить текст транскрипции
4. Сохранить в таблицу `transcriptions`
5. Показывать на странице урока (для поиска, SEO)

**Стоимость:** ~$0.006 за минуту аудио

---

## 9. ИНТЕГРАЦИИ

### 9.1. Telegram Integration

#### 9.1.1. Telegram Bot (AI-Наставник)

**Назначение:** Отправка отчетов и уведомлений студентам

**Функции:**
- Отправка отчетов AI-наставника
- Напоминания о незавершенных уроках
- Уведомления о новых достижениях
- Подключение Telegram к аккаунту

**Подключение:**
1. Студент заходит в Settings → Telegram Connection
2. Нажимает "Подключить Telegram"
3. Получает 6-значный код
4. Переходит в Telegram бота
5. Отправляет `/start {код}`
6. Бот проверяет код и связывает `telegram_chat_id` с `user_id`
7. Статус "Подключено" в интерфейсе

**Отправка сообщений:**
```typescript
// Backend
import TelegramBot from 'node-telegram-bot-api';

const bot = new TelegramBot(process.env.TELEGRAM_BOT_TOKEN, { polling: true });

async function sendMentorReport(userId: string, message: string) {
  const user = await db.users.findOne({ id: userId });
  if (!user.telegram_chat_id) return;
  
  await bot.sendMessage(user.telegram_chat_id, message, {
    parse_mode: 'Markdown'
  });
}
```

**Команды бота:**
- `/start {код}` - Подключение аккаунта
- `/help` - Помощь
- `/progress` - Показать мой прогресс
- `/unsubscribe` - Отключить уведомления

#### 9.1.2. Автоматическое переключение ботов

**Требование:**
- На localhost (development) использовать TEST_BOT_TOKEN
- На production использовать PRODUCTION_BOT_TOKEN
- Переключение автоматически на основе `NODE_ENV`

**Реализация:**
```typescript
// backend/src/config/telegram.ts
const TELEGRAM_BOT_TOKEN = 
  process.env.NODE_ENV === 'production'
    ? process.env.TELEGRAM_MENTOR_BOT_TOKEN  // production bot
    : process.env.TELEGRAM_MENTOR_BOT_TOKEN_TEST; // test bot

export { TELEGRAM_BOT_TOKEN };
```

### 9.2. Email Integration

**Назначение:** Отправка email уведомлений

**Функции:**
- Welcome email после регистрации (с временным паролем для Tripwire)
- Email подтверждение (verification link)
- Восстановление пароля (reset link)
- Уведомления о продлении доступа
- Уведомления об истечении подписки (за 7 дней)

**Требования к Email сервису:**
- SMTP или HTTP API
- Transactional emails
- Template support (HTML emails)
- Delivery tracking (webhooks)

**Шаблоны писем:**

**Welcome Email (Tripwire):**
```
Тема: Добро пожаловать в Tripwire: Integrator 0 to $1000

Привет, {name}!

Ваш аккаунт создан. Вот ваши данные для входа:

Email: {email}
Временный пароль: {temp_password}

Войдите на платформу: https://example.com/tripwire/login

С уважением,
Команда onAI Academy
```

**Email Verification:**
```
Тема: Подтвердите ваш email

Здравствуйте!

Перейдите по ссылке для подтверждения email:
{verification_link}

Ссылка действительна 24 часа.
```

### 9.3. Payment Gateway Integration (Tripwire)

**Назначение:** Прием оплаты за Tripwire курс

**Требования:**
- Поддержка казахстанских карт
- Цена: 5000₸
- Webhook для уведомления об успешной оплате

**Flow:**
1. Пользователь на Landing Page нажимает "Купить"
2. Перенаправление на Payment Page
3. Заполнение формы:
   - ФИО
   - Email
   - Telegram ID (опционально)
4. Нажатие "Оплатить"
5. Перенаправление на Payment Gateway
6. Успешная оплата → Webhook на Backend
7. Backend:
   - Создает аккаунт в Tripwire DB
   - Генерирует временный пароль
   - Отправляет welcome email
   - Записывает в таблицу sales (sales_manager_id если есть)
8. Перенаправление на `/tripwire/login` с сообщением "Проверьте email"

**Webhook Endpoint:**
```typescript
POST /api/webhooks/payment

Body:
{
  "payment_id": "xxx",
  "status": "success",
  "amount": 5000,
  "email": "user@example.com",
  "metadata": {
    "full_name": "Иван Иванов",
    "telegram_id": "123456789"
  }
}

Response:
{
  "success": true,
  "user_id": "uuid",
  "message": "Account created"
}
```

### 9.4. Video CDN Integration

**Назначение:** Хранение и стриминг видео

**Требования:**
- Поддержка больших файлов (до 5GB)
- Автоматическая транскодировка в HLS (.m3u8)
- Adaptive bitrate (несколько качеств: 360p, 720p, 1080p)
- CDN для быстрой доставки
- Webhooks (уведомление когда видео готово)

**Upload Flow:**
1. Админ загружает видео через Admin Panel
2. Frontend отправляет `POST /api/stream/upload` (multipart/form-data)
3. Backend:
   - Принимает файл (до 5GB)
   - Загружает в Video CDN API
   - Получает `video_id`
   - Сохраняет в БД со статусом "processing"
4. CDN транскодирует видео (5-30 минут)
5. CDN отправляет Webhook → `POST /api/webhooks/video`
6. Backend обновляет статус на "ready" и сохраняет HLS URL

**HLS URL формат:**
```
https://cdn.example.com/{video_id}/playlist.m3u8
```

**Thumbnail:**
```
https://cdn.example.com/{video_id}/thumbnail.jpg
```

### 9.5. File Storage Integration

**Назначение:** Хранение материалов (PDF, изображения, файлы)

**Требования:**
- Публичный доступ с подписанными URL (для безопасности)
- Поддержка CORS (для загрузки из браузера)
- 100 GB общего объема

**Upload Flow:**
1. Админ загружает материал
2. Frontend → `POST /api/materials/upload`
3. Backend → Storage API
4. Получение публичного URL
5. Сохранение в `lesson_materials`

**Download Flow:**
1. Студент открывает урок
2. Видит список материалов
3. Клик "Скачать" → direct link на Storage

---

## 10. ТЕХНИЧЕСКИЙ СТЕК

### 10.1. Frontend

**Core:**
- React 18+
- TypeScript 5+
- Vite (build tool)

**UI Components:**
- Tailwind CSS
- shadcn/ui (Radix UI)
- Framer Motion (animations)
- Lucide Icons
- Phosphor Icons

**State Management:**
- React Query (TanStack Query)
- Zustand (global state)
- React Context (auth, theme)

**Forms:**
- React Hook Form
- Zod (validation)

**Video Player:**
- HLS.js (для HLS стриминга)
- Plyr (UI плеера)

**Charts:**
- Recharts

**3D Graphics (optional):**
- Three.js
- React Three Fiber

**Markdown:**
- React Markdown
- remark-gfm

**Other:**
- Axios (HTTP client)
- date-fns (date utilities)
- canvas-confetti (celebrations)

### 10.2. Backend

**Core:**
- Node.js 18+
- TypeScript 5+
- Express.js 5

**Database:**
- PostgreSQL 14+
- pg (node-postgres)

**Authentication:**
- JWT (jsonwebtoken)
- bcrypt (password hashing)

**File Upload:**
- Multer (multipart/form-data)

**Validation:**
- express-validator
- Zod

**Security:**
- Helmet (security headers)
- CORS

**Utilities:**
- dotenv (environment variables)
- node-cron (scheduled tasks)
- uuid (unique IDs)

**Media Processing:**
- ffmpeg (video/audio processing)
- sharp (image processing)
- pdf-parse (PDF text extraction)
- mammoth (DOCX to HTML)

**AI Integration:**
- openai (OpenAI API)
- groq-sdk (альтернативный AI, опционально)

**Telegram:**
- node-telegram-bot-api

**Email:**
- nodemailer
- resend (современная альтернатива)

**PDF Generation:**
- puppeteer (HTML to PDF)

### 10.3. DevOps

**Version Control:**
- Git
- GitHub

**CI/CD:**
- GitHub Actions (или аналог)
- Автоматический деплой при push в `main`

**Process Management:**
- PM2 (для Node.js процессов)

**Web Server:**
- Nginx (reverse proxy, SSL termination)

**SSL:**
- Let's Encrypt (бесплатные SSL сертификаты)

**Monitoring:**
- PM2 logs
- Server logs (nginx access/error logs)

---

## 11. ТРЕБОВАНИЯ К БЕЗОПАСНОСТИ

### 11.1. Аутентификация

- JWT токены (access token + refresh token)
- Access token: срок жизни 15 минут
- Refresh token: срок жизни 7 дней
- Хранение refresh token в HttpOnly cookie
- Bcrypt для хеширования паролей (10 раундов)

### 11.2. Авторизация (Role-Based Access Control)

**Роли:**
- `student` - доступ к своим данным
- `sales_manager` - доступ к своим студентам (Tripwire)
- `curator` - доступ к чатам студентов
- `admin` - полный доступ

**Middleware проверка:**
```typescript
// Пример
GET /api/admin/students
→ Проверка JWT токена
→ Проверка роли (admin ИЛИ curator)
→ Если не прошел → 403 Forbidden
```

### 11.3. SQL Injection Protection

- Использование параметризованных запросов
- Никогда не использовать конкатенацию строк для SQL
- Валидация всех входных данных (Zod, express-validator)

### 11.4. XSS Protection

- Helmet middleware (устанавливает security headers)
- Санитизация пользовательского ввода
- CSP (Content Security Policy)

### 11.5. CORS

- Белый список разрешенных origins
- Credentials: true (для cookies)
- Методы: GET, POST, PUT, DELETE, PATCH

### 11.6. Rate Limiting

- Ограничение запросов на API:
  - Регистрация: 5 попыток / 15 минут
  - Логин: 10 попыток / 15 минут
  - AI запросы: 30 запросов / минута
  - File upload: 10 запросов / час

### 11.7. Data Privacy

- Email адреса хранятся в зашифрованном виде (опционально)
- Пароли ТОЛЬКО в виде bcrypt хешей
- Логи НЕ содержат пароли, токены, API ключи

### 11.8. API Keys

- OpenAI API Key в .env (НЕ в коде)
- Telegram Bot Token в .env
- Email API Key в .env
- Payment Gateway credentials в .env
- НИКОГДА не коммитить .env в Git (.gitignore)

### 11.9. File Upload Security

- Валидация типа файла (MIME type + расширение)
- Максимальный размер:
  - Видео: 5GB
  - PDF/DOCX: 50MB
  - Изображения: 10MB
- Сканирование на вирусы (опционально)

---

## 12. ТРЕБОВАНИЯ К ПРОИЗВОДИТЕЛЬНОСТИ

### 12.1. Frontend

- First Contentful Paint (FCP): < 1.5 сек
- Time to Interactive (TTI): < 3 сек
- Lazy loading компонентов (React.lazy)
- Code splitting (автоматически через Vite)
- Image optimization (WebP, lazy loading)

### 12.2. Backend

- API response time: < 200ms (95 percentile)
- Database queries с индексами
- Connection pooling (max 20 connections)
- Caching частых запросов (опционально: Redis)

### 12.3. Video Streaming

- HLS adaptive bitrate (автоматический выбор качества)
- CDN для доставки видео
- Preloading первых секунд видео

### 12.4. Database

- Индексы на часто запрашиваемые поля:
  - users.email
  - users.role
  - student_progress.user_id
  - student_progress.lesson_id
  - lessons.module_id
  - modules.course_id
- Foreign keys для целостности данных
- Автоматические backups (ежедневные)

### 12.5. Scheduled Tasks

- AI-наставник: каждые 3 дня в 08:00
- AI-аналитик: каждый день в 09:00
- Очистка старых логов: раз в месяц
- Напоминания об истечении подписки: ежедневно в 10:00

---

## 📊 ИТОГОВАЯ СВОДКА

### Масштаб проекта:

**Frontend:**
- 50+ компонентов React
- 30+ страниц
- 2 изолированных интерфейса (Main + Tripwire)

**Backend:**
- 60+ API endpoints
- 10+ Services
- 4 AI агента (Curator, Mentor, Analyst, Content Generator)
- 3 Telegram бота (Main Platform, Tripwire, Test)
- 5 Schedulers (cron jobs)

**Базы данных:**
- Database #1: 50+ таблиц (Main Platform)
- Database #2: 15 таблиц (Tripwire)

**Интеграции:**
- Video CDN (HLS streaming)
- File Storage
- OpenAI API (GPT-4, Whisper)
- Telegram Bot API
- Email Service
- Payment Gateway

**Функционал:**
- Полная система обучения с видео
- Геймификация (XP, Levels, Achievements, Streaks, Rankings)
- 3 AI агента для поддержки студентов
- Продающая воронка (Tripwire)
- Sales Manager Dashboard
- Admin Panel с аналитикой
- Telegram интеграция
- Автоматическая генерация сертификатов

---

## 🎯 ЧТО НУЖНО ДЛЯ ОЦЕНКИ

Это техническое задание описывает **полный функционал** существующей платформы.

Для оценки стоимости разработки программисту нужно:

1. Оценить время на каждый модуль:
   - Frontend (Main Platform)
   - Frontend (Tripwire)
   - Frontend (Admin Panel)
   - Backend API
   - Database design & migrations
   - AI Integration
   - Telegram Integration
   - Email Integration
   - Payment Integration
   - Video CDN Integration
   - Testing & QA

2. Учесть:
   - Стоимость инфраструктуры (hosting, database, CDN, storage)
   - Стоимость API (OpenAI, Telegram, Email, Payment)
   - Поддержка и обслуживание

3. Предоставить:
   - Общую стоимость разработки
   - Сроки (в неделях/месяцах)
   - Разбивку по этапам (если нужно)

---

**Конец технического задания**



