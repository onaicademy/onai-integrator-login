# 🔐 СРОЧНО: Смена пароля на Digital Ocean сервере

## Проблема
Сервер требует обязательной смены пароля, SSH ключи не помогают.

## Решение (2 минуты)

### Шаг 1: Зайди на сервер
```powershell
ssh root@207.154.231.30
```

### Шаг 2: Введи текущий пароль
(Тот который знаешь)

### Шаг 3: Система попросит новый пароль
- Придумай новый пароль
- Введи его 2 раза

### Шаг 4: После смены пароля выйди
```bash
exit
```

### Шаг 5: Деплой бэкенда
```powershell
ssh root@207.154.231.30 "cd /var/www/onai-integrator-login-main && git pull origin main && cd backend && npm install --production && npm run build && pm2 restart onai-backend --update-env && pm2 logs onai-backend --lines 20 --nostream"
```

---

## Альтернатива: Через Digital Ocean Dashboard

1. Заходи на https://cloud.digitalocean.com/
2. Найди дроплет (207.154.231.30)
3. Нажми "Access" → "Reset Root Password"
4. Новый пароль придет на email
5. После этого зайди через SSH и смени пароль как выше

---

## После деплоя - проверь

```powershell
# Health check
curl https://api.onai.academy/api/health

# Tripwire lesson (должен вернуть 200, не 404)
curl https://api.onai.academy/api/tripwire/lessons/67
```

Если все ОК - обновляй фронтенд и тестируй Tripwire!
