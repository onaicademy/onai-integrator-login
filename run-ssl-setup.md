# 🔒 Настройка SSL для onai.academy

## DNS уже настроен! ✅

- ✅ `onai.academy` → `178.128.203.40`
- ✅ `www.onai.academy` → `178.128.203.40`

---

## Запуск настройки SSL

Я создал автоматический скрипт `setup-ssl-on-server.sh`, который:

1. ✅ Проверит DNS
2. ✅ Обновит Nginx конфиг (если нужно)
3. ✅ Проверит синтаксис Nginx
4. ✅ Установит Certbot
5. ✅ Получит SSL сертификат
6. ✅ Настроит HTTPS
7. ✅ Включит авто-обновление сертификата

---

## Как запустить

### Вариант 1: Автоматически через GitHub Actions

Я могу добавить этот скрипт в деплой и запустить через GitHub Actions.

**Скажи:** "Запусти SSL setup через Actions"

### Вариант 2: Вручную через SSH

```bash
# Подключись к серверу
ssh root@178.128.203.40

# Скопируй скрипт на сервер
cat > setup-ssl.sh << 'EOF'
# [содержимое скрипта setup-ssl-on-server.sh]
EOF

# Сделай исполняемым
chmod +x setup-ssl.sh

# Запусти
sudo ./setup-ssl.sh
```

### Вариант 3: Прямые команды (без скрипта)

```bash
ssh root@178.128.203.40

# 1. Обновить Nginx конфиг (если нужно)
sed -i 's/integratoronai\.kz/onai.academy/g' /etc/nginx/sites-available/onai-integrator-login.conf

# 2. Проверить синтаксис
nginx -t

# 3. Перезагрузить Nginx
systemctl reload nginx

# 4. Установить Certbot
apt update
apt install -y certbot python3-certbot-nginx

# 5. Получить SSL сертификат
certbot --nginx -d onai.academy -d www.onai.academy
```

При запуске Certbot:
- Введи свой email (для уведомлений об обновлении сертификата)
- Согласись с Terms of Service (Y)
- Certbot автоматически настроит Nginx для HTTPS

---

## После настройки SSL

Открой в браузере:
```
https://onai.academy
```

Должен появиться **зелёный замочек** 🔒 и форма логина!

---

## Что делать дальше

1. **Проверь сайт:** https://onai.academy
2. **Проверь логин:** попробуй залогиниться
3. **Проверь SSL:** нажми на замочек в браузере → должен быть валидный сертификат от Let's Encrypt

---

## Если нужна помощь

Скажи что выбираешь:
- **"Запусти автоматически"** — я добавлю в деплой и запущу
- **"Покажи команды"** — дам пошаговые команды для ручной настройки
- **"Есть проблема: [описание]"** — помогу решить

---

**Готово к запуску!** 🚀

