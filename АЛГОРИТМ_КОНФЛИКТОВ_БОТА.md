# 🤖 АЛГОРИТМ ОБНАРУЖЕНИЯ КОНФЛИКТОВ БОТА

**Дата:** 7 ноября 2025  
**Версия:** 1.0

---

## 🎯 ЦЕЛЬ:

Автоматически обнаруживать ошибки, конфликты и проблемы AI-куратора для улучшения его работы.

---

## 📊 ТИПЫ КОНФЛИКТОВ:

### 1. 🔴 **НЕВЕРНЫЙ ОТВЕТ**
**Признаки:**
- Студент пишет: "Это неправильно", "Ошибка", "Не так"
- Студент задаёт тот же вопрос повторно
- Низкая оценка ответа (если будет feedback)

**Обнаружение:**
```typescript
if (
  userMessage.includes("неправильно") ||
  userMessage.includes("ошибка") ||
  userMessage.includes("не так") ||
  userMessage.includes("wrong") ||
  previousQuestion === currentQuestion
) {
  → Конфликт: INCORRECT_ANSWER
}
```

---

### 2. 💭 **ФАНТАЗИИ (Галлюцинации)**
**Признаки:**
- Бот придумывает факты
- Бот ссылается на несуществующие уроки
- Бот даёт информацию вне контекста курса

**Обнаружение:**
```typescript
if (
  aiResponse.includes("урок") && !lessonExists(extractedLessonNumber) ||
  aiResponse.includes("модуль") && !moduleExists(extractedModuleNumber) ||
  containsUnverifiedFacts(aiResponse)
) {
  → Конфликт: HALLUCINATION
}
```

---

### 3. ❓ **НЕ ПОНЯЛ ВОПРОС**
**Признаки:**
- Бот отвечает не по теме
- Студент переспрашивает: "Я не про это", "Не понял"
- Ответ слишком общий

**Обнаружение:**
```typescript
if (
  userFollowUp.includes("не про это") ||
  userFollowUp.includes("не понял") ||
  userFollowUp.includes("что?") ||
  aiResponse.length < 50 && isVagueResponse(aiResponse)
) {
  → Конфликт: MISUNDERSTOOD_QUESTION
}
```

---

### 4. 🔁 **ПОВТОР ОТВЕТА**
**Признаки:**
- Бот даёт одинаковый ответ на разные вопросы
- Студент: "Ты уже это сказал"

**Обнаружение:**
```typescript
if (
  similarityScore(currentResponse, previousResponse) > 0.8 ||
  userMessage.includes("уже сказал") ||
  userMessage.includes("повтор")
) {
  → Конфликт: REPETITIVE_ANSWER
}
```

---

### 5. 😠 **ТОКСИЧНЫЙ/НЕУМЕСТНЫЙ ОТВЕТ**
**Признаки:**
- Грубость, сарказм (не должно быть)
- Слишком формальный или холодный тон
- Студент: "Грубо", "Не нравится тон"

**Обнаружение:**
```typescript
if (
  containsRudeWords(aiResponse) ||
  userMessage.includes("грубо") ||
  userMessage.includes("неуместно") ||
  sentimentAnalysis(aiResponse) === "negative"
) {
  → Конфликт: INAPPROPRIATE_TONE
}
```

---

### 6. ⚠️ **ТЕХНИЧЕСКИЕ ОШИБКИ**
**Признаки:**
- Бот не ответил (timeout)
- Бот выдал ошибку API
- Бот не обработал файл

**Обнаружение:**
```typescript
if (
  responseTime > 30000 || // 30 секунд
  apiError ||
  aiResponse.includes("error") ||
  aiResponse.includes("не могу обработать")
) {
  → Конфликт: TECHNICAL_ERROR
}
```

---

### 7. 📚 **НЕПОЛНЫЙ ОТВЕТ**
**Признаки:**
- Студент: "А дальше?", "Это всё?", "Подробнее"
- Ответ обрывается на полуслове
- Слишком краткий ответ на сложный вопрос

**Обнаружение:**
```typescript
if (
  userFollowUp.includes("дальше") ||
  userFollowUp.includes("подробнее") ||
  userFollowUp.includes("это всё") ||
  (questionComplexity === "high" && aiResponse.length < 200)
) {
  → Конфликт: INCOMPLETE_ANSWER
}
```

---

## 🔍 АВТОМАТИЧЕСКОЕ ОБНАРУЖЕНИЕ:

### **В РЕАЛЬНОМ ВРЕМЕНИ:**

При каждом ответе бота проверяем:

```typescript
async function detectConflicts(
  userMessage: string,
  aiResponse: string,
  threadHistory: Message[],
  metadata: {
    responseTime: number,
    tokenCount: number,
    model: string,
  }
) {
  const conflicts: Conflict[] = [];

  // 1. Проверяем негативные ключевые слова
  if (hasNegativeFeedback(userMessage)) {
    conflicts.push({
      type: "INCORRECT_ANSWER",
      severity: "high",
      details: extractFeedback(userMessage),
    });
  }

  // 2. Проверяем на галлюцинации
  if (containsHallucination(aiResponse)) {
    conflicts.push({
      type: "HALLUCINATION",
      severity: "critical",
      details: extractHallucination(aiResponse),
    });
  }

  // 3. Проверяем таймауты
  if (metadata.responseTime > 30000) {
    conflicts.push({
      type: "TECHNICAL_ERROR",
      severity: "medium",
      details: `Timeout: ${metadata.responseTime}ms`,
    });
  }

  // 4. Проверяем повторы
  if (isRepetitive(aiResponse, threadHistory)) {
    conflicts.push({
      type: "REPETITIVE_ANSWER",
      severity: "low",
      details: "Similar to previous response",
    });
  }

  // 5. Сохраняем в БД
  if (conflicts.length > 0) {
    await logConflicts(conflicts, threadId, messageId);
  }

  return conflicts;
}
```

---

## 📊 СТРУКТУРА ДАННЫХ:

### **Таблица: `ai_bot_conflicts`**

```sql
CREATE TABLE ai_bot_conflicts (
  id UUID PRIMARY KEY,
  
  -- Ссылки
  thread_id UUID REFERENCES ai_curator_threads(id),
  message_id UUID REFERENCES ai_curator_messages(id),
  user_id UUID REFERENCES auth.users(id),
  
  -- Тип конфликта
  conflict_type VARCHAR(50) NOT NULL,
  -- 'INCORRECT_ANSWER', 'HALLUCINATION', 'MISUNDERSTOOD_QUESTION',
  -- 'REPETITIVE_ANSWER', 'INAPPROPRIATE_TONE', 'TECHNICAL_ERROR', 'INCOMPLETE_ANSWER'
  
  severity VARCHAR(20) NOT NULL,
  -- 'critical', 'high', 'medium', 'low'
  
  -- Детали
  user_message TEXT,
  ai_response TEXT,
  detected_issue TEXT,
  
  -- Метаданные
  response_time_ms INTEGER,
  token_count INTEGER,
  model VARCHAR(50),
  
  -- Статус
  status VARCHAR(20) DEFAULT 'new',
  -- 'new', 'reviewing', 'resolved', 'ignored'
  
  resolution_notes TEXT,
  resolved_at TIMESTAMPTZ,
  resolved_by UUID REFERENCES auth.users(id),
  
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

---

## 💡 РЕКОМЕНДАЦИИ ПО РЕШЕНИЮ:

### 🔴 **CRITICAL (Критично):**

**1. HALLUCINATION (Фантазии):**
```
ПРОБЛЕМА: Бот придумывает несуществующие уроки
РЕШЕНИЕ:
→ Обновить prompt: "Отвечай ТОЛЬКО на основе данных из курса"
→ Добавить в context актуальный список уроков
→ Включить fact-checking перед отправкой ответа
```

**2. INCORRECT_ANSWER (Неверный ответ):**
```
ПРОБЛЕМА: Бот даёт неправильные технические ответы
РЕШЕНИЕ:
→ Проверить knowledge base (обновить?)
→ Добавить disclaimer: "Проверь в документации"
→ Отправить уведомление админу для ручной проверки
```

---

### 🟠 **HIGH (Высокий):**

**3. MISUNDERSTOOD_QUESTION (Не понял):**
```
ПРОБЛЕМА: Бот отвечает не по теме
РЕШЕНИЕ:
→ Улучшить prompt: "Если не уверен - спроси уточнение"
→ Добавить clarification question перед ответом
→ Обучить на примерах из этой категории
```

**4. INAPPROPRIATE_TONE (Неуместный тон):**
```
ПРОБЛЕМА: Бот слишком формальный или грубый
РЕШЕНИЕ:
→ Обновить prompt: примеры дружелюбного тона
→ Убрать фразы-триггеры из ответов
→ Добавить tone checker перед отправкой
```

---

### 🟡 **MEDIUM (Средний):**

**5. TECHNICAL_ERROR (Техническая ошибка):**
```
ПРОБЛЕМА: Таймауты, API ошибки
РЕШЕНИЕ:
→ Увеличить timeout до 60 секунд
→ Добавить retry механизм (3 попытки)
→ Fallback: "Извини, проблемы с подключением. Попробуй позже"
```

**6. INCOMPLETE_ANSWER (Неполный ответ):**
```
ПРОБЛЕМА: Слишком краткие ответы на сложные вопросы
РЕШЕНИЕ:
→ Обновить prompt: "Давай развёрнутые ответы на сложные вопросы"
→ Добавить follow-up вопрос: "Хочешь подробнее?"
→ Увеличить max_tokens для сложных тем
```

---

### 🟢 **LOW (Низкий):**

**7. REPETITIVE_ANSWER (Повтор):**
```
ПРОБЛЕМА: Бот повторяет одинаковые ответы
РЕШЕНИЕ:
→ Проверять similarity перед отправкой
→ Если > 80% похож - перефразировать или дополнить
→ Добавить в context: "Избегай повторов"
```

---

## 📈 МЕТРИКИ КАЧЕСТВА:

### **Отслеживаем:**

1. **Частота конфликтов:**
   - Конфликтов на 100 сообщений
   - Цель: < 5%

2. **Время до resolution:**
   - Среднее время исправления
   - Цель: < 24 часа для critical

3. **Повторные конфликты:**
   - Тот же тип конфликта от того же студента
   - Цель: < 10%

4. **Удовлетворённость после resolution:**
   - Студент продолжает диалог?
   - Цель: > 80%

---

## 🎯 UI В AI-АНАЛИТИКЕ:

### **Новая вкладка: "Конфликты бота"**

**Показываем:**

1. **Карточки конфликтов:**
   ```
   🔴 HALLUCINATION (Critical)
   Студент: Мария Сидорова
   Вопрос: "Где урок 25?"
   Ответ: "Урок 25 про квантовые нейросети..."
   ПРОБЛЕМА: Урока 25 не существует!
   РЕШЕНИЕ: → Обновить context с актуальным списком уроков
   Статус: NEW
   ```

2. **Статистика:**
   - Всего конфликтов за период
   - По типам (диаграмма)
   - По severity (critical/high/medium/low)
   - Тренд (растёт/падает)

3. **Топ проблем:**
   - Самые частые конфликты
   - Самые проблемные темы
   - Студенты с наибольшим числом конфликтов

4. **Действия:**
   - Кнопка "Пометить как исправлено"
   - Добавить resolution notes
   - Отправить в разработку

---

## 🔄 ПРОЦЕСС УЛУЧШЕНИЯ:

```
1. ОБНАРУЖЕНИЕ (автоматически)
   ↓
2. ЛОГИРОВАНИЕ (в БД)
   ↓
3. УВЕДОМЛЕНИЕ (админ видит в UI)
   ↓
4. АНАЛИЗ (админ/AI-аналитик смотрит детали)
   ↓
5. РЕШЕНИЕ (обновление prompt/context/code)
   ↓
6. ТЕСТИРОВАНИЕ (проверка, что исправлено)
   ↓
7. МОНИТОРИНГ (повторится ли конфликт?)
```

---

## ✅ ЧЕКЛИСТ КАЧЕСТВА БОТА:

**Еженедельно проверяем:**

- [ ] Конфликтов < 5% от всех сообщений
- [ ] Critical конфликтов = 0
- [ ] Все High конфликты reviewed
- [ ] Prompt обновлён на основе feedback
- [ ] Knowledge base актуален
- [ ] Среднее время ответа < 5 секунд
- [ ] Студенты не жалуются на бота

---

**ЭТОТ АЛГОРИТМ БУДЕТ ИНТЕГРИРОВАН В AI-АНАЛИТИКУ!** 🤖

