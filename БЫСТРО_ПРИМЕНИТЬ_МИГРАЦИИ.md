# ⚡ БЫСТРО ПРИМЕНИТЬ МИГРАЦИИ

**Время:** 5 минут (если делать быстро)

---

## 🎯 САМЫЙ БЫСТРЫЙ СПОСОБ:

### СПОСОБ 1: Через Supabase Dashboard (РЕКОМЕНДУЮ)

```bash
# 1. Открой:
https://supabase.com/dashboard/project/capdjvokjdivxjfddmx/editor

# 2. SQL Editor (слева)

# 3. Скопируй И ЗАПУСТИ каждый файл:
```

#### Миграция 1:
```bash
cat supabase/migrations/20251103164013_remote_schema.sql | pbcopy
```
→ Вставь в SQL Editor → Run

#### Миграция 2:
```bash
cat supabase/migrations/20251103182016_remote_schema.sql | pbcopy
```
→ Вставь → Run

#### Миграция 3:
```bash
cat supabase/migrations/20251104_add_diagnostics_tables.sql | pbcopy
```
→ Вставь → Run

#### Миграция 4:
```bash
cat supabase/migrations/20250115_add_ai_curator_chat_tables.sql | pbcopy
```
→ Вставь → Run

#### Миграция 5:
```bash
cat supabase/migrations/20250116_achievements_system.sql | pbcopy
```
→ Вставь → Run

#### Миграция 6:
```bash
cat supabase/migrations/20250117_students_management_system.sql | pbcopy
```
→ Вставь → Run

#### Миграция 7:
```bash
cat supabase/migrations/20250118_ai_mentor_and_analyst.sql | pbcopy
```
→ Вставь → Run

#### Миграция 8:
```bash
cat supabase/migrations/20250109_token_usage_tracking.sql | pbcopy
```
→ Вставь → Run

#### Миграция 9:
```bash
cat supabase/migrations/20250110_student_messaging.sql | pbcopy
```
→ Вставь → Run

---

### СПОСОБ 2: Через Terminal (если есть Supabase CLI)

```bash
# Установи Supabase CLI (если нет):
brew install supabase/tap/supabase

# Логин:
supabase login

# Link проект:
supabase link --project-ref capdjvokjdivxjfddmx

# Применить ВСЕ миграции:
supabase db push
```

---

## ✅ ПРОВЕРКА:

```sql
-- Запусти в SQL Editor:
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public'
ORDER BY table_name;
```

Должно быть ~26 таблиц:
```
✅ ai_curator_threads
✅ ai_curator_messages
✅ ai_token_usage
✅ ai_token_usage_daily
✅ student_private_chats
✅ user_achievements
✅ ... и т.д.
```

---

## 🚀 ПОСЛЕ ПРИМЕНЕНИЯ:

```bash
# Перезапусти сервер
npm run dev

# Открой:
http://localhost:8080/admin/token-usage

# Пообщайся с AI-куратором
# Вернись на /admin/token-usage
# Обнови страницу → Увидишь токены!
```

---

**ПРИМЕНЯЙ СЕЙЧАС!** 🚀

