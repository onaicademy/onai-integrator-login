# 🚨 СРОЧНЫЙ ДЕПЛОЙ - ШАГ ЗА ШАГОМ

## ⚡ КРИТИЧНО! НА СЕРВЕРЕ СТАРЫЙ КОД!

**Ошибка на скриншоте:**
```
Error submitting lead: Error: Email обязателен для заполнения
```

**Это значит backend НЕ ОБНОВЛЁН!**

---

## 🔥 КОМАНДЫ ДЛЯ СРОЧНОГО ДЕПЛОЯ:

### **1️⃣ BACKEND (Digital Ocean):**

```bash
# SSH на сервер
ssh -i /Users/miso/onai-integrator-login/.ssh/digitalocean-onai root@207.154.231.30

# Перейди в папку backend
cd /var/www/onai-integrator-login-main/backend

# Подтяни изменения
git pull origin main

# Перезапусти backend
pm2 restart onai-backend

# Проверь логи
pm2 logs onai-backend --lines 50
```

**Должно быть в логах:**
```
✅ Server running on port 5000
✅ Sentry initialized
```

---

### **2️⃣ FRONTEND (собери локально, потом залей):**

```bash
# ЛОКАЛЬНО на твоём компе:
cd /Users/miso/onai-integrator-login

# Очисти кэш
rm -rf dist node_modules/.vite

# Собери production build
npm run build

# Проверь что собралось
ls -lh dist/assets/index-*.js

# Залей на сервер
rsync -avz --delete dist/ root@207.154.231.30:/var/www/html/

# Очисти Nginx кэш на сервере
ssh root@207.154.231.30 "rm -rf /var/cache/nginx/* && systemctl reload nginx"
```

---

## ✅ ПРОВЕРКА:

### **Тест 1: Форма должна работать БЕЗ email**
1. Зайди на https://onai.academy/integrator/expresscourse
2. Кликни "КУПИТЬ СЕЙЧАС"
3. Заполни **ТОЛЬКО ИМЯ И ТЕЛЕФОН** (email оставь пустым)
4. Кликни любую кнопку оплаты
5. ✅ Должно появиться: **"СПАСИБО ЗА ЗАЯВКУ!"**

### **Тест 2: Админка должна показать 127 лидов**
1. Зайди на https://onai.academy/integrator/admin/leads
2. Логин: smmmcwin@gmail.com
3. Пароль: Saintcom
4. ✅ Счетчик должен показать **127 лидов** (не 100!)

### **Тест 3: AmoCRM**
1. Зайди в AmoCRM
2. Воронка "onAI Agency Integration"
3. ✅ Новый лид должен появиться с именем и телефоном (email пустой - ОК!)

---

## 🎯 ЧТО ИСПРАВЛЕНО:

### **Коммит 1: Email опционален**
```
🚨 URGENT FIX: Make email optional in landing form (only name + phone required)
```
- Backend: убрал обязательность email
- Frontend: убрал required атрибут
- AmoCRM: создаёт контакт без email

### **Коммит 2: Лимит лидов увеличен**
```
Fix: Increase lead limit from 100 to 1000 in admin panel
```
- Строка 66: `.limit(100)` → `.limit(1000)`
- Строка 75: `.limit(100)` → `.limit(1000)`

---

## 🆘 ЕСЛИ НЕ РАБОТАЕТ:

### Backend не обновился:
```bash
ssh root@207.154.231.30

# Проверь git статус
cd /var/www/onai-integrator-login-main/backend
git status
git log --oneline -3

# Если нужно - форсированно подтяни
git fetch origin
git reset --hard origin/main

# Перезапусти
pm2 restart onai-backend
pm2 logs onai-backend --lines 100
```

### Frontend не обновился:
```bash
# Полная пересборка
rm -rf dist node_modules/.vite
npm run build

# Проверь что файлы новые
ls -lah dist/assets/index-*.js

# Залей заново
rsync -avz --delete dist/ root@207.154.231.30:/var/www/html/

# Перезапусти Nginx
ssh root@207.154.231.30 "systemctl restart nginx"
```

---

## 📞 ВАЖНО:

- ✅ Изменения уже в GitHub
- ✅ Нужно только подтянуть на сервер
- ✅ Backend на порту 5000
- ✅ Frontend в /var/www/html/

**ДЕПЛОЙ И ПРОВЕРЬ!** 🚀

---

**Создано:** 2025-12-14  
**Статус:** 🚨 ТРЕБУЕТ НЕМЕДЛЕННОГО ДЕПЛОЯ




