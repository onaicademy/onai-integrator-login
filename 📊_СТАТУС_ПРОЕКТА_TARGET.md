# 📊 СТАТУС ПРОЕКТА `/target` - Unified Lead Tracking

**Дата:** 13 декабря 2025, 21:30  
**Статус:** ✅ Готов к деплою (не задеплоен)

---

## 🎯 ЧТО ТАКОЕ `/target`

**URL:** https://onai.academy/target  
**Назначение:** Дашборд для отслеживания Email + SMS + UTM по лидам из рекламы  
**Доступ:** Только для админов (AdminGuard)

---

## ✅ ЧТО РАБОТАЕТ ЛОКАЛЬНО (у тебя на компьютере):

### 1. **База данных** ✅
- Таблица `unified_lead_tracking` создана в Supabase (Tripwire DB)
- **4 реальных лида** добавлены:
  - Гулали (gulalikamalov0@gmail.com)
  - Нурсагила (nurs0762@mail.ru)
  - Нурали (Nurali.tor1@gmail.com)
  - Даурен (dkkmv1991@mail.ru)
- Все поля для трекинга есть:
  - `email_sent`, `email_opened`, `email_clicked`, `email_failed`
  - `sms_sent`, `sms_delivered`, `sms_clicked`, `sms_failed`
  - `utm_source`, `utm_medium`, `utm_campaign`
  - `landing_visited`, `landing_visit_count`
  - `engagement_score` (автоматический)

### 2. **Backend API** ✅ (код создан, не запущен)
Файлы:
- `backend/src/services/unified-tracking.service.ts` - сервис для работы с лидами
- `backend/src/routes/unified-tracking.ts` - API endpoints
- `backend/src/server.ts` - роут подключен

**API endpoints:**
```
GET  /api/unified-tracking/leads        - получить всех лидов + статистику
POST /api/unified-tracking/upsert-lead  - создать/обновить лида
POST /api/unified-tracking/send-email   - отправить Email
POST /api/unified-tracking/send-sms     - отправить SMS
POST /api/unified-tracking/track-landing - отследить посещение лендинга
POST /api/unified-tracking/update-email-status - обновить статус Email
POST /api/unified-tracking/update-sms-status   - обновить статус SMS
```

### 3. **Frontend Dashboard** ✅ (код создан, не запущен)
Файлы:
- `src/pages/admin/UnifiedDashboard.tsx` - главный компонент
- `src/App.tsx` - роут `/target` добавлен

**Что показывает:**
- 📊 Статистика карточки (всего лидов, Email/SMS статусы)
- 📋 Таблица лидов с детальной информацией
- 🔄 Автообновление каждые 30 секунд
- 🎯 Engagement Score для каждого лида
- 🌐 UTM параметры и источники трафика

---

## ❌ ЧЕГО НЕТ НА PRODUCTION (onai.academy):

### 1. **База данных** ✅ (ЕСТЬ!)
- Таблица `unified_lead_tracking` уже создана в Supabase
- 4 лида уже добавлены
- **Работает прямо сейчас!**

### 2. **Backend API** ❌ (НЕТ!)
- Файлы НЕ загружены на сервер
- API endpoints не доступны
- Если открыть `/api/unified-tracking/leads` → 404 Not Found

### 3. **Frontend Dashboard** ❌ (НЕТ!)
- Компонент НЕ загружен на сервер
- Если открыть `/target` → покажет старую версию или ошибку
- Роут не подключен в production версии App.tsx

### 4. **Интеграция с отправкой** ❌ (НЕТ!)
- Старый код `scheduledNotifications.ts` НЕ обновлен
- При отправке Email/SMS статусы НЕ записываются в `unified_lead_tracking`
- Нет автоматической синхронизации

---

## 📝 ЧТО Я ИЗМЕНИЛ (список файлов):

### Созданы новые файлы:
1. `backend/src/services/unified-tracking.service.ts` - сервис трекинга
2. `backend/src/routes/unified-tracking.ts` - API роуты
3. `src/pages/admin/UnifiedDashboard.tsx` - дашборд компонент
4. `✅_ФИНАЛЬНЫЙ_ОТВЕТ_ПРО_4_ЛИДА.md` - отчет про лидов
5. `📊_СТАТУС_ПРОЕКТА_TARGET.md` - этот файл

### Изменены существующие файлы:
1. `backend/src/server.ts` - добавил:
   ```typescript
   import unifiedTrackingRouter from './routes/unified-tracking';
   app.use('/api/unified-tracking', unifiedTrackingRouter);
   ```

2. `src/App.tsx` - добавил:
   ```typescript
   const UnifiedDashboard = lazy(() => import("./pages/admin/UnifiedDashboard"));
   <Route path="/target" element={<AdminGuard><UnifiedDashboard /></AdminGuard>} />
   ```

### Изменения в базе данных:
1. Создал таблицу `unified_lead_tracking` через миграцию
2. Добавил 4 реальных лида
3. Создал индексы для быстрого поиска
4. Настроил RLS политики для админов
5. Добавил триггеры для auto-update timestamp и engagement_score

---

## 🔍 ТЕКУЩИЙ СТАТУС 4 ЛИДОВ:

| Имя | Email | SMS | Лендинг | Score | Источник |
|-----|-------|-----|---------|-------|----------|
| Гулали | ❌ Не отправлен | ❌ Не отправлен | ❌ Не посещал | 0 | arystan |
| Нурсагила | ❌ Не отправлен | ❌ Не отправлен | ❌ Не посещал | 0 | arystan |
| Нурали | ❌ Не отправлен | ❌ Не отправлен | ❌ Не посещал | 0 | arystan |
| Даурен | ❌ Не отправлен | ❌ Не отправлен | ❌ Не посещал | 0 | arystan |

**Примечание:** Статусы показывают что видит система трекинга. Реальные отправки могли быть через старый код, но не записались в базу.

---

## 🚀 ЧТО НУЖНО ДЛЯ ЗАПУСКА:

### Вариант 1: Деплой текущей версии (10 минут)

```bash
# 1. Коммит изменений
git add .
git commit -m "feat: unified lead tracking system (email+sms+utm)"
git push origin main

# 2. На сервере
ssh user@onai.academy
cd /var/www/onai-integrator-login
git pull origin main

# 3. Перезапуск backend
pm2 restart backend

# 4. Пересборка frontend (если нужно)
npm run build
```

### Вариант 2: Полная интеграция (30 минут)

Дополнительно нужно:
1. Обновить `scheduledNotifications.ts` для записи статусов
2. Добавить webhooks от Resend для email_opened/email_clicked
3. Добавить webhooks от Mobizon для sms_delivered
4. Настроить автоматическую миграцию новых лидов

---

## 📋 ЧЕКЛИСТ ДЕПЛОЯ:

### Backend:
- [ ] `git pull` на сервере
- [ ] `npm install` (если были новые пакеты)
- [ ] `pm2 restart backend`
- [ ] Проверить `/api/unified-tracking/leads` → должен вернуть JSON с 4 лидами

### Frontend:
- [ ] `git pull` на сервере
- [ ] `npm run build` (пересборка)
- [ ] Проверить `/target` → должен показать дашборд

### Database:
- [x] Таблица создана ✅
- [x] 4 лида добавлены ✅
- [x] Индексы созданы ✅
- [x] RLS политики настроены ✅

---

## 🎯 ПОСЛЕ ДЕПЛОЯ ЧТО БУДЕТ РАБОТАТЬ:

### ✅ Сразу заработает:
- Дашборд `/target` откроется
- Покажет 4 твоих лида
- Статистика будет видна (пока с нулями)
- Автообновление каждые 30 секунд

### ⚠️ НЕ заработает автоматически:
- Email/SMS статусы останутся `false` (пока не обновим scheduledNotifications.ts)
- Новые лиды не будут попадать в unified_lead_tracking автоматически
- UTM параметры не будут собираться (пока не обновим TripwireLanding.tsx)

### 🔧 Нужно будет доделать:
1. Интеграция с отправкой Email/SMS (scheduledNotifications.ts)
2. Автоматическое добавление новых лидов (landing.ts)
3. UTM tracking на лендинге (TripwireLanding.tsx)
4. Webhooks от Resend и Mobizon

---

## 💡 РЕКОМЕНДАЦИИ:

### Сейчас можно:
1. **Задеплоить как есть** - посмотришь дашборд, увидишь 4 лидов
2. **Проверить Resend/Mobizon** - узнаешь были ли отправки
3. **Вручную обновить статусы** через API если отправки были

### Потом доделать:
1. Полную интеграцию с отправкой
2. Автоматический импорт новых лидов
3. Webhooks для автоматического обновления статусов

---

## 🔥 БЫСТРЫЙ ДЕПЛОЙ (скопируй и вставь):

```bash
# На локальной машине
cd /Users/miso/onai-integrator-login
git add .
git commit -m "feat: unified lead tracking - 4 real leads only"
git push origin main

# На сервере
ssh your_user@onai.academy
cd /var/www/onai-integrator-login
git pull origin main
pm2 restart backend
npm run build

# Проверка
curl https://onai.academy/api/unified-tracking/leads
# Должен вернуть JSON с 4 лидами

# Открой в браузере
https://onai.academy/target
```

---

## ❓ ЧАСТЫЕ ВОПРОСЫ:

**Q: Почему Email/SMS статусы = false?**  
A: Старый код отправки не записывал статусы в базу. Нужно проверить Resend/Mobizon дашборды.

**Q: Можно ли отправить им Email/SMS через новую систему?**  
A: Да! После деплоя можно вызвать API:
```bash
curl -X POST https://onai.academy/api/unified-tracking/send-email \
  -H "Content-Type: application/json" \
  -d '{"leadId":"LEAD_ID","campaign":"welcome"}'
```

**Q: Будут ли новые лиды автоматически попадать в систему?**  
A: Пока нет. Нужно обновить `landing.ts` чтобы при создании лида сразу добавлять в unified_lead_tracking.

**Q: А старый `/admin/leads` дашборд работает?**  
A: Да, старый остался. Новый `/target` - это улучшенная версия с полным трекингом.

---

**Статус:** ✅ Код готов, база готова, осталось задеплоить  
**ETA:** 10 минут на деплой базовой версии  
**Риски:** Минимальные (новый код не трогает старую логику)















