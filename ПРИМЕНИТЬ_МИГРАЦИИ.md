# 📋 ПРИМЕНИТЬ 9 МИГРАЦИЙ В SUPABASE

**Время:** 15 минут  
**Важность:** 🔴 КРИТИЧНО

---

## 🎯 ЧТО НУЖНО СДЕЛАТЬ:

Применить 9 SQL файлов к базе данных Supabase **В СТРОГОМ ПОРЯДКЕ**

---

## 📝 ИНСТРУКЦИЯ:

### ШАГ 1: Открой Supabase Dashboard

```
https://supabase.com/dashboard/project/capdjvokjdivxjfddmx
```

Логин → Твой проект → SQL Editor (слева в меню)

---

### ШАГ 2: Применить миграции ПО ПОРЯДКУ

#### Миграция 1:
```bash
cat supabase/migrations/20251103164013_*.sql
```
→ Скопируй весь текст  
→ SQL Editor → New Query → Вставь  
→ Run (или Cmd/Ctrl + Enter)  
→ Жди "Success"

#### Миграция 2:
```bash
cat supabase/migrations/20251103182016_*.sql
```
→ Повтори процесс

#### Миграция 3:
```bash
cat supabase/migrations/20251104_add_diagnostics_tables.sql
```

#### Миграция 4:
```bash
cat supabase/migrations/20250115_add_ai_curator_chat_tables.sql
```

#### Миграция 5:
```bash
cat supabase/migrations/20250116_achievements_system.sql
```

#### Миграция 6:
```bash
cat supabase/migrations/20250117_students_management_system.sql
```

#### Миграция 7:
```bash
cat supabase/migrations/20250118_ai_mentor_and_analyst.sql
```

#### Миграция 8:
```bash
cat supabase/migrations/20250109_token_usage_tracking.sql
```

#### Миграция 9:
```bash
cat supabase/migrations/20250110_student_messaging.sql
```

---

## ✅ КАК ПРОВЕРИТЬ ЧТО ВСЁ ПРИМЕНИЛОСЬ:

### В Supabase Dashboard:

```
Table Editor (слева) → Должны появиться таблицы:

✅ ai_curator_threads
✅ ai_curator_messages
✅ ai_curator_attachments
✅ ai_token_usage
✅ ai_token_usage_daily
✅ student_private_chats
✅ student_private_messages
✅ user_achievements
✅ user_statistics
✅ ... (всего ~26 таблиц)
```

### Или через SQL:

```sql
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public'
ORDER BY table_name;
```

→ Run → Должно быть ~26 таблиц

---

## 🚨 ЕСЛИ ОШИБКА:

### "relation already exists"
```
Это нормально! Значит таблица уже создана
→ Продолжай дальше
```

### "syntax error"
```
1. Проверь что скопировал весь файл целиком
2. Проверь что применяешь в правильном порядке
3. Если не помогает → напиши мне
```

### "permission denied"
```
1. Проверь что ты админ проекта
2. Проверь что SQL Editor открыт
3. Попробуй через Dashboard → Database → SQL Editor
```

---

## ⏱️ ВРЕМЯ НА КАЖДУЮ МИГРАЦИЮ:

```
Миграция 1: ~30 секунд
Миграция 2: ~30 секунд
Миграция 3: ~20 секунд
Миграция 4: ~1 минута (AI-куратор)
Миграция 5: ~2 минуты (достижения)
Миграция 6: ~1 минута (ученики)
Миграция 7: ~2 минуты (AI-наставник/аналитик)
Миграция 8: ~1 минута (токены)
Миграция 9: ~1 минута (чат)

ИТОГО: ~10-15 минут
```

---

## 🎉 ПОСЛЕ ПРИМЕНЕНИЯ:

### ШАГ 1: Перезапусти сервер

```bash
# Ctrl+C
npm run dev
```

### ШАГ 2: Открой страницы

```
http://localhost:8080/admin/token-usage
→ Должны загружаться данные (пока пустые)

http://localhost:8080/admin/students-activity
→ Должны загружаться ученики (пока пустые)
```

### ШАГ 3: Протестируй логирование

```
1. Открой http://localhost:8080/profile
2. Пообщайся с AI-куратором
3. Открой http://localhost:8080/admin/token-usage
4. Обнови страницу → Должны появиться токены!
```

---

## 📊 ЧТО БУДЕТ ПОСЛЕ МИГРАЦИЙ:

```
✅ 26 таблиц созданы
✅ RLS policies активны
✅ Функции работают
✅ Логирование токенов работает
✅ Чат готов к использованию
✅ Достижения готовы
✅ AI-наставник готов (отключён)
✅ AI-аналитик готов (отключён)
```

---

**ПРИМЕНЯЙ МИГРАЦИИ СЕЙЧАС!** 🚀

**Открой:** https://supabase.com/dashboard/project/capdjvokjdivxjfddmx

**Следуй инструкции выше!**

---

**P.S.** Если что-то непонятно или не работает → напиши мне, помогу! 💪

