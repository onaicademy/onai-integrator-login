# 🚀 ДЕПЛОЙ НА PRODUCTION (Digital Ocean)

**Дата:** 10 ноября 2025  
**Версия:** После исправления загрузки списка студентов  
**Коммит:** `5bd1f79` - fix: restore student list loading from profiles table

---

## ✅ Что готово для деплоя

### Исправления:
- ✅ Список студентов загружается (15 пользователей)
- ✅ Создание пользователей работает
- ✅ Авторизация работает корректно
- ✅ Роли определяются правильно (admin/student)
- ✅ Нет критичных ошибок в консоли

### Build:
- ✅ Production build создан (`npm run build`)
- ✅ Размер bundle: ~1.8 MB (minified + gzipped)
- ✅ Все assets в папке `dist/`

### Git:
- ✅ Изменения закоммичены
- ✅ Push на GitHub выполнен
- ✅ Репозиторий: `onaicademy/onai-integrator-login`

---

## 📋 Шаги для деплоя

### Вариант 1: Через SSH (Рекомендуется)

```bash
# 1. Подключись к серверу
ssh root@your-server-ip

# 2. Перейди в папку проекта
cd /var/www/onai-academy

# 3. Подтяни изменения с GitHub
git pull origin main

# 4. Установи зависимости (если нужно)
npm install

# 5. Собери production build
npm run build

# 6. Перезапусти Nginx (если нужно)
sudo systemctl reload nginx

# 7. Проверь статус
sudo systemctl status nginx
```

### Вариант 2: Через скрипт деплоя

Используй готовый скрипт `deploy.sh`:

```bash
# Из локальной машины запусти:
./deploy.sh
```

**Или PowerShell версию:**
```powershell
.\deploy.ps1
```

---

## 🔧 Настройка Nginx (если нужно)

Убедись что Nginx настроен для SPA:

```nginx
server {
    listen 80;
    server_name onai.academy www.onai.academy;

    root /var/www/onai-academy/dist;
    index index.html;

    # SPA routing - все запросы отправляем на index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Assets кешируем
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Compression
    gzip on;
    gzip_types text/css application/javascript application/json image/svg+xml;
    gzip_comp_level 6;
}
```

---

## 🌐 После деплоя проверь

### 1. Главная страница
```
https://onai.academy/
```
Должен открыться экран логина

### 2. Админ-панель
```
https://onai.academy/admin/students-activity
```
После логина должен загрузиться список из 15 студентов

### 3. Создание пользователя
- Открой админ-панель
- Нажми "Добавить пользователя"
- Заполни форму
- Проверь что пользователь создался

### 4. Выход из профиля
- Открой `/settings`
- Нажми "Выйти"
- Должен редиректнуть на `/login`

---

## 🐛 Если что-то не работает

### Проблема: 404 на роутах (например /admin)
**Решение:** Проверь Nginx конфиг - должен быть `try_files $uri $uri/ /index.html;`

### Проблема: Assets не загружаются
**Решение:** Проверь права на папку `dist`:
```bash
chmod -R 755 /var/www/onai-academy/dist
chown -R www-data:www-data /var/www/onai-academy/dist
```

### Проблема: "Invalid API key"
**Решение:** Проверь `.env` на сервере - должны быть правильные ключи Supabase

### Проблема: Список студентов не загружается
**Решение:** Проверь консоль браузера (F12) - там будут логи с точной ошибкой

---

## 📊 Мониторинг

После деплоя проверь логи:

```bash
# Логи Nginx
sudo tail -f /var/log/nginx/access.log
sudo tail -f /var/log/nginx/error.log

# Логи приложения (если используешь PM2)
pm2 logs onai-academy
```

---

## 🔄 Откат (если нужно)

Если что-то пошло не так:

```bash
# 1. Вернись на предыдущий коммит
git reset --hard HEAD~1

# 2. Пересобери
npm run build

# 3. Перезапусти Nginx
sudo systemctl reload nginx
```

---

## ✅ Чеклист после деплоя

- [ ] Сайт открывается по https://onai.academy
- [ ] Логин работает
- [ ] Админ-панель загружается
- [ ] Список студентов показывает 15 пользователей
- [ ] Можно создать нового пользователя
- [ ] Выход из профиля работает
- [ ] Нет ошибок в консоли браузера
- [ ] Нет ошибок в логах Nginx

---

## 📝 Информация о сервере

**IP:** (вставь IP твоего Digital Ocean дроплета)  
**SSH:** `ssh root@your-server-ip`  
**Путь к проекту:** `/var/www/onai-academy`  
**Nginx конфиг:** `/etc/nginx/sites-available/onai-academy`

---

## 🎯 Готово к деплою!

Всё протестировано на localhost и работает корректно.
Можешь деплоить на production! 🚀

