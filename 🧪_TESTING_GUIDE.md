# 🧪 TRAFFIC DASHBOARD - РУКОВОДСТВО ПО ТЕСТИРОВАНИЮ

**Дата:** 19 декабря 2025, 22:15 UTC+6  
**Статус:** ✅ Миграции применены - Ready for testing

---

## ✅ ЧТО УЖЕ СДЕЛАНО

- [x] **Миграции применены** - Все 5 миграций успешно
- [x] **База данных готова** - 4 команды, 5 пользователей
- [x] **Backend запущен** - Traffic routes работают
- [x] **Frontend code готов** - Все страницы написаны

---

## 🚀 КАК ТЕСТИРОВАТЬ

### 1️⃣ Запустить Backend (если не запущен)

```bash
# Terminal 1
cd /Users/miso/onai-integrator-login/backend
npm run dev
```

**Ожидаемый output:**
```
✅ Traffic Dashboard schedulers initialized
✅ All background services initialized
⚠️ Redis warnings (игнорируй - не критично)
```

**Проверка:**
- Открой браузер: `http://localhost:3000/health`
- Должен показать: `{"status":"ok",...}`

---

### 2️⃣ Запустить Frontend

```bash
# Terminal 2 (новый терминал)
cd /Users/miso/onai-integrator-login
npm run dev
```

**Ожидаемый output:**
```
VITE v5.x.x ready in XXX ms
➜  Local:   http://localhost:8080/
```

---

### 3️⃣ Открыть Login Page

**URL:** `http://localhost:8080/traffic/login`

**Ожидаемый результат:**
- ✅ Страница загрузилась
- ✅ Черный фон + неоново-зеленые элементы
- ✅ Форма логина (Email + Password)
- ✅ Кнопка "Войти"

---

### 4️⃣ Логин как Admin

**Credentials:**
```
Email: admin@onai.academy
Password: [пароль админа из БД]
```

**Если не знаешь пароль:**
```sql
-- В Supabase SQL Editor:
SELECT email, role FROM traffic_users WHERE role = 'admin';
```

**Или создай нового админа:**
```sql
-- Пароль: admin123 (bcrypt hash ниже)
INSERT INTO traffic_users (email, password_hash, role, team_name, full_name)
VALUES (
  'test-admin@onai.academy',
  '$2b$10$YourBcryptHashHere',
  'admin',
  'onai_admin',
  'Тестовый Админ'
);
```

---

### 5️⃣ Проверить Admin Panel

**После логина должно открыться:** `/traffic/admin`

**Что проверить:**

#### Вкладка "Дашборд"
- [ ] Статистика показывается (Users, Teams, Plans)
- [ ] Quick Actions карточки видны
- [ ] Нет ошибок в Console (F12)

#### Вкладка "Пользователи"
- [ ] Таблица пользователей загружается
- [ ] Показываются 5 пользователей
- [ ] Кнопка 📤 "Send Credentials" видна
- [ ] Email, Team, Role отображаются правильно

#### Вкладка "Настройки AI"
- [ ] Поля growth_percentage, roas_target, cpa_target
- [ ] Можно ввести значения
- [ ] Кнопка "Сохранить" работает

#### Вкладка "Генерация планов"
- [ ] Кнопка "Генерировать планы для всех команд"
- [ ] Показывается статус генерации

---

### 6️⃣ Проверить Team Constructor

**URL:** `/traffic/admin/team-constructor`

**Что проверить:**
- [ ] Dropdown "Команда" показывает **4 команды** (Kenesary, Arystan, Muha, Traf4)
- [ ] Эмодзи команд отображаются правильно
- [ ] Форма создания пользователя работает
- [ ] Checkbox "Отправить данные на email" есть
- [ ] Кнопка "Создать пользователя" активна

**Попробуй создать тестового пользователя:**
```
Email: test-targetologist@example.com
Full Name: Тестовый Таргетолог
Role: targetologist
Team: Kenesary
Password: test123
[ ] Отправить email (НЕ ставь галочку для теста)
```

**Ожидаемый результат:**
- ✅ Пользователь создан
- ✅ Показывается success message
- ✅ Таблица пользователей обновилась

---

### 7️⃣ Проверить Settings Page

**URL:** `/traffic/settings`

**Что проверить:**

#### Секция "UTM Traffic Sources"
- [ ] Показываются 5 источников (Facebook, YouTube, TikTok, Google, Instagram)
- [ ] Можно toggle каждый источник (включить/выключить)
- [ ] Можно ввести UTM label (например: `fb_kenesary`)
- [ ] Кнопка "Сохранить настройки" работает

**Попробуй:**
1. Включи Facebook Ads
2. Введи UTM label: `fb_test`
3. Нажми "Сохранить"
4. Перезагрузи страницу (F5)
5. Проверь что настройки сохранились

#### Секция "Facebook Ad Accounts" (collapsible)
- [ ] Можно развернуть/свернуть
- [ ] Кнопка "Load FB Accounts" работает
- [ ] Если есть токен - показывает аккаунты

---

### 8️⃣ Проверить Security Panel

**URL:** `/traffic/security`

**Что проверить:**
- [ ] Страница открывается без ошибок
- [ ] Показывается Empty State (т.к. логов пока нет)
- [ ] Нет 500 error
- [ ] Console чистый (нет критичных ошибок)

**После нескольких логинов:**
- [ ] Таблица логов заполняется
- [ ] Показывается IP, device, browser
- [ ] Время логина корректное

---

## 🐛 ЧЕКЛИСТ ВОЗМОЖНЫХ ПРОБЛЕМ

### Проблема: Команды не загружаются

**Проверка:**
```sql
SELECT COUNT(*) FROM traffic_teams;
-- Должно быть: 4
```

**Решение:**
- Если 0 - миграция не применилась, примени вручную
- Если 4 - проверь API endpoint

### Проблема: Login не работает

**Проверка:**
```sql
SELECT email, role FROM traffic_users WHERE role = 'admin';
```

**Решение:**
- Проверь что пользователь существует
- Проверь пароль (bcrypt hash правильный)
- Проверь токен в localStorage: `localStorage.getItem('traffic_token')`

### Проблема: Settings не сохраняются

**Проверка:**
```sql
SELECT * FROM traffic_targetologist_settings;
```

**Решение:**
- Таблица должна существовать
- Проверь backend логи
- Проверь Network tab (Chrome DevTools)

### Проблема: 500 errors в API

**Проверка:**
- Проверь backend логи: `tail -50 /tmp/backend-clean.log`
- Проверь .env: `cat backend/env.env | grep TRIPWIRE`

**Решение:**
- Перезапусти backend
- Проверь что таблицы существуют в БД

---

## ✅ SUCCESS CRITERIA

**Тестирование пройдено успешно если:**

- [x] Миграции применены (4 команды в БД)
- [ ] Backend отвечает на `/health`
- [ ] Teams API возвращает 4 команды
- [ ] Frontend открывается на :8080
- [ ] Login работает
- [ ] Admin panel загружается
- [ ] Team Constructor показывает команды из БД
- [ ] Settings сохраняются
- [ ] Нет критичных ошибок в Console

---

## 🎯 AFTER TESTING

**Когда всё протестировано:**

1. **Зафиксируй результаты**
   - Создай `TESTING_RESULTS.md` с чеклистом
   - Запиши найденные баги

2. **Начни Critical Tasks**
   - Task #1: Security Panel Empty State
   - Task #2: UTM Sources Real Data
   - Task #3: Admin Panel Real Stats

3. **Потом Deploy**
   - См. `TRAFFIC_DEPLOY_PRODUCTION.md`

---

## 📊 CHECKLIST

### Pre-Testing
- [x] Миграции применены
- [x] Backend запущен
- [ ] Frontend запущен
- [ ] Логи backend чистые

### During Testing
- [ ] Login работает
- [ ] Все routes доступны
- [ ] API calls успешные (200 OK)
- [ ] Данные загружаются из БД
- [ ] Console без критичных ошибок

### Post-Testing
- [ ] Все issues задокументированы
- [ ] Critical bugs исправлены
- [ ] Ready for production deploy

---

## 🚀 QUICK COMMANDS

```bash
# Start backend
cd /Users/miso/onai-integrator-login/backend && npm run dev

# Start frontend (new terminal)
cd /Users/miso/onai-integrator-login && npm run dev

# Check backend
curl http://localhost:3000/health

# Check teams API
curl http://localhost:3000/api/traffic-constructor/teams

# Open frontend
open http://localhost:8080/traffic/login

# Check database
# В Supabase SQL Editor:
SELECT name FROM traffic_teams;
```

---

**Created:** 2025-12-19 22:15  
**Status:** Ready for testing  
**Next:** Test all features → Fix issues → Deploy

**Удачи с тестированием!** 🚀
