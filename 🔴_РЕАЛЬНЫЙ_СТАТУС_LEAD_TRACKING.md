# 🔴 РЕАЛЬНЫЙ СТАТУС: Lead Tracking System

## ⚠️ КРИТИЧНО: НА ПРОДЕ НИЧЕГО НЕ РАБОТАЕТ!

Я создал код локально, но **НЕ ЗАДЕПЛОИЛ**. Извини за путаницу.

---

## 📊 ЧТО РЕАЛЬНО СДЕЛАНО (локально):

### ✅ 1. База данных (Supabase Tripwire)
**Статус:** РАБОТАЕТ ✅

```sql
-- Таблица создана и готова
CREATE TABLE lead_tracking (
  id UUID PRIMARY KEY,
  full_name TEXT,
  email TEXT,
  phone TEXT,
  source TEXT,
  
  -- AmoCRM
  amocrm_deal_id TEXT,
  amocrm_contact_id TEXT,
  amocrm_status TEXT,
  
  -- Email статусы
  email_sent BOOLEAN,
  email_sent_at TIMESTAMPTZ,
  email_opened BOOLEAN,
  email_error TEXT,
  
  -- SMS статусы
  sms_sent BOOLEAN,
  sms_sent_at TIMESTAMPTZ,
  sms_delivered BOOLEAN,
  sms_error TEXT,
  
  -- UTM и переходы
  utm_source TEXT,
  utm_medium TEXT,
  utm_campaign TEXT,
  landing_visited BOOLEAN,
  landing_visits_count INTEGER,
  email_link_clicked BOOLEAN,
  sms_link_clicked BOOLEAN,
  traffic_source TEXT,
  
  created_at TIMESTAMPTZ,
  updated_at TIMESTAMPTZ
);
```

**Проверка:**
```bash
# Таблица существует, 0 записей (тестовые удалены)
SELECT COUNT(*) FROM lead_tracking; -- Результат: 0
```

### ✅ 2. Backend API код
**Статус:** НАПИСАН, НО НЕ ЗАПУЩЕН ❌

**Файл:** `backend/src/routes/lead-tracking.ts` (новый, 663 строки)

**Endpoints:**
- `GET /api/lead-tracking/leads` - получить всех лидов + статистику
- `POST /api/lead-tracking/sync` - синхронизация с amoCRM
- `POST /api/lead-tracking/track-click` - отслеживание клика по ссылке
- `POST /api/lead-tracking/update-email-status` - обновить статус Email
- `POST /api/lead-tracking/update-sms-status` - обновить статус SMS
- `GET /api/lead-tracking/stats` - детальная статистика

**Проблема:** Backend не перезапущен, endpoints НЕ работают!

### ✅ 3. Frontend дашборд
**Статус:** НАПИСАН, НО НЕ СОБРАН ❌

**Файл:** `src/pages/admin/LeadTracking.tsx` (новый, 476 строк)

**URL:** `/target`

**Что показывает:**
- 11 метрик (всего лидов, Email, SMS, переходы, клики)
- Таблица лидов с UTM данными
- Автообновление каждые 30 сек
- Кнопка синхронизации с amoCRM

**Проблема:** Frontend не собран, страница НЕ работает!

### ✅ 4. Роуты подключены
**Файлы изменены:**
- `backend/src/server.ts` - добавлен импорт и роут
- `src/App.tsx` - добавлен роут `/target`

---

## 🚨 ЧТО НЕ РАБОТАЕТ:

### ❌ 1. Backend API
```bash
curl https://onai.academy/api/lead-tracking/leads
# Вернет: 404 Not Found (роут не существует)
```

**Причина:** Backend не перезапущен после изменений

### ❌ 2. Frontend дашборд
```bash
https://onai.academy/target
# Вернет: 404 или белая страница
```

**Причина:** Frontend не собран после изменений

### ❌ 3. Синхронизация с amoCRM
```bash
# Нет данных о 4 лидах из CRM
SELECT COUNT(*) FROM lead_tracking WHERE source = 'amocrm';
# Результат: 0
```

**Причина:** API не работает, синхронизация не запущена

### ❌ 4. Отслеживание кликов
```bash
# Нельзя отследить кто перешел по ссылке
```

**Причина:** API не работает, трекинг код не добавлен на лендинг

---

## 🔧 КАК ИСПРАВИТЬ (пошагово):

### Шаг 1: Git Commit (на локалке)
```bash
cd /Users/miso/onai-integrator-login

# Добавить новые файлы
git add backend/src/routes/lead-tracking.ts
git add src/pages/admin/LeadTracking.tsx
git add backend/src/server.ts
git add src/App.tsx
git add LEAD_TRACKING_SYSTEM.md
git add UTM_TRACKING_GUIDE.md
git add DEPLOY_LEAD_TRACKING.md

# Коммит
git commit -m "feat: add lead tracking dashboard with UTM analytics

- Created lead_tracking table in Supabase
- Added backend API endpoints for lead management
- Built admin dashboard at /target route
- Added UTM tracking for email/sms campaigns
- Implemented click tracking system
- Added amoCRM sync functionality"

# Пуш на GitHub
git push origin main
```

### Шаг 2: Деплой на сервер (SSH)
```bash
# Подключиться к серверу
ssh user@your-server

# Перейти в директорию проекта
cd /var/www/onai-integrator-login

# Стянуть изменения
git pull origin main

# КРИТИЧНО: Перезапустить backend
pm2 restart backend

# Проверить что запустился
pm2 logs backend --lines 50

# Если нужно - собрать frontend
npm run build
pm2 restart frontend
```

### Шаг 3: Проверка что работает
```bash
# 1. Проверить API
curl https://onai.academy/api/lead-tracking/leads

# Должен вернуть:
# {"success":true,"stats":{...},"leads":[]}

# 2. Проверить синхронизацию
curl -X POST https://onai.academy/api/lead-tracking/sync

# Должен подтянуть лидов из amoCRM

# 3. Открыть дашборд
open https://onai.academy/target

# Должен показать страницу с метриками
```

---

## 📋 ЗАПРОС ДЛЯ AI АРХИТЕКТОРА:

```
Контекст:
Я работаю над системой отслеживания лидов для платформы onai.academy.

Что сделано:
1. Создана таблица lead_tracking в Supabase (Tripwire база)
2. Написан backend API в файле backend/src/routes/lead-tracking.ts
3. Создан frontend дашборд в src/pages/admin/LeadTracking.tsx
4. Роуты подключены в server.ts и App.tsx

Проблема:
Изменения локальные, не задеплоены. Backend использует Express.js + TypeScript, 
роуты подключаются в backend/src/server.ts. Frontend - React + Vite + TypeScript.

Технологии:
- Backend: Node.js, Express, TypeScript, Supabase, Axios
- Frontend: React 18, TypeScript, Vite, TanStack Query, Tailwind CSS
- Deploy: PM2 на VPS сервере
- База: Supabase (PostgreSQL)

Задача:
Нужно задеплоить систему на продакшн сервер. После деплоя:
1. Проверить работу API endpoints
2. Синхронизировать 4 лида из amoCRM
3. Убедиться что дашборд /target работает
4. Настроить отслеживание кликов по ссылкам из Email/SMS

Вопросы:
1. Какие команды выполнить для безопасного деплоя?
2. Как проверить что backend правильно подхватил новый роут?
3. Нужно ли перезапускать nginx или достаточно pm2 restart backend?
4. Как протестировать работу amoCRM синхронизации?
5. Какие логи проверить если что-то пойдет не так?

Дополнительно:
У нас уже есть работающие роуты в server.ts:
- /api/landing
- /api/tripwire
- /api/admin
Новый роут /api/lead-tracking нужно добавить аналогично.

AmoCRM настройки в .env:
AMOCRM_DOMAIN=onaiagencykz.amocrm.ru
AMOCRM_ACCESS_TOKEN=[токен]
AMOCRM_PIPELINE_ID=10350882

Дай пошаговую инструкцию для деплоя с проверками на каждом этапе.
```

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ (в порядке важности):

### 1. ДЕПЛОЙ (критично!)
- [ ] Сделать git commit локально
- [ ] Пушнуть на GitHub
- [ ] SSH на сервер
- [ ] git pull
- [ ] pm2 restart backend
- [ ] Проверить логи: `pm2 logs backend`

### 2. ПРОВЕРКА API
- [ ] Тест: `curl https://onai.academy/api/lead-tracking/leads`
- [ ] Должен вернуть JSON с пустым массивом leads
- [ ] Если 404 - роут не подключен
- [ ] Если 500 - ошибка в коде, смотреть логи

### 3. СИНХРОНИЗАЦИЯ amoCRM
- [ ] Открыть дашборд: https://onai.academy/target
- [ ] Нажать "Синхронизировать с AmoCRM"
- [ ] Проверить что появились 4 лида
- [ ] Проверить что у них есть amocrm_deal_id

### 4. НАСТРОЙКА ТРЕКИНГА
- [ ] Добавить код отслеживания на лендинг (см. UTM_TRACKING_GUIDE.md)
- [ ] Настроить Email с UTM ссылками
- [ ] Настроить SMS с UTM ссылками
- [ ] Протестировать клик по ссылке

---

## 🔍 ДИАГНОСТИКА ПРОБЛЕМ:

### Проблема: API возвращает 404
```bash
# Проверить что роут подключен
ssh user@server
cd /var/www/onai-integrator-login
grep -n "lead-tracking" backend/src/server.ts

# Должно быть:
# import leadTrackingRouter from './routes/lead-tracking';
# app.use('/api/lead-tracking', leadTrackingRouter);

# Если нет - git pull не выполнен
git status
git pull origin main
pm2 restart backend
```

### Проблема: API возвращает 500
```bash
# Смотреть логи
pm2 logs backend --lines 100

# Типичные ошибки:
# - Cannot find module './routes/lead-tracking'
#   Решение: проверить что файл существует
# - Supabase connection error
#   Решение: проверить .env переменные
# - AmoCRM token expired
#   Решение: обновить AMOCRM_ACCESS_TOKEN
```

### Проблема: Frontend показывает белую страницу
```bash
# Пересобрать frontend
cd /var/www/onai-integrator-login
npm run build
pm2 restart frontend

# Проверить что роут добавлен в App.tsx
grep -n "LeadTracking" src/App.tsx
grep -n "/target" src/App.tsx
```

### Проблема: amoCRM не возвращает лидов
```bash
# Проверить токен
curl -H "Authorization: Bearer $AMOCRM_ACCESS_TOKEN" \
  https://onaiagencykz.amocrm.ru/api/v4/leads?limit=5

# Если 401 - токен истек, нужен refresh
# Если 403 - нет прав доступа
# Если 200 - токен работает, проверить код синхронизации
```

---

## 💾 БЭКАП ПЛАН (если что-то сломается):

### Откат изменений:
```bash
# На сервере
cd /var/www/onai-integrator-login
git log --oneline | head -5
git reset --hard <ПРЕДЫДУЩИЙ_КОММИТ>
pm2 restart backend
```

### Удаление таблицы (если нужно):
```sql
-- В Supabase SQL Editor
DROP TABLE IF EXISTS lead_tracking CASCADE;
```

---

## 📊 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ (после деплоя):

### 1. Дашборд /target работает
- Показывает статистику: 0/0/0/0 (нет данных)
- Кнопки работают
- Нет ошибок в консоли

### 2. Синхронизация работает
- После клика "Синхронизировать" появляются 4 лида
- У каждого лида есть:
  - Имя
  - Email (если есть)
  - Телефон (если есть)
  - amoCRM Deal ID
  - Статус сделки

### 3. API отвечает
```bash
curl https://onai.academy/api/lead-tracking/leads
# {"success":true,"stats":{"total_leads":4,...},"leads":[...]}
```

### 4. Данные о 4 лидах
После синхронизации можно проверить:
```sql
SELECT 
  full_name,
  email,
  phone,
  amocrm_deal_id,
  email_sent,
  sms_sent
FROM lead_tracking
ORDER BY created_at DESC;
```

---

## 🎯 ЧТО ДАЛЬШЕ (после деплоя):

1. **Проверить Email/SMS статусы у 4 лидов**
   - Были ли отправлены письма?
   - Были ли отправлены SMS?
   - Кто открыл письма?

2. **Настроить Email рассылку с UTM**
   - Добавить UTM параметры в ссылки
   - Настроить webhook для отслеживания открытий
   - Тестировать на себе

3. **Настроить SMS рассылку с UTM**
   - Добавить UTM параметры в короткие ссылки
   - Настроить отслеживание доставки
   - Тестировать на себе

4. **Добавить трекинг на лендинг**
   - Код из UTM_TRACKING_GUIDE.md
   - Тестировать переходы
   - Проверять метрики в дашборде

---

## 📞 ЕСЛИ НУЖНА ПОМОЩЬ:

### Вариант 1: Другой AI
Скопируй раздел "ЗАПРОС ДЛЯ AI АРХИТЕКТОРА" выше и отправь ChatGPT/Claude/другому AI.

### Вариант 2: Разработчик
Покажи этот файл разработчику. Все файлы и изменения описаны.

### Вариант 3: Я помогу
Скажи что именно не работает, покажу команды для исправления.

---

**Создано:** 13 декабря 2025, 21:00  
**Статус:** 🔴 НЕ РАБОТАЕТ НА ПРОДЕ  
**Что нужно:** Деплой (git push + pm2 restart backend)






