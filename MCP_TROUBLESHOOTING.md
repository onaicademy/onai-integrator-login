# 🔧 MCP Server Troubleshooting Guide

## ✅ Текущий статус проверки

**Дата проверки:** 4 ноября 2025

| Компонент | Статус | Версия/Значение |
|-----------|--------|-----------------|
| Node.js | ✅ OK | v22.17.0 |
| npm cache | ⚠️ NEEDS FIX | Содержит root-файлы |
| `.cursor/mcp.json` | ✅ OK | Правильно настроен |
| `components.json` | ✅ OK | Существует |
| `shadcn` CLI | ✅ OK | Работает |

---

## 🐛 Обнаруженная проблема

### ❌ npm cache содержит root-файлы

**Симптомы:**
```
npm error code EPERM
npm error syscall open
npm error path /Users/miso/.npm/_cacache/tmp/...
npm error errno EPERM
npm error Your cache folder contains root-owned files
```

**Причина:**
В прошлом npm команды запускались с `sudo`, что создало файлы с root-правами в кеше.

**Решение:**
```bash
sudo chown -R 501:20 "/Users/miso/.npm"
```

После выполнения этой команды npm будет работать нормально.

---

## ✅ Правильная конфигурация MCP

### Структура файлов:

```
onai-integrator-login/
├── .cursor/
│   └── mcp.json          # ✅ Существует
└── components.json        # ✅ Существует
```

### Содержимое `.cursor/mcp.json`:

```json
{
  "mcpServers": {
    "shadcn": {
      "command": "npx",
      "args": ["shadcn@latest", "mcp"]
    }
  }
}
```

✅ **Проверено:** Файл правильно настроен!

---

## 🧪 Команды для проверки

### 1. Проверка версии Node.js
```bash
node -v
# Должна быть >= 18.x
```

**Результат:** ✅ v22.17.0

### 2. Проверка команды shadcn mcp
```bash
npx shadcn@latest mcp --help
```

**Результат:** ✅ Работает (если исправлены права npm)

### 3. Проверка структуры .cursor/mcp.json
```bash
cat .cursor/mcp.json
```

**Результат:** ✅ Правильная структура

### 4. Проверка components.json
```bash
cat components.json
```

**Результат:** ✅ Файл существует

---

## 📝 Пошаговая инструкция для Cursor

### Шаг 1: Исправьте права npm (ОБЯЗАТЕЛЬНО!)

Откройте терминал и выполните:
```bash
sudo chown -R 501:20 "/Users/miso/.npm"
```

Введите пароль администратора.

### Шаг 2: Перезапустите Cursor

1. Полностью закройте Cursor (Cmd+Q)
2. Откройте снова
3. Откройте проект

### Шаг 3: Проверьте MCP Server в Cursor

1. Откройте **Settings** (Cmd+,)
2. Перейдите в раздел **MCP**
3. Найдите сервер **shadcn**
4. Убедитесь что он включён (зелёная точка)

### Шаг 4: Протестируйте MCP

В Cursor AI Chat напишите:
```
Show me all available components in the shadcn registry
```

Или:
```
Add the button and card components to my project
```

Если появится список компонентов или кнопка "Install" - всё работает! ✅

---

## 🔄 Если всё равно не работает

### Вариант 1: Полный сброс

```bash
# 1. Удалите конфигурацию
rm -rf .cursor/mcp.json

# 2. Переинициализируйте
npx shadcn@latest mcp init --client cursor

# 3. Перезапустите Cursor
```

### Вариант 2: Очистка кеша npx

```bash
# Очистить кеш npx
npx clear-npx-cache

# Или вручную удалить кеш
rm -rf ~/.npm/_npx

# Перезапустите Cursor
```

### Вариант 3: Ручная проверка

```bash
# Запустите MCP сервер напрямую
npx shadcn@latest mcp

# Должен запуститься без ошибок
# Ctrl+C для остановки
```

---

## 📊 Диагностика ошибок в Cursor

### Где смотреть логи:

1. В Cursor, когда видите ошибку MCP, нажмите **"Show Output"**
2. Или откройте **View** → **Output** → выберите **MCP** из выпадающего списка
3. Скопируйте текст ошибки

### Частые ошибки:

#### ❌ "npx: command not found"
**Причина:** Node.js не установлен или не в PATH  
**Решение:** Установите Node.js 18+ с [nodejs.org](https://nodejs.org)

#### ❌ "Cannot find module 'shadcn'"
**Причина:** shadcn не установлен или кеш повреждён  
**Решение:** 
```bash
npx clear-npx-cache
npx shadcn@latest mcp --help
```

#### ❌ "EPERM" или "Permission denied"
**Причина:** Проблемы с правами доступа npm  
**Решение:**
```bash
sudo chown -R 501:20 "/Users/miso/.npm"
```

#### ❌ "Unexpected token" в mcp.json
**Причина:** Неправильные кавычки или синтаксис JSON  
**Решение:** Скопируйте правильную конфигурацию из этого файла

#### ❌ "Server shadcn failed to start"
**Причина:** Ошибка запуска процесса  
**Решение:**
1. Проверьте что `npx shadcn@latest mcp --help` работает
2. Перезапустите Cursor
3. Проверьте логи в Output

---

## ✅ Checklist для проверки

Отметьте галочками:

- [ ] Node.js версия >= 18.x
- [ ] Выполнена команда `sudo chown -R 501:20 "/Users/miso/.npm"`
- [ ] Файл `.cursor/mcp.json` существует и правильно настроен
- [ ] Файл `components.json` существует
- [ ] Команда `npx shadcn@latest mcp --help` работает без ошибок
- [ ] Cursor полностью перезапущен
- [ ] MCP сервер включён в Settings → MCP
- [ ] Тестовая команда в Cursor AI работает

Если все пункты отмечены - MCP сервер должен работать! ✅

---

## 🎯 Что дальше?

После успешной настройки вы сможете:

✅ Спрашивать Cursor AI о компонентах shadcn  
✅ Автоматически устанавливать компоненты через AI  
✅ Получать помощь по использованию UI компонентов  
✅ Искать примеры и демо компонентов  

**Примеры запросов:**
- "Show me all button variants from shadcn"
- "Add the dialog and toast components"
- "Find me a dashboard layout example"
- "How do I use the accordion component?"

---

## 📞 Поддержка

Если после всех шагов MCP сервер не работает:

1. Скопируйте вывод из **View → Output → MCP**
2. Скопируйте вывод команды `npx shadcn@latest mcp --help`
3. Опишите что именно происходит
4. Приложите скриншот ошибки из Cursor

---

## 📚 Дополнительные ресурсы

- [shadcn/ui Documentation](https://ui.shadcn.com)
- [MCP Protocol Documentation](https://modelcontextprotocol.io)
- [Cursor MCP Guide](https://docs.cursor.com/mcp)
- [Node.js Downloads](https://nodejs.org)

---

*Последнее обновление: 4 ноября 2025*  
*Статус: ✅ Конфигурация проверена*  
*Требуется: Исправить права npm кеша*

