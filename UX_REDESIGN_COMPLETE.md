# ✅ UX ПЕРЕРАБОТКА ЗАВЕРШЕНА!

**Дата:** 17 ноября 2025  
**Статус:** 🟢 ЕДИНЫЙ ПРОЦЕСС ЗАГРУЗКИ С PROGRESS BAR

---

## 🎯 ЧТО СДЕЛАНО:

### 1️⃣ MaterialsManager - убрана отдельная кнопка загрузки ✅

**Файл:** `src/components/admin/MaterialsManager.tsx`

**Изменения:**
- ✅ Добавлен prop `onMaterialsChange` для экспорта материалов наружу
- ✅ Удалена функция `handleUploadAll` (загрузка теперь через LessonEditDialog)
- ✅ Удалена кнопка "Загрузить все материалы"
- ✅ Убрано автосоздание урока

**Код:**
```typescript
interface MaterialsManagerProps {
  lessonId: number | null;
  onLessonCreated?: (lessonId: number) => void;
  moduleId: number;
  onMaterialsChange?: (materials: Material[]) => void; // ✅ НОВОЕ
}

// ✅ Экспортируем материалы наружу
useEffect(() => {
  onMaterialsChange?.(materials);
}, [materials, onMaterialsChange]);
```

---

### 2️⃣ LessonEditDialog - единая кнопка с progress bar ✅

**Файл:** `src/components/admin/LessonEditDialog.tsx`

**Изменения:**

#### A) Добавлены state для progress tracking:
```typescript
// ✅ Progress Bar для единой загрузки
const [uploadProgress, setUploadProgress] = useState(0); // 0-100%
const [uploadStatus, setUploadStatus] = useState(''); // Текущий статус
const [isUploading, setIsUploading] = useState(false); // Флаг загрузки

// Материалы (из MaterialsManager)
const [materials, setMaterials] = useState<any[]>([]);
```

#### B) Полностью переписана функция `handleSubmit`:

**Пошаговая загрузка (4 этапа):**

1. **ШАГ 1 (0% → 10%):** Создание урока в БД
   ```typescript
   const lessonRes = await api.post('/api/lessons', {...});
   setUploadProgress(10);
   ```

2. **ШАГ 2 (10% → 50%):** Загрузка видео на Cloudflare R2
   ```typescript
   if (videoFile) {
     setUploadStatus('📹 Загружаем видео на Cloudflare R2...');
     await api.post(`/api/videos/upload/${newLessonId}`, formData);
     setUploadProgress(50);
   }
   ```

3. **ШАГ 3 (50% → 90%):** Загрузка материалов в Supabase Storage
   ```typescript
   for (let i = 0; i < totalMaterials; i++) {
     const material = materialsToUpload[i];
     const percent = 50 + Math.floor(((i + 1) / totalMaterials) * 40);
     setUploadProgress(percent);
     setUploadStatus(`📚 Загружаем материал ${i + 1}/${totalMaterials}...`);
     await api.post('/api/materials/upload', formData);
   }
   ```

4. **ШАГ 4 (90% → 100%):** Финализация и переход к уроку
   ```typescript
   setUploadProgress(100);
   setTimeout(() => {
     setIsUploading(false);
     window.location.href = `/course/${courseId}/module/${moduleId}/lesson/${newLessonId}`;
   }, 1000);
   ```

#### C) Добавлен Progress Bar в UI:

```tsx
{/* ✅ Progress Bar для единой загрузки */}
{isUploading && (
  <div className="mt-4 space-y-2 p-4 bg-[#1a1a24] border border-[#00ff00]/30 rounded-lg">
    {/* Процент и статус */}
    <div className="flex items-center justify-between text-sm">
      <span className="font-medium text-white">{uploadStatus}</span>
      <span className="text-[#00ff00] font-bold">{uploadProgress}%</span>
    </div>
    
    {/* Прогресс-бар с градиентом */}
    <div className="w-full bg-gray-700 rounded-full h-3 overflow-hidden">
      <div
        className="bg-gradient-to-r from-blue-500 via-purple-500 to-[#00ff00] h-full"
        style={{ width: `${uploadProgress}%` }}
      />
    </div>
    
    {/* Детальный статус каждого этапа */}
    <div className="text-xs text-gray-400 space-y-1 pt-2">
      {uploadProgress >= 10 && <div>✅ Урок создан в базе данных</div>}
      {uploadProgress >= 50 && <div>✅ Видео загружено на Cloudflare R2</div>}
      {uploadProgress >= 90 && <div>✅ Материалы загружены в Supabase Storage</div>}
      {uploadProgress === 100 && <div className="text-[#00ff00] animate-pulse">🎉 Готово! Переходим к уроку...</div>}
    </div>
  </div>
)}
```

#### D) Изменена кнопка "Создать урок":

```tsx
<Button 
  onClick={handleSubmit} 
  disabled={!title.trim() || isUploading}
  className="bg-[#00ff00] text-black hover:bg-[#00cc00] font-semibold flex-1"
>
  {isUploading ? (
    <>⏳ Загрузка {uploadProgress}%...</>
  ) : (
    <>🚀 Создать урок и загрузить все материалы</>
  )}
</Button>
```

#### E) Интегрирован MaterialsManager:

```tsx
<MaterialsManager
  lessonId={savedLessonId}
  onLessonCreated={(id) => setSavedLessonId(id)}
  moduleId={moduleId}
  onMaterialsChange={(newMaterials) => setMaterials(newMaterials)} // ✅ НОВОЕ
/>
```

---

## 🚀 НОВЫЙ UX WORKFLOW:

### Старый workflow (БЫЛО):
```
1. Создать урок → кнопка "Создать урок"
2. Загрузить видео → таб "Видео" → кнопка "Загрузить видео"
3. Загрузить материалы → таб "Материалы" → кнопка "Загрузить все материалы"
4. Вручную закрыть диалог
5. Вручную найти урок в списке
```

### Новый workflow (СТАЛО):
```
1. Заполнить ВСЁ в одном диалоге:
   - Таб "Основное": название, описание, длительность
   - Таб "Видео": выбрать файл
   - Таб "Материалы": добавить файлы (без загрузки)

2. Нажать ОДНУ кнопку: "🚀 Создать урок и загрузить все материалы"

3. Смотреть детальный progress bar:
   📝 Создаём урок в базе данных... 10%
   ✅ Урок создан в базе данных
   
   📹 Загружаем видео на Cloudflare R2... 35%
   ✅ Видео загружено на Cloudflare R2
   
   📚 Загружаем материал 1/3: document.pdf 60%
   📚 Загружаем материал 2/3: presentation.pptx 75%
   📚 Загружаем материал 3/3: code.zip 90%
   ✅ Материалы загружены в Supabase Storage
   
   ✅ Завершаем создание урока... 100%
   🎉 Готово! Переходим к уроку...

4. Автоматический переход к просмотру урока
```

---

## 📊 ВИЗУАЛЬНЫЙ ПРОГРЕСС:

```
0%   ████░░░░░░░░░░░░░░░░  📝 Создаём урок...
10%  ██████░░░░░░░░░░░░░░  ✅ Урок создан
35%  █████████████░░░░░░░  📹 Загружаем видео...
50%  ██████████████████░░  ✅ Видео загружено
60%  ████████████████████  📚 Материал 1/3...
75%  ████████████████████  📚 Материал 2/3...
90%  ████████████████████  ✅ Материалы загружены
100% ████████████████████  🎉 Готово!
```

---

## 🎨 ЦВЕТОВАЯ СХЕМА PROGRESS BAR:

- **Фон:** `bg-gray-700` (темно-серый)
- **Градиент:** `from-blue-500 via-purple-500 to-[#00ff00]`
  - 0-10%: синий (создание урока)
  - 10-50%: переход к фиолетовому (видео)
  - 50-90%: переход к зеленому (материалы)
  - 90-100%: зеленый (#00ff00 - финализация)
- **Анимация:** `transition-all duration-300 ease-out`
- **Финальный статус:** `animate-pulse` (пульсация)

---

## ✅ ОСТАЛОСЬ СДЕЛАТЬ (ОПЦИОНАЛЬНО):

### 3️⃣ Создать страницу LessonView ⚠️

**Файл:** `src/pages/LessonView.tsx` (НУЖНО СОЗДАТЬ)

**Описание:** Страница для просмотра урока с видео и материалами

**Функционал:**
- ✅ Показать название и описание урока
- ✅ Видеоплеер с `lesson.video_url`
- ✅ Список материалов с кнопками "Скачать"
- ✅ Кнопка "Назад к модулю"

**Код (шаблон):**
```typescript
import { useParams, useNavigate } from 'react-router-dom';
import { useEffect, useState } from 'react';
import { api } from '@/utils/apiClient';
import { Button } from '@/components/ui/button';

export default function LessonView() {
  const { id, moduleId, lessonId } = useParams();
  const navigate = useNavigate();
  const [lesson, setLesson] = useState<any>(null);
  const [materials, setMaterials] = useState<any[]>([]);

  useEffect(() => {
    const load = async () => {
      const lessonRes = await api.get(`/api/lessons/${lessonId}`);
      setLesson(lessonRes.lesson || lessonRes);

      const materialsRes = await api.get(`/api/materials/${lessonId}`);
      setMaterials(materialsRes.materials || []);
    };
    load();
  }, [lessonId]);

  if (!lesson) return <div>Загрузка...</div>;

  return (
    <div className="container mx-auto p-6">
      <Button onClick={() => navigate(`/course/${id}/module/${moduleId}`)}>
        ← Назад к модулю
      </Button>

      <h1 className="text-4xl font-bold mt-6">{lesson.title}</h1>

      {lesson.video_url && (
        <video src={lesson.video_url} controls className="w-full mt-6" />
      )}

      {/* Материалы */}
      {materials.length > 0 && (
        <div className="mt-6">
          <h2 className="text-2xl font-bold">Материалы</h2>
          {materials.map((m) => (
            <div key={m.id} className="flex justify-between p-4 border mt-2">
              <span>{m.display_name}</span>
              <Button onClick={() => window.open(m.file_url, '_blank')}>
                Скачать
              </Button>
            </div>
          ))}
        </div>
      )}
    </div>
  );
}
```

### 4️⃣ Добавить роут ⚠️

**Файл:** `src/App.tsx`

```typescript
import LessonView from '@/pages/LessonView';

<Route path="/course/:id/module/:moduleId/lesson/:lessonId" element={<LessonView />} />
```

---

## 🧪 ТЕСТИРОВАНИЕ:

### Сценарий 1: Урок с видео и материалами

1. Открой http://localhost:8080/course/1/module/1
2. Нажми **"Добавить урок"**
3. Заполни:
   - **Название:** "Тест progress bar"
   - **Описание:** "Проверка пошаговой загрузки"
   - **Длительность:** 10
4. Перейди на таб **"Видео"**
5. Выбери файл (5-10 MB)
6. Перейди на таб **"Материалы"**
7. Добавь 2-3 файла (PDF, DOCX)
8. Вернись на таб **"Основное"**
9. Нажми **"🚀 Создать урок и загрузить все материалы"**

**Ожидаемый результат:**

```
📝 Создаём урок в базе данных... 10%
✅ Урок создан в базе данных

📹 Загружаем видео на Cloudflare R2... 35%
✅ Видео загружено на Cloudflare R2

📚 Загружаем материал 1/2: document.pdf 70%
📚 Загружаем материал 2/2: presentation.pptx 90%
✅ Материалы загружены в Supabase Storage

✅ Завершаем создание урока... 100%
🎉 Готово! Переходим к уроку...
```

**Затем автоматический переход на:**
```
http://localhost:8080/course/1/module/1/lesson/20
```

---

### Сценарий 2: Урок только с названием (без файлов)

1. Нажми **"Добавить урок"**
2. Заполни только **название:** "Простой урок"
3. Нажми **"🚀 Создать урок и загрузить все материалы"**

**Ожидаемый результат:**

```
📝 Создаём урок в базе данных... 10%
✅ Урок создан в базе данных

✅ Завершаем создание урока... 100%
🎉 Готово! Переходим к уроку...
```

**Прогресс:** `10% → 50% → 90% → 100%` (пропускаются шаги без файлов)

---

## 📁 ИЗМЕНЕННЫЕ ФАЙЛЫ:

1. ✅ `src/components/admin/MaterialsManager.tsx`
   - Добавлен `onMaterialsChange` prop
   - Удалена кнопка "Загрузить все материалы"
   - Удалена функция `handleUploadAll`

2. ✅ `src/components/admin/LessonEditDialog.tsx`
   - Добавлены state для progress tracking
   - Переписана функция `handleSubmit` с 4 этапами
   - Добавлен Progress Bar UI
   - Изменена кнопка на "Создать урок и загрузить все материалы"
   - Интегрирован MaterialsManager с `onMaterialsChange`

3. ⚠️ `src/pages/LessonView.tsx` - НУЖНО СОЗДАТЬ (опционально)
4. ⚠️ `src/App.tsx` - НУЖНО ДОБАВИТЬ РОУТ (опционально)

---

## 🎯 ИТОГО:

**До переработки:**
- ❌ 3 отдельные кнопки (Создать → Загрузить видео → Загрузить материалы)
- ❌ Нет индикации прогресса
- ❌ Нужно вручную закрывать диалог
- ❌ Нужно вручную искать урок

**После переработки:**
- ✅ ОДНА кнопка для всего
- ✅ Детальный progress bar (0-100%)
- ✅ Статусы каждого этапа
- ✅ Автоматическое закрытие диалога
- ✅ Автоматический переход к уроку

---

**СЕРВЕРЫ ПЕРЕЗАПУЩЕНЫ - ТЕСТИРУЙ!** 🚀

