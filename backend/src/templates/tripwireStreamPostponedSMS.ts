/**
 * 📱 SMS TEMPLATE: Перенос эфира с 18 на 20 декабря
 * 
 * Лимит: 69 символов (для дешёвого SMS-тарифа)
 * Отправка: Mobizon API
 */

export const tripwireStreamPostponedSMS = (): string => {
  // ⚡ 67 символов (в пределах лимита 69)
  return `⚡Эфир перенесли на 20 декабря! Пройди модули: onai.academy/integrator`;
};

/**
 * 📱 АЛЬТЕРНАТИВНЫЕ ВАРИАНТЫ SMS (на выбор):
 */

// Вариант 2 (65 символов)
export const tripwireStreamPostponedSMS_v2 = (): string => {
  return `🔥Эфир 20 декабря в 20:00! Успей пройти модули onai.academy/integrator`;
};

// Вариант 3 (68 символов) - более мотивирующий
export const tripwireStreamPostponedSMS_v3 = (): string => {
  return `⚡Эфир перенесён на субботу 20.12! Готовься: onai.academy/integrator`;
};

// Вариант 4 (64 символа) - короткий и ёмкий
export const tripwireStreamPostponedSMS_v4 = (): string => {
  return `Эфир 20 декабря 20:00! Подготовься сейчас: onai.academy/integrator`;
};
