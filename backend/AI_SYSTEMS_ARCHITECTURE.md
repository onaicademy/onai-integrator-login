# 🤖 АРХИТЕКТУРА AI-СИСТЕМ ПЛАТФОРМЫ onAI Academy

> **Версия:** 1.0  
> **Дата:** 03.12.2025  
> **Системы:** AI-наставник, AI-аналитика, AI-куратор

---

## 📋 ОГЛАВЛЕНИЕ

1. [Обзор систем](#обзор-систем)
2. [AI-наставник](#ai-наставник)
3. [AI-аналитика](#ai-аналитика)
4. [AI-куратор](#ai-куратор)
5. [Telegram интеграция](#telegram-интеграция)
6. [Расписание и автоматизация](#расписание-и-автоматизация)
7. [Тестирование](#тестирование)

---

## 🎯 ОБЗОР СИСТЕМ

### Общая архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                    onAI Academy Platform                     │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌─────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │ AI-Наставник│  │ AI-Аналитика │  │ AI-Куратор   │       │
│  │             │  │              │  │              │       │
│  │ - Мотивация │  │ - Метрики    │  │ - Вопросы    │       │
│  │ - Отчеты    │  │ - AI анализ  │  │ - Помощь     │       │
│  │ - Telegram  │  │ - Отчеты     │  │ - Чат 24/7   │       │
│  └──────┬──────┘  └──────┬───────┘  └──────┬───────┘       │
│         │                │                 │                │
│         └────────────────┴─────────────────┘                │
│                          │                                   │
│         ┌────────────────▼──────────────────┐               │
│         │     OpenAI Assistant API          │               │
│         │  - GPT-4 Turbo                    │               │
│         │  - Персонализация                 │               │
│         │  - Контекст                       │               │
│         └────────────────┬──────────────────┘               │
│                          │                                   │
│         ┌────────────────▼──────────────────┐               │
│         │       Telegram Bot API            │               │
│         │  - Mentor Bot (студенты)         │               │
│         │  - Admin Bot (администратор)     │               │
│         └───────────────────────────────────┘               │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

### Компоненты

| Система | Назначение | Получатели | Частота |
|---------|-----------|------------|---------|
| **AI-Наставник** | Мотивация студентов, отчеты по прогрессу | Студенты (мотивация), Админ (отчеты) | Ежедневно в 9:00 |
| **AI-Аналитика** | Анализ платформы, метрики, инсайты | Админ | Ежедневно в 9:00 |
| **AI-Куратор** | Ответы на вопросы студентов 24/7 | Студенты | По запросу |

---

## 🤖 AI-НАСТАВНИК

### Назначение

AI-наставник автоматически отслеживает прогресс студентов и:
1. Отправляет персонализированные мотивационные сообщения студентам
2. Генерирует ежедневные отчеты для администратора
3. Генерирует еженедельные отчеты для администратора

### Архитектура

```
┌──────────────────────────────────────────────────────────────┐
│                      AI-НАСТАВНИК                             │
├──────────────────────────────────────────────────────────────┤
│                                                               │
│  1. СБОР ДАННЫХ                                              │
│     ├─ Получить всех студентов из БД                        │
│     ├─ Подсчитать прогресс каждого (уроки, XP, стрик)       │
│     └─ Определить последнюю активность                      │
│                                                               │
│  2. АНАЛИЗ (для мотивации)                                   │
│     ├─ Фильтр: неактивны >3 дня ИЛИ стрик >5 ИЛИ прогресс >70% │
│     ├─ Для каждого → OpenAI Assistant API                   │
│     └─ Генерация персонализированного сообщения             │
│                                                               │
│  3. ОТПРАВКА                                                  │
│     ├─ Telegram Mentor Bot → студенту                       │
│     └─ Логирование в ai_mentor_advice_log                   │
│                                                               │
│  4. ОТЧЕТЫ                                                    │
│     ├─ Ежедневный отчет (активность за вчера)               │
│     ├─ Еженедельный отчет (топ студенты, проблемы)          │
│     └─ Telegram Admin Bot → администратору                  │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

### Файлы

| Файл | Назначение |
|------|-----------|
| `backend/src/services/aiMentorScheduler.ts` | Основная логика AI-наставника |
| `backend/src/routes/ai-mentor.ts` | API endpoints для тестирования |
| `backend/src/services/telegramService.ts` | Отправка сообщений в Telegram |

### Функции

#### `getStudentsProgress()`
- Получает всех студентов из БД
- Подсчитывает прогресс по урокам
- Определяет дни неактивности
- Возвращает массив `StudentProgress[]`

#### `analyzeAndMotivateStudent(student)`
- Формирует контекст для OpenAI
- Вызывает OpenAI Assistant API
- Получает персонализированное сообщение
- Отправляет через Telegram Mentor Bot
- Логирует в `ai_mentor_advice_log`

#### `checkAndMotivateStudents()`
- Получает всех студентов
- Фильтрует по критериям мотивации
- Для каждого вызывает `analyzeAndMotivateStudent()`
- Запускается автоматически в 9:00 каждый день

#### `generateDailyReport()`
- Собирает статистику за вчера:
  - Активных студентов
  - Завершенных уроков
  - Новых регистраций
- Формирует отчет
- Отправляет через Telegram Admin Bot администратору
- Сохраняет в `ai_mentor_advice_log`
- **Запускается: ежедневно в 9:00 утра (Almaty time)**

#### `generateWeeklyReport()`
- Собирает статистику за неделю:
  - Топ-3 студента
  - Средний прогресс
  - Проблемные зоны
- Формирует отчет
- Отправляет через Telegram Admin Bot
- Сохраняет в `ai_mentor_advice_log`
- **Запускается: каждый понедельник в 9:00 утра (Almaty time)**

### Расписание (Cron Jobs)

```javascript
// Ежедневный отчет в 9:00 Almaty (3:00 UTC)
cron.schedule('0 3 * * *', () => {
  generateDailyReport();
});

// Еженедельный отчет в понедельник 9:00 Almaty (3:00 UTC)
cron.schedule('0 3 * * 1', () => {
  generateWeeklyReport();
});
```

### API Endpoints

```bash
# Статус AI-наставника
GET /api/ai-mentor/status

# Ручной запуск ежедневного отчета (для тестирования)
POST /api/ai-mentor/trigger/daily-report

# Ручной запуск еженедельного отчета (для тестирования)
POST /api/ai-mentor/trigger/weekly
```

### База данных

**Таблица: `ai_mentor_advice_log`**

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | UUID | ID записи |
| `user_id` | UUID | ID студента (NULL для отчетов админу) |
| `advice_type` | TEXT | Тип: motivation, daily_report, weekly_report |
| `advice_text` | TEXT | Текст сообщения/отчета |
| `context` | JSONB | Контекст (данные студента) |
| `sent_via` | TEXT | Канал отправки: telegram, system |
| `created_at` | TIMESTAMPTZ | Дата создания |

### Environment Variables

```bash
# OpenAI
OPENAI_API_KEY=sk-...
OPENAI_ASSISTANT_MENTOR_ID=asst_...

# Telegram
TELEGRAM_MENTOR_BOT_TOKEN=...
TELEGRAM_ADMIN_BOT_TOKEN=...
TELEGRAM_ADMIN_CHAT_ID=...
```

---

## 📊 AI-АНАЛИТИКА

### Назначение

AI-аналитика автоматически собирает метрики платформы, анализирует их через OpenAI и отправляет инсайты администратору.

### Архитектура

```
┌──────────────────────────────────────────────────────────────┐
│                     AI-АНАЛИТИКА                              │
├──────────────────────────────────────────────────────────────┤
│                                                               │
│  1. СБОР МЕТРИК                                              │
│     ├─ Пользователи (всего, новые, активные)                │
│     ├─ Уроки (всего, завершено)                             │
│     ├─ Видео (время просмотра, завершение)                  │
│     ├─ Достижения (разблокировано)                          │
│     ├─ AI-куратор (вопросы, время ответа)                   │
│     └─ Проблемы (без прогресса, неактивны)                  │
│                                                               │
│  2. AI-АНАЛИЗ                                                 │
│     ├─ Формирование контекста для OpenAI                     │
│     ├─ Вызов OpenAI Assistant API (Analyst)                 │
│     └─ Получение инсайтов и рекомендаций                    │
│                                                               │
│  3. ГЕНЕРАЦИЯ ОТЧЕТА                                         │
│     ├─ Ключевые метрики                                      │
│     ├─ AI-анализ (тренды, проблемы, рекомендации)           │
│     └─ Проблемные зоны                                       │
│                                                               │
│  4. ОТПРАВКА                                                  │
│     ├─ Telegram Admin Bot → администратору                  │
│     └─ Сохранение в ai_analytics_reports                    │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

### Файлы

| Файл | Назначение |
|------|-----------|
| `backend/src/services/aiAnalyticsScheduler.ts` | Основная логика AI-аналитики |
| `backend/src/routes/ai-analytics.ts` | API endpoints для управления |

### Функции

#### `getPlatformMetrics()`
- Собирает метрики из всех таблиц БД:
  - **Пользователи:** всего, новые (сегодня, неделя), активные
  - **Уроки:** всего, завершено (сегодня, неделя)
  - **Курсы:** всего, средний прогресс
  - **Видео:** время просмотра, степень завершения
  - **Достижения:** разблокировано (сегодня, неделя)
  - **AI-куратор:** вопросов, среднее время ответа
  - **Проблемы:** без прогресса, неактивны 7+ дней, низкая вовлеченность
- Возвращает объект `PlatformMetrics`

#### `analyzeMetricsWithAI(metrics)`
- Формирует контекст с метриками для OpenAI
- Вызывает OpenAI Assistant API (Analyst)
- Получает AI-анализ: тренды, проблемы, рекомендации
- Возвращает текст анализа

#### `generateDailyAnalyticsReport()`
- Вызывает `getPlatformMetrics()` для сбора данных
- Вызывает `analyzeMetricsWithAI()` для AI-анализа
- Формирует финальный отчет:
  - Ключевые метрики
  - AI-анализ
  - Проблемные зоны
- Сохраняет в `ai_analytics_reports`
- Отправляет через Telegram Admin Bot администратору
- **Запускается: ежедневно в 9:00 утра (Almaty time)**

### Расписание (Cron Jobs)

```javascript
// Ежедневный отчет в 9:00 Almaty (3:00 UTC)
cron.schedule('0 3 * * *', () => {
  generateDailyAnalyticsReport();
});
```

### API Endpoints

```bash
# Статус AI-аналитики
GET /api/ai-analytics/status

# Ручной запуск ежедневного отчета (для тестирования)
POST /api/ai-analytics/trigger/daily

# Получить историю отчетов
GET /api/ai-analytics/reports?limit=10&type=daily
```

### База данных

**Таблица: `ai_analytics_reports`**

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | UUID | ID отчета |
| `report_type` | TEXT | Тип: daily, weekly, monthly |
| `report_text` | TEXT | Текст отчета |
| `metrics` | JSONB | Собранные метрики |
| `ai_analysis` | TEXT | AI-анализ (если OpenAI настроен) |
| `created_at` | TIMESTAMPTZ | Дата создания |

### Метрики платформы

**Собираемые данные:**

```typescript
interface PlatformMetrics {
  // Пользователи
  totalUsers: number;
  newUsersToday: number;
  newUsersWeek: number;
  activeUsersToday: number;
  activeUsersWeek: number;
  
  // Уроки
  totalLessons: number;
  lessonsCompletedToday: number;
  lessonsCompletedWeek: number;
  
  // Курсы
  totalCourses: number;
  avgCourseProgress: number;
  
  // Видео
  totalVideoWatchTime: number; // минуты
  videoWatchTimeToday: number;
  avgVideoCompletionRate: number;
  
  // Достижения
  achievementsUnlockedToday: number;
  achievementsUnlockedWeek: number;
  
  // AI-куратор
  curatorQuestionsToday: number;
  curatorQuestionsWeek: number;
  avgCuratorResponseTime: number;
  
  // Проблемы
  usersWithNoProgress: number;
  usersInactive7Days: number;
  lowEngagementUsers: number;
}
```

### Environment Variables

```bash
# OpenAI
OPENAI_API_KEY=sk-...
OPENAI_ASSISTANT_ANALYST_ID=asst_...  # Опционально (для AI-анализа)

# Telegram
TELEGRAM_ADMIN_BOT_TOKEN=...
TELEGRAM_ADMIN_CHAT_ID=...
```

---

## 🎓 AI-КУРАТОР

### Назначение

AI-куратор отвечает на вопросы студентов 24/7 в режиме реального времени через чат на платформе.

### Архитектура

```
┌──────────────────────────────────────────────────────────────┐
│                      AI-КУРАТОР                               │
├──────────────────────────────────────────────────────────────┤
│                                                               │
│  Студент → Frontend чат → Backend API → OpenAI → Ответ       │
│                                                               │
│  1. Студент задает вопрос в чате                             │
│  2. Frontend отправляет POST /api/openai/threads/{id}/run    │
│  3. Backend:                                                  │
│     ├─ Создает/получает thread студента                      │
│     ├─ Добавляет сообщение в thread                          │
│     ├─ Запускает OpenAI Assistant (Curator)                  │
│     └─ Получает ответ                                        │
│  4. Сохранение в БД (student_questions_log)                  │
│  5. Ответ → Frontend → Студент                               │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

### Файлы

| Файл | Назначение |
|------|-----------|
| `backend/src/routes/openai.ts` | API endpoints для чата с AI-куратором |
| `backend/src/services/openaiService.ts` | Работа с OpenAI Assistant API |
| `backend/src/services/supabaseService.ts` | Сохранение истории чата в БД |

### База данных

**Таблицы:**

1. **`ai_curator_threads`** - чаты студентов с куратором
2. **`ai_curator_messages`** - история сообщений
3. **`student_questions_log`** - лог вопросов для аналитики

---

## 📱 TELEGRAM ИНТЕГРАЦИЯ

### Архитектура

```
┌────────────────────────────────────────────────────────┐
│              TELEGRAM BOTS                             │
├────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─────────────────┐         ┌─────────────────┐      │
│  │  MENTOR BOT     │         │   ADMIN BOT     │      │
│  │  (Студенты)     │         │ (Администратор) │      │
│  ├─────────────────┤         ├─────────────────┤      │
│  │ - Мотивация     │         │ - Ежедневные    │      │
│  │ - Напоминания   │         │   отчеты        │      │
│  │ - Достижения    │         │ - Еженедельные  │      │
│  │ - Стрики        │         │   отчеты        │      │
│  └─────────────────┘         │ - Уведомления   │      │
│                              └─────────────────┘      │
│                                                         │
└────────────────────────────────────────────────────────┘
```

### Настройка Telegram

#### 1. Создание ботов

```bash
# Telegram BotFather
/newbot

# Mentor Bot (для студентов)
Name: onAI Mentor
Username: @onai_mentor_bot
Token: TELEGRAM_MENTOR_BOT_TOKEN

# Admin Bot (для администратора)
Name: onAI Admin
Username: @onai_admin_bot
Token: TELEGRAM_ADMIN_BOT_TOKEN
```

#### 2. Получение Admin Chat ID

```bash
# 1. Напиши сообщение боту @onai_admin_bot
# 2. Выполни запрос:
curl https://api.telegram.org/bot<TELEGRAM_ADMIN_BOT_TOKEN>/getUpdates

# 3. Найди "chat":{"id": 123456789}
# 4. Это твой TELEGRAM_ADMIN_CHAT_ID
```

#### 3. Environment Variables

```bash
# Mentor Bot (отправка студентам)
TELEGRAM_MENTOR_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# Admin Bot (отправка администратору)
TELEGRAM_ADMIN_BOT_TOKEN=9876543210:ZYXwvuTSRqponMLKjiHGFedcBA

# Admin Chat ID (куда отправлять отчеты)
TELEGRAM_ADMIN_CHAT_ID=123456789
```

### Файлы

| Файл | Назначение |
|------|-----------|
| `backend/src/config/telegram.ts` | Конфигурация Telegram ботов |
| `backend/src/services/telegramService.ts` | Отправка сообщений, шаблоны |

### Функции отправки

```typescript
// Отправка студенту через Mentor Bot
sendMentorMessage(chatId: string, message: string): Promise<boolean>

// Отправка админу через Admin Bot
sendAdminMessage(chatId: string, message: string): Promise<boolean>

// Отправка админу (использует TELEGRAM_ADMIN_CHAT_ID из env)
sendAdminNotification(message: string): Promise<boolean>
```

### Шаблоны сообщений

**Для студентов (MENTOR_TEMPLATES):**
- `motivation` - мотивационные сообщения
- `reminder` - напоминания
- `achievement` - разблокировка достижений
- `streak` - поздравления со стриком
- `warning` - предупреждение о потере стрика

**Для админа (ADMIN_TEMPLATES):**
- `dailyReport` - ежедневный отчет
- `weeklyReport` - еженедельный отчет
- `alert` - уведомления

---

## ⏰ РАСПИСАНИЕ И АВТОМАТИЗАЦИЯ

### Cron Jobs

Все cron jobs используют **Almaty time (UTC+6)**.

| Задача | Время (Almaty) | Время (UTC) | Cron | Частота |
|--------|----------------|-------------|------|---------|
| AI-наставник: ежедневный отчет | 9:00 | 3:00 | `0 3 * * *` | Каждый день |
| AI-наставник: еженедельный отчет | 9:00 (пн) | 3:00 (пн) | `0 3 * * 1` | Понедельник |
| AI-аналитика: ежедневный отчет | 9:00 | 3:00 | `0 3 * * *` | Каждый день |

### Запуск планировщиков

**В `backend/src/server.ts`:**

```typescript
// После успешного запуска сервера
server.listen(PORT, () => {
  console.log(`✅ Backend запущен на порту ${PORT}`);
  
  // Запуск планировщиков
  startReminderScheduler();      // Напоминания об уроках
  startAIMentorScheduler();      // AI-наставник (отчеты в 9:00)
  startAIAnalyticsScheduler();   // AI-аналитика (отчеты в 9:00)
});
```

### Логирование

Все системы логируют свою работу:

```bash
# В консоли backend
✅ [AI Mentor Scheduler] Started successfully
📅 Schedule:
  - Daily report: 9:00 AM каждый день (Almaty time)
  - Weekly report: Monday 9:00 AM (Almaty time)

✅ [AI Analytics Scheduler] Started successfully
📅 Schedule:
  - Daily analytics report: 9:00 AM каждый день (Almaty time)
```

---

## 🧪 ТЕСТИРОВАНИЕ

### Ручной запуск отчетов

**Через API:**

```bash
# AI-наставник: ежедневный отчет
curl -X POST https://api.onai.academy/api/ai-mentor/trigger/daily-report \
  -H "Authorization: Bearer YOUR_ADMIN_JWT_TOKEN"

# AI-аналитика: ежедневный отчет
curl -X POST https://api.onai.academy/api/ai-analytics/trigger/daily \
  -H "Authorization: Bearer YOUR_ADMIN_JWT_TOKEN"
```

**Через тестовый скрипт:**

```bash
# 1. Настрой токен в backend/.env
TEST_ADMIN_TOKEN=твой_jwt_токен_админа

# 2. Запусти скрипт
node backend/test-ai-reports.js
```

### Проверка статуса

```bash
# AI-наставник
curl https://api.onai.academy/api/ai-mentor/status \
  -H "Authorization: Bearer YOUR_ADMIN_JWT_TOKEN"

# AI-аналитика
curl https://api.onai.academy/api/ai-analytics/status \
  -H "Authorization: Bearer YOUR_ADMIN_JWT_TOKEN"
```

### Ожидаемый результат

После запуска тестов ты получишь **2 сообщения в Telegram** (в чат админа):

1. **Ежедневный отчет AI-наставника:**
   - Активность за вчера
   - Общая статистика студентов
   - Студенты, требующие внимания

2. **Ежедневный отчет AI-аналитики:**
   - Ключевые метрики платформы
   - AI-анализ (тренды, рекомендации)
   - Проблемные зоны

---

## 📊 МОНИТОРИНГ

### Логи на сервере

```bash
# Проверка логов PM2
ssh root@207.154.231.30
pm2 logs onai-backend --lines 100

# Поиск логов AI-систем
pm2 logs onai-backend | grep "AI Mentor"
pm2 logs onai-backend | grep "AI Analytics"
```

### Проверка Telegram

```bash
# Получить updates бота
curl https://api.telegram.org/bot<BOT_TOKEN>/getUpdates

# Проверить что бот работает
curl https://api.telegram.org/bot<BOT_TOKEN>/getMe
```

### База данных

```sql
-- Последние отчеты AI-наставника
SELECT * FROM ai_mentor_advice_log 
WHERE advice_type IN ('daily_report', 'weekly_report') 
ORDER BY created_at DESC 
LIMIT 10;

-- Последние отчеты AI-аналитики
SELECT * FROM ai_analytics_reports 
ORDER BY created_at DESC 
LIMIT 10;
```

---

## 🔧 TROUBLESHOOTING

### Проблема: Отчеты не приходят в Telegram

**Решение:**
1. Проверь Environment Variables:
   ```bash
   echo $TELEGRAM_ADMIN_BOT_TOKEN
   echo $TELEGRAM_ADMIN_CHAT_ID
   ```
2. Проверь что бот работает:
   ```bash
   curl https://api.telegram.org/bot<TOKEN>/getMe
   ```
3. Проверь логи:
   ```bash
   pm2 logs onai-backend | grep "Telegram"
   ```

### Проблема: AI-анализ не генерируется

**Решение:**
1. Проверь `OPENAI_ASSISTANT_ANALYST_ID` в .env
2. Если не настроен - отчеты будут **базовыми** (без AI-анализа)
3. Это нормально! Основные метрики всё равно отправляются

### Проблема: Cron job не запускается

**Решение:**
1. Проверь что backend запущен через PM2
2. Проверь что `startAIMentorScheduler()` и `startAIAnalyticsScheduler()` вызываются в `server.ts`
3. Проверь логи при старте:
   ```bash
   pm2 logs onai-backend | grep "Scheduler"
   ```

---

## 📞 КОНТАКТЫ

- **Документация:** `/backend/AI_SYSTEMS_ARCHITECTURE.md`
- **Тестовый скрипт:** `/backend/test-ai-reports.js`
- **Код AI-наставника:** `/backend/src/services/aiMentorScheduler.ts`
- **Код AI-аналитики:** `/backend/src/services/aiAnalyticsScheduler.ts`
- **Telegram сервис:** `/backend/src/services/telegramService.ts`

---

**💡 Совет:** Запускай тестовый скрипт `test-ai-reports.js` каждый раз после изменений для проверки работоспособности систем!



