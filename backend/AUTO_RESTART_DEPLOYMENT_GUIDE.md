# 🔄 AUTO-RESTART DEPLOYMENT GUIDE

**Дата:** 15 декабря 2025  
**Цель:** Настройка агрессивного автоматического перезапуска бэкенда при падении

---

## ✅ ЧТО БЫЛО СДЕЛАНО

### 1. Улучшена PM2 конфигурация (`backend/ecosystem.config.js`)

**Новые параметры:**
```javascript
autorestart: true,                    // Автоматический перезапуск
max_restarts: 50,                     // Увеличено с 10 до 50 попыток
min_uptime: '5s',                     // Уменьшено с 10s до 5s
restart_delay: 2000,                  // 2 секунды между рестартами
exp_backoff_restart_delay: 100,       // Экспоненциальная задержка
watch: false,                         // Не перезапускаем при изменении файлов
ignore_watch: ['node_modules', 'logs', '.git', 'dist']
```

**Что это даёт:**
- ⚡ Мгновенный перезапуск через 2-4 секунды после падения
- 📈 Экспоненциальная задержка при повторных падениях (2s → 4s → 8s → 16s...)
- 🔄 До 50 попыток перезапуска вместо 10
- 🛡️ Защита от loop crash через растущую задержку

### 2. Добавлен детальный Health Check endpoint

**Новый endpoint:** `GET /api/health/deep`

**Возвращает:**
```json
{
  "status": "healthy",
  "timestamp": "2025-12-15T13:00:00.000Z",
  "uptime": "2h 15m 30s",
  "uptimeSeconds": 8130,
  "memory": {
    "heapUsed": "245MB",
    "heapTotal": "512MB",
    "rss": "650MB",
    "external": "12MB",
    "usagePercent": "48%"
  },
  "process": {
    "pid": 12345,
    "version": "v18.17.0",
    "platform": "linux",
    "arch": "x64"
  },
  "warnings": []
}
```

**Статусы:**
- `healthy` - всё отлично (память < 75%)
- `degraded` - предупреждение (память 75-90%)
- `critical` - критично (память > 90%)

---

## 🚀 ДЕПЛОЙ НА СЕРВЕР

### Шаг 1: Подготовка

```bash
# На локальной машине
cd /Users/miso/onai-integrator-login

# Проверить изменения
git status

# Закоммитить изменения
git add backend/ecosystem.config.js backend/src/server.ts
git commit -m "feat: aggressive auto-restart configuration for backend stability"

# Запушить на GitHub
git push origin main
```

### Шаг 2: Сборка Backend

```bash
# Собрать TypeScript → JavaScript
cd backend
npx tsc --skipLibCheck

# Проверить что dist/ обновлён
ls -lh dist/
```

### Шаг 3: Деплой на DigitalOcean

```bash
# Задеплоить backend файлы
rsync -avz \
  -e "ssh -i ~/.ssh/id_rsa" \
  backend/dist/ \
  root@207.154.231.30:/var/www/onai-integrator-login-main/backend/dist/

# Обновить ecosystem.config.js
rsync -avz \
  -e "ssh -i ~/.ssh/id_rsa" \
  backend/ecosystem.config.js \
  root@207.154.231.30:/var/www/onai-integrator-login-main/backend/
```

### Шаг 4: Перезапуск PM2 с новой конфигурацией

```bash
ssh -i ~/.ssh/id_rsa root@207.154.231.30 '
  cd /var/www/onai-integrator-login-main/backend
  
  # Удалить старый процесс
  pm2 delete onai-backend
  
  # Запустить с новой конфигурацией
  pm2 start ecosystem.config.js
  
  # Сохранить конфигурацию
  pm2 save
  
  # Настроить автозапуск при перезагрузке сервера
  pm2 startup
  
  # Проверить статус
  pm2 status
'
```

---

## ✅ ПРОВЕРКА РАБОТОСПОСОБНОСТИ

### 1. Базовая проверка

```bash
# Проверить статус PM2
ssh -i ~/.ssh/id_rsa root@207.154.231.30 'pm2 status'

# Должно быть:
# ┌────┬─────────────────┬─────────┬────────┐
# │ id │ name            │ status  │ mem    │
# ├────┼─────────────────┼─────────┼────────┤
# │ 0  │ onai-backend    │ online  │ 150MB  │
# └────┴─────────────────┴─────────┴────────┘
```

### 2. Проверка Health endpoints

```bash
# Базовый health check
curl -s https://api.onai.academy/api/health | jq '.'

# Детальный health check
curl -s https://api.onai.academy/api/health/deep | jq '.'
```

**Ожидаемый результат:**
```json
{
  "status": "healthy",
  "uptime": "0h 2m 15s",
  "memory": {
    "usagePercent": "45%"
  },
  "warnings": []
}
```

### 3. Тест автоперезапуска

```bash
# Симулировать падение бэкенда
ssh -i ~/.ssh/id_rsa root@207.154.231.30 '
  # Убить процесс
  pm2 kill onai-backend
  
  # Подождать 5 секунд
  sleep 5
  
  # Проверить что PM2 перезапустил
  pm2 status
'

# Должен показать:
# status: online (процесс перезапустился!)
```

### 4. Проверка логов

```bash
# Смотреть логи в реальном времени
ssh -i ~/.ssh/id_rsa root@207.154.231.30 'pm2 logs onai-backend --lines 50'

# Проверить что нет повторяющихся ошибок
ssh -i ~/.ssh/id_rsa root@207.154.231.30 'pm2 logs onai-backend --err --lines 100 --nostream'
```

---

## 📊 МОНИТОРИНГ ПЕРЕЗАПУСКОВ

### Команды для мониторинга

```bash
# Информация о процессе PM2
ssh -i ~/.ssh/id_rsa root@207.154.231.30 'pm2 info onai-backend'

# Показывает:
# - Количество рестартов (restarts)
# - Uptime (сколько времени работает)
# - Статус (online/errored/stopped)
# - Использование памяти
```

### Счётчик перезапусков

```bash
ssh -i ~/.ssh/id_rsa root@207.154.231.30 '
  pm2 jlist | jq ".[0] | {
    name: .name,
    status: .pm2_env.status,
    restarts: .pm2_env.restart_time,
    uptime: .pm2_env.pm_uptime,
    memory: .monit.memory
  }"
'
```

---

## ⚠️ ЧТО ДЕЛАТЬ ЕСЛИ БЭКЕНД ПАДАЕТ ЧАСТО

### 1. Проверить логи ошибок

```bash
ssh -i ~/.ssh/id_rsa root@207.154.231.30 '
  # Последние 100 строк ошибок
  tail -100 /root/.pm2/logs/onai-backend-error.log
'
```

### 2. Анализировать паттерны

**Ищите:**
- `out of memory` - проблемы с памятью
- `ECONNREFUSED` - проблемы с подключением к БД/сервисам
- `timeout` - долгие запросы
- Повторяющиеся stack traces

### 3. Временные решения

**Если память переполняется:**
```bash
# Увеличить max_memory_restart в ecosystem.config.js
max_memory_restart: '7G',  # Было 6G
```

**Если слишком много перезапусков:**
```bash
# Увеличить restart_delay
restart_delay: 5000,  # 5 секунд вместо 2
```

---

## 🔧 ОТКАТ ИЗМЕНЕНИЙ (если что-то пошло не так)

### Быстрый откат через Git

```bash
# На локальной машине
git revert HEAD
git push origin main

# Задеплоить старую версию
cd backend
npx tsc --skipLibCheck
rsync -avz -e "ssh -i ~/.ssh/id_rsa" \
  backend/dist/ backend/ecosystem.config.js \
  root@207.154.231.30:/var/www/onai-integrator-login-main/backend/

# Перезапустить
ssh -i ~/.ssh/id_rsa root@207.154.231.30 '
  cd /var/www/onai-integrator-login-main/backend
  pm2 delete onai-backend
  pm2 start ecosystem.config.js
'
```

---

## 📈 МЕТРИКИ УСПЕХА

После деплоя мониторим:

1. **Количество рестартов** - должно быть 0-2 в день (не больше)
2. **Uptime** - должен расти (часы/дни)
3. **Response time** - должен быть стабильным (~100-300ms)
4. **Memory usage** - должен быть < 75%

### Алерты:

- ⚠️ **5+ рестартов в час** = нужно срочно искать проблему
- 🔴 **50 рестартов достигнуто** = PM2 перестанет перезапускать
- 💾 **Memory > 90%** = скоро будет crash

---

## 🎯 ИТОГО

### ✅ Что работает:
- Автоматический перезапуск через 2-4 секунды
- Экспоненциальная задержка при повторных падениях
- До 50 попыток перезапуска
- Детальный мониторинг через `/api/health/deep`

### ⚠️ Что нужно помнить:
- Это **временное решение** - маскирует проблему, не решает
- Нужно **анализировать логи** и находить причину падений
- При частых падениях (5+ в час) - **срочно искать root cause**
- Использовать **Sentry** для трекинга ошибок

### 📞 В случае проблем:
1. Проверить `pm2 logs onai-backend`
2. Проверить `/api/health/deep`
3. Проверить логи ошибок в `/root/.pm2/logs/`
4. Анализировать memory leaks и external service failures

---

**Версия:** 1.0  
**Автор:** AI Assistant  
**Дата:** 15 декабря 2025
