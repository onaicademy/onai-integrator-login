# 🔔 Telegram Leads Bot - Инструкция по настройке

## ✅ Что уже сделано

1. **Добавлены переменные окружения** в `backend/env.env`:
   ```env
   TELEGRAM_LEADS_BOT_TOKEN=8275130868:AAGiH466WmyoUQUKN4VfwI3nM1qZAmlUJOQ
   TELEGRAM_LEADS_CHAT_ID=789638302
   ```

2. **Обновлен конфигурационный файл** `backend/src/config/telegram.ts`:
   - Добавлена поддержка `leadsBotToken` и `leadsChatId`

3. **Создан сервис уведомлений** в `backend/src/services/telegramService.ts`:
   - Функция `sendLeadNotification()` для отправки уведомлений о новых лидах

4. **Интегрирован в роут** `backend/src/routes/landing.ts`:
   - Уведомления отправляются автоматически при создании нового лида с экспресс курса
   - Работает в фоновом режиме, не блокирует ответ пользователю

## 📋 Что нужно сделать вручную

### 1. Обновить продакшен переменные окружения

**ВАЖНО!** Необходимо добавить эти переменные в файл `.env.production`:

```env
# 🔔 Telegram бот для уведомлений о лидах с экспресс курса
TELEGRAM_LEADS_BOT_TOKEN=8275130868:AAGiH466WmyoUQUKN4VfwI3nM1qZAmlUJOQ
TELEGRAM_LEADS_CHAT_ID=789638302
```

**Как это сделать:**
1. Открыть файл `backend/.env.production`
2. Добавить эти две строки в секцию с Telegram ботами
3. Сохранить файл

### 2. Перезапустить backend сервер

После обновления переменных окружения перезапустите сервер:

```powershell
# Перейти в директорию backend
cd backend

# Остановить текущий процесс (если запущен через pm2)
pm2 stop all

# Запустить заново
pm2 start ecosystem.config.js --env production

# Или просто перезапустить
pm2 restart all
```

### 3. Проверить работу

После перезапуска:
1. Откройте страницу экспресс курса: `https://onai.academy/integrator/expresscourse`
2. Заполните форму с тестовыми данными
3. Выберите любой способ оплаты (Kaspi, Карта, или Менеджер)
4. Отправьте форму
5. Проверьте Telegram - должно прийти уведомление на ID `789638302`

## 📱 Формат уведомления

Бот будет отправлять сообщения в таком формате:

```
🎯 НОВАЯ ЗАЯВКА С ЭКСПРЕСС КУРСА

👤 Имя: Иван Иванов
📱 Телефон: +77771234567
📧 Email: ivan@example.com
💳 Способ оплаты: 💳 Kaspi банк
📍 Источник: expresscourse

⏰ 15.12.2025, 15:30:45
```

## 🔍 Отладка

Если уведомления не приходят, проверьте логи сервера:

```powershell
# Посмотреть логи pm2
pm2 logs

# Или логи конкретного процесса
pm2 logs onai-backend
```

В логах ищите:
- ✅ `Telegram: Lead notification sent successfully` - успешная отправка
- ⚠️ `TELEGRAM_LEADS_BOT_TOKEN not configured` - токен не настроен
- ❌ `Failed to send lead notification` - ошибка отправки

## 🛠️ Проверка токена бота

Чтобы убедиться что токен работает, можно выполнить тестовый запрос:

```powershell
curl "https://api.telegram.org/bot8275130868:AAGiH466WmyoUQUKN4VfwI3nM1qZAmlUJOQ/getMe"
```

Должен вернуться JSON с информацией о боте.

## 📝 Технические детали

### Интеграция в код

Уведомления отправляются в файле `backend/src/routes/landing.ts` в роуте `POST /api/landing/submit`:

```typescript
// Вызов функции отправки уведомления
const telegramSent = await sendLeadNotification({
  name,
  phone,
  email: email || undefined,
  paymentMethod: paymentMethod as 'kaspi' | 'card' | 'manager' | undefined,
  source,
});
```

### Безопасность

- Токен бота и Chat ID хранятся в переменных окружения
- Уведомления отправляются в фоновом режиме и не блокируют основной процесс
- Ошибки отправки логируются, но не влияют на сохранение лида в базе данных

## ✅ Готово!

После выполнения всех шагов, вы будете получать уведомления в Telegram о каждом новом лиде с экспресс курса! 🎉



