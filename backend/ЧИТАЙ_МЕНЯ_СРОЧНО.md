# 🔥 СРОЧНО! НЕ ПРИХОДЯТ ЗАЯВКИ В TELEGRAM?

## ⚡ БЫСТРОЕ РЕШЕНИЕ (30 секунд)

### 1️⃣ Открой PowerShell в папке `backend`

```powershell
cd C:\onai-integrator-login\onai-integrator-login\backend
```

### 2️⃣ Запусти БЫСТРЫЙ ФИКС:

```powershell
.\БЫСТРЫЙ_ФИКС.ps1
```

**Этот скрипт:**
- ✅ Добавит токены в `.env.production`
- ✅ Перезапустит сервер с новыми токенами
- ✅ Покажет статус

### 3️⃣ Проверь работу:

```powershell
.\ПРОВЕРКА.ps1
```

Этот скрипт:
- Проверит все настройки
- Запустит тест бота
- Отправит тестовое сообщение в Telegram

### 4️⃣ Проверь Telegram!

- **Telegram ID:** `789638302`
- Там должно быть **тестовое сообщение**

---

## 🚨 Если всё равно НЕ работает:

### Вариант 1: Логи

Посмотри логи сервера:

```powershell
pm2 logs --lines 50
```

Ищи строки с ошибками:
- ❌ `TELEGRAM_LEADS_BOT_TOKEN not configured`
- ❌ `Failed to send lead notification`

### Вариант 2: Ручной перезапуск

```powershell
pm2 stop all
pm2 start ecosystem.config.js --env production
pm2 logs
```

### Вариант 3: Проверка токена

Убедись что токен правильный:

```powershell
curl "https://api.telegram.org/bot8275130868:AAGiH466WmyoUQUKN4VfwI3nM1qZAmlUJOQ/getMe"
```

Должен вернуть информацию о боте.

---

## 📝 Что могло пойти не так:

1. ❌ **Токены не добавлены** в `.env.production`
   - **Решение:** Запусти `.\БЫСТРЫЙ_ФИКС.ps1`

2. ❌ **Сервер не перезапущен** с новыми токенами
   - **Решение:** Запусти `pm2 restart all`

3. ❌ **Бот заблокирован** в Telegram
   - **Решение:** Найди бота, напиши `/start`

4. ❌ **Неправильный Chat ID**
   - **Решение:** Убедись что ID = `789638302`

---

## ✅ ПРАВИЛЬНАЯ ПОСЛЕДОВАТЕЛЬНОСТЬ:

```powershell
# Шаг 1: Фикс
.\БЫСТРЫЙ_ФИКС.ps1

# Шаг 2: Проверка
.\ПРОВЕРКА.ps1

# Шаг 3: Смотри логи
pm2 logs

# Шаг 4: Тестируй на сайте
# Открой: https://onai.academy/integrator/expresscourse
# Заполни форму
# Проверь Telegram!
```

---

## 🎯 ИТОГО:

1. **Запусти `БЫСТРЫЙ_ФИКС.ps1`** ← НАЧНИ С ЭТОГО!
2. **Запусти `ПРОВЕРКА.ps1`** ← Проверь настройки
3. **Проверь Telegram** (ID: 789638302)
4. **Отправь тестовую заявку** на сайте

---

**ВОПРОСЫ?** Смотри логи: `pm2 logs`

🔥🔥🔥




