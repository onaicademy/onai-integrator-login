# 🚀 TRIPWIRE PRODUCTION DEPLOY - ИЗМЕНЕНИЯ

**Дата:** 17 декабря 2024  
**Статус:** ✅ Код готов к тестированию  

---

## 📋 Внесенные изменения

### 1. ✅ Скрипт сброса прогресса всех студентов

**Файл:** `backend/scripts/reset-all-students-progress.ts`

**Что делает:**
- ❌ Удаляет ВСЕ записи из `tripwire_progress`
- ❌ Удаляет ВСЕ записи из `module_unlocks`
- ❌ Удаляет ВСЕ записи из `user_achievements`
- ❌ Удаляет ВСЕ записи из `certificates`
- ✅ Создает начальный прогресс для урока 67 (0%)
- ✅ Разблокирует ТОЛЬКО модуль 1 (ID: 16)

**Исключения:**
- Admin: `saint@onaiacademy.kz`
- Sales менеджеры (если есть)

**Как запустить:**
```bash
cd backend
npx tsx scripts/reset-all-students-progress.ts
# Скрипт попросит подтверждение: введите "YES"
```

**КРИТИЧНО:** Скрипт требует явного подтверждения перед выполнением!

---

### 2. ✅ Модалка "Все модули завершены"

**Файлы:**
- `src/components/tripwire/Module3CompleteModal.tsx` (новый)
- `src/pages/tripwire/TripwireLesson.tsx` (изменен)

**Функционал:**
- 🎉 Показывается после завершения урока 69 (модуль 3)
- 🏆 Уведомление о доступе к сертификату
- 📺 Уведомление о завершающем эфире
- 🔘 Кнопка "ПОЛУЧИТЬ СЕРТИФИКАТ" → редирект в `/integrator/profile`

**Дизайн:**
- Animated icon с glow эффектом
- Gradient cards для сертификата и эфира
- Cyber-style (JetBrains Mono, #00FF88 accent)
- Responsive (mobile-friendly)

**Логика:**
```typescript
// В handleCompleteModule():
if (lessonId === '69' && response.data?.moduleCompleted) {
  setShowModule3Modal(true);
  return; // Не редиректим, модалка сама редиректит
}
```

---

## 🔍 Что НЕ изменилось (уже работает)

### Разблокировка модулей
- ✅ Модуль 1 открыт по умолчанию
- ✅ Модуль 2 разблокируется после завершения модуля 1
- ✅ Модуль 3 разблокируется после завершения модуля 2
- ✅ Live Stream открывается после завершения всех 3 модулей

### Генерация сертификатов
- ✅ `CertificateSection.tsx` работает корректно
- ✅ SSE прогресс-бар генерации
- ✅ Кнопка "ПОЛУЧИТЬ СЕРТИФИКАТ" в профиле

### Backend API
- ✅ `/api/tripwire/complete` - завершение урока
- ✅ Auto-unlock следующего модуля
- ✅ Achievement system
- ✅ AmoCRM integration

---

## 🧪 План тестирования (ЛОКАЛЬ)

### Шаг 1: Сбросить прогресс
```bash
cd backend
npx tsx scripts/reset-all-students-progress.ts
# Введите "YES"
```

**Проверить в Supabase:**
- `tripwire_progress`: только урок 67 (0%)
- `module_unlocks`: только модуль 16
- `user_achievements`: пусто
- `certificates`: пусто

---

### Шаг 2: Запустить локальный dev
```bash
# Terminal 1: Backend
cd backend
npm run dev

# Terminal 2: Frontend
npm run dev
```

**Открыть:** `http://localhost:8080`

---

### Шаг 3: Тест как студент

**3.1 Логин**
- Зайти как тестовый студент (НЕ admin)
- Перейти на `/integrator`

**Ожидается:**
- ✅ Модуль 1 (Вводный) - ОТКРЫТ
- 🔒 Модуль 2 (GPT-бот) - ЗАБЛОКИРОВАН
- 🔒 Модуль 3 (Reels) - ЗАБЛОКИРОВАН
- 🔒 Live Stream - ЗАБЛОКИРОВАН

---

**3.2 Завершение модуля 1**
- Открыть урок 67
- Досмотреть до 80%+ (или использовать seek)
- Нажать "ЗАВЕРШИТЬ МОДУЛЬ"

**Ожидается:**
- 🎊 Confetti анимация
- ✅ Редирект на `/integrator`
- 🔓 Модуль 2 разблокирован
- 🏆 Achievement "Первый модуль завершен"

---

**3.3 Завершение модуля 2**
- Открыть урок 68
- Досмотреть до 80%+
- Нажать "ЗАВЕРШИТЬ МОДУЛЬ"

**Ожидается:**
- 🎊 Confetti
- ✅ Редирект на `/integrator`
- 🔓 Модуль 3 разблокирован

---

**3.4 Завершение модуля 3** 🔥 ГЛАВНЫЙ ТЕСТ!
- Открыть урок 69
- Досмотреть до 80%+
- Нажать "ЗАВЕРШИТЬ МОДУЛЬ"

**Ожидается:**
- 🎊 Confetti анимация
- 🎉 **МОДАЛКА "Поздравляем! Все модули завершены!"**
- 📝 Текст: "Вам открыт доступ к сертификату и завершающему эфиру"
- 🔘 Кнопка "ПОЛУЧИТЬ СЕРТИФИКАТ"
- ✅ Клик по кнопке → редирект в `/integrator/profile`

---

**3.5 Генерация сертификата**
- В профиле должна быть кнопка "ПОЛУЧИТЬ СЕРТИФИКАТ"
- Нажать на кнопку
- Проверить SSE прогресс-бар (0% → 100%)
- Проверить что PDF сгенерирован
- Проверить кнопку "СКАЧАТЬ СЕРТИФИКАТ"

---

### Шаг 4: Тест как админ

**Зайти как `saint@onaiacademy.kz`**

**Ожидается:**
- ✅ Все модули ОТКРЫТЫ (god mode)
- ✅ Нет блокировок
- ✅ Кнопка "Редактировать урок" видна

---

## 🚀 Деплой на продакшен

**КРИТИЧНО:** Перед деплоем проверить URL!

### Проверка локальных URL
```bash
cd /Users/miso/onai-integrator-login

# Проверить упоминания localhost
grep -r "localhost" src/ --include="*.tsx" --include="*.ts" | grep -v "node_modules"

# Проверить hardcoded API URLs
grep -r "http://localhost:3000" src/
grep -r "http://localhost:8080" src/
```

**Допустимы localhost только в:**
- Комментариях
- `.env.local`, `.env.development`
- README.md

---

### Шаги деплоя

**1. Коммит изменений:**
```bash
git add .
git commit -m "feat: Module 3 complete modal + reset all students progress script"
git push origin main
```

**2. Подключиться к серверу:**
```bash
ssh root@207.154.231.30
cd /var/www/onai.academy
```

**3. Обновить код:**
```bash
git pull origin main
```

**4. Backend:**
```bash
cd backend
npm install
pm2 restart onai-backend
pm2 logs onai-backend --lines 50
```

**5. Frontend:**
```bash
cd ..
npm install
npm run build
sudo chown -R www-data:www-data dist/
sudo systemctl reload nginx
```

**6. Запустить скрипт сброса:**
```bash
cd backend
npx tsx scripts/reset-all-students-progress.ts
# Введите "YES"
```

---

### Post-Deploy проверка

**1. Browser Console (DevTools F12):**
- Network tab
- ✅ Все запросы на `https://api.onai.academy`
- ❌ НЕ ДОЛЖНО БЫТЬ запросов на `localhost`

**2. Функциональное тестирование:**
- Зайти как студент
- Пройти модули 1 → 2 → 3
- Проверить модалку после модуля 3
- Проверить генерацию сертификата

**3. Проверить логи:**
```bash
pm2 logs onai-backend --lines 100
sudo tail -f /var/log/nginx/error.log
```

---

## 📊 Итоговый чеклист

### Перед деплоем:
- [ ] Код протестирован на локали
- [ ] Модалка показывается после модуля 3
- [ ] Сброс прогресса работает корректно
- [ ] Нет упоминаний `localhost` в production коде
- [ ] Все environment variables корректны

### После деплоя:
- [ ] Сайт работает: `https://onai.academy`
- [ ] Backend online (PM2)
- [ ] Скрипт сброса запущен успешно
- [ ] Тест студента: модули разблокируются
- [ ] Модалка модуля 3 показывается
- [ ] Сертификат генерируется
- [ ] Нет ошибок в логах

---

## 🔥 Готово к деплою!

**Следующий шаг:** Протестировать на локали → Подтвердить → Деплой на продакшен

**Команда для быстрого старта локального теста:**
```bash
# Backend
cd /Users/miso/onai-integrator-login/backend && npm run dev

# Frontend (в новом терминале)
cd /Users/miso/onai-integrator-login && npm run dev
```

**Открыть:** `http://localhost:8080/integrator`
