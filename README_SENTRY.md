# 🛡️ SENTRY MONITORING - Полная документация

> **Мониторинг ошибок, производительности и зацикливаний для Tripwire продукта**

---

## 📋 Содержание

1. [Что такое Sentry?](#что-такое-sentry)
2. [Быстрый старт](#быстрый-старт)
3. [Документация](#документация)
4. [Что отслеживается?](#что-отслеживается)
5. [Примеры использования](#примеры-использования)
6. [FAQ](#faq)

---

## Что такое Sentry?

**Sentry** - это платформа для мониторинга ошибок и производительности в реальном времени.

### Проблемы, которые решает Sentry:

❌ **Было:** Серверы падают неожиданно, непонятно почему  
✅ **Стало:** Видим все ошибки в реальном времени с полным контекстом

❌ **Было:** Процессы зацикливаются, не понятно где  
✅ **Стало:** Автоматические алерты при долгих операциях (> 5-10 сек)

❌ **Было:** Пользователи жалуются на ошибки, но мы не видим детали  
✅ **Стало:** Session Replay - видим что делал пользователь перед ошибкой

❌ **Было:** Не понятно какие API медленные  
✅ **Стало:** Performance monitoring - видим узкие места

---

## Быстрый старт

### 1. Установка (✅ УЖЕ СДЕЛАНО)

```bash
# Frontend
npm install @sentry/react @sentry/tracing

# Backend
npm install @sentry/node @sentry/profiling-node
```

### 2. Настройка Sentry

1. Зайдите на [sentry.io](https://sentry.io)
2. Создайте 2 проекта: **React** (frontend) и **Node.js** (backend)
3. Получите DSN ключи для каждого проекта

### 3. Добавьте переменные окружения

#### Frontend (.env)
```env
VITE_SENTRY_DSN=https://YOUR_FRONTEND_DSN@sentry.io/123456
VITE_APP_VERSION=1.0.0
```

#### Backend (backend/.env)
```env
SENTRY_DSN=https://YOUR_BACKEND_DSN@sentry.io/123456
SERVER_NAME=onai-backend-production
NODE_ENV=production
```

📖 **Подробнее:** [SENTRY_ENV_VARIABLES.md](./SENTRY_ENV_VARIABLES.md)

### 4. Запустите приложение

```bash
# Frontend
npm run dev

# Backend
cd backend
npm run dev
```

В консоли должно появиться:
```
✅ Sentry initialized for frontend monitoring
✅ Sentry initialized for backend monitoring
```

### 5. Проверьте работу

Создайте тестовую ошибку:

```typescript
// Frontend
<button onClick={() => {
  throw new Error('Test Sentry - Frontend');
}}>Test</button>

// Backend
app.get('/test-sentry', () => {
  throw new Error('Test Sentry - Backend');
});
```

Ошибка должна появиться в Sentry Dashboard через 5-10 секунд.

---

## Документация

### 📚 Полные гайды

1. **[SENTRY_SETUP.md](./SENTRY_SETUP.md)** - Полная инструкция по настройке
   - Создание проектов в Sentry
   - Настройка alerts и notifications
   - Интеграция с Telegram/Slack
   - Как читать отчеты Sentry

2. **[SENTRY_EXAMPLE_USAGE.md](./SENTRY_EXAMPLE_USAGE.md)** - Примеры использования
   - Frontend: мониторинг уроков, видео, AI curator
   - Backend: мониторинг AI генерации, email, database
   - Critical Tripwire flows
   - Common patterns

3. **[SENTRY_ENV_VARIABLES.md](./SENTRY_ENV_VARIABLES.md)** - Переменные окружения
   - Как получить DSN
   - Разные окружения (dev/staging/prod)
   - Безопасность
   - Troubleshooting

---

## Что отслеживается?

### Frontend (автоматически)

✅ **Ошибки:**
- Необработанные exceptions
- React component errors (ErrorBoundary)
- Async errors (Promise rejections)
- Network errors (fetch/axios)

✅ **Производительность:**
- Page load time
- Component render time
- Navigation (React Router)
- API requests duration

✅ **User Experience:**
- Session Replay при ошибках
- Breadcrumbs (что делал пользователь)
- User context (ID, email, роль)

### Backend (автоматически)

✅ **Ошибки:**
- Uncaught exceptions
- Unhandled Promise rejections
- API endpoint errors (500, 404)
- Database query errors

✅ **Производительность:**
- API endpoint response time
- Database query duration
- External API calls (AmoCRM, Facebook)
- File processing time

✅ **System Health:**
- Memory usage (alerts при > 80%)
- CPU usage (profiling)
- Infinite loop detection (> 1000 iterations)

---

## Примеры использования

### Frontend - Мониторинг урока

```typescript
import { monitorLessonLoading } from '@/utils/sentryMonitoring';

const loadLesson = async (lessonId: string) => {
  // ✅ Автоматически отслеживает:
  // - Длительность загрузки
  // - Ошибки при загрузке
  // - Если > 5 сек - отправляет warning
  return await monitorLessonLoading(lessonId, async () => {
    const response = await fetch(`/api/tripwire/lessons/${lessonId}`);
    return response.json();
  });
};
```

### Backend - Мониторинг AI генерации

```typescript
import { monitorAIGeneration } from '../utils/sentryMonitoring';

const generateDescription = async (videoId: string) => {
  // ✅ Автоматически отслеживает:
  // - AI response time
  // - Если > 30 сек - отправляет error
  // - Rate limits, API errors
  return await monitorAIGeneration(
    'groq',
    'lesson_description',
    { videoId, lessonId: 123 },
    async () => {
      const completion = await groq.chat.completions.create({
        model: 'llama-3.1-70b-versatile',
        messages: [{ role: 'user', content: prompt }],
      });
      return completion.choices[0].message.content;
    }
  );
};
```

### Обнаружение зацикливаний

```typescript
import { detectInfiniteLoop } from '../utils/sentryMonitoring';

let iterations = 0;
for (const lesson of lessons) {
  iterations++;
  
  // ✅ Каждые 100 итераций проверяем на зацикливание
  if (iterations % 100 === 0) {
    detectInfiniteLoop('process_lessons', iterations, {
      current_lesson: lesson.id,
    });
  }
  
  await processLesson(lesson);
}
```

📖 **Больше примеров:** [SENTRY_EXAMPLE_USAGE.md](./SENTRY_EXAMPLE_USAGE.md)

---

## FAQ

### Q: Нужно ли платить за Sentry?

**A:** Бесплатный план включает:
- 5,000 errors в месяц
- 10,000 performance transactions в месяц
- 50 session replays в месяц

Для большинства проектов этого достаточно на старте.

---

### Q: Будет ли Sentry замедлять приложение?

**A:** Нет, Sentry работает асинхронно:
- События отправляются в фоне
- Не блокирует основной поток
- Минимальное влияние на производительность (< 1%)

---

### Q: Что делать если Sentry не инициализируется?

**A:** Проверьте:
1. `.env` файл находится в корне проекта
2. Переменная называется `VITE_SENTRY_DSN` (frontend) или `SENTRY_DSN` (backend)
3. DSN правильный (скопирован с sentry.io)
4. Dev сервер перезапущен после изменения `.env`

📖 **Подробнее:** [SENTRY_ENV_VARIABLES.md](./SENTRY_ENV_VARIABLES.md#troubleshooting)

---

### Q: Как отключить Sentry для локальной разработки?

**A:** Просто закомментируйте или удалите DSN из `.env`:

```env
# VITE_SENTRY_DSN=https://...
```

Sentry не будет инициализирован, но приложение продолжит работать.

---

### Q: Можно ли видеть что делал пользователь перед ошибкой?

**A:** Да! Используйте **Session Replay** (включён автоматически):
- Записывается видео сессии при ошибке
- Видно все клики, скроллы, navigation
- Чувствительные данные (пароли) маскируются автоматически

---

### Q: Как настроить alerts в Telegram?

**A:** 
1. Зайдите в Sentry → Settings → Integrations
2. Выберите Telegram
3. Создайте бота через @BotFather
4. Добавьте webhook URL в Sentry
5. Выберите какие события отправлять

📖 **Подробнее:** [SENTRY_SETUP.md](./SENTRY_SETUP.md#alerts-и-notifications)

---

### Q: Что если я превышу лимиты бесплатного плана?

**A:** События просто перестанут отправляться в Sentry:
- Приложение продолжит работать нормально
- Вы увидите предупреждение в Sentry Dashboard
- Можно перейти на платный план или дождаться следующего месяца

---

### Q: Как посмотреть самые медленные API?

**A:**
1. Зайдите в Sentry Dashboard
2. Performance → Transactions
3. Отсортируйте по "Duration" (по убыванию)
4. Кликните на transaction для подробностей

---

### Q: Можно ли интегрировать Sentry с AmoCRM?

**A:** Да, через webhooks:
1. Sentry → Settings → Integrations → Webhooks
2. Создайте endpoint в AmoCRM для приема событий
3. Настройте какие события отправлять (errors, issues)

---

## 🚀 Что дальше?

1. ✅ Настройте Sentry DSN в `.env` файлах
2. ✅ Запустите приложение и убедитесь что Sentry работает
3. ✅ Добавьте мониторинг в критичные места (см. примеры)
4. ✅ Настройте alerts для важных событий
5. ✅ Интегрируйте с Telegram/Slack для получения уведомлений

---

## 📞 Support

Если у вас остались вопросы:

1. 📖 Читайте полную документацию в [SENTRY_SETUP.md](./SENTRY_SETUP.md)
2. 💡 Смотрите примеры в [SENTRY_EXAMPLE_USAGE.md](./SENTRY_EXAMPLE_USAGE.md)
3. 🔧 Troubleshooting в [SENTRY_ENV_VARIABLES.md](./SENTRY_ENV_VARIABLES.md#troubleshooting)
4. 📚 Официальная документация: [docs.sentry.io](https://docs.sentry.io)

---

## 🎉 Результат

После настройки Sentry вы получите:

✅ **Полную видимость** - где и когда происходят ошибки  
✅ **Быструю диагностику** - стек трейсы, breadcrumbs, session replay  
✅ **Обнаружение зацикливаний** - автоматические алерты при долгих операциях  
✅ **Performance insights** - какие API медленные, где узкие места  
✅ **Проактивный мониторинг** - узнаете о проблемах до пользователей  

🚀 **Серверы больше не будут падать неожиданно!**

---

**Сделано с ❤️ для onAI Academy Tripwire продукта**
