# 🚀 VERCEL DEPLOY INSTRUCTIONS

## ⚡ БЫСТРЫЙ ДЕПЛОЙ (Рекомендуемый метод)

### Через Deploy Hook (БЕЗ CLI):
```bash
# Загрузить переменные из .vercel.env
source .vercel.env

# Триггер деплоя на Vercel
curl -X POST "$VERCEL_DEPLOY_HOOK_URL"
```

✅ **Это автоматически:**
- Деплоит последний коммит из `main` ветки
- Собирает production билд
- Деплоит на `https://onai.academy`

---

## 🔍 ПРОВЕРКА СТАТУСА ДЕПЛОЯ

### 1. Через Vercel Dashboard:
https://vercel.com/onaicademy/onai-integrator-login/deployments

### 2. Проверка последнего коммита на production:
```bash
# Последний коммит на GitHub
git log -1 --format="%H %s" origin/main

# Ждём 2-3 минуты после curl trigger
# Открываем https://onai.academy и проверяем через DevTools:
# - Console -> смотрим на логи загрузки
# - Network -> проверяем загружаемые файлы
```

---

## 📋 ПОЛНЫЙ ЧЕКЛИСТ ДЕПЛОЯ

### 1. **Подготовка:**
```bash
# Проверить, что все изменения закоммичены
git status

# Проверить последний коммит
git log -1 --oneline
```

### 2. **Push на GitHub:**
```bash
git push origin main
```

### 3. **Деплой на Vercel:**
```bash
# Загрузить токены
source .vercel.env

# Триггер деплоя
curl -X POST "$VERCEL_DEPLOY_HOOK_URL"

# Результат:
# {"job":{"id":"...","state":"PENDING","createdAt":...}}
```

### 4. **Ожидание (2-3 минуты):**
- Vercel автоматически:
  - Клонирует последний коммит
  - Запускает `npm run build`
  - Деплоит на production

### 5. **Проверка:**
```bash
# Открыть в браузере
# https://onai.academy

# Сделать hard refresh (Cmd+Shift+R на Mac, Ctrl+F5 на Windows)

# Проверить в DevTools Console:
# - Нет ли ошибок
# - Загружаются ли новые изменения
```

---

## 🔐 ВАЖНЫЕ ТОКЕНЫ

**ВСЕ ТОКЕНЫ ХРАНЯТСЯ В `.vercel.env`** (не коммитится в git!)

```bash
# Всегда загружать перед деплоем:
source .vercel.env
```

---

## ⚠️ РАСПРОСТРАНЁННЫЕ ОШИБКИ

### 1. "Token is not valid"
**Решение:** Проверь `.vercel.env`, загрузи через `source .vercel.env`

### 2. Vercel деплоит старый коммит
**Решение:** 
```bash
# Проверь, что push был успешным
git log -1 origin/main

# Подожди 1-2 минуты после curl trigger
```

### 3. Build ошибки на Vercel
**Решение:**
- Проверь логи деплоя в Vercel Dashboard
- Убедись, что `npm run build` работает локально
- Проверь синтаксис в изменённых файлах

---

## 🎯 ПРАВИЛА ДЕПЛОЯ

1. **ВСЕГДА** делай `git push` ПЕРЕД деплоем на Vercel
2. **ВСЕГДА** загружай токены через `source .vercel.env`
3. **ВСЕГДА** проверяй, что последний коммит на GitHub совпадает с деплоем
4. **ВСЕГДА** делай hard refresh после деплоя
5. **ВСЕГДА** проверяй DevTools Console на ошибки

---

## 📝 ШПАРГАЛКА

```bash
# ✅ ПРАВИЛЬНЫЙ ДЕПЛОЙ (3 команды):
git push origin main
source .vercel.env && curl -X POST "$VERCEL_DEPLOY_HOOK_URL"
# Подожди 2-3 минуты, открой https://onai.academy, сделай hard refresh
```

---

**ВАЖНО:** Vercel автоматически деплоится при push на main, НО через Deploy Hook деплой происходит МГНОВЕННО!

