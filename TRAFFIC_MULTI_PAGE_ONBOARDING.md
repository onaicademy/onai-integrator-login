# 🎓 Traffic Dashboard - Multi-Page Onboarding

**Дата:** 22 декабря 2025  
**Статус:** ✅ DEPLOYED TO PRODUCTION  
**Версия:** 3.0 (Multi-Page)

---

## 🎯 ЧТО ИЗМЕНЕНО:

### ДО (старый onboarding):
❌ Показывал только кнопки на главной странице Dashboard  
❌ Не объяснял как подключать кабинеты  
❌ Не объяснял как делать анализ

### ПОСЛЕ (новый многостраничный):
✅ **3 страницы с автоматическими переходами:**
   1. Dashboard → объясняет метрики
   2. Settings → объясняет подключение кабинетов
   3. Analytics → объясняет AI-анализ

✅ **Автоматическая навигация:**
   - Нажимаешь "Далее" на Dashboard → автоматически переключается на Settings
   - Нажимаешь "Далее" на Settings → автоматически переключается на Analytics

✅ **Подробные инструкции:**
   - Как выбирать рекламные кабинеты (галочки)
   - Как выбирать кампании для трекинга
   - Как запускать AI-анализ
   - По каким критериям идет анализ (CTR, CPM, CPC, ROAS)

---

## 📋 СТРУКТУРА ONBOARDING (11 шагов):

### 🏠 DASHBOARD (шаги 0-3):

**Шаг 0:** Главные метрики
```
- Доход (сколько заработал)
- ROAS (эффективность рекламы, цель > 2.0x)
- CPA (стоимость одной продажи)
```

**Шаг 1:** Фильтр "Мои результаты"
```
- Показывает только твои данные
- Повторный клик = показать всю команду
```

**Шаг 2:** Таблица результатов
```
- Результаты всех таргетологов
- Твоя строка выделена зеленым
- AI-анализ только для твоих кампаний
```

**Шаг 3:** Переходный экран
```
🚀 Далее: Настройки
- Сейчас перейдем в Настройки
- Там подключишь рекламные кабинеты
- ⚠️ Без этого ROI не считается!
💡 Нажми "Далее" → автоматически перейдем
```

**[АВТОМАТИЧЕСКИЙ ПЕРЕХОД: Dashboard → Settings]**

---

### ⚙️ SETTINGS (шаги 4-6):

**Шаг 4:** Страница Настройки
```
- Здесь подключаешь рекламные кабинеты
- Выбираешь активные кампании для трекинга
```

**Шаг 5:** Выбор Рекламных Кабинетов
```
1️⃣ Поставь галочки на те ad accounts, которые используешь
💡 Если кабинет серый — он не активен в Facebook
```

**Шаг 6:** Выбор Активных Кампаний
```
2️⃣ Поставь галочки на кампании, которые приносят продажи
⚠️ ВАЖНО: При запуске НОВОЙ кампании — добавь её здесь!
💡 Иначе её результаты не попадут в Dashboard
```

**Шаг 7:** Переходный экран
```
🚀 Далее: Детальная Аналитика
- Теперь перейдем к Анализу РК
- Получишь AI-анализ кампаний
- Оценку настроек аудиторий
- Рекомендации по улучшению
💡 Нажми "Далее" → перейдем на страницу Анализа
```

**[АВТОМАТИЧЕСКИЙ ПЕРЕХОД: Settings → Analytics]**

---

### 📊 DETAILED ANALYTICS (шаги 8-10):

**Шаг 8:** Детальная Аналитика РК
```
- Здесь получаешь AI-анализ своих кампаний
📌 Анализ занимает ~10 секунд
```

**Шаг 9:** Запуск AI-Анализа
```
🤖 Нажми эту кнопку, чтобы получить:
- Оценку эффективности кампаний
- Анализ аудиторий и креативов
- Рекомендации по улучшению ROAS
- Предупреждения о проблемах
💡 Анализ обновляется при каждом запуске
```

**Шаг 10:** Критерии Анализа
```
📈 AI оценивает кампании по:
- CTR (Click-Through Rate) — кликабельность
- CPM (Cost Per Mille) — стоимость 1000 показов
- CPC (Cost Per Click) — стоимость клика
- Conversion Rate — % продаж от кликов
- ROAS — возврат инвестиций

⚡ Цель: CTR > 2%, ROAS > 2.0x, Conversion > 5%
```

**Шаг 11:** Обучение завершено
```
🎉 Теперь ты знаешь как:
✅ Смотреть свои результаты на Dashboard
✅ Подключать рекламные кабинеты в Настройках
✅ Получать AI-анализ кампаний
✅ Интерпретировать метрики
💡 Совет: Делай анализ 1-2 раза в неделю!
```

---

## 🔧 ТЕХНИЧЕСКАЯ РЕАЛИЗАЦИЯ:

### 1. Навигация между страницами:
```typescript
// OnboardingTour.tsx - строка 420
if (action === ACTIONS.NEXT && type === EVENTS.STEP_AFTER) {
  // Dashboard (шаг 3) → переходим на Settings
  if (index === 3 && currentPage === 'dashboard') {
    navigate('/traffic/settings');
    setStepIndex(4);
    return;
  }
  
  // Settings (шаг 6) → переходим на Analytics
  if (index === 6 && currentPage === 'settings') {
    navigate('/traffic/detailed-analytics');
    setStepIndex(7);
    return;
  }
}
```

### 2. Отслеживание текущей страницы:
```typescript
// OnboardingTour.tsx - строка 229
useEffect(() => {
  const path = location.pathname;
  if (path.includes('/settings')) {
    setCurrentPage('settings');
  } else if (path.includes('/detailed-analytics')) {
    setCurrentPage('analytics');
  } else if (path.includes('/dashboard')) {
    setCurrentPage('dashboard');
  }
}, [location.pathname]);
```

### 3. Data-tour атрибуты:
```typescript
// TrafficSettings.tsx
<div data-tour="fb-accounts-list">
  {/* Список рекламных кабинетов */}
</div>

<div data-tour="campaigns-list">
  {/* Список кампаний */}
</div>

// TrafficDetailedAnalytics.tsx
<Button data-tour="analyze-button">
  AI Analysis
</Button>

<div data-tour="metrics-details">
  {/* Метрики кампаний */}
</div>
```

---

## 🎨 ДИЗАЙН:

### Premium стиль (как Stripe/Notion):
```typescript
overlayColor: 'rgba(0, 0, 0, 0.5)'  // Легкое затемнение
primaryColor: '#00FF88'               // onAI green
backgroundColor: 'rgba(10, 10, 10, 0.98)'  // Темный с прозрачностью
boxShadow: '0 0 30px rgba(0, 255, 136, 0.3)'  // Glow эффект
spotlight: '0 0 0 9999px rgba(0, 0, 0, 0.5)'  // Spotlight на элементе
```

### Кнопки:
```
Next: Зеленая (#00FF88) с черным текстом
Back: Прозрачная с серым текстом
Skip: Маленькая серая (rgba(255,255,255,0.4))
```

---

## 📊 ЛОГИКА РАБОТЫ:

### 1. Запуск onboarding:
```
- Проверяется traffic_onboarding_progress.is_completed
- Если false → запускается tour
- Если true → tour не показывается
```

### 2. Сохранение прогресса:
```
POST /api/traffic-onboarding/progress
{
  user_id: "Kenesary",
  is_completed: true,
  tour_type: "targetologist"
}
```

### 3. Observability logging:
```
🎓 [ONBOARDING] joyride_event {
  action: "next",
  index: 3,
  status: "running",
  type: "step:after",
  currentPage: "dashboard"
}

🎓 [ONBOARDING] Navigation: Dashboard → Settings
```

---

## 🧪 ТЕСТИРОВАНИЕ:

### Шаг 1: Очистка прогресса (для повторного просмотра):
```sql
UPDATE traffic_onboarding_progress
SET is_completed = false
WHERE user_id = 'Kenesary';
```

### Шаг 2: Открой Dashboard:
```
http://localhost:8080/traffic/dashboard
(или https://onai.academy/traffic/dashboard на production)
```

### Шаг 3: Проверь автоматические переходы:
```
1. Dashboard → видишь 4 подсказки → нажми "Далее" 4 раза
2. На 4-м "Далее" → должен автоматически перейти на /traffic/settings
3. Settings → видишь 3 подсказки → нажми "Далее" 3 раза
4. На 3-м "Далее" → должен автоматически перейти на /traffic/detailed-analytics
5. Analytics → видишь 4 подсказки → завершаешь onboarding
```

### Шаг 4: Проверь консоль браузера:
```javascript
// DevTools Console должен показать:
🎓 [ONBOARDING] joyride_event { index: 3, action: "next" }
🎓 [ONBOARDING] Navigation: Dashboard → Settings
🎓 [ONBOARDING] joyride_event { index: 6, action: "next" }
🎓 [ONBOARDING] Navigation: Settings → Analytics
✅ [ONBOARDING SUCCESS] tour_completed
```

---

## 📁 ИЗМЕНЕННЫЕ ФАЙЛЫ:

```
✅ src/components/traffic/OnboardingTour.tsx
   - Added useNavigate, useLocation
   - Multi-page navigation logic
   - 11 steps (4 + 3 + 4)
   - Auto-navigation on steps 3 and 6

✅ src/pages/traffic/TrafficSettings.tsx
   - Added data-tour="fb-accounts-list"
   - Added data-tour="campaigns-list"

✅ src/pages/traffic/TrafficDetailedAnalytics.tsx
   - Changed data-tour="ai-analysis-button" → "analyze-button"
   - Added data-tour="metrics-details"

✅ Database:
   - Обнулены настройки таргетологов (чистая установка)
   - fb_ad_accounts = []
   - tracked_campaigns = []
   - facebook_connected = false
```

---

## 🎉 ГОТОВО К ИСПОЛЬЗОВАНИЮ!

### Локальное тестирование:
```bash
# 1. Backend уже запущен (localhost:3000)
# 2. Frontend уже запущен (localhost:8080)
# 3. Открой: http://localhost:8080/traffic/login
# 4. Войди: kenesary@onai.academy / onai2024
# 5. Увидишь многостраничный onboarding!
```

### Production тестирование:
```bash
# Задеплоено на: https://onai.academy/traffic/dashboard
# Можешь тестировать прямо сейчас!
```

---

## 🚀 BONUS: Как повторно показать onboarding:

### Вариант 1: SQL (для любого пользователя):
```sql
UPDATE traffic_onboarding_progress
SET is_completed = false
WHERE user_id = 'Kenesary';
```

### Вариант 2: Console браузера (для текущего):
```javascript
// В DevTools Console:
localStorage.removeItem('traffic_onboarding_completed');
// Reload страницы
location.reload();
```

### Вариант 3: Добавить "Reset Onboarding" кнопку в UI:
```typescript
// В ProfileSettings или TrafficSettings
<Button onClick={async () => {
  await axios.post(`${API_URL}/api/traffic-onboarding/progress`, {
    user_id: userId,
    is_completed: false
  });
  toast.success('Onboarding сброшен! Reload страницы.');
}}>
  🔄 Сбросить обучение
</Button>
```

---

## 📊 СТАТИСТИКА:

```
Количество шагов: 11 (было 7)
Количество страниц: 3 (было 1)
Автопереходов: 2 (Dashboard→Settings, Settings→Analytics)
Время прохождения: ~3-4 минуты
```

---

## 🎓 USER FLOW:

```
START
  ↓
📍 DASHBOARD (4 шага)
  ├─ Метрики
  ├─ Фильтр "Мои результаты"
  ├─ Таблица результатов
  └─ [Нажать "Далее"] → ПЕРЕХОД
       ↓
📍 SETTINGS (3 шага)
  ├─ Инфо о странице
  ├─ Выбор Ad Accounts (галочки)
  ├─ Выбор Campaigns (галочки)
  └─ [Нажать "Далее"] → ПЕРЕХОД
       ↓
📍 ANALYTICS (4 шага)
  ├─ Инфо о странице
  ├─ Кнопка "AI Analysis"
  ├─ Критерии анализа (CTR, CPM, CPC, ROAS)
  └─ Завершение
       ↓
END (прогресс сохранен в БД)
```

---

## 💡 ВАЖНЫЕ ДЕТАЛИ:

### 1. Delay между переходами:
```typescript
setTimeout(() => {
  navigate('/traffic/settings');
  setStepIndex(4);
}, 300); // 300ms delay для плавности
```

### 2. Проверка текущей страницы:
```typescript
useEffect(() => {
  const path = location.pathname;
  if (path.includes('/settings')) setCurrentPage('settings');
  // ...
}, [location.pathname]);
```

### 3. Conditional navigation:
```typescript
if (index === 3 && currentPage === 'dashboard') {
  // Переход только если мы на Dashboard и на шаге 3
  navigate('/traffic/settings');
}
```

---

## 🐛 TROUBLESHOOTING:

### Проблема: Onboarding не запускается
**Решение:**
```sql
-- Проверь БД:
SELECT * FROM traffic_onboarding_progress WHERE user_id = 'Kenesary';
-- Если is_completed = true → сбрось на false
```

### Проблема: Переходы не работают
**Решение:**
```typescript
// Проверь консоль на наличие:
🎓 [ONBOARDING] Navigation: Dashboard → Settings
// Если нет → проверь что data-tour атрибуты существуют
```

### Проблема: Элементы не подсвечиваются
**Решение:**
```typescript
// Проверь что элементы существуют на странице:
document.querySelector('[data-tour="fb-accounts-list"]')  // Settings
document.querySelector('[data-tour="analyze-button"]')     // Analytics
```

---

## 📝 CHANGELOG:

### v3.0 (22 декабря 2025):
- ✅ Multi-page onboarding (3 страницы)
- ✅ Автоматическая навигация между страницами
- ✅ 11 шагов вместо 7
- ✅ Подробные инструкции для каждой страницы
- ✅ Критерии AI-анализа (CTR, CPM, CPC, ROAS, Conversion)
- ✅ Observability logging

### v2.0 (21 декабря 2025):
- Premium дизайн (Stripe/Notion style)
- Spotlight эффект
- Затемнение фона

### v1.0 (20 декабря 2025):
- Базовый onboarding на Dashboard

---

**Статус:** ✅ DEPLOYED TO PRODUCTION  
**Доступен:** https://onai.academy/traffic/dashboard  
**Локально:** http://localhost:8080/traffic/dashboard

🎉 **ГОТОВ К ТЕСТИРОВАНИЮ!**
