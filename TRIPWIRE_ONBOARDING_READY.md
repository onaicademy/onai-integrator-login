# ✅ TRIPWIRE ONBOARDING SYSTEM - ГОТОВО К ТЕСТИРОВАНИЮ

**Дата**: 19 декабря 2025  
**Статус**: 🟢 Готово к локальному тестированию

---

## 📦 Что реализовано

### Frontend (React)

✅ **Компоненты**:
- `TripwireOnboardingContext.tsx` - React Context для управления состоянием
- `OnboardingWelcomeModal.tsx` - Приветственное модальное окно с киберпанк анимациями
- `TripwireOnboardingTour.tsx` - Главный компонент тура с driver.js
- `TripwireLayout.tsx` - Интеграция onboarding в layout (Provider + Modal + Tour)

✅ **Конфигурация**:
- `tripwireOnboardingSteps.ts` - 10 шагов интерактивного тура
- `tripwire-onboarding.css` - Кастомные стили driver.js с неоновыми эффектами

✅ **Data-атрибуты** (для driver.js):
- `TripwireSidebar.tsx`:
  - `data-onboarding="sidebar-container"` - весь sidebar
  - `data-onboarding="menu-home"` - кнопка "Главная"
  - `data-onboarding="menu-profile"` - кнопка "Мой профиль"
  - `data-onboarding="menu-locked"` - первый заблокированный элемент (NeuroHUB)
  
- `TripwireProductPage.tsx`:
  - `data-onboarding="modules-container"` - контейнер модулей
  - `data-onboarding="module-active"` - Модуль 1 (активный)
  - `data-onboarding="module-locked"` - Модуль 2 (заблокированный)
  - `data-onboarding="progress-panel"` - панель прогресса (LiveStreamModule)

### Backend (Node.js/Express)

✅ **API Endpoints**:
- `POST /api/tripwire/onboarding/complete` - Завершить onboarding
- `GET /api/tripwire/onboarding/status` - Получить статус onboarding

✅ **База данных**:
- Миграция `20251219032036_add_onboarding_flag.sql` создана
- Добавлены поля:
  - `onboarding_completed` (BOOLEAN, DEFAULT FALSE)
  - `onboarding_completed_at` (TIMESTAMP)
- Индекс для быстрого поиска пользователей без onboarding

### Библиотеки

✅ **driver.js v1.4.0** установлен

---

## 🧪 Как протестировать локально

### 1. Применить миграцию БД

**Подключитесь к Tripwire БД и выполните:**

```bash
cd /Users/miso/onai-integrator-login

# Применяем миграцию
psql "$TRIPWIRE_DATABASE_URL" -f supabase/migrations/20251219032036_add_onboarding_flag.sql
```

**Проверка:**
```sql
SELECT column_name, data_type, column_default 
FROM information_schema.columns 
WHERE table_name = 'tripwire_users' 
AND column_name IN ('onboarding_completed', 'onboarding_completed_at');
```

### 2. Создать тестового пользователя

```sql
-- Создаем тестового пользователя БЕЗ onboarding
INSERT INTO tripwire_users (email, full_name, onboarding_completed)
VALUES ('test-onboarding@example.com', 'Test Onboarding User', FALSE)
ON CONFLICT (email) DO UPDATE 
SET onboarding_completed = FALSE, onboarding_completed_at = NULL;
```

### 3. Запустить dev сервер

```bash
# Terminal 1: Frontend
cd /Users/miso/onai-integrator-login
npm run dev

# Terminal 2: Backend
cd /Users/miso/onai-integrator-login/backend
npm run dev
```

### 4. Тестовые сценарии

#### ✅ Сценарий 1: Первый вход (Happy Path)

1. Откройте `http://localhost:8080/integrator/login`
2. Войдите как `test-onboarding@example.com`
3. **Ожидаемое поведение**:
   - ✅ Появляется Welcome Modal с неоновыми эффектами
   - ✅ Кнопка "НАЧАТЬ ТУР" (единственная)
   - ✅ Анимация пульсации логотипа

4. Кликните "НАЧАТЬ ТУР"
5. **Ожидаемое поведение**:
   - ✅ Modal закрывается
   - ✅ Запускается driver.js тур
   - ✅ Подсветка первого элемента (sidebar) с неоновым свечением
   - ✅ Попап с текстом "БОКОВОЕ МЕНЮ - ВАШ ЦЕНТР УПРАВЛЕНИЯ"

6. Пройдите все 10 шагов (кнопка "Далее")
7. **Ожидаемое поведение**:
   - ✅ Каждый шаг подсвечивает правильный элемент
   - ✅ Анимация пульсации на подсвеченных элементах
   - ✅ На шаге 10 кнопка "НАЧАТЬ ОБУЧЕНИЕ"

8. Кликните "НАЧАТЬ ОБУЧЕНИЕ"
9. **Ожидаемое поведение**:
   - ✅ Тур закрывается
   - ✅ В консоли: "🎉 TripwireOnboarding: Onboarding completed successfully"
   - ✅ В БД: `onboarding_completed = TRUE`

10. **Проверка БД**:
```sql
SELECT email, onboarding_completed, onboarding_completed_at 
FROM tripwire_users 
WHERE email = 'test-onboarding@example.com';
```

#### ✅ Сценарий 2: Повторный вход

1. Выйдите из системы
2. Войдите снова как `test-onboarding@example.com`
3. **Ожидаемое поведение**:
   - ✅ Welcome Modal НЕ появляется
   - ✅ Тур НЕ запускается
   - ✅ Обычная работа платформы

#### ✅ Сценарий 3: Мобильная версия

1. Откройте DevTools (F12)
2. Переключитесь на мобильный вид (iPhone 12, 390x844)
3. Войдите как новый пользователь (сбросьте onboarding_completed в БД)
4. **Ожидаемое поведение**:
   - ✅ Welcome Modal адаптирован под мобилку
   - ✅ Попапы не выходят за границы экрана
   - ✅ Текст читабелен
   - ✅ Кнопки кликабельны

#### ✅ Сценарий 4: Keyboard Navigation

1. Запустите onboarding
2. Нажмите `Tab` на клавиатуре
3. **Ожидаемое поведение**:
   - ✅ Фокус переключается между кнопками
   - ✅ `Enter` = "Далее"
   - ✅ `Esc` = ничего не делает (нельзя пропустить)

#### ✅ Сценарий 5: API тест

```bash
# Получить токен пользователя (из dev tools -> Application -> Local Storage)
TOKEN="your_access_token_here"

# Проверить статус
curl -X GET http://localhost:3001/api/tripwire/onboarding/status \
  -H "Authorization: Bearer $TOKEN"

# Завершить onboarding вручную
curl -X POST http://localhost:3001/api/tripwire/onboarding/complete \
  -H "Authorization: Bearer $TOKEN"
```

---

## 🐛 Известные проблемы и решения

### Проблема: Welcome Modal не появляется

**Причина**: onboarding_completed уже TRUE в БД  
**Решение**:
```sql
UPDATE tripwire_users 
SET onboarding_completed = FALSE, onboarding_completed_at = NULL 
WHERE email = 'your-email@example.com';
```

### Проблема: Driver.js не находит элементы

**Причина**: DOM еще не загрузился  
**Решение**: Уже реализовано - задержка 500ms в `TripwireOnboardingTour.tsx`

### Проблема: Попапы выходят за экран на мобилках

**Причина**: Фиксированная позиция  
**Решение**: Уже реализовано - адаптивные стили в `tripwire-onboarding.css` (media query @768px)

### Проблема: Ошибка 401 при вызове API

**Причина**: Неверный токен  
**Решение**: Проверьте что токен актуальный (копируйте из Application -> Local Storage -> sb-*-auth-token)

---

## 📊 Метрики успеха (после деплоя)

- **Completion Rate**: > 80% пользователей завершают тур
- **Time to Complete**: < 3 минуты среднее время
- **Drop-off Rate**: < 20% пользователей бросают тур
- **Mobile vs Desktop**: Одинаковое UX на обеих платформах
- **Activation Rate**: > 90% пользователей кликают на Модуль 1 после тура

---

## 🚀 Что дальше

### Перед продакшеном:

- [ ] Протестировать все 5 сценариев локально
- [ ] Проверить консоль на ошибки
- [ ] Lighthouse Performance > 90
- [ ] Проверить на iPhone и Android (реальные устройства)
- [ ] Применить миграцию на production БД
- [ ] Задеплоить backend (новый роут `/api/tripwire/onboarding`)
- [ ] Задеплоить frontend (новые компоненты + стили)

### После деплоя:

- [ ] Протестировать на production
- [ ] Мониторить метрики в первые 24 часа
- [ ] Собрать фидбек от первых пользователей
- [ ] Оптимизировать если нужно

---

## 📝 Дополнительные возможности (опционально)

### 1. Аналитика событий

Добавить трекинг в `TripwireOnboardingTour.tsx`:
```typescript
// При старте тура
analytics.track('onboarding_started');

// При каждом шаге
analytics.track('onboarding_step_completed', { step: currentStep });

// При завершении
analytics.track('onboarding_completed', { duration: timeElapsed });
```

### 2. Сохранение прогресса в localStorage

Если пользователь закрыл вкладку - восстановить тур:
```typescript
localStorage.setItem('onboarding_current_step', currentStep.toString());
```

### 3. A/B тестирование

Тестировать разные варианты текстов/анимаций для оптимизации completion rate.

---

## 🎯 Итоговый чеклист

### Frontend
- [x] driver.js установлен
- [x] TripwireOnboardingContext создан
- [x] OnboardingWelcomeModal создан
- [x] TripwireOnboardingTour создан
- [x] Кастомные стили driver.js созданы
- [x] Data-атрибуты добавлены в TripwireSidebar
- [x] Data-атрибуты добавлены в TripwireProductPage
- [x] TripwireLayout обернут в Provider
- [x] Компоненты Modal и Tour добавлены в Layout

### Backend
- [x] Миграция создана
- [x] API endpoint `/complete` создан
- [x] API endpoint `/status` создан
- [x] Роут зарегистрирован в server.ts

### Тестирование
- [ ] Миграция применена локально
- [ ] Тестовый пользователь создан
- [ ] Сценарий 1 протестирован (первый вход)
- [ ] Сценарий 2 протестирован (повторный вход)
- [ ] Сценарий 3 протестирован (мобильная версия)
- [ ] Сценарий 4 протестирован (keyboard navigation)
- [ ] Сценарий 5 протестирован (API)

---

**🎉 Система готова к локальному тестированию!**

Следующий шаг: Запустите dev сервер и протестируйте все сценарии.



