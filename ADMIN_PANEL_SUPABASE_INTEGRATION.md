# 🎯 Интеграция Supabase в Админ-панель активности

## 📋 Обзор

Админ-панель теперь полностью интегрирована с Supabase и отображает **реальные данные** о пользователях, их прогрессе, достижениях и активности. Система анализирует данные из базы и предоставляет детальную аналитику для каждого пользователя.

---

## 🆕 Что нового

### 1. Утилиты для работы с данными (`src/lib/admin-utils.ts`)

Создан новый модуль с функциями для получения и анализа данных пользователей:

#### 🔹 Функции для достижений:
- `getAllAchievements()` - получить все достижения из базы
- `getUserAchievements(userId)` - получить достижения пользователя
- `getUserAchievementsWithDetails(userId)` - получить достижения с полной информацией и статусом выполнения

#### 🔹 Функции для прогресса:
- `getUserProgress(userId)` - получить прогресс по урокам
- `getUserStats(userId)` - получить полную статистику пользователя:
  - Общее количество XP
  - Количество завершенных уроков
  - Общее время просмотра (в секундах)
  - Количество достижений
  - Текущий стрик (дней подряд)
  - Среднее время обучения в день

#### 🔹 Функции для активности:
- `getUserDailyActivity(userId, days)` - получить ежедневную активность за период
- `getUserActivityLog(userId, limit)` - получить лог действий пользователя
- `getUserLatestDiagnostics(userId)` - получить последнюю AI-диагностику
- `getUserDiagnosticsHistory(userId)` - получить историю всех диагностик

#### 🔹 Функции для админ-аналитики:
- `getPlatformStats()` - получить общую статистику платформы:
  - Активные пользователи сегодня
  - Активные пользователи за неделю
  - Количество сообщений
  - Пользователи в риске оттока (без активности 7+ дней)
- `getWeeklyActivityData()` - получить данные активности по дням для графиков
- `getAllUsers()` - получить список всех пользователей

---

## 🎨 Обновленная админ-панель

### Новые секции:

#### 1. **📊 Метрики платформы** (реальные данные)
Карточки с ключевыми показателями:
- **Активных сегодня** - пользователи с активностью сегодня
- **Сообщений сегодня** - количество отправленных сообщений
- **Активных за неделю** - уникальные пользователи за 7 дней
- **В риске оттока** - пользователи без активности 7+ дней

#### 2. **📈 Графики активности**
- **Активность за неделю** - график пользователей и сообщений по дням
- **Тренд по неделям** - динамика активных пользователей и риска оттока

#### 3. **👥 Таблица пользователей** (новое!)
Список всех зарегистрированных пользователей с:
- Email
- Имя
- Роль (admin/student)
- Дата регистрации
- Кнопка "Просмотр" для детальной информации

#### 4. **🔍 Модальное окно пользователя** (новое!)
При клике на "Просмотр" открывается детальная информация с 3 вкладками:

##### **Вкладка "Обзор"**
- Карточки с метриками:
  - 🔥 Всего XP
  - 🎯 Завершено уроков
  - 🏆 Получено достижений
  - ⏱️ Среднее время в день (минут)
- Информация о пользователе:
  - Email, имя, роль
  - Дата регистрации
  - Текущий стрик (🔥 дней подряд)

##### **Вкладка "Достижения"**
- Список всех достижений с визуальной индикацией:
  - ✅ Выполненные (яркие, с датой получения)
  - ⬜ Не выполненные (затемненные)
- Редкость достижений (common, rare, epic, legendary) с цветовыми маркерами

##### **Вкладка "Диагностика"**
- **Метрики обучения** (из AI-диагностики):
  - Завершено уроков
  - Среднее время в день
  - Текущий стрик
  - Флаг низкой вовлеченности (да/нет)
- **Застрявшие уроки** (если есть) - список уроков, на которых пользователь застрял
- **💡 Рекомендация AI** - персонализированный совет от AI-диагноста

---

## 🗃️ Структура данных

### Таблицы Supabase, используемые админ-панелью:

#### `users`
```sql
- id (uuid)
- email (text)
- full_name (text)
- avatar_url (text)
- role (text) - 'student' | 'admin'
- created_at (timestamptz)
```

#### `achievements`
```sql
- id (uuid)
- title (text)
- description (text)
- icon (text)
- rarity ('common' | 'rare' | 'epic' | 'legendary')
```

#### `user_achievements`
```sql
- id (uuid)
- user_id (uuid) → users(id)
- achievement_id (uuid) → achievements(id)
- unlocked_at (timestamptz)
```

#### `progress`
```sql
- id (uuid)
- user_id (uuid) → users(id)
- lesson_id (uuid) → lessons(id)
- is_completed (boolean)
- xp_earned (integer)
- seconds_watched (integer)
- updated_at (timestamptz)
```

#### `daily_activity`
```sql
- id (uuid)
- user_id (uuid) → users(id)
- date (date)
- minutes (integer)
- lessons_watched (integer)
- xp_earned (integer)
- created_at (timestamptz)
```

#### `diagnostics_log`
```sql
- id (uuid)
- user_id (uuid) → users(id)
- data_json (jsonb) - содержит:
  {
    lessons_completed: number,
    avg_minutes_per_day: number,
    current_streak: number,
    flag_low_engagement: boolean,
    stuck_lessons: string[],
    recommendation: string
  }
- created_at (timestamptz)
```

#### `user_activity`
```sql
- id (uuid)
- user_id (uuid) → users(id)
- page (text)
- action (text)
- meta (jsonb)
- created_at (timestamptz)
```

---

## 🚀 Как использовать

### 1. Открыть админ-панель
Перейдите на `/admin/activity`

### 2. Просмотр общей статистики
На главной странице отображаются:
- Ключевые метрики платформы
- Графики активности
- Список всех пользователей

### 3. Просмотр деталей пользователя
1. В таблице пользователей нажмите на иконку 👁️ (Eye) напротив нужного пользователя
2. Откроется модальное окно с 3 вкладками:
   - **Обзор** - общая статистика
   - **Достижения** - список всех достижений с отметками о выполнении
   - **Диагностика** - AI-анализ и рекомендации

---

## 🧪 Тестирование с тестовыми данными

### Создание тестовых достижений

Функция `seedAchievements()` в `admin-utils.ts` создает 5 тестовых достижений:

```typescript
import { seedAchievements } from '@/lib/admin-utils';

// В консоли браузера или в коде:
await seedAchievements();
```

Тестовые достижения:
1. **Первый шаг** (common) - Завершите первый урок
2. **Быстрый старт** (common) - Завершите 5 уроков за неделю
3. **Марафонец** (rare) - Занимайтесь 7 дней подряд
4. **Знаток AI** (epic) - Завершите курс по AI
5. **Мастер интеграции** (legendary) - Завершите все проектные модули

### Добавление тестовых данных вручную

#### Создать пользователя:
```sql
INSERT INTO users (email, full_name, role) 
VALUES ('test@example.com', 'Тестовый пользователь', 'student');
```

#### Добавить прогресс:
```sql
INSERT INTO progress (user_id, lesson_id, is_completed, xp_earned, seconds_watched)
VALUES 
  ('user-uuid', 'lesson-uuid-1', true, 100, 1200),
  ('user-uuid', 'lesson-uuid-2', true, 150, 1800);
```

#### Добавить активность:
```sql
INSERT INTO daily_activity (user_id, date, minutes, lessons_watched, xp_earned)
VALUES 
  ('user-uuid', '2025-11-04', 45, 3, 250),
  ('user-uuid', '2025-11-03', 30, 2, 150);
```

#### Разблокировать достижение:
```sql
INSERT INTO user_achievements (user_id, achievement_id)
VALUES ('user-uuid', 'achievement-uuid');
```

#### Добавить диагностику:
```sql
INSERT INTO diagnostics_log (user_id, data_json)
VALUES (
  'user-uuid',
  '{
    "lessons_completed": 8,
    "avg_minutes_per_day": 35,
    "current_streak": 5,
    "flag_low_engagement": false,
    "stuck_lessons": [],
    "recommendation": "Отличная регулярность! Продолжай в том же духе."
  }'::jsonb
);
```

---

## 📊 Расчет метрик

### Текущий стрик (Current Streak)
Вычисляется на основе `daily_activity`:
- Берутся последние 7 дней
- Считаются последовательные дни с активностью (minutes > 0)
- Стрик прерывается при первом пропущенном дне

### Среднее время в день
```
avg_minutes_per_day = (сумма minutes за 7 дней) / 7
```

### Пользователи в риске оттока
Пользователи, у которых **нет записей** в `daily_activity` за последние 7 дней.

### Активные пользователи сегодня
Уникальные `user_id` в `daily_activity` с `date = сегодня` и `minutes > 0`.

---

## 🎯 Дальнейшие улучшения

### Планируется добавить:

1. **Фильтрация и поиск** в таблице пользователей
2. **Экспорт данных** пользователей в CSV/Excel
3. **Графики прогресса пользователя** на вкладке "Обзор"
4. **Уведомления** для пользователей в риске оттока
5. **Сравнение пользователей** - выбрать нескольких и сравнить метрики
6. **Детальная хронология активности** - полный лог действий пользователя
7. **Интеграция с Edge Function** для запуска диагностики прямо из админ-панели

---

## 🐛 Известные ограничения

1. **Таблица `admin_reports`** может не существовать - это нормально, код обрабатывает эту ситуацию
2. **Сообщения** (`messages_today`) пока считаются из `user_activity` где `action` содержит 'message' - требует доработки под вашу модель данных
3. **Графики** используют моковые данные для недельного тренда - можно заменить на реальные данные из БД

---

## 📖 Примеры использования API

### Получить статистику пользователя
```typescript
import { getUserStats } from '@/lib/admin-utils';

const stats = await getUserStats('user-uuid');
console.log(stats);
// {
//   totalXP: 1240,
//   completedLessons: 15,
//   totalSecondsWatched: 18600,
//   achievementsCount: 3,
//   currentStreak: 5,
//   avgMinutesPerDay: 35
// }
```

### Получить достижения пользователя с деталями
```typescript
import { getUserAchievementsWithDetails } from '@/lib/admin-utils';

const achievements = await getUserAchievementsWithDetails('user-uuid');
// Возвращает массив всех достижений с полем completed: boolean
```

### Получить последнюю диагностику
```typescript
import { getUserLatestDiagnostics } from '@/lib/admin-utils';

const diagnostics = await getUserLatestDiagnostics('user-uuid');
console.log(diagnostics.data_json.recommendation);
// "Отличная регулярность! Продолжай в том же духе."
```

---

## ✅ Чеклист готовности

- [x] Создан модуль `admin-utils.ts` с функциями для работы с данными
- [x] Обновлена админ-панель для получения реальных данных из Supabase
- [x] Добавлена таблица пользователей с кнопкой просмотра
- [x] Создано модальное окно с детальной информацией о пользователе
- [x] Реализованы 3 вкладки: Обзор, Достижения, Диагностика
- [x] Интегрированы метрики: XP, уроки, достижения, стрик
- [x] Отображаются графики активности на основе реальных данных
- [x] Обработаны случаи отсутствия данных (пустые состояния)
- [ ] Добавить тестовые данные в Supabase для проверки
- [ ] Протестировать все функции с реальными данными

---

## 🎉 Готово!

Теперь ваша админ-панель полностью интегрирована с Supabase и показывает реальные данные о пользователях, их прогрессе, достижениях и активности. Все готово для тестирования и дальнейшего развития!

**Следующий шаг**: Добавьте тестовые данные в Supabase и проверьте работу всех функций.

