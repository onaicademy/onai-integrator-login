# 🚀 План доведения Traffic Dashboard до 100% продакшена

**Дата**: 2025-12-27  
**Текущая готовность**: 85%  
**Целевая готовность**: 100%  
**Оценка времени**: 5-7 часов

---

## 📊 Текущее состояние

### Что уже сделано (85%)

| Категория | Статус | Готовность |
|-----------|---------|------------|
| Архитектура | ✅ Отлично | 90% |
| Безопасность | ✅ Хорошо | 85% |
| Производительность | ✅ Хорошо | 80% |
| Надежность | ✅ Хорошо | 85% |
| Тестируемость | ⚠️ Требует улучшения | 60% |
| Документация | ✅ Хорошо | 75% |
| **Общая готовность** | **✅ Хорошо** | **85%** |

### Реализованные улучшения

1. ✅ Дедупликация AmoCRM Webhook
2. ✅ Унифицированный сервис маппинга таргетологов
3. ✅ Circuit Breaker & Retry Logic
4. ✅ Интеграция Circuit Breaker в Facebook Ads API
5. ✅ Диагностика интеграций

---

## 🎯 План действий для 100% готовности

### Phase 1: Критические улучшения (2-3 часа)

#### 1.1 Добавить валидацию входных данных (1-2 часа)

**Проблема**: Webhook данные не валидируются перед обработкой

**Решение**:
- Создать middleware для валидации webhook данных
- Использовать Zod для схем валидации
- Добавить валидацию для всех webhook endpoints

**Файлы**:
- Создать: `backend/src/middleware/validation.ts`
- Обновить: `backend/src/routes/amocrm-sales-webhook.ts`
- Обновить: `backend/src/routes/amocrm-express-webhook.ts`
- Обновить: `backend/src/routes/amocrm-flagship-webhook.ts`

**Пример кода**:
```typescript
// backend/src/middleware/validation.ts
import { z } from 'zod';

const AmoCRMWebhookSchema = z.object({
  leads: z.object({
    status: z.array(z.object({
      lead_id: z.number(),
      pipeline_id: z.number(),
      status_id: z.number(),
      price: z.number().optional(),
      custom_fields: z.array(z.object({
        id: z.number(),
        values: z.array(z.object({
          value: z.string().optional(),
        })),
      })).optional(),
    })),
  }),
});

export function validateAmoCRMWebhook(req: Request, res: Response, next: NextFunction) {
  try {
    AmoCRMWebhookSchema.parse(req.body);
    next();
  } catch (error) {
    console.error('❌ Webhook validation error:', error);
    return res.status(400).json({ error: 'Invalid webhook data' });
  }
}
```

#### 1.2 Улучшить обработку ошибок (1-2 часа)

**Проблема**: Некоторые ошибки не обрабатываются корректно

**Решение**:
- Создать централизованный error handler
- Добавить детальное логирование ошибок
- Добавить обработку специфических ошибок (AmoCRM, Facebook, Supabase)

**Файлы**:
- Создать: `backend/src/middleware/errorHandler.ts`
- Обновить: `backend/src/server.ts`

**Пример кода**:
```typescript
// backend/src/middleware/errorHandler.ts
import { Request, Response, NextFunction } from 'express';

export function errorHandler(err: Error, req: Request, res: Response, next: NextFunction) {
  console.error('❌ Error:', {
    message: err.message,
    stack: err.stack,
    path: req.path,
    method: req.method,
  });

  if (err.name === 'ValidationError') {
    return res.status(400).json({ error: 'Validation error', details: err.message });
  }

  if (err.name === 'UnauthorizedError') {
    return res.status(401).json({ error: 'Unauthorized' });
  }

  res.status(500).json({ error: 'Internal server error' });
}
```

### Phase 2: Интеграции (2-3 часа)

#### 2.1 Создать AmoCRM Leads Fetcher (1-2 часа)

**Проблема**: Нет сервиса для получения лидов из AmoCRM

**Решение**:
- Создать сервис для получения лидов из пайплайнов
- Реализовать функции для извлечения UTM данных
- Определить таргетолога и источник лида

**Файлы**:
- Создать: `backend/src/services/amocrm-leads-fetcher.ts`

**Пример кода**:
```typescript
// backend/src/services/amocrm-leads-fetcher.ts
import axios from 'axios';

const AMOCRM_BASE_URL = process.env.AMOCRM_DOMAIN;
const AMOCRM_ACCESS_TOKEN = process.env.AMOCRM_ACCESS_TOKEN;

export async function fetchLeadsFromPipeline(pipelineId: string, limit: number = 50) {
  const response = await axios.get(`${AMOCRM_BASE_URL}/api/v4/leads`, {
    params: {
      filter: { pipeline_id: pipelineId },
      limit,
      with: 'contacts,custom_fields',
    },
    headers: { Authorization: `Bearer ${AMOCRM_ACCESS_TOKEN}` },
  });

  return response.data._embedded.leads;
}

export function extractUTMData(lead: any) {
  const customFields = lead.custom_fields || [];
  const utmSource = customFields.find((f: any) => f.id === 12345)?.values[0]?.value;
  const utmCampaign = customFields.find((f: any) => f.id === 12346)?.values[0]?.value;
  const utmMedium = customFields.find((f: any) => f.id === 12347)?.values[0]?.value;

  return { utmSource, utmCampaign, utmMedium };
}
```

#### 2.2 Создать Total Sales Endpoint (1 час)

**Проблема**: Нет эндпоинта для получения всех продаж

**Решение**:
- Создать эндпоинт для получения всех продаж
- Группировка по источникам
- Фильтры по дате, пайплайну, продукту

**Файлы**:
- Создать: `backend/src/routes/traffic-total-sales.ts`
- Обновить: `backend/src/server.ts`

**API**: `GET /api/traffic/admin/total-sales`

**Параметры**:
- `startDate` (optional) - Начальная дата
- `endDate` (optional) - Конечная дата
- `pipelineId` (optional) - ID пайплайна
- `productId` (optional) - ID продукта

#### 2.3 Создать Leads by Funnel Endpoint (1 час)

**Проблема**: Нет эндпоинта для получения лидов по воронкам

**Решение**:
- Создать эндпоинт для получения лидов по пайплайнам
- Фильтры по пайплайну, статусу, дате
- Отображение UTM тегов и таргетолога

**Файлы**:
- Создать: `backend/src/routes/traffic-leads-by-funnel.ts`
- Обновить: `backend/src/server.ts`

**API**: `GET /api/traffic/admin/leads-by-funnel`

**Параметры**:
- `pipelineId` (optional) - ID пайплайна
- `statusId` (optional) - ID статуса
- `startDate` (optional) - Начальная дата
- `endDate` (optional) - Конечная дата

### Phase 3: Подготовка к тестированию (1 час)

#### 3.1 Проверить все конфигурации

**Действия**:
- Проверить `.env` файлы
- Убедиться, что все API ключи валидны
- Проверить соединения с базами данных

**Команды**:
```bash
# Проверить Supabase соединения
npx tsx scripts/check-db-connections.ts

# Проверить AmoCRM токен
npx tsx scripts/check-amocrm-token.ts

# Проверить Facebook токен
npx tsx scripts/check-facebook-token.ts
```

#### 3.2 Создать скрипт для локального тестирования

**Файл**: `scripts/test-traffic-dashboard.sh`

```bash
#!/bin/bash

echo "🧪 Testing Traffic Dashboard..."

# 1. Проверить backend
echo "1️⃣ Testing backend..."
curl -f http://localhost:3001/health || exit 1

# 2. Проверить Traffic Dashboard API
echo "2️⃣ Testing Traffic Dashboard API..."
curl -f http://localhost:3001/api/traffic/health || exit 1

# 3. Проверить интеграции
echo "3️⃣ Testing integrations..."
curl -f -X POST http://localhost:3001/api/admin/integrations/diagnostics \
  -H "Authorization: Bearer $JWT_TOKEN" || exit 1

echo "✅ All tests passed!"
```

---

## 🧪 План локального тестирования

### Тесты для выполнения

#### 1. Базовые тесты (10 минут)

- [ ] Backend запускается без ошибок
- [ ] Frontend запускается без ошибок
- [ ] Health check endpoints работают
- [ ] Redis соединение работает (или отключено)

#### 2. Аутентификация (10 минут)

- [ ] Логин администратора работает
- [ ] Логин таргетолога работает
- [ ] JWT токены валидируются
- [ ] Защищенные роуты недоступны без токена

#### 3. Traffic Dashboard API (20 минут)

- [ ] GET /api/traffic/health работает
- [ ] GET /api/traffic/combined-analytics работает
- [ ] GET /api/traffic/team-analytics работает
- [ ] POST /api/admin/integrations/diagnostics работает

#### 4. Webhook тесты (15 минут)

- [ ] AmoCRM Sales Webhook принимает данные
- [ ] Дедупликация работает
- [ ] Данные сохраняются в БД
- [ ] Таргетолог определяется корректно

#### 5. Facebook Ads API (15 минут)

- [ ] GET /api/facebook-ads работает
- [ ] Circuit Breaker работает
- [ ] Retry работает при ошибках
- [ ] Данные сохраняются в БД

#### 6. Интеграции (20 минут)

- [ ] Диагностика интеграций работает
- [ ] AmoCRM соединение работает
- [ ] Facebook Ads соединение работает
- [ ] Supabase соединение работает

---

## 🚀 План деплоя на продакшн

### Предварительные действия

1. **Создать бэкап продакшена**
```bash
ssh root@207.154.231.30 "pm2 stop onai-backend"
ssh root@207.154.231.30 "cd /var/www/onai-integrator-login-main && tar -czf backup-$(date +%Y%m%d-%H%M%S).tar.gz ."
```

2. **Собрать проект**
```bash
cd backend
npm run build
```

3. **Загрузить на сервер**
```bash
rsync -avz --exclude 'node_modules' --exclude '.env' ./ root@207.154.231.30:/var/www/onai-integrator-login-main/
```

4. **Установить зависимости**
```bash
ssh root@207.154.231.30 "cd /var/www/onai-integrator-login-main/backend && npm install --production"
```

5. **Перезапустить сервис**
```bash
ssh root@207.154.231.30 "pm2 restart onai-backend"
```

6. **Проверить логи**
```bash
ssh root@207.154.231.30 "pm2 logs onai-backend --lines 50"
```

### Пост-деплой проверки

1. **Проверить health endpoints**
```bash
curl https://onai.academy/api/health
curl https://onai.academy/api/traffic/health
```

2. **Проверить интеграции**
```bash
curl -X POST https://onai.academy/api/admin/integrations/diagnostics \
  -H "Authorization: Bearer $JWT_TOKEN"
```

3. **Проверить вебхуки**
```bash
# Отправить тестовый webhook на AmoCRM Sales Webhook
curl -X POST https://onai.academy/api/amocrm/sales-webhook \
  -H "Content-Type: application/json" \
  -d '{"leads":{"status":[{"lead_id":12345,"pipeline_id":10350882,"status_id":142}]}}'
```

---

## ⚠️ Почему нужно тестировать на продакшене

### 1. **Различия в окружении**

Локальное окружение и продакшн могут отличаться:
- **Версии Node.js**: Локально может быть Node.js 20, а на продакшене Node.js 18
- **Переменные окружения**: Локально `.env` может отличаться от продакшена
- **Базы данных**: Локально может быть тестовая БД, а на продакшене реальная
- **Сетевые настройки**: Локально нет firewall, а на продакшене есть
- **API лимиты**: Локально нет rate limiting, а на продакшене есть

**Пример**: Facebook Ads API может работать локально, но на продакшене вернуть ошибку из-за разных IP адресов.

### 2. **Реальные данные**

Локально вы тестируете с тестовыми данными, а на продакшене будут реальные:
- **UTM теги**: Реальные UTM теги могут отличаться от тестовых
- **ID таргетологов**: Реальные ID могут отличаться
- **ID пайплайнов**: Реальные ID могут отличаться
- **Кастомные поля**: Реальные кастомные поля могут отличаться

**Пример**: Локально UTM source может быть `test_source`, а на продакшене `facebook_kenesary`.

### 3. **Интеграции с внешними сервисами**

Некоторые интеграции работают только на продакшене:
- **AmoCRM webhooks**: AmoCRM отправляет webhooks только на продакшен URL
- **Facebook Ads API**: Facebook может блокировать запросы с локальных IP
- **Supabase**: RLS policies могут отличаться между тестовой и продакшен БД

**Пример**: AmoCRM не будет отправлять webhooks на `http://localhost:3001/api/amocrm/sales-webhook`, только на `https://onai.academy/api/amocrm/sales-webhook`.

### 4. **Производительность**

Локально производительность может отличаться от продакшена:
- **Сетевая задержка**: Локально нет задержки, а на продакшене есть
- **Нагрузка**: Локально нет нагрузки, а на продакшене есть
- **Кэширование**: Локально кэш может быть отключен, а на продакшене включен

**Пример**: Локально запрос к Supabase выполняется за 10ms, а на продакшене за 100ms.

### 5. **Безопасность**

На продакшене включены дополнительные меры безопасности:
- **HTTPS**: Локально HTTP, на продакшене HTTPS
- **CORS**: На продакшене строгие CORS политики
- **Rate limiting**: На продакшене включен rate limiting
- **Security headers**: На продакшене добавлены security headers

**Пример**: Локально CORS разрешает все источники, а на продакшене только `onai.academy`.

### 6. **Cron задачи и фоновые процессы**

На продакшене работают cron задачи, которых нет локально:
- **Facebook Ads Loader**: Загружает данные каждые 5 минут
- **Token Health Monitor**: Проверяет токены каждый час
- **Scheduler**: Проверяет уведомления каждую минуту

**Пример**: Локально данные Facebook Ads не загружаются автоматически, а на продакшене загружаются каждые 5 минут.

### 7. **Логирование и мониторинг**

На продакшене настроены логирование и мониторинг:
- **PM2 logs**: Логи всех процессов
- **Health checks**: Проверки здоровья каждые 5 минут
- **Alerts**: Уведомления о проблемах

**Пример**: Локально нет логов в PM2, а на продакшене все логи сохраняются в PM2.

### 8. **Redis кэширование**

На продакшене используется Redis для кэширования:
- **Memory Cache**: Кэширование данных в памяти
- **Funnel Cache**: Кэширование данных воронок
- **Session Cache**: Кэширование сессий

**Пример**: Локально Redis не работает (ECONNREFUSED), а на продакшене работает корректно.

---

## 📋 Чеклист готовности к продакшену

### Критические проверки (должны быть ✅)

- [ ] Backend запускается без ошибок
- [ ] Frontend запускается без ошибок
- [ ] Health check endpoints работают
- [ ] Аутентификация работает
- [ ] Traffic Dashboard API работает
- [ ] Webhooks принимают данные
- [ ] Дедупликация работает
- [ ] Circuit Breaker работает
- [ ] Retry работает
- [ ] Диагностика интеграций работает

### Важные проверки (должны быть ✅)

- [ ] AmoCRM соединение работает
- [ ] Facebook Ads соединение работает
- [ ] Supabase соединение работает
- [ ] Redis соединение работает (или отключено)
- [ ] JWT токены валидируются
- [ ] RLS policies работают
- [ ] Rate limiting работает
- [ ] CORS настроен корректно

### Желательные проверки (желательно ✅)

- [ ] Логирование работает
- [ ] Мониторинг работает
- [ ] Alerts работают
- [ ] Backup создан
- [ ] Rollback план готов
- [ ] Документация обновлена

---

## 🎯 Итог

**Текущая готовность**: 85%  
**Целевая готовность**: 100%  
**Оценка времени**: 5-7 часов  
**Риск**: Низкий

**Следующие шаги**:
1. Реализовать Phase 1 (2-3 часа)
2. Реализовать Phase 2 (2-3 часа)
3. Провести локальное тестирование (1 час)
4. Создать бэкап продакшена
5. Задеплоить на продакшен
6. Провести пост-деплой проверки
7. Мониторить логи и метрики

---

**Отчет подготовил**: Kilo Code (AI Assistant)  
**Дата**: 2025-12-27  
**Версия**: 1.0
