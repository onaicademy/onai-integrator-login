# План комплексной диагностики и синхронизации лидов

## 📊 Текущее состояние: ~60% готовности по AmoCRM интеграции

---

## 🎯 Критические задачи

### Задача 1: Диагностика текущего состояния интеграций

**Что нужно проверить:**
1. ✅ Express Course (pipeline 10350882) - webhook работает
2. ❌ Flagship Course (pipeline 10418746) - webhook НЕ настроен
3. ❌ Tripwire BD - нет интеграции
4. ❌ ProofTest - нет интеграции
5. ⚠️ Landing BD - частичная синхронизация (только email/phone)

**Текущие проблемы:**
- `traffic-sales-aggregator.ts` не реализован (TODO)
- Нет webhook для Flagship Course
- Нет интеграции с Tripwire BD
- Нет интеграции с ProofTest

---

### Задача 2: Создание сервиса для получения всех лидов из AmoCRM

**Файл:** `backend/src/services/amocrm-leads-fetcher.ts`

**Функционал:**
- Получать лиды из всех воронок
- Извлекать UTM метки
- Определять таргетолога по UTM
- Определять органические лиды (без UTM)
- Пагинация для больших объемов данных

**API AmoCRM:**
```typescript
// Получить лиды из Express Course
GET /api/v4/leads?filter[pipeline_id]=10350882&with=contacts&limit=250

// Получить лиды из Flagship Course
GET /api/v4/leads?filter[pipeline_id]=10418746&with=contacts&limit=250

// Получить лиды из всех воронок
GET /api/v4/leads?with=contacts&limit=250
```

---

### Задача 3: Создание эндпоинта для валовых продаж

**Эндпоинт:** `GET /api/traffic/admin/total-sales`

**Функционал:**
- Показывать все сделки из всех воронок
- Не фильтровать по таргетологам
- Группировать по источникам (UTM source)
- Показывать органические лиды отдельно
- Фильтры по дате, воронке, продукту

**Структура ответа:**
```typescript
{
  total_sales: 150,
  total_revenue: 25000000,
  by_source: {
    "fb_kenesary": { sales: 50, revenue: 8000000 },
    "fb_arystan": { sales: 30, revenue: 5000000 },
    "organic": { sales: 20, revenue: 3000000 },
    "unknown": { sales: 50, revenue: 9000000 }
  },
  by_pipeline: {
    "express_course": { sales: 91, revenue: 455000 },
    "flagship_course": { sales: 59, revenue: 28910000 }
  },
  by_product: {
    "express": { sales: 91, revenue: 455000 },
    "flagman": { sales: 59, revenue: 28910000 }
  }
}
```

---

### Задача 4: Создание эндпоинта для лидов по воронкам

**Эндпоинт:** `GET /api/traffic/admin/leads-by-funnel`

**Функционал:**
- Показывать лиды по воронкам
- Фильтры по воронке, статусу, дате
- Показывать UTM метки
- Показывать таргентолога (если определен)
- Показывать источник (organic/UTM)

**Структура ответа:**
```typescript
{
  pipeline_id: 10350882,
  pipeline_name: "Express Course",
  total_leads: 500,
  leads: [
    {
      id: 12345,
      name: "Иван Иванов",
      email: "ivan@example.com",
      phone: "+77001234567",
      status_id: 142,
      status_name: "Успешно реализовано",
      price: 5000,
      created_at: "2025-12-27T10:00:00Z",
      utm_source: "fb_kenesary",
      utm_campaign: "tripwire_test_1",
      utm_medium: "cpc",
      targetologist: "Kenesary",
      source: "UTM" // или "organic"
    }
  ]
}
```

---

### Задача 5: Реализация определения органических лидов

**Логика:**
```typescript
function determineLeadSource(utmData: UTMData): {
  source: 'UTM' | 'organic' | 'unknown';
  targetologist?: string;
} {
  // Если есть UTM метки
  if (utmData.utm_source || utmData.utm_campaign) {
    const targetologist = determineTargetologistFromUTM(utmData);
    return {
      source: 'UTM',
      targetologist: targetologist || 'Unknown'
    };
  }

  // Если нет UTM меток
  return {
    source: 'organic',
    targetologist: undefined
  };
}
```

---

## 📋 План реализации

### Фаза 1: Диагностика (1-2 часа)

**Задачи:**
1. Проверить webhook для Express Course (10350882)
   - Работает ли webhook?
   - Сохраняются ли продажи в `express_course_sales`?
   - Правильно ли извлекаются UTM метки?

2. Проверить webhook для Flagship Course (10418746)
   - Настроен ли webhook?
   - Создать webhook если не настроен

3. Проверить синхронизацию Landing BD с AmoCRM
   - Работает ли `/api/admin/landing/sync-amocrm`?
   - Сопоставляются ли лиды по email/phone?

4. Проверить интеграцию с Tripwire BD
   - Есть ли таблица лидов в Tripwire DB?
   - Есть ли интеграция с AmoCRM?

5. Проверить интеграцию с ProofTest
   - Есть ли интеграция?
   - Откуда приходят лиды?

**Результат:** Диагностический отчет с текущим статусом всех интеграций

---

### Фаза 2: Создание сервиса AmoCRM Leads Fetcher (2-3 часа)

**Задачи:**
1. Создать файл `backend/src/services/amocrm-leads-fetcher.ts`
2. Реализовать функции:
   - `fetchLeadsFromPipeline(pipelineId, limit)` - получить лиды из воронки
   - `fetchAllLeads(pipelineIds)` - получить лиды из всех воронок
   - `extractUTMData(lead)` - извлечь UTM метки
   - `determineTargetologist(utmData)` - определить таргетолога
   - `determineLeadSource(utmData)` - определить источник (UTM/organic)

3. Использовать `targetologist-mapper.ts` для определения таргетологов

**Результат:** Сервис для получения всех лидов из AmoCRM

---

### Фаза 3: Создание эндпоинта для валовых продаж (2-3 часа)

**Задачи:**
1. Создать файл `backend/src/routes/traffic-total-sales.ts`
2. Реализовать эндпоинт `GET /api/traffic/admin/total-sales`
3. Функционал:
   - Получить все сделки из AmoCRM
   - Группировать по источникам (UTM source)
   - Определить органические лиды
   - Агрегировать по воронкам и продуктам
   - Добавить фильтры по дате, воронке, продукту

4. Добавить в `backend/src/server.ts`:
   ```typescript
   import trafficTotalSalesRouter from './routes/traffic-total-sales';
   app.use('/api/traffic/admin', trafficTotalSalesRouter);
   ```

**Результат:** Эндпоинт для просмотра валовых продаж

---

### Фаза 4: Создание эндпоинта для лидов по воронкам (2-3 часа)

**Задачи:**
1. Создать файл `backend/src/routes/traffic-leads-by-funnel.ts`
2. Реализовать эндпоинт `GET /api/traffic/admin/leads-by-funnel`
3. Функционал:
   - Получить лиды из указанной воронки
   - Извлечь UTM метки
   - Определить таргентолога
   - Определить источник (UTM/organic)
   - Добавить фильтры по статусу, дате, источнику

4. Добавить в `backend/src/server.ts`:
   ```typescript
   import trafficLeadsByFunnelRouter from './routes/traffic-leads-by-funnel';
   app.use('/api/traffic/admin', trafficLeadsByFunnelRouter);
   ```

**Результат:** Эндпоинт для просмотра лидов по воронкам

---

### Фаза 5: Интеграция с Tripwire BD (2-3 часа)

**Задачи:**
1. Проверить структуру Tripwire DB
2. Создать сервис `backend/src/services/tripwire-sync.ts`
3. Реализовать функции:
   - `fetchTripwireLeads()` - получить лиды из Tripwire DB
   - `syncTripwireLeadsToAmoCRM()` - синхронизировать с AmoCRM
   - `syncAmoCRMLeadsToTrafficDB()` - синхронизировать с Traffic DB

**Результат:** Интеграция с Tripwire BD

---

### Фаза 6: Интеграция с ProofTest (1-2 часа)

**Задачи:**
1. Проверить откуда приходят лиды ProofTest
2. Создать сервис `backend/src/services/proftest-sync.ts`
3. Реализовать функции:
   - `fetchProofTestLeads()` - получить лиды ProofTest
   - `syncProofTestLeadsToAmoCRM()` - синхронизировать с AmoCRM

**Результат:** Интеграция с ProofTest

---

### Фаза 7: Обновление Traffic Dashboard UI (3-4 часа)

**Задачи:**
1. Создать страницу "Валовые продажи" в Traffic Dashboard
   - Показывать все сделки
   - Группировать по источникам
   - Показывать органические лиды отдельно
   - Фильтры по дате, воронке, продукту

2. Создать страницу "Лиды по воронкам" в Traffic Dashboard
   - Показывать лиды по воронкам
   - Фильтры по воронке, статусу, дате
   - Показывать UTM метки
   - Показывать таргентолога

**Результат:** Обновленный UI Traffic Dashboard

---

## 📊 Итоговая оценка

| Фаза | Время | Результат |
|-------|--------|----------|
| Фаза 1: Диагностика | 1-2 часа | Диагностический отчет |
| Фаза 2: AmoCRM Leads Fetcher | 2-3 часа | Сервис для получения лидов |
| Фаза 3: Валовые продажи | 2-3 часа | Эндпоинт для валовых продаж |
| Фаза 4: Лиды по воронкам | 2-3 часа | Эндпоинт для лидов по воронкам |
| Фаза 5: Tripwire BD | 2-3 часа | Интеграция с Tripwire BD |
| Фаза 6: ProofTest | 1-2 часа | Интеграция с ProofTest |
| Фаза 7: UI обновление | 3-4 часа | Обновленный UI |

**Общее время:** ~13-20 часов

**Готовность после реализации:** ~95%

---

## 🎯 Приоритет задач

### Критический (сделать первым)
1. ✅ Диагностика текущего состояния
2. ✅ Создание сервиса AmoCRM Leads Fetcher
3. ✅ Создание эндпоинта для валовых продаж
4. ✅ Создание эндпоинта для лидов по воронкам

### Высокий приоритет
5. ✅ Интеграция с Tripwire BD
6. ✅ Интеграция с ProofTest

### Средний приоритет
7. ✅ Обновление Traffic Dashboard UI

---

## 📝 Заключение

### Текущее состояние:
- ✅ Express Course webhook работает
- ❌ Flagship Course webhook НЕ настроен
- ❌ Нет интеграции с Tripwire BD
- ❌ Нет интеграции с ProofTest
- ⚠️ Частичная синхронизация Landing BD
- ❌ Нет эндпоинта для валовых продаж
- ❌ Нет эндпоинта для лидов по воронкам

### Что нужно сделать:
1. Провести полную диагностику всех интеграций
2. Создать сервис для получения всех лидов из AmoCRM
3. Создать эндпоинты для валовых продаж и лидов по воронкам
4. Интегрировать Tripwire BD и ProofTest
5. Обновить Traffic Dashboard UI

**Оценочное время:** ~13-20 часов

**Готовность после реализации:** ~95%

---

**Дата создания:** 27 декабря 2025  
**Версия:** 1.0  
**Автор:** Kilo Code Assistant
